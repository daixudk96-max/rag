# Phase 6 Plan — Tree Formal Rollup

## Goal
建立 tree_nodes / tree_node_spans 与 parent-child 组织，并支持 leaf 命中回到 parent context。

## Scope
### In
- tree_nodes migration
- tree_node_spans migration
- tree generator
- tree loader
- tree query / rollup
- 与既有 vector_chunks 的 node_id 关联

### Out
- Neo4j graph path
- Deep Hybrid
- HitDistributionAnalyzer
- Reranker

## Tasks
1. 写 Tree schema 的 RED 测试
2. 增加 migration：tree_nodes / tree_node_spans / vector_chunks.node_id
3. 实现 generator / loader / query
4. 跑 integration + full suite

## Exit Criteria
- tree_nodes / tree_node_spans 可写入
- 可从 leaf hit 回到 parent context
- tree 与 version / span / provenance 主干一致
