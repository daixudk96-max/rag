# 当前三条任务线

## 任务状态总览

### 白绫线（树状检索）- 当前实现

**已完成功能**：

1. **PageIndexTreeAdapter** (`llamaindex_runtime/tree/pageindex_adapter.py`)
   - ✓ 树结构提取（PDF + Markdown）
   - ✓ Frozen provenance（UUID + version_id）
   - ✓ Unified LLM seam（RuntimeSettings配置）
   - ✓ retrieve_tree_hits接口

**当前局限**：

```python
# retrieve_tree_hits实现（pageindex_adapter.py:100）
def retrieve_tree_hits(self, query_text, version_id, registry):
    nodes = registry.query_tree_nodes_by_version(version_id)

    # 问题：返回所有nodes（无筛选）
    for node in nodes:
        hits.append(BackendHit(node_id=node["node_id"], ...))

    return hits  # 14个节点全返回，无聚类筛选
```

**缺失功能**：
- ❌ 聚类筛选（预计算embedding）
- ❌ 父节点定位（快速导航）
- ❌ Agent reasoning检索（PageIndex原版）
- ❌ Workspace管理（文档持久化）

**本质**：基础树结构提取 + 简单检索，**无效率优化**

---

## 手头三件事

### 任务1：聚类功能实现

**目标**：实现预计算embedding + 聚类筛选 + 父节点导航

**理想流程**：
```
Index阶段：
  计算节点embedding → 聚类（KMeans）→ 标记父节点

Query阶段：
  Query embedding → 匹配cluster → 定位父节点 → 往下导航

效果：Token消耗降低90%（100-300 vs 1000-5000）
```

**独立实现**：3天（在PageIndex架构外独立实现）

---

### 任务2：PageIndex移植改造完成

**目标**：完整移植PageIndex功能，融入我们的架构

**包括**：
- PageIndexClient（workspace管理、文档持久化）
- CLI工具（run_pageindex.py）
- 工具函数（get_document, get_document_structure, get_page_content）
- Agent reasoning检索（可选）
- tree_thinning（节点合并优化）

**工程量**：10天（完整移植）

---

### 任务3：PageIndex OpenAI接口接到RuntimeSettings

**目标**：PageIndex的LLM调用统一走我们的RuntimeSettings配置

**当前状态**：
- PageIndex有自己的LLM配置（config.yaml）
- 我们有RuntimeSettings（.env统一配置）

**需要改造**：
- PageIndex的LiteLLM调用 → RuntimeSettings
- Monkey-patch llm_acompletion → unified seam
- 统一模型配置（gpt-5.4-mini from .env）

**工程量**：1天（接口对接）

---

## 优先级建议

### **推荐方案：合并任务1+2，一次完成**

**用户观点**：聚类可以直接在PageIndex里实现，没必要先独立实现再借鉴

**正确性**：✓ **完全同意**

**合并方案**：

```
PageIndex完整改造（包含聚类）：
├── Phase 1：PageIndexClient移植（3天）
│   ├── workspace管理
│   ├── 文档持久化
│   ├── 工具函数（get_page_content）
│
├── Phase 2：检索增强（4天）
│   ├── 聚类功能实现（预计算embedding + cluster）
│   ├── retrieve_tree_hits改造（聚类筛选 + 父节点导航）
│   ├── Agent reasoning移植（可选）
│
└── Phase 3：配置统一（1天）
    ├── OpenAI接口 → RuntimeSettings
    ├── .env参数统一
    ├── 测试验证

总计：8天（合并后）
```

**对比独立实现**：

| 方案 | 总工程量 | 优势 |
|------|----------|------|
| **独立实现** | 3+10+1 = 14天 | - |
| **合并实现** | 8天 | ✓ 减少6天，避免重复工作 |

---

## 推荐实现顺序

### **任务合并后顺序**

```
Week 1（8天）：
  Phase 1（3天）：PageIndexClient移植
    - workspace管理
    - 文档持久化
    - CLI工具
    - get_page_content

  Phase 2（4天）：检索增强（聚类 + reasoning）
    - 预计算embedding + 聚类
    - retrieve_tree_hits改造（聚类筛选）
    - Agent reasoning移植（可选）
    - Hybrid检索（tree + graph + vector）

  Phase 3（1天）：配置统一
    - OpenAI接口 → RuntimeSettings
    - .env参数统一
    - 测试验证

Week 2（可选）：
  - Hybrid检索优化
  - 其他backend融合
```

---

## 白绫线当前本质

**现状总结**：

| 维度 | 当前实现 | 缺失 |
|------|----------|------|
| **树结构提取** | ✓ PageIndexTreeAdapter | - |
| **Frozen provenance** | ✓ UUID + version_id | - |
| **Unified seam** | ✓ RuntimeSettings配置 | - |
| **检索效率** | ❌ 返回所有nodes | ✓ 聚类筛选 |
| **Workspace** | ❌ 无持久化 | ✓ PageIndexClient |
| **CLI工具** | ❌ 无命令行 | ✓ run_pageindex.py |
| **Agent workflow** | ❌ 无LLM导航 | ✓ Reasoning backend |

**本质**：**基础树框架，检索功能未完善**

---

## 下一步决策

### **立即行动：PageIndex完整改造（包含聚类）**

**理由**：
1. ✓ 合并任务1+2，减少6天工程量
2. ✓ PageIndex已有完整架构，聚类在此架构内实现更合理
3. ✓ 避免重复工作（独立实现聚类 → 再借鉴 → 再移植）
4. ✓ 最终成果：完整PageIndex + 高效聚类检索

**不推荐独立实现聚类**：
- ❌ 独立实现后还需要移植PageIndex（重复）
- ❌ 独立实现无法利用PageIndex workspace、CLI等基础设施
- ❌ 工程量增加6天

---

## 任务清单更新

### **合并后的任务清单**

1. **PageIndex完整改造**（8天）
   - 包含：PageIndexClient + workspace + CLI + 聚类检索 + 配置统一

2. **Hybrid检索融合**（可选，3天）
   - tree + graph + vector路由
   - 进阶功能

**总工程量**：8天（核心） + 3天（可选） = **11天**

---

## 决策确认

**是否同意合并方案？**

- ✓ 任务1+2合并：PageIndex改造包含聚类功能
- ✓ 任务3并行：OpenAI接口统一到RuntimeSettings
- ✓ 总工程量：8天（减少6天）
- ✓ 最终成果：完整PageIndex + 高效聚类检索 + 统一配置

**立即启动PageIndex完整改造？**

我可以开始编写PageIndexClient移植代码（Phase 1）。