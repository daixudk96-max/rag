# 检索增强方案对比分析

## 核心问题

1. **PageIndex检索消耗token问题**：
   - PageIndex reasoning-based retrieval：每次查询LLM导航树
   - 每次从头筛选节点，不缓存，不预计算
   - 高频查询场景：token消耗巨大

2. **当前流程问题**：
   - tree/graph/vector hybrid理论框架
   - 实际实现：tree path太弱（只返回nodes，无筛选）
   - 唯一亮点：聚类功能（semantic clustering for subtree hotspots）

3. **初始目标回顾**：
   - 一路：PageIndex donor不改（frozen contracts）
   - 一路：我们自己的流程（tree + graph + vector hybrid）
   - 当前状态：PageIndex只移植tree structure，未移植retrieval

## 目标

**完成高效检索能力**：
- 减少LLM token消耗（预计算、缓存、索引）
- 提升检索效果（精准召回，不依赖纯LLM reasoning）
- 保留PageIndex优势（树结构、context-aware）
- 融合我们的优势（聚类、graph、vector）

## 三大方案对比

### 方案A：改PageIndex（融入我们的逻辑）

**思路**：修改PageIndex donor，加入我们的retrieval机制

**改动内容**：
1. **修改PageIndex retrieve.py**：
   - 加入预计算索引（embedding缓存）
   - 加入graph关系（KG retrieval）
   - 加入聚类索引（subtree hotspots）

2. **改动PageIndex tree_parser**：
   - 加入聚类功能（semantic clustering）
   - 加入graph extraction（节点间关系）

3. **改动PageIndex agent workflow**：
   - 保留reasoning-based（可选）
   - 新增：embedding-based筛选 + graph reasoning

**工程量**：
- ❌ **违反frozen contracts**（控制包明确禁止）
- ❌ **改动donor repo**（PageIndex在Downloads，不在我们repo）
- ❌ **维护成本高**（donor repo独立，我们修改需同步）
- ❌ **违背unified seam原则**（PageIndex有自己的LLM config）
- ⏱ **预计工作量**：2-3周（改donor + 测试 + 合并）

**可行性**：✗ **不推荐**（违反架构原则）

---

### 方案B：移植PageIndex内容到我们系统

**思路**：把PageIndex功能完整移植，替换当前tree部分

**移植内容**：
1. **完整移植PageIndex tree_parser**：
   - ✓ 已完成：tree structure extraction
   - 需新增：tree_thinning（节点合并）
   - 需新增：get_page_content（内容提取）

2. **移植PageIndex reasoning retrieval**：
   - 新增backend：`ReasoningTreeBackend`
   - LLM导航树（可选路径）
   - 集成到hybrid retrieval（tree/graph/vector）

3. **移植PageIndex agent workflow**：
   - OpenAI Agents SDK集成
   - 工具函数：get_document_structure, get_page_content
   - 保留我们：graph backend, vector backend

4. **融合我们的聚类功能**：
   - tree indexing阶段：预计算subtree hotspots
   - 缓存embedding（减少查询token）
   - graph extraction（节点关系）

**工程量**：
- ✓ **符合frozen contracts**（PageIndex donor不改）
- ✓ **在我们系统内扩展**（llamaindex_runtime/tree）
- ✓ **保留PageIndex优势**（树结构、reasoning retrieval）
- ✓ **保留我们优势**（聚类、graph、vector）
- ⏱ **预计工作量**：1-1.5周

**具体任务**：
```
1. 新增tree_thinning（2天）
   - 配置参数（.env: TREE_THINNING_ENABLED）
   - 修改PageIndexTreeAdapter
   - 测试验证

2. 新增ReasoningTreeBackend（3天）
   - 实现LLM导航树逻辑
   - 集成到TreeBackendAdapter
   - 配置参数（REASONING_TREE_ENABLED）

3. 新增get_page_content工具（1天）
   - 根据page_no/line_num提取内容
   - 集成到PageIndexTreeAdapter

4. 新增预计算索引（2天）
   - 聚类：subtree hotspots embedding
   - Graph：节点关系extraction
   - 缓存：减少查询LLM调用

5. 融合hybrid retrieval（2天）
   - tree backend + graph backend + vector backend
   - 配置路由策略
```

**可行性**：✓ **推荐**

---

### 方案C：混合方案（PageIndex不改，我们借鉴）

**思路**：PageIndex donor保持原样，我们系统独立实现PageIndex优势

**实现内容**：
1. **PageIndex donor**：
   - ✓ 保持frozen（不改）
   - ✓ 只用作参考（学习PageIndex思路）
   - ✓ 已移植：tree structure extraction

2. **我们系统**：
   - 独立实现reasoning retrieval（借鉴PageIndex思路）
   - 独立实现tree_thinning
   - 独立实现聚类 + graph + vector hybrid

3. **双路并存**：
   - 一路：PageIndex donor（原始功能，不动）
   - 一路：我们的enhanced tree（融合PageIndex优势 + 我们优势）

**工程量**：
- ✓ **符合frozen contracts**
- ✓ **架构清晰**（双路分离）
- ⚠ **重复实现**（PageIndex功能在我们系统重新实现）
- ⏱ **预计工作量**：1.5-2周

**可行性**：✓ **可行**，但不如方案B高效

---

## 方案对比矩阵

| 维度 | 方案A | 方案B | 方案C |
|------|-------|-------|-------|
| **符合frozen contracts** | ✗ | ✓ | ✓ |
| **改动PageIndex donor** | ✗ | ✓ 不改 | ✓ 不改 |
| **保留PageIndex优势** | ✓ | ✓ | ✓ |
| **保留我们优势** | ✓ | ✓ | ✓ |
| **减少LLM token消耗** | ✓ | ✓ | ✓ |
| **架构清晰度** | ✗ 混乱 | ✓ 清晰 | ⚠ 双路并存 |
| **维护成本** | ✗ 高 | ✓ 低 | ⚠ 中 |
| **工程量** | ✗ 2-3周 | ✓ 1-1.5周 | ⚠ 1.5-2周 |
| **推荐度** | ✗ | ✓✓✓ | ⚠ |

## 推荐：方案B

### 核心架构

```
llamaindex_runtime/
├── tree/
│   ├── pageindex_adapter.py  # PageIndex移植（tree_parser + md_to_tree）
│   ├── reasoning_backend.py  # 新增：LLM导航树（PageIndex reasoning移植）
│   ├── thinning.py           # 新增：tree_thinning移植
│   ├── clustering.py         # 我们的聚类功能
│   └── hybrid_retrieval.py   # 融合：tree + graph + vector
├── graph/
│   ├── kg_backend.py         # 我们的graph backend
├── vector/
│   ├── vector_backend.py     # 我们的vector backend
```

### 配置参数（.env）

```dotenv
# Tree backend options
TREE_BACKEND=hybrid  # reasoning | clustering | hybrid
TREE_THINNING_ENABLED=true
TREE_MIN_TOKEN_THRESHOLD=100

# Reasoning-based retrieval（PageIndex移植）
REASONING_TREE_ENABLED=true  # 可选启用
REASONING_TREE_MODEL=gpt-5.4-mini
REASONING_TREE_TEMPERATURE=0.0

# Clustering index（我们的优势）
TREE_CLUSTERING_ENABLED=true
TREE_CLUSTERING_MIN_NODES=5

# Hybrid retrieval路由
TREE_GRAPH_VECTOR_ROUTING=adaptive  # adaptive | fixed
TREE_WEIGHT=0.4
GRAPH_WEIGHT=0.3
VECTOR_WEIGHT=0.3
```

### 检索流程对比

**PageIndex原版**（方案A）：
```
Query → LLM reasoning导航树 → 提取内容 → 生成答案
（每次查询都调用LLM，消耗token）
```

**方案B**：
```
Index阶段：
  Tree → 预计算聚类（embedding缓存）
       → 预计算graph关系
       → tree_thinning优化

Query阶段：
  Query → Tree backend（聚类筛选，embedding匹配，无需LLM）
       → Graph backend（KG reasoning）
       → Vector backend（semantic similarity）
       → Hybrid路由（adaptive权重）
       → 生成答案

（预计算索引，查询减少LLM调用）
```

### Token消耗对比

| 场景 | PageIndex原版 | 方案B |
|------|---------------|-------|
| **单次查询** | 1000-5000 tokens（LLM导航树） | 100-300 tokens（embedding匹配） |
| **高频查询**（100次） | 100k-500k tokens | 10k-30k tokens（节省90%+） |
| **冷启动** | 0（无需预计算） | 一次索引（聚类+graph） |

### 具体实现步骤

**Phase 1：基础移植**（3天）
1. tree_thinning集成（节点合并）
2. get_page_content工具（内容提取）
3. 配置参数（.env扩展）

**Phase 2：Reasoning backend**（3天）
1. 新增ReasoningTreeBackend（LLM导航树）
2. 集成到TreeBackendAdapter（可选backend）
3. 工具函数：get_document_structure, get_page_content

**Phase 3：聚类 + 预计算**（2天）
1. subtree hotspots clustering
2. embedding缓存（减少查询LLM）
3. graph关系extraction

**Phase 4：Hybrid retrieval**（2天）
1. tree + graph + vector路由
2. adaptive权重策略
3. 配置参数（TREE_WEIGHT, GRAPH_WEIGHT, VECTOR_WEIGHT）

**总工程量**：1-1.5周（10天）

---

## 决策建议

### 立即行动（方案B Phase 1）

1. **tree_thinning集成**（优先级最高）：
   - 文件：`llamaindex_runtime/tree/thinning.py`
   - 移植PageIndex tree_thinning_for_index
   - 配置：TREE_THINNING_ENABLED, TREE_MIN_TOKEN_THRESHOLD
   - 测试：14节点文档 → 合并小节点

2. **配置参数扩展**：
   - RuntimeSettings新增tree相关字段
   - .env.example更新模板

3. **测试验证**：
   - 现有14节点markdown文档
   - 验证tree_thinning效果（节点数减少）
   - 验证provenance integrity（UUID关系正确）

### 后续行动（Phase 2-4）

根据实际需求决定：
- Phase 2（Reasoning backend）：如果需要PageIndex原版retrieval能力
- Phase 3（聚类 + 预计算）：如果需要高频查询优化（减少token）
- Phase 4（Hybrid retrieval）：如果需要完整tree/graph/vector融合

---

## 总结

**推荐方案B**：
- ✓ 工程量最小（1-1.5周 vs 2-3周）
- ✓ 符合架构原则（frozen contracts, unified seam）
- ✓ 保留双方优势（PageIndex树 + 我们聚类/graph/vector）
- ✓ 减少LLM token消耗（预计算索引）
- ✓ 维护成本低（在我们系统内，不依赖donor repo）

**不推荐方案A**：
- ✗ 违反frozen contracts（改PageIndex donor）
- ✗ 工程量大（2-3周）
- ✗ 维护成本高（donor repo独立）

**方案C可行但不高效**：
- ⚠ 工程量中等（1.5-2周）
- ⚠ 重复实现（PageIndex功能在我们系统重新实现）

**下一步**：
1. 决策确认方案B
2. 立即启动Phase 1（tree_thinning集成）
3. 规划Phase 2-4（根据实际需求）