# 聚类功能现状与实现顺序建议

## 1. 聚类功能现状

### 当前已实现

**位置**：`llamaindex_runtime/registry/tree_generator.py`

```python
# line 23-63: 简单关键词聚类
def _detect_semantic_cluster(texts: list[str]) -> list[int]:
    """Minimal semantic clustering: detect divergent content domains."""

    # 关键词检测分组（硬编码domain）
    domain_keywords = {
        "ml": ["machine learning", "neural network", ...],
        "cooking": ["cooking", "recipe", "pasta", ...],
        "medicine": ["medical", "health", "doctor", ...],
        "finance": ["finance", "investment", "stock", ...],
        "sports": ["sports", "game", "team", ...],
    }

    # 遍历texts，检测关键词，返回cluster indices
```

**功能**：
- ✓ 检测相同heading_path下的语义分叉
- ✓ 自动split成多个leaf nodes（cluster_id标记）
- ⚠ **但是很简单**：只关键词匹配，不embedding

### 当前缺失

**没有实现的理想功能**：

```
Index阶段：
1. 计算所有节点embedding（预计算）
2. 聚类（KMeans或hierarchical clustering）
3. 标记每个cluster的父节点（subtree hotspot）

Query阶段：
1. 计算query embedding
2. 匹配最相关cluster → 定位父节点
3. 从父节点往下导航（LLM或embedding）
```

---

## 2. 检索效率对比

### 纯树状检索（PageIndex原版）

**流程**：
```
Query → LLM获取完整树结构（1000-5000 tokens）
     → LLM逐节点判断相关性（每个节点推理）
     → 提取相关节点内容
     → 生成答案

总token消耗：1000-5000 tokens/query
```

**问题**：
- ❌ 每次查询从头开始
- ❌ 需要LLM推理每个节点
- ❌ 高频查询消耗大
- ❌ 无法预计算

### 聚类检索（理想流程）

**流程**：
```
Index阶段（一次性）：
  计算所有节点embedding → 聚类 → 标记父节点
  （预计算，后续查询无需重复）

Query阶段：
  Query embedding → 匹配最相关cluster（100-300 tokens）
                  → 定位父节点（无LLM调用）
                  → 从父节点往下导航：
                     - Option 1: embedding匹配子节点（无需LLM）
                     - Option 2: LLM精准判断（只相关分支）

总token消耗：100-300 tokens/query（节省90%+）
```

**优势**：
- ✓ 预计算embedding（一次性）
- ✓ 快速定位父节点（embedding匹配）
- ✓ 只需导航相关分支（减少节点数）
- ✓ 可选LLM精准判断（只在最后阶段）

---

## 3. 当前检索实现状态

### TreeBackendAdapter

**协议已定义**：
```python
# llamaindex_runtime/tree/backend_adapter.py
class TreeBackendAdapter(Protocol):
    def index_tree(self, source_path, version_id, registry):
        """构建树结构"""
        pass

    def retrieve_tree_hits(self, query_text, version_id, registry):
        """检索树节点"""
        pass
```

### PageIndexTreeAdapter

**已实现**：
```python
# llamaindex_runtime/tree/pageindex_adapter.py:100
def retrieve_tree_hits(self, query_text, version_id, registry):
    nodes = registry.query_tree_nodes_by_version(version_id)

    # 当前逻辑：遍历所有nodes，返回每个node
    for node in nodes:
        hit = BackendHit(
            heading_path=node["heading_path"],
            node_id=node["node_id"],
            text_preview=node["summary_text"],
            ...
        )
        hits.append(hit)

    return hits  # 返回所有nodes，无筛选
```

**问题**：
- ❌ 返回所有nodes（14个节点全返回）
- ❌ 无聚类筛选
- ❌ 无embedding匹配
- ❌ 无父节点定位

---

## 4. 实现顺序建议

### **推荐：先实现聚类功能**

**理由**：

1. **核心检索逻辑**：
   - 聚类是检索效率的核心
   - PageIndex移植只是框架，聚类才是实质改进

2. **工程量小**：
   - 聚类功能：3天
   - PageIndex移植：10天
   - 先做小任务，快速验证

3. **独立性**：
   - 聚类功能可在当前架构实现
   - 不依赖PageIndex移植
   - 可独立测试验证

4. **立即可用**：
   - 实现后立即提升检索效率
   - PageIndex移植是锦上添花

### 具体实现步骤

**Phase 1：Embedding-based聚类**（3天）

**Day 1：预计算embedding**

```python
# llamaindex_runtime/tree/clustering_backend.py

class ClusteringTreeBackend:
    def index_tree(self, source_path, version_id, registry):
        # 1. 构建树结构（PageIndexTreeAdapter）
        nodes = pageindex_adapter.index_tree(...)

        # 2. 预计算embedding
        node_embeddings = {}
        for node in nodes:
            text = node["summary_text"] or node["title"]
            embedding = embedding_model.encode(text)
            node_embeddings[node["node_id"]] = embedding

        # 3. 聚类
        clusters = self._cluster_nodes(node_embeddings)

        # 4. 标记父节点（subtree hotspot）
        cluster_parents = self._find_cluster_parents(nodes, clusters)

        # 5. 持久化
        registry.write_cluster_mapping(
            version_id=version_id,
            cluster_mapping={
                "node_embeddings": node_embeddings,
                "clusters": clusters,
                "cluster_parents": cluster_parents,
            }
        )
```

**Day 2：Query检索**

```python
def retrieve_tree_hits(self, query_text, version_id, registry):
    # 1. 计算query embedding
    query_embedding = embedding_model.encode(query_text)

    # 2. 加载聚类映射
    cluster_mapping = registry.query_cluster_mapping(version_id)
    clusters = cluster_mapping["clusters"]
    cluster_parents = cluster_mapping["cluster_parents"]

    # 3. 匹配最相关cluster
    best_cluster = self._match_best_cluster(query_embedding, clusters)

    # 4. 定位父节点
    parent_node_id = cluster_parents[best_cluster]

    # 5. 从父节点往下导航
    hits = self._navigate_from_parent(
        parent_node_id,
        query_embedding,
        registry,
        mode="embedding"  # embedding匹配或LLM判断
    )

    return hits
```

**Day 3：导航逻辑**

```python
def _navigate_from_parent(self, parent_id, query_embedding, registry, mode):
    # Option 1: Embedding匹配子节点
    if mode == "embedding":
        children = registry.query_children_nodes(parent_id)
        child_embeddings = registry.query_node_embeddings(children)

        # 计算相似度，筛选top-k
        similarities = cosine_similarity(query_embedding, child_embeddings)
        top_k_children = children[similarities.argsort()[-5:]]

        return [BackendHit(node_id=child["node_id"], ...) for child in top_k_children]

    # Option 2: LLM精准判断
    if mode == "llm":
        children = registry.query_children_nodes(parent_id)
        # 调用LLM判断哪些children相关（PageIndex agent workflow）
        relevant_children = llm_reasoning(query_text, children)
        return [BackendHit(node_id=child["node_id"], ...) for child in relevant_children]
```

**配置参数**（.env）：

```dotenv
# Clustering backend配置
TREE_BACKEND=clustering  # clustering | reasoning | hybrid
CLUSTERING_METHOD=embedding  # embedding | keyword
CLUSTERING_ALGORITHM=kmeans  # kmeans | hierarchical
CLUSTERING_MIN_NODES=5

# Embedding配置
CLUSTERING_EMBEDDING_MODEL=all-MiniLM-L6-v2
CLUSTERING_TOP_K=5

# 导航模式
CLUSTERING_NAVIGATE_MODE=embedding  # embedding | llm
```

**测试验证**：

```python
# tests/test_clustering_backend.py

def test_clustering_retrieve_efficiency():
    # 14节点markdown文档
    backend = ClusteringTreeBackend()
    backend.index_tree(...)

    # Query
    hits = backend.retrieve_tree_hits("爱复盘SWOT分析")

    # 验证：
    # 1. 只返回相关节点（不是全部14个）
    # 2. 定位父节点正确（"3. 竞品深度剖析"）
    # 3. Token消耗 < 300（对比PageIndex原版1000-5000）
```

---

## 5. 后续顺序

### 完整时间线

```
Week 1：聚类功能（核心检索逻辑）
  Day 1-3: Embedding-based聚类
  ✓ 立即可用，检索效率提升90%+

Week 2：PageIndex移植（框架完善）
  Day 4-7: PageIndexClient、CLI、workspace
  Day 8-10: Reasoning backend（可选），配置统一

Week 3（可选）：Hybrid检索融合
  tree + graph + vector hybrid路由
```

### 优先级矩阵

| 功能 | 优先级 | 工程量 | 独立性 | 立即可用 |
|------|--------|--------|--------|----------|
| **聚类检索** | ✓✓✓ 最高 | 3天 | ✓ 独立 | ✓ 立即可用 |
| **PageIndex移植** | ⚠ 中 | 10天 | ⚠ 依赖架构 | ⚠ 锦上添花 |
| **Hybrid检索** | ⚠ 低 | 3天 | ⚠ 依赖聚类 | ⚠ 进阶功能 |

---

## 6. 聚类功能实现细节

### 聚类算法选择

**KMeans聚类**（推荐）：
```python
from sklearn.cluster import KMeans

# 计算所有节点embedding
embeddings = [embedding_model.encode(node["summary_text"]) for node in nodes]

# KMeans聚类
kmeans = KMeans(n_clusters=5, random_state=42)
cluster_labels = kmeans.fit_predict(embeddings)

# 结果：每个节点分配cluster_id（0-4）
```

**Hierarchical聚类**（可选）：
```python
from sklearn.cluster import AgglomerativeClustering

# 层次聚类（树状结构天然适合）
clustering = AgglomerativeClustering(n_clusters=5)
cluster_labels = clustering.fit_predict(embeddings)
```

### 父节点定位

**逻辑**：
```python
def _find_cluster_parents(nodes, cluster_labels):
    """找到每个cluster的最上层父节点"""

    cluster_parents = {}

    for cluster_id in set(cluster_labels):
        # 找到属于该cluster的所有节点
        cluster_nodes = [nodes[i] for i, label in enumerate(cluster_labels) if label == cluster_id]

        # 找到最上层的父节点（level_no最小）
        parent = min(cluster_nodes, key=lambda n: n["level_no"])
        cluster_parents[cluster_id] = parent["node_id"]

    return cluster_parents
```

**示例**：
```
Cluster 0（爱复盘相关）：
  Nodes: "竞品A：爱复盘" (level 2), "SWOT分析" (level 3), ...
  Parent: "3. 竞品深度剖析" (level 1)

Cluster 1（DeepSeek相关）：
  Nodes: "竞品B：DeepSeek" (level 2), "场控看板" (level 3), ...
  Parent: "3. 竞品深度剖析" (level 1)

Cluster 2（其他）：
  Nodes: "目的" (level 1), "1. 竞品识别" (level 1), ...
  Parent: "第二阶段-竞品分析-代旭" (level 0)
```

---

## 7. Token消耗对比（预期）

| 场景 | PageIndex原版 | 聚类检索（embedding导航） | 聚类检索（LLM导航） |
|------|---------------|-------------------------|-------------------|
| **单次查询** | 1000-5000 tokens | 100-300 tokens | 300-800 tokens |
| **高频查询**（100次） | 100k-500k tokens | 10k-30k tokens | 30k-80k tokens |
| **节省比例** | - | **90%+** | **60-80%** |

---

## 8. 决策建议

### **立即行动：实现聚类功能**

**理由总结**：

1. ✓ **核心检索逻辑**（不是附属功能）
2. ✓ **工程量小**（3天 vs 10天）
3. ✓ **独立性**（不依赖PageIndex移植）
4. ✓ **立即可用**（检索效率提升90%+）
5. ✓ **可验证**（14节点markdown立即测试）

**PageIndex移植是锦上添花**：
- 框架完善（workspace、CLI）
- Agent reasoning（可选）
- 但不改变核心检索逻辑

**推荐顺序**：
```
Week 1（立即）：聚类功能 → 检索效率提升90%
Week 2（可选）：PageIndex移植 → 框架完善
Week 3（可选）：Hybrid检索 → 进阶功能
```

---

## 9. 下一步

**立即开始Phase 1**：

1. 创建 `llamaindex_runtime/tree/clustering_backend.py`
2. 实现预计算embedding + 聚类
3. 实现Query检索（cluster匹配 + 父节点导航）
4. 配置参数（.env）
5. 测试验证（14节点markdown）

**是否立即启动聚类功能实现？**

我可以开始编写 `clustering_backend.py` 的完整代码。