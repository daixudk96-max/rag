---
phase: 12-cluster-hot-hotspot-selection-redesign
plan: 04
type: execute
wave: 4
depends_on: [03]
files_modified:
  - tests/llamaindex_runtime/test_tree_semantic_cluster_hotspot.py
autonomous: false
requirements: [D-09, D-10]
must_haves:
  truths:
    - "The p6 DNA query AI产品经理的核心DNA是什么？ still returns evidence 数据驱动 / 非确定性 / 持续性 from the 产品特性对比 region and does NOT select 抖音案例 or 特斯拉案例 (D-10)"
    - "DNA regression assertions are NOT modified in wave 4; failures are fixed in the implementation, and any assertion change requires user approval recorded in the SUMMARY (W2)"
    - "Cluster densest-shared-ancestor and root-avoidance regression tests still pass (D-10)"
    - "Selector rollback paths route_subtree and cluster still resolve via get_hotspot_selector and remain behaviorally unchanged (D-09)"
    - "The full tests/llamaindex_runtime/ suite is green, including anti-hardcode and jieba extraction (D-10)"
    - "gitnexus_detect_changes() is run before commit and affected scope is reported (safety gate)"
  artifacts:
    - path: "tests/llamaindex_runtime/test_tree_semantic_cluster_hotspot.py"
      provides: "Preserved p6 DNA regression + any hybrid_cluster cross-check the validation adds"
      contains: "数据驱动"
  key_links:
    - from: "p6 DNA regression test"
      to: "expected evidence 数据驱动/非确定性/持续性"
      via: "assertion on returned hotspot/evidence region"
      pattern: "数据驱动|产品特性对比"
---

<objective>
Prove the cluster-hot redesign did not regress the locked p6 behavior and that rollback paths remain intact, then run the pre-commit safety gates. The p6 DNA query must still resolve to the `产品特性对比 > AI产品经理核心DNA` region with evidence `数据驱动 / 非确定性 / 持续性`, and must not select `抖音案例` / `特斯拉案例`. This plan runs the full selector suite (including anti-hardcode, jieba, densest-ancestor, root-avoidance), confirms `route_subtree`/`cluster` rollback, runs GitNexus detect-changes, and prepares safe commit.

Purpose: Closes D-10 (regression preservation) and D-09 (rollback intact) and satisfies the mandatory `gitnexus_detect_changes()` pre-commit gate.
Output: A green full `tests/llamaindex_runtime/` suite, a recorded detect-changes scope, and a committed Phase 12 change set.

NOTE: This plan is `autonomous: false` because Task 3 is a `checkpoint:manual` GitNexus detect-changes + commit gate (no CLI/API equivalent; runs via the GitNexus MCP, paused for the operator to record the affected-scope table and commit hash).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/12-cluster-hot-hotspot-selection-redesign/12-CONTEXT.md
@.planning/hotspot-cluster-redesign-EXPLORATION/00-SUMMARY.md
@.planning/phases/12-cluster-hot-hotspot-selection-redesign/12-03-SUMMARY.md

<interfaces>
<!-- Regression anchors. Extracted; do not re-explore. -->

Preserve-as-is tests in tests/llamaindex_runtime/test_tree_semantic_cluster_hotspot.py:
- TestClusterHotspotSelector.test_cluster_hotspot_selector_selects_densest_shared_ancestor (:182)
- ...avoids_root_when_local_cluster_exists (:354)
- ...short_exact_heading_node_survives_long_related_text (:513)
- ...p6_ai_product_manager_core_dna_routes_to_product_characteristics (:656)
- ...p6_dna_query_selects_expected_hotspot_not_forbidden (:915)

p6 ground truth (verification/p6_validation/p6_final_sample_structured.md):
- DNA region: 00:31 - 产品特性对比 > AI产品经理核心DNA, children 数据驱动 / 非确定性 / 持续性 (md:1,:18,:38,:39).
- Forbidden primary hotspots for the DNA query: 抖音案例, 特斯拉案例.

Rollback factory (semantic_distribution.py:1694–1716): route_subtree→SubtreeHotspotSelector, cluster→ClusterHotspotSelector, hybrid_cluster→HybridClusterHotspotSelector.
Config validation (config.py:19): VALID_HOTSPOT_SELECTORS = {route_subtree, cluster, hybrid_cluster}.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run p6 DNA regression + cluster/root-avoidance preservation (D-10)</name>
  <read_first>
    - tests/llamaindex_runtime/test_tree_semantic_cluster_hotspot.py (densest-ancestor :182, root-avoidance :354, short-exact :513, p6 DNA :656/:915)
    - llamaindex_runtime/tree/semantic_distribution.py (post-12-03 HybridClusterHotspotSelector)
    - verification/p6_validation/p6_final_sample_structured.md (DNA region :18,:38,:39)
  </read_first>
  <action>
    Run `python -m pytest tests/llamaindex_runtime/test_tree_semantic_cluster_hotspot.py -q`. All preserved cluster tests MUST stay green — the DNA regression (`test_p6_..._routes_to_product_characteristics` :656 and `test_p6_dna_query_selects_expected_hotspot_not_forbidden` :915) proves the redesign did not move the DNA query off the `产品特性对比` region. (W2) Do NOT modify the p6 DNA regression assertions in this wave. If a DNA test fails, fix the IMPLEMENTATION (in 12-03 scope) to satisfy the existing assertion — never weaken or re-target the assertion to make it pass. Any change to a DNA assertion requires explicit user approval recorded verbatim in the SUMMARY before it is applied. The guard is absolute: `抖音案例` / `特斯拉案例` are never acceptable as the DNA-query primary hotspot. Record the outcome in SUMMARY. Do NOT add p6-specific keyword literals to production code (anti-hardcode).
  </action>
  <verify>
    <automated>python -m pytest tests/llamaindex_runtime/test_tree_semantic_cluster_hotspot.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `python -m pytest tests/llamaindex_runtime/test_tree_semantic_cluster_hotspot.py -q` exits 0.
    - `python -m pytest tests/llamaindex_runtime/test_tree_semantic_cluster_hotspot.py -q -k "dna or product_characteristics or forbidden"` exits 0.
    - SUMMARY states the DNA query's selected region is inside `产品特性对比 > AI产品经理核心DNA` and lists evidence 数据驱动/非确定性/持续性.
    - (W2) `git diff` shows NO change to the assertion lines of `test_p6_..._routes_to_product_characteristics` (:656) and `test_p6_dna_query_selects_expected_hotspot_not_forbidden` (:915) unless the SUMMARY records explicit user approval.
  </acceptance_criteria>
  <done>p6 DNA region preserved; densest-ancestor and root-avoidance regressions green.</done>
</task>

<task type="auto">
  <name>Task 2: Full selector suite + rollback verification (D-09/D-10)</name>
  <read_first>
    - tests/llamaindex_runtime/test_tree_hybrid_hotspot_selector.py (registration :20, anti-hardcode :587, jieba :643, config :737)
    - llamaindex_runtime/tree/semantic_distribution.py (get_hotspot_selector :1694)
    - llamaindex_runtime/config.py (VALID_HOTSPOT_SELECTORS :19)
  </read_first>
  <action>
    Run the full `python -m pytest tests/llamaindex_runtime/ -q`. Confirm: (a) registration test proves `get_hotspot_selector("route_subtree")`→SubtreeHotspotSelector and `("cluster")`→ClusterHotspotSelector unchanged; (b) anti-hardcode test green (no p6-specific keyword constants leaked into production scoring); (c) jieba extraction tests green; (d) config registration test green (`hybrid_cluster` still valid, default still `route_subtree`). No source edits expected here — this is a regression gate. If a non-DNA preserved test fails, fix the implementation in scope of 12-03 semantics (do not weaken preserved contracts).
  </action>
  <verify>
    <automated>python -m pytest tests/llamaindex_runtime/ -q</automated>
  </verify>
  <acceptance_criteria>
    - `python -m pytest tests/llamaindex_runtime/ -q` exits 0.
    - `python -m pytest tests/llamaindex_runtime/test_tree_hybrid_hotspot_selector.py -q -k "registration or hardcode or jieba or config"` exits 0.
    - SUMMARY lists total passed count for `tests/llamaindex_runtime/`.
  </acceptance_criteria>
  <done>Full selector suite green; rollback paths and anti-hardcode/jieba/config contracts intact.</done>
</task>

<task type="checkpoint:manual" gate="blocking">
  <name>Task 3 [BLOCKING]: GitNexus detect-changes then safe commit (safety gate, D-09)</name>
  <read_first>
    - ./CLAUDE.md (detect-changes-before-commit; never find-and-replace rename)
    - .planning/phases/12-cluster-hot-hotspot-selection-redesign/12-01-SUMMARY.md, 12-02-SUMMARY.md, 12-03-SUMMARY.md
  </read_first>
  <action>
    1. Run `gitnexus_detect_changes()` and report the affected scope (files, symbols, affected processes, risk level). If HIGH/CRITICAL risk is reported, WARN the user explicitly before committing. Expected touched symbols: `HotspotSelectionContext`, `_build_report`, `HybridClusterHotspotSelector.select_hotspots`, new `_compute_child_coverage_score`, runtime hybrid_cluster context. Confirm NO unexpected symbols/processes outside the Phase 12 selection layer changed.
    2. Stage ONLY Phase 12 files: `llamaindex_runtime/tree/semantic_distribution.py`, `llamaindex_runtime/tree/runtime.py`, `tests/llamaindex_runtime/test_tree_hybrid_hotspot_selector.py`, `tests/llamaindex_runtime/test_tree_semantic_cluster_hotspot.py`, and `.planning/phases/12-cluster-hot-hotspot-selection-redesign/*`. Do NOT sweep unrelated dirty-tree files (per STATE.md git-hygiene concerns). Commit with message `feat(12): cluster-hot hotspot selection with dual-hot coverage gate`. Use `gitnexus_rename` (not find-and-replace) if any symbol rename is needed — none is expected in this plan.
  </action>
  <verify>
    <manual>GitNexus MCP detect-changes + git commit (no automated command). Completion is proven by the file-existence check below.</manual>
  </verify>
  <acceptance_criteria>
    - 12-04-SUMMARY.md contains the detect-changes affected-scope table (files, symbols, processes, risk level).
    - SUMMARY records the commit hash and confirms only Phase 12 selection-layer files + planning artifacts were staged.
    - If HIGH/CRITICAL risk, SUMMARY records the explicit user-facing WARNING line.
  </acceptance_criteria>
  <resume-signal>Type "detect-changes-recorded" once the detect-changes scope table and commit hash are written to 12-04-SUMMARY.md.</resume-signal>
  <done>detect-changes run and scope reported; Phase 12 change set committed within approved scope.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Regression + commit gate. No new runtime input, auth, or network boundary. The only "boundary" of note is the git commit scope, controlled by explicit staging. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-08 | Tampering / scope creep | git commit scope | mitigate | Stage only Phase 12 selection-layer files + planning artifacts; explicitly exclude unrelated dirty-tree/scratch/secret-sensitive files flagged in STATE.md. `gitnexus_detect_changes()` confirms affected scope before commit. |
| T-12-09 | Repudiation / fabrication | p6 DNA regression assertions | mitigate | Do not weaken DNA assertions to pass; the test must still require real evidence (数据驱动/非确定性/持续性) from a node inside 产品特性对比, preserving the evidence-bearing-only contract. |

Block on `high` severity. No new high-severity threat; commit-scope risk is mitigated by explicit staging + detect-changes.
</threat_model>

<verification>
- `python -m pytest tests/llamaindex_runtime/ -q` is fully green (hybrid + cluster + regression).
- p6 DNA query region preserved (产品特性对比 / 数据驱动 / 非确定性 / 持续性); 抖音/特斯拉 not selected.
- `get_hotspot_selector` rollback paths and config `VALID_HOTSPOT_SELECTORS` unchanged.
- `gitnexus_detect_changes()` scope recorded; commit limited to Phase 12 scope.
</verification>

<success_criteria>
- p6 DNA regression and cross-domain anti-hardcode tests pass (D-10).
- route_subtree and cluster rollback paths intact (D-09).
- Full `tests/llamaindex_runtime/` suite green.
- detect-changes run before commit; affected scope reported and within Phase 12 bounds.
</success_criteria>

<output>
After completion, create `.planning/phases/12-cluster-hot-hotspot-selection-redesign/12-04-SUMMARY.md`.
</output>
