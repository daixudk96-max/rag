---
phase: 12-cluster-hot-hotspot-selection-redesign
plan: 03
type: execute
wave: 3
depends_on: [01, 02]
files_modified:
  - llamaindex_runtime/tree/semantic_distribution.py
autonomous: false
requirements: [D-01, D-02, D-03, D-04, D-05, D-06, D-08, D-09]
must_haves:
  truths:
    - "A parent is a hotspot iff coverage(P) ≥ θ AND dual-hot-child count ≥ min_support, where child_hot(c) = vector_hot(c) AND keyword_hot(c) (D-01, D-02)"
    - "Coverage denominator is the COMPLETE direct-child set from context.parent_to_children, including no-vector children (D-07 consumed)"
    - "vector-only or keyword-only children do NOT count toward coverage — the candidate set is an INTERSECTION, not the old UNION (D-02)"
    - "θ and min_support are configurable with defaults θ=0.5, min_support=2; vector_hot=normalized≥0.5; keyword_hot=≥1 matched term (D-03, D-04, D-05)"
    - "A strong dual-hot leaf is still returned when no parent meets coverage (leaf fallback); root not promoted by coverage alone (D-06)"
    - "Output is still SubtreeHotspot(node_id=...); traverse_tree_for_query interface unchanged (D-08)"
    - "Change stays behind RAG_TREE_HOTSPOT_SELECTOR=hybrid_cluster; route_subtree and cluster selectors are not modified or deleted (D-09)"
  artifacts:
    - path: "llamaindex_runtime/tree/semantic_distribution.py"
      provides: "Cluster-hot coverage scoring + dual-hot gate + configurable θ + leaf fallback in HybridClusterHotspotSelector"
      contains: "_compute_child_coverage_score"
  key_links:
    - from: "HybridClusterHotspotSelector.select_hotspots"
      to: "context.parent_to_children"
      via: "coverage denominator"
      pattern: "context\\.parent_to_children"
    - from: "child_hot computation"
      to: "vector_hot AND keyword_hot intersection"
      via: "set intersection of dual-hot children"
      pattern: "vector_hot.*keyword_hot|both_hot|dual_hot"
---

<objective>
Replace the single-node Top-K UNION fusion in `HybridClusterHotspotSelector.select_hotspots` with true cluster-hot coverage selection so the RED tests from Plan 12-02 turn GREEN. The current code builds `candidate_node_ids = set(vector_by_node) | set(keyword_by_node)` (semantic_distribution.py:955) and sorts per-node fusion. The new rule: a parent is a hotspot only when a coverage ratio of its DIRECT children are dual-hot (vector_hot AND keyword_hot). Keep the output shape (`SubtreeHotspot`) and traversal interface unchanged.

Purpose: Implements D-01 (coverage ≥ θ) + D-02 (dual-hot intersection) + D-03/D-04 (concrete hotness criteria) + D-05 (configurable θ) + D-06 (leaf fallback / root avoidance), selection-layer-only (D-08), switch-gated (D-09).
Output: A new `_compute_child_coverage_score` helper and a rewritten `select_hotspots` body in `HybridClusterHotspotSelector`. No edits to runtime.py, no edits to other selectors.

NOTE: This plan is `autonomous: false` because Task 1 is a `checkpoint:manual` GitNexus impact-analysis gate (no CLI/API equivalent).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/12-cluster-hot-hotspot-selection-redesign/12-CONTEXT.md
@.planning/hotspot-cluster-redesign-EXPLORATION/00-SUMMARY.md
@.planning/phases/12-cluster-hot-hotspot-selection-redesign/12-01-SUMMARY.md
@.planning/phases/12-cluster-hot-hotspot-selection-redesign/12-02-SUMMARY.md

<interfaces>
<!-- Exact current state of the code to be rewritten. Extracted; do not re-explore. -->

Current select_hotspots core (semantic_distribution.py:858–1013) — relevant existing pieces to KEEP:
- embedding-dimension validation (:876–886) — KEEP.
- `vector_by_node` from normalized vector candidates (:902–907) — KEEP as the source for vector_hot.
- `keyword_by_node` + `terms_by_node` + `exact_keyword_nodes` (:912–940) — KEEP as the source for keyword_hot.
- rerank normalization (:942–952) — KEEP (default-off seam, tests at :536).
- SubtreeHotspot construction from `stats` (:997–1011) — KEEP shape exactly.
The UNION candidate set + per-node distribution/fusion + Top-K (:954–995) — REPLACE.

Class constants already present (semantic_distribution.py:851–856):
_VECTOR_WEIGHT=0.40, _KEYWORD_WEIGHT=0.30, _EXACT_KEYWORD_WEIGHT=2.00, _RERANK_WEIGHT=0.20, _DISTRIBUTION_WEIGHT=0.10.

Context now carries (from 12-01): `parent_to_children: dict[UUID|None, tuple[UUID,...]]`.

Chosen concrete defaults (justified vs p6 histogram {0:34, 2:5, 3:6, 5:1, 13:1}):
- _COVERAGE_THETA: float = 0.5  → discriminates 1/3 (fail) from 2/3, 3/3 (pass); θ=1.0 over-prunes 2–3-child sections.
- _MIN_SUPPORT: int = 2        → a PARENT needs ≥2 dual-hot children; single dual-hot child → leaf fallback, not a cluster. Forces 1/3 to fail on both θ and support.
- _VECTOR_HOT_THRESHOLD: float = 0.5  → normalized (min-max) vector score in the upper half.
- _KEYWORD_HOT_MIN_TERMS: int = 1     → ≥1 matched query term against heading_path (term coverage > 0), via existing jieba path upstream.
- Root avoidance: a node whose direct-child count exceeds a breadth cap (reuse the root-avoidance pattern from ClusterHotspotSelector at :757/:770) OR whose level_no == 0 / parent_node_id is None is never promoted by coverage alone.
</interfaces>
</context>

<tasks>

<task type="checkpoint:manual" gate="blocking">
  <name>Task 1 [BLOCKING]: GitNexus impact analysis before editing HybridClusterHotspotSelector</name>
  <read_first>
    - llamaindex_runtime/tree/semantic_distribution.py (HybridClusterHotspotSelector :835–1013, _compute_child_distribution_score :1161–1226, _compute_fusion_score :1229–1261)
    - ./CLAUDE.md (impact-before-edit, HIGH/CRITICAL warn rule)
    - .planning/hotspot-cluster-redesign-EXPLORATION/00-SUMMARY.md (E1 anchors :954, :968, :993)
  </read_first>
  <action>
    Manual GitNexus MCP gate — no CLI/API equivalent. Run `gitnexus_impact({target: "HybridClusterHotspotSelector", direction: "upstream"})` and `gitnexus_impact({target: "select_hotspots", direction: "upstream"})`. Report blast radius (direct callers, affected processes, risk level). Known HIGH/CRITICAL on this path: `_retrieve_tree_hits_from_backend` (CRITICAL), `_map_query_hits_to_backend_hits` (CRITICAL), `SubtreeHotspotSelector`/`RecursiveTreeTraversalRunner`/`QueryHit` (HIGH). If HIGH/CRITICAL is returned, WARN the user explicitly in SUMMARY before editing. Confirm the edit is confined to `HybridClusterHotspotSelector` (the only selector behind `hybrid_cluster`), keeping CRITICAL runtime-path edits incremental and switch-gated. Refresh the GitNexus index first if it reports stale.
  </action>
  <verify>
    <manual>GitNexus MCP step (no automated command). Completion is proven by the file-existence check below.</manual>
  </verify>
  <acceptance_criteria>
    - File-existence gate: 12-03-SUMMARY.md exists and contains a blast-radius table for `HybridClusterHotspotSelector` and `select_hotspots` with risk levels.
    - If HIGH/CRITICAL, an explicit WARNING line is recorded before edits.
  </acceptance_criteria>
  <resume-signal>Type "impact-recorded" once the blast-radius table for both symbols is written to 12-03-SUMMARY.md.</resume-signal>
  <done>Impact recorded; HIGH/CRITICAL surfaced before editing the selector.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement dual-hot coverage selection (D-01/D-02/D-03/D-04/D-05/D-06)</name>
  <read_first>
    - llamaindex_runtime/tree/semantic_distribution.py (select_hotspots :858–1013, class constants :851–856, _build_parent_to_children :313, ClusterHotspotSelector root-avoidance :757/:770)
    - tests/llamaindex_runtime/test_tree_hybrid_hotspot_selector.py (RED tests from 12-02: TestClusterCoverageGate, TestDualHotGate, TestConfigurableTheta, TestLeafFallback)
    - .planning/hotspot-cluster-redesign-EXPLORATION/00-SUMMARY.md (section 9 minimal-implementation sketch)
  </read_first>
  <behavior>
    - 1/3 dual-hot parent → not selected; 2/3 and 3/3 → selected; 3/3 outranks isolated high-vector single node (D-01).
    - vector-only child and keyword-only child excluded from coverage (D-02).
    - θ=0.5 vs θ=0.75 flips the 2/3 parent (D-05).
    - focused dual-hot leaf returned when no parent qualifies; root not promoted by coverage alone (D-06).
    - SubtreeHotspot(node_id=...) shape preserved; no signature change to select_hotspots beyond optional θ/min_support config.
  </behavior>
  <action>
    1. RETAIN the existing fusion weight constants `_VECTOR_WEIGHT=0.40`, `_KEYWORD_WEIGHT=0.30`, `_DISTRIBUTION_WEIGHT=0.10`, `_RERANK_WEIGHT=0.20`, `_EXACT_KEYWORD_WEIGHT=2.00` (:851–856) UNCHANGED — they are still referenced by `_compute_fusion_score` and the preserved fusion-helper test (test_tree_hybrid_hotspot_selector.py:334). ADD SEPARATE NEW constants for the cluster-coverage path (do not overload the old weights): `_COVERAGE_THETA: float = 0.5`, `_MIN_SUPPORT: int = 2`, `_VECTOR_HOT_THRESHOLD: float = 0.5`, `_KEYWORD_HOT_MIN_TERMS: int = 1`, `_ROOT_CHILD_BREADTH_CAP: int = 8` (root has 13 children in p6; cap < 13 excludes root, > 5 keeps the 特斯拉案例 5-child section eligible), plus the new parent-scoring weights `_COVERAGE_WEIGHT: float = 0.50`, `_COVERAGE_VECTOR_WEIGHT: float = 0.30`, `_COVERAGE_SUPPORT_WEIGHT: float = 0.20` (used in step 5). Allow override via optional constructor args `def __init__(self, *, coverage_theta: float = _COVERAGE_THETA, min_support: int = _MIN_SUPPORT) -> None` storing on `self`. Keep `get_hotspot_selector("hybrid_cluster")` returning `HybridClusterHotspotSelector()` with defaults (do NOT change the factory).
    2. Compute hotness sets inside `select_hotspots` after the existing normalization blocks (reuse `vector_by_node` :905 and `keyword_by_node`/`terms_by_node` :921/:920):
       - `vector_hot = {nid for nid, s in vector_by_node.items() if s >= self._VECTOR_HOT_THRESHOLD}`
       - `keyword_hot = {nid for nid, terms in terms_by_node.items() if len(terms) >= self._KEYWORD_HOT_MIN_TERMS}`
       - `dual_hot = vector_hot & keyword_hot`  (INTERSECTION — replaces the old UNION at :955).
    3. Add module-level helper `def _compute_child_coverage_score(*, parent_id: UUID, parent_to_children: dict[UUID | None, tuple[UUID, ...]], dual_hot: set[UUID]) -> tuple[float, int]:` returning `(coverage_ratio, dual_hot_child_count)` where `children = parent_to_children.get(parent_id, ())`, `denom = len(children)`, `hot = sum(1 for c in children if c in dual_hot)`, `ratio = hot/denom if denom else 0.0`. Denominator is the COMPLETE direct-child set (includes no-vector children).
    4. Build candidate PARENTS = the set of `parent_node_id` for every `dual_hot` child. All dual-hot children are GUARANTEED present in `context.node_stats` (vector_hot ⇒ vector-candidate membership ⇒ a node_stats entry exists), so look up the parent DIRECTLY via `context.node_stats[child_id]["parent_node_id"]` — no fallback scan is needed or permitted. For each candidate parent compute coverage via the helper. Select parent as hotspot iff `ratio >= self._coverage_theta and hot_count >= self._min_support` AND it passes root-avoidance: `parent is not None`, `node_stats[parent].get("level_no")` not the top level, and `len(parent_to_children.get(parent, ())) <= self._ROOT_CHILD_BREADTH_CAP`.
    5. Score selected parents by a transparent combination using the NEW coverage weights from step 1 (separate from the old fusion weights): `parent_score = coverage_ratio * self._COVERAGE_WEIGHT + avg(vector_by_node[c] for dual-hot children) * self._COVERAGE_VECTOR_WEIGHT + min(hot_count / 5.0, 1.0) * self._COVERAGE_SUPPORT_WEIGHT` (i.e. 0.50/0.30/0.20). Sort desc; take top `limit`.
    6. Leaf fallback (D-06): if NO parent qualifies, fall back to the strongest dual-hot leaf/node — rank dual_hot nodes (and, if dual_hot empty, the existing `exact_keyword_nodes`/top vector node so focused exact-leaf queries keep their position) by `vector_by_node.get(nid,0)*0.5 + keyword_by_node.get(nid,0)*0.5`, emit up to `limit`. NEVER emit the root via fallback (skip nodes with `parent_node_id is None`/top level unless it is the only candidate). This preserves the `数据清洗标注的具体方法是什么？` leaf result.
    7. Build `SubtreeHotspot` objects using the EXISTING construction block pattern (:997–1011): pull `stats = node_by_id.get(node_id)`, set `reason="cluster_coverage"` for parents and `reason="leaf_fallback"` for fallback nodes, carry `support_count`/`dispersion`/`entropy` from stats. Keep returning `list[SubtreeHotspot]`. Do NOT change the method's return type or the `limit<=0 → []` / empty-`node_stats` → `[]` guards.
    8. Do NOT delete `_compute_child_distribution_score` or `_compute_fusion_score` (they may still be referenced by preserved unit tests / fusion helper test at :334) — leave them as available helpers; the new path simply no longer drives final selection. Do NOT edit runtime.py, route_subtree, or cluster selectors (D-09).
  </action>
  <verify>
    <automated>python -m pytest tests/llamaindex_runtime/test_tree_hybrid_hotspot_selector.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "def _compute_child_coverage_score(" llamaindex_runtime/tree/semantic_distribution.py` matches.
    - `grep -n "vector_hot" llamaindex_runtime/tree/semantic_distribution.py` and `grep -n "keyword_hot" llamaindex_runtime/tree/semantic_distribution.py` both match inside select_hotspots; an intersection (`&` or explicit `dual_hot`) is present.
    - `grep -n "context.parent_to_children" llamaindex_runtime/tree/semantic_distribution.py` matches (denominator sourced from full tree).
    - `grep -n "_COVERAGE_THETA\|_MIN_SUPPORT\|_VECTOR_HOT_THRESHOLD\|_KEYWORD_HOT_MIN_TERMS" llamaindex_runtime/tree/semantic_distribution.py` shows all four constants.
    - (W3) `grep -n "_VECTOR_WEIGHT\|_COVERAGE_THETA" llamaindex_runtime/tree/semantic_distribution.py` shows BOTH the retained old fusion weight `_VECTOR_WEIGHT` AND the new `_COVERAGE_THETA` coexisting (no accidental deletion of the old fusion constants).
    - (B5) The parent lookup reads `context.node_stats[child_id]["parent_node_id"]` for dual-hot children with NO fallback scan; `grep -n "node_stats\[" llamaindex_runtime/tree/semantic_distribution.py` confirms the lookup targets node_stats, and a unit test (or the coverage tests) drives at least one dual-hot child through selection without a KeyError.
    - `python -m pytest tests/llamaindex_runtime/test_tree_hybrid_hotspot_selector.py -q -k "coverage or dual or theta or fallback"` exits 0 (RED→GREEN).
    - `python -m pytest tests/llamaindex_runtime/test_tree_hybrid_hotspot_selector.py -q` exits 0 (all preserved contracts still green).
    - `grep -n "def get_hotspot_selector" llamaindex_runtime/tree/semantic_distribution.py` body still returns SubtreeHotspotSelector/ClusterHotspotSelector for route_subtree/cluster unchanged.
  </acceptance_criteria>
  <done>Coverage + dual-hot selection implemented; 12-02 RED tests are GREEN; preserved contracts pass; rollback selectors untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Selector consumes already-ingested DB-derived `node_stats`/keyword/vector signals plus the in-memory `parent_to_children` map from 12-01. No new external input, auth, or network boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-05 | Spoofing / fabrication of provenance | SubtreeHotspot emission | mitigate | Emit a hotspot only for a `node_id` present in `context.node_stats` (real `node_id`/span backing), exactly as the existing construction block (:997–1011) does. Never synthesize a hotspot id. Preserves Phase 10/11 evidence-bearing-only contract; traversal (unchanged) still returns only evidence-bearing child hits. |
| T-12-06 | Denial of service | coverage loop over parent_to_children | mitigate | Coverage is a single pass over each candidate parent's direct children (bounded by tree fan-out). No recursive subtree walk is added in this plan; if any ancestor walk is reused, keep the existing cycle/depth guards from ClusterHotspotSelector (`_build_path_to_root` visited-set). Root breadth cap also bounds work. |
| T-12-07 | Tampering | configurable θ/min_support | accept | θ/min_support are internal constants/constructor args, not external input. Defaults are safe (θ=0.5, min_support=2). No untrusted caller sets them. |

Block on `high` severity. No high-severity threat present — internal selection logic over already-trusted signals, provenance preserved.
</threat_model>

<verification>
- `python -m pytest tests/llamaindex_runtime/test_tree_hybrid_hotspot_selector.py -q` is fully GREEN.
- Selector still emits `SubtreeHotspot(node_id=...)`; `traverse_tree_for_query` signature untouched (no edit to runtime.py / traversal).
- `grep -n "candidate_node_ids = set(vector_by_node) | set(keyword_by_node)" llamaindex_runtime/tree/semantic_distribution.py` no longer drives final selection (the UNION is replaced by an intersection-based parent-coverage path).
- route_subtree and cluster selectors and the factory are unchanged.
</verification>

<success_criteria>
- A parent with ≥2 dual-hot children at coverage ≥ θ outranks an isolated high-vector single node (D-01).
- vector-only / keyword-only children excluded from coverage (D-02).
- θ configurable; leaf fallback preserves focused exact-leaf positions; root not coverage-promoted (D-05/D-06).
- Output contract and traversal interface preserved (D-08); switch-gated, no deletions (D-09).
</success_criteria>

<output>
After completion, create `.planning/phases/12-cluster-hot-hotspot-selection-redesign/12-03-SUMMARY.md`.
</output>
