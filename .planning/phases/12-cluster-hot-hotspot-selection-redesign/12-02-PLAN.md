---
phase: 12-cluster-hot-hotspot-selection-redesign
plan: 02
type: tdd
wave: 2
depends_on: [01]
files_modified:
  - tests/llamaindex_runtime/test_tree_hybrid_hotspot_selector.py
autonomous: true
requirements: [D-01, D-02, D-05, D-06, D-10]
must_haves:
  truths:
    - "A failing (RED) test asserts a parent with coverage 1/3 dual-hot children is NOT a hotspot (below θ=0.5 and below min_support=2) (D-01)"
    - "A failing (RED) test asserts a parent with coverage 2/3 and 3/3 dual-hot children IS a hotspot and outranks an isolated high-vector single node (D-01)"
    - "A failing (RED) test asserts a vector-only child and a keyword-only child are EXCLUDED from coverage (dual-hot intersection, not union) (D-02)"
    - "A failing (RED) test asserts a focused exact-leaf query returns its strong dual-hot leaf even when no parent meets θ (leaf fallback) (D-06)"
    - "A failing (RED) test asserts the root (13-child analog) is NOT promoted by coverage alone (D-06)"
    - "A failing (RED) test asserts θ is configurable (passing a different θ flips a 2/3 parent from selected to not-selected) (D-05)"
    - "Existing preserved contract tests (registration, context, normalization, rerank-off, anti-hardcode, jieba) remain present and unmodified in intent (D-10)"
    - "The two exact-heading prioritization tests (:359, :447) from exploration §6.1 are explicitly classified preserve-as-is OR rewritten to coverage+dual-hot semantics — neither left unhandled (D-10)"
  artifacts:
    - path: "tests/llamaindex_runtime/test_tree_hybrid_hotspot_selector.py"
      provides: "Rewritten coverage+dual-hot distribution tests; θ-configurable and leaf-fallback tests"
      contains: "coverage"
  key_links:
    - from: "new coverage tests"
      to: "selector coverage helper (to be implemented in 12-03)"
      via: "import of the new coverage helper / select_hotspots with parent_to_children"
      pattern: "coverage|child_hot|dual"
---

<objective>
Author the RED tests that define cluster-hot semantics BEFORE the selector implementation lands (TDD). This plan rewrites the distribution-scoring tests to coverage-ratio + dual-hot semantics and adds θ-configurability and leaf-fallback tests, so that Plan 12-03's implementation has an executable specification. These tests are expected to FAIL at the end of this plan (RED) because the coverage helper / dual-hot gate does not exist yet — that is the intended state.

Purpose: Lock the contract for D-01 (coverage ≥ θ AND support ≥ min_support), D-02 (dual-hot intersection), D-05 (configurable θ), D-06 (leaf fallback + root avoidance) as failing tests. Preserve all D-10 contract tests.
Output: A rewritten `TestChildDistributionScoring` (now coverage-based) plus new `TestClusterCoverageGate`, `TestDualHotGate`, `TestLeafFallback`, and `TestConfigurableTheta` classes — all RED.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/12-cluster-hot-hotspot-selection-redesign/12-CONTEXT.md
@.planning/phases/12-cluster-hot-hotspot-selection-redesign/12-RESEARCH.md
@.planning/hotspot-cluster-redesign-EXPLORATION/00-SUMMARY.md
@.planning/phases/12-cluster-hot-hotspot-selection-redesign/12-01-SUMMARY.md

<interfaces>
<!-- Contracts to write tests against. The coverage helper does NOT exist yet (RED). -->

Chosen concrete defaults (justified against p6 histogram {0:34, 2:5, 3:6, 5:1, 13:1}) — tests MUST use these:
- θ (coverage threshold) default = 0.5 — H2 sections have 2–3 children; 1/3 fails, 2/3 and 3/3 pass; θ=1.0 over-prunes.
- min_support default = 2 — at least 2 dual-hot children to call a PARENT a cluster hotspot; a single dual-hot child is leaf-fallback territory, not a cluster. Forces 1/3 to fail on BOTH θ and support.
- vector_hot default = normalized vector score ≥ 0.5 (upper half of min-max-normalized range).
- keyword_hot default = ≥ 1 matched query term against heading_path (term coverage > 0), via existing jieba `_extract_keywords_from_query`.

Existing HotspotSelectionContext now has `parent_to_children` (added in 12-01):
```python
HotspotSelectionContext(
    query_text, query_embedding, node_stats, tree_signals,
    vector_candidates: list[NodeSemanticHit], keyword_hits: list[KeywordSpanHit],
    rerank_scores=None, parent_to_children: dict[UUID|None, tuple[UUID,...]] = {},
)
```

NodeSemanticHit (semantic_distribution.py:528): node_id, similarity, heading_path, parent_node_id, path_to_root.
KeywordSpanHit (semantic_distribution.py:497): span_id, node_id, score, matched_terms, source.
SubtreeHotspot (semantic_distribution.py:485): node_id, score, reason, support_count, dispersion, entropy.

Tests to REWRITE (D-10): TestChildDistributionScoring at line 195 — specifically
`test_parent_with_multiple_child_hits_beats_isolated_high_score_node` (:198).
Tests to PRESERVE unchanged (D-10): registration (:20), context (:62), KeywordSpanHit (:141),
normalization (:164), fusion helper (:312/:334), default-off rerank seam (:536),
anti-hardcode (:587), jieba extraction (:643), config registration (:737).
</interfaces>
</context>

<feature>
  <name>Cluster-hot coverage + dual-hot test specification (RED)</name>
  <files>tests/llamaindex_runtime/test_tree_hybrid_hotspot_selector.py</files>
  <behavior>
    Coverage gate (D-01), all using θ=0.5, min_support=2:
    - parent with 3 direct children, 1 dual-hot → coverage 1/3 → parent NOT a hotspot.
    - parent with 3 direct children, 2 dual-hot → coverage 2/3 ≥ θ AND support 2 ≥ min_support → parent IS a hotspot.
    - parent with 3 direct children, 3 dual-hot → coverage 1.0 → parent IS a hotspot AND outranks an isolated single node with very high vector but no sibling support.
    Dual-hot gate (D-02):
    - a child that is vector-hot only (high similarity, zero matched keyword terms) does NOT count toward coverage.
    - a child that is keyword-hot only (matched term in heading, low/zero vector) does NOT count toward coverage.
    - construct a parent where 2 children are vector-only and keyword-only respectively and only 1 is dual-hot → coverage 1/3 → NOT a hotspot (proves union→intersection fix).
    Configurable θ (D-05):
    - same 2/3 parent: with θ=0.5 → selected; re-run with θ=0.75 → NOT selected. θ passed via selector config (constructor arg or context — see 12-03 contract note).
    Leaf fallback + root avoidance (D-06):
    - focused exact-leaf query: only one leaf is dual-hot, its parent coverage < θ → the dual-hot leaf is still returned as a result/hotspot (position preserved).
    - root analog with many children (≥5) where < θ fraction are dual-hot → root NOT promoted by coverage alone.
    Denominator from full tree (D-07 consumed here):
    - include a no-vector child in `parent_to_children` (absent from node_stats); coverage denominator must count it (so 1 dual-hot of 3 stays 1/3, not 1/2).
  </behavior>
  <implementation>
    1. Rewrite `TestChildDistributionScoring.test_parent_with_multiple_child_hits_beats_isolated_high_score_node` into coverage semantics: build `node_stats` + `parent_to_children` + `vector_candidates` + `keyword_hits` so the parent has exactly 3 direct children with a controlled dual-hot count, and assert selection outcome via `HybridClusterHotspotSelector.select_hotspots(context=..., limit=...)` rather than the old `_compute_child_distribution_score` numeric comparison. Keep the other two tests in that class (`...requires_parent_node_id...`, `...requires_level_no...`) intact — they validate `_build_node_stats` contract and still hold.
    2. Add new test classes: `TestClusterCoverageGate` (1/3, 2/3, 3/3), `TestDualHotGate` (vector-only excluded, keyword-only excluded, mixed 1/3), `TestConfigurableTheta` (0.5 vs 0.75 flip), `TestLeafFallback` (focused leaf preserved, root not promoted).
    3. Reference the coverage helper the implementation will expose. Per the 12-03 contract, tests may either (a) call `select_hotspots` end-to-end with a constructed context (preferred — black-box), or (b) import a named helper `_compute_child_coverage_score`. Use (a) as the primary assertion path so tests survive internal refactors; add at most one direct helper-unit test that imports `_compute_child_coverage_score` to lock the signature.
    4. Use `pytest.mark`-free plain test functions consistent with the existing file style. Name tests so `-k coverage`, `-k dual`, `-k fallback`, `-k theta` each select the right subset (the VALIDATION map keys on `-k coverage` and `-k fallback`).
    5. Do NOT modify any preserved contract test (registration/context/normalization/rerank-off/anti-hardcode/jieba/config). Do NOT introduce p6-specific keyword literals into production-pathing tests (anti-hardcode rule) — use neutral synthetic headings/terms for the coverage/dual-hot tests; the p6 DNA regression stays in the cluster test file (Plan 12-04).
    6. (B4 / D-10 full §6.1 rewrite scope) Exploration 00-SUMMARY.md §6.1 lists FIVE tests in the rewrite surface. Beyond the distribution test in step 1, explicitly handle the two exact-heading prioritization tests and record the classification in the SUMMARY:
       - `test_selector_prefers_multi_term_heading_match_over_broad_single_term` (test_tree_hybrid_hotspot_selector.py:359)
       - `test_selector_prioritizes_exact_heading_match_over_high_vector_broad_match` (test_tree_hybrid_hotspot_selector.py:447)
       For EACH, choose ONE disposition and apply it now:
       (a) REWRITE to cluster-hot semantics: keep the assertion that the exact-heading LEAF is still returned/ranked (leaf fallback preserves it), but add sibling/parent structure so the test ALSO asserts a broad high-vector parent does NOT outrank the coverage rule — i.e. the exact leaf wins as a leaf result, not because a cold parent was promoted; OR
       (b) PRESERVE-AS-IS with an explicit acceptance criterion that the test stays GREEN unchanged after 12-03 lands (only valid if the test's existing assertion is still satisfied by the leaf-fallback path — the exact-heading single node remains a valid result).
       Default recommendation: PRESERVE both as leaf-fallback coverage (they assert single exact-heading nodes win, which the leaf fallback in 12-03 preserves) UNLESS running them after 12-03 shows the coverage path changed their outcome, in which case REWRITE per (a). Do NOT leave either test unhandled or silently failing.
  </implementation>
</feature>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Test-only plan. No production code edited. No external input, auth, or network boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-04 | Tampering (test-as-spec drift) | rewritten distribution tests | mitigate | Preserve all D-10 contract tests untouched; rewrite ONLY the distribution-scoring test that encoded the old union semantics. Anti-hardcode test stays green to forbid domain-specific literals. |

No new high-severity threats — this plan adds no runtime code and no provenance-emitting path.
</threat_model>

<verification>
- `python -m pytest tests/llamaindex_runtime/test_tree_hybrid_hotspot_selector.py -q -k "coverage or dual or theta or fallback"` runs and the NEW tests FAIL (RED) — implementation lands in 12-03.
- `python -m pytest tests/llamaindex_runtime/test_tree_hybrid_hotspot_selector.py -q -k "registration or context or normaliz or rerank or hardcode or jieba or config"` stays GREEN (preserved contracts unaffected).
- `grep -c "def test_" tests/llamaindex_runtime/test_tree_hybrid_hotspot_selector.py` increased relative to baseline (new coverage/dual-hot/fallback/theta tests added). Use `grep -v '^#' | grep -c "def test_"` to avoid counting commented lines.
- (B4) The SUMMARY records a per-test disposition (preserve-as-is OR rewritten) for `test_selector_prefers_multi_term_heading_match_over_broad_single_term` (:359) and `test_selector_prioritizes_exact_heading_match_over_high_vector_broad_match` (:447). Both are named in `grep -n "prefers_multi_term_heading_match\|prioritizes_exact_heading_match" tests/llamaindex_runtime/test_tree_hybrid_hotspot_selector.py`. After 12-03, `python -m pytest tests/llamaindex_runtime/test_tree_hybrid_hotspot_selector.py -q -k "prefers_multi_term_heading_match or prioritizes_exact_heading_match"` exits 0.
</verification>

<success_criteria>
- New coverage/dual-hot/θ/leaf-fallback tests exist and are RED (D-01/D-02/D-05/D-06).
- The old union-semantics distribution test is rewritten to coverage semantics (D-10).
- All preserved contract tests remain present and GREEN (D-10).
- Tests use the chosen defaults θ=0.5, min_support=2 and assert the 1/3, 2/3, 3/3 outcomes.
</success_criteria>

<output>
After completion, create `.planning/phases/12-cluster-hot-hotspot-selection-redesign/12-02-SUMMARY.md` (record which tests are RED and the exact `-k` selectors).
</output>
