# Retrieval fixes 2026-09-06 — migration 022 promotion + OCR lazy-init RECORD

## Authorization chain
- User (2026-09-06, verbatim): "官方迁移链缺一块…这个部分我确认" — keyword FTS columns promoted into the official root migration chain.
- User (2026-09-06, verbatim): "每次提问这个新建一个TRELLIS任务吧，然后把这个OCR改成如果识别到有图片才开始迁移，启动OCR引擎" — OCR lazy-init executed; Trellis task creation BLOCKED: `trellis_task_create` error '项目不在 allowlist 内' (E:\github\rag not in trellis-workflow.allowlist). User must add the project to the Web GUI allowlist before the task can be created.

## Part 1 — migration 022 promotion (DONE, live-verified)
- New file `llamaindex_runtime/registry/migrations/022_keyword_fts_indexes.sql`: CREATE EXTENSION pg_trgm; ALTER canonical_spans ADD raw_text_tsvector tsvector; backfill UPDATE; trigger `canonical_spans_tsvector_update` (BEFORE INSERT OR UPDATE OF raw_text); 3 indexes — idx_spans_fts (GIN tsvector), idx_spans_trgm (GIN gin_trgm_ops), idx_spans_version_page (version_id,page_no). All IF NOT EXISTS idempotent.
- `llamaindex_runtime/registry/migration_catalog.py`: FULL_MIGRATION_CATALOG 21→22 (022 appended; SSOT — run_migrations.py KEY_MIGRATIONS alias follows).
- Tail contract evolved 020/021→021/022 in 4 files: `tests/llamaindex_runtime/okf/test_e2b_migration_catalog.py` (docstring, new 022-after-021 test, final_entry, KEY_MIGRATIONS[-1], pageindex order), `tests/llamaindex_runtime/okf/test_e2b_migration_gate_runner.py` (:102 _CATALOG_TAIL, :386-390 assertions), `verification/phase16-raw-corpus-entity-layer/run_e2b_migration_gate.py` (:93 _CATALOG_TAIL, :163 docstring), `run_c2_entry_acceptance.py` (:144 _CATALOG_TAIL, :471 message 'must end with 021 and 022').
- NOT touched (historical evidence-replay contracts): `run_e2b_migration_gate_live.py:43` and `test_e2b_migration_gate_live_harness.py:55` keep (019,020).
- Live proof `.tmp/verify_022_keyword_fts.py` (disposable pgvector:pg16, 127.0.0.1:15444, no tmpfs): 22 migrations applied, trigger backfill zero NULL, 3 indexes present, query_spans_by_keyword('air quality')→1 row, retrieve_keyword_hits('AQI')→1 hit. Container removed, credentials discarded.
- pytest: test_e2b_migration_catalog + test_e2b_migration_gate_runner = 82 passed / 3 failed — the 3 are the documented PRE-EXISTING evidence-leftover failures (test_blocked_main_zero_parser_connect_and_orchestration_attempt / test_run_redacted_cli_maps_value_error_to_fixed_message / test_cli_subprocess_default_blocked_with_sanitized_env; all assert e2b_migration_gate_evidence.md absent while a historical file exists on disk). Zero new failures.
- Impact method: GitNexus CLI unavailable (precedent); manual mapping of 97 FULL_MIGRATION_CATALOG references converged to the 4-file list above.

## Part 2 — OCR lazy-init (DONE, TDD RED→GREEN)
- Root cause: query-time tree path (`llamaindex_runtime/tree/runtime.py:14` → `build_docling_bundle`) constructed a fresh DocumentConverter with docling defaults (do_ocr=True) → two RapidOCR GPU inits (~13s each) per question even for text-only PDFs.
- Fix `llamaindex_runtime/ingestion/bundle.py`: new `_pdf_has_raster_images(path)` (pypdf pre-scan /Resources→/XObject→/Subtype==/Image; any import/parse failure → fail-safe True preserving old behavior) + `_build_default_converter(path)` (non-PDF→plain DocumentConverter(); PDF→PdfPipelineOptions(do_ocr=has_images) + PdfFormatOption for InputFormat.PDF only). `build_docling_bundle` converter=None branch now calls `_build_default_converter(resolved_path)`. All heavy imports stay function-local (W7 light-import contract intact).
- Canonical tests `tests/llamaindex_runtime/test_ingestion_docling_ocr_lazy.py` (5): 2 real-PDF probes (reportlab no-image / with-image via PIL 1x1 PNG) + 3 monkeypatch fake-docling probes capturing do_ocr (no-image False / image True / non-PDF format_options None). RED=ImportError → GREEN 5/5 (1.76s).
- Smoke evidence (verification/smoke_retrieval_rerank_2026-09-05.py, PASS): query-time log shows ZERO RapidOCR/engine init lines (only sentence_transformers pynvml) — OCR engine never started for the text-only PDF across two hybrid queries.

## Part 3 — retrieval+rerank sanity smoke (user question ②)
- Script `verification/smoke_retrieval_rerank_2026-09-05.py`: reportlab 2-topic English PDF → disposable pgvector container → 22 migrations → register_document + 6 CanonicalSpan → MiniLM local embedder → per query: query(mode='hybrid', limit=8) sanity + manual 3-path retrieve_* → fuse_candidates → rerank_pipeline, printed RerankedHit list.
- Q1 air-quality: fused top1 0.7584 (AQI paragraph), scores descending, top3_on_topic 2/3; Q2 composting: top1 0.7796, 3/3 on-topic. per-path keyword=0 (expected: ILIKE/AND semantics do not match natural-language questions — by design; vector+tree carry), vector=3 tree=1 → fused=4 → reranked=4 (rerank scores 0.0164/0.0164/0.0161).
- SMOKE RESULT: PASS; cleanup container removed + credentials discarded.

## Final verification
- Post-black rerun: test_ingestion_docling_ocr_lazy + test_okf_lazy_heavy_import + test_e2b_migration_catalog = 16 passed (5.50s).
- mypy bundle.py: zero errors in bundle.py itself (2 pre-existing pipeline.py arg-type errors remain, in pre-existing baseline); full-repo mypy baseline unchanged at 'Found 59 errors in 18 files' (pre-existing).
- black reformatted touched files; tests green post-format.

## Bookkeeping
- STATE.md: status=keyword_fts_022_promoted_ocr_lazy_init, last_updated 2026-09-06T01:05:00Z.
- Open: Trellis task creation pending user adding E:\github\rag to trellis-workflow.allowlist.
