---
phase: 16-raw-corpus-entity-layer
plan: 16
type: tdd
wave: 6
macro_wave: W6
depends_on: [16-03, 16-08, 16-09]
files_modified:
  - llamaindex_runtime/entity/coref_rules.py
  - tests/llamaindex_runtime/entity/test_coref_rules.py
autonomous: true
requirements: [R-OKF-09]
must_haves:
  truths:
    - Local conservative coreference rules build clusters only within the same document/version and only over already-selected resolved mentions; they never merge canonical entities and never write relation_mentions or model values.
    - Each cluster supports a per-cluster tombstone soft delete so a cluster can be retired without deleting rows.
    - The rules are pure and deterministic: identical inputs produce identical clusters, and no canonical merge/entity creation occurs.
  artifacts:
    - path: llamaindex_runtime/entity/coref_rules.py
      provides: Same-document/version conservative coreference rule engine with per-cluster tombstone semantics.
    - path: tests/llamaindex_runtime/entity/test_coref_rules.py
      provides: RED tests for local clustering, tombstone soft-delete, and no-merge guarantees.
  key_links:
    - from: llamaindex_runtime/entity/coref_rules.py
      to: llamaindex_runtime/entity/resolution.py
      via: resolved mentions input
      pattern: ResolutionResult
    - from: llamaindex_runtime/entity/coref_rules.py
      to: llamaindex_runtime/registry/migrations/021_ner_coref_clusters.sql
      via: coref_cluster_mentions membership
      pattern: coref_cluster_mentions
---

<objective>
Implement the C2 local coreference rule engine that produces conservative, reversible, default-off clusters without any canonical merge.

Purpose: R-OKF-09 requires Chinese local coreference chains as a conservative, disable-able, default-off capability whose functional readiness gate lives in Phase 16; net benefit is adjudicated at Phase 19 G3.
Output: TDD-tested pure coref rules with tombstone soft-delete semantics.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/16-raw-corpus-entity-layer/16-RESEARCH.md
@.planning/OKF-LOW-TOKEN-GAPS-SUPPLEMENT-HANDOFF-2026-07-12.md
@llamaindex_runtime/entity/contracts.py
@llamaindex_runtime/entity/resolution.py
</context>

<tasks>

<tdd_sequence>Task 1 = RED → Task 2 = GREEN → Task 2 = REFACTOR; each selector result recorded separately.</tdd_sequence>

<task type="auto" tdd="true">
  <name>Task 1 (RED): Write RED coref-rules tests</name>
  <read_first>@llamaindex_runtime/entity/contracts.py</read_first>
  <files>tests/llamaindex_runtime/entity/test_coref_rules.py</files>
  <behavior>
    - Given resolved mentions within one document/version, the rules produce coref_clusters whose memberships are all same-scope, with no mention in more than one cluster and no cluster spanning documents/versions.
    - Applying a per-cluster tombstone marks the cluster retired (soft delete) without deleting membership rows and without affecting other clusters.
    - The rules never create or merge canonical entities, never write relation_mentions, and never attach model confidence values; identical inputs give byte-identical clusters.
    - An empty or single-mention input produces no cluster; a "rules" resolver gate is required before clustering (default off).
  </behavior>
  <action>Write failing tests for `llamaindex_runtime/entity/coref_rules.py`. Specify `build_coref_clusters(resolved: ResolutionResult, *, coref_rules_version: str) -> CorefClusterSet` where each `CorefCluster` (frozen) carries cluster_id, version_id, member_mention_ids, tombstoned: bool, and a rules provenance record, plus `CorefClusterSet.tombstone(cluster_id) -> CorefClusterSet` returning a new set (immutability). Encode the local conservative rules (same document/version only; lexical/structural cues only; no canonical merge; no model values). Assert determinism under shuffled input, same-scope enforcement, tombstone soft-delete immutability, and that clustering requires an explicit resolver gate. No model, DB, or network access. Run the selector in `<verify>` and record the expected RED result: the tests FAIL with ImportError/NameError because `llamaindex_runtime/entity/coref_rules.py` does not exist yet. Do NOT write any implementation in this RED task.</action>
  <acceptance_criteria>RED coref-rules tests exist under tests/llamaindex_runtime/entity/test_coref_rules.py; running the selector fails with ImportError/NameError for the missing `llamaindex_runtime/entity/coref_rules.py` (recorded RED); no coref_rules implementation file was created in this task.</acceptance_criteria>
  <verify><automated>rtk env -u DATABASE_URL -u FORMAL_RUNTIME_DATABASE_URL -u OKF_MIGRATION_TEST_DATABASE_DISPOSABLE -u OKF_REBUILD_EXPECTED_DATABASE -u OKF_FAILURE_AUDIT_ACCEPTANCE -u OKF_REBUILD_DOCKER_ACCEPTANCE -u OKF_E2A_DISPOSABLE_TEST_AUTHORIZED -u OKF_E2B_DISPOSABLE_TEST_AUTHORIZED -u OKF_E2B_MIGRATION_TEST_AUTHORIZED -u OKF_E2B_MODELSCOPE_MIRROR_AUTHORIZED -u OKF_E2B_RANER_SMOKE_AUTHORIZED -u OKF_E2B_C2_ENTRY_AUTHORIZED pytest tests/llamaindex_runtime/entity/test_coref_rules.py -q</automated></verify>
  <done>Failing tests specify same-scope clustering, per-cluster tombstone soft delete, no-merge/no-model-value guarantees, and default-off gating.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 (GREEN → REFACTOR): Implement the coref rules engine</name>
  <read_first>@llamaindex_runtime/entity/resolution.py</read_first>
  <files>llamaindex_runtime/entity/coref_rules.py, tests/llamaindex_runtime/entity/test_coref_rules.py</files>
  <behavior>
    - build_coref_clusters returns a frozen CorefClusterSet with same-version_id memberships, deterministic cluster_id, and a per-cluster tombstone soft delete that retains membership rows.
    - Empty/single-mention input produces no cluster; without an explicit "rules" resolver mode the result is empty.
    - No canonical entity merge, relation_mentions write, or model confidence value ever occurs.
  </behavior>
  <action>GREEN: write the minimal implementation so the SAME selector passes; record the GREEN selector result. REFACTOR: then apply only behavior-preserving cleanup and re-run the SAME selector; record the REFACTOR selector result separately. Do not alter test assertions between GREEN and REFACTOR. Implement `llamaindex_runtime/entity/coref_rules.py`: `build_coref_clusters(resolved, *, coref_rules_version)` that (1) requires an explicit resolver mode "rules" (default off; otherwise returns an empty set), (2) restricts clustering to mentions sharing the same version_id, (3) applies conservative same-scope lexical/structural rules only, (4) returns a frozen `CorefClusterSet` whose `tombstone(cluster_id)` returns a new set with the cluster's `tombstoned=True` and all membership rows retained (soft delete), (5) never merges canonical entities, never writes relation_mentions, and never assigns model confidence values. Use E2b `deterministic_id` for cluster_id over (version_id, sorted member mention ids, coref_rules_version). Export `build_coref_clusters`, `CorefCluster`, and `CorefClusterSet` from `llamaindex_runtime/entity/__init__.py`. Do NOT add the C2 schema migration, config switch, or live acceptance here; those are the C2 entry plan.</action>
  <acceptance_criteria>The same selector passes twice: once after minimal GREEN implementation and once after behavior-preserving REFACTOR; both selector results are recorded separately; the assertions in test_coref_rules.py are unchanged between the GREEN and REFACTOR runs.</acceptance_criteria>
  <verify><automated>rtk env -u DATABASE_URL -u FORMAL_RUNTIME_DATABASE_URL -u OKF_MIGRATION_TEST_DATABASE_DISPOSABLE -u OKF_REBUILD_EXPECTED_DATABASE -u OKF_FAILURE_AUDIT_ACCEPTANCE -u OKF_REBUILD_DOCKER_ACCEPTANCE -u OKF_E2A_DISPOSABLE_TEST_AUTHORIZED -u OKF_E2B_DISPOSABLE_TEST_AUTHORIZED -u OKF_E2B_MIGRATION_TEST_AUTHORIZED -u OKF_E2B_MODELSCOPE_MIRROR_AUTHORIZED -u OKF_E2B_RANER_SMOKE_AUTHORIZED -u OKF_E2B_C2_ENTRY_AUTHORIZED pytest tests/llamaindex_runtime/entity/test_coref_rules.py -q</automated></verify>
  <done>All coref-rules tests pass; clustering is local, conservative, tombstonable, default-off, and performs no canonical merge.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Resolved mentions → clusters | Clustering must never cross document/version boundaries or merge canonical entities. |
| Cluster tombstones → rows | A soft delete must be reversible and scoped to one cluster. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-16-57 | T | scope isolation | mitigate | Same-version_id only; cross-scope membership rejected by construction. |
| T-16-58 | D | canonical merge | mitigate | No entities DML, no relation_mentions, no model values (C2 contract). |
| T-16-59 | I | tombstone integrity | mitigate | Immutable CorefClusterSet; tombstone returns a new set with rows retained. |
| T-16-60 | R | default-off | mitigate | Requires explicit "rules" resolver mode; otherwise empty set (R-OKF-09 default off). |
</threat_model>

<verification>
Run only pure unit selectors. No model, DB, or network access. Determinism verified under shuffled input.
</verification>

<success_criteria>
- Clusters are local to one document/version and conservative.
- Per-cluster tombstone soft delete is immutable and reversible.
- The rules are pure, default-off, and perform no canonical merge.
</success_criteria>

<output>
After completion, create `.planning/phases/16-raw-corpus-entity-layer/16-16-SUMMARY.md`.
</output>
