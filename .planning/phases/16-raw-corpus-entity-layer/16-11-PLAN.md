---
phase: 16-raw-corpus-entity-layer
plan: 11
type: execute
wave: 4
macro_wave: W3
depends_on: [16-01, 16-09, 16-10, 16-07]
files_modified:
  - scripts/rebuild_from_okf_e2b.py
  - tests/llamaindex_runtime/okf/test_rebuild_from_okf_e2b_cli.py
autonomous: true
requirements: [R-OKF-01, R-OKF-04, R-OKF-06]
must_haves:
  truths:
    - A CLI entry point (analogous to scripts/rebuild_from_okf_e2a.py) routes a bundle through config → extractor → segmentation → merger → resolution → materialization, defaulting to off and refusing to run when RAG_ENTITY_EXTRACTOR is not raner.
    - The runner prints only redacted typed outcomes and never prints or logs DATABASE_URL, credentials, or the disposable target.
    - Live materialization and live RaNER execution are not performed by this plan; the runner wires the composed pipeline and its tests use spies/fakes.
    - The runner drives the E2b repository's reconcile transaction on E2a's EXACT shared advisory key okf:e2a:parent:{document_id}:{version_id} (never okf:e2b:parent), so E2b and E2a serialize per document/version on the same lock.
  artifacts:
    - path: scripts/rebuild_from_okf_e2b.py
      provides: E2b CLI orchestrator with redacted outcomes, default-off enforcement, and E2a-shared per-document/version locking.
    - path: tests/llamaindex_runtime/okf/test_rebuild_from_okf_e2b_cli.py
      provides: Tests for argument parsing, switch gating, and redacted outcome formatting.
  key_links:
    - from: scripts/rebuild_from_okf_e2b.py
      to: llamaindex_runtime/entity/extractor.py
      via: EntityExtractor.extract
      pattern: EntityExtractor
    - from: scripts/rebuild_from_okf_e2b.py
      to: llamaindex_runtime/entity/materialization_repository.py
      via: E2bMaterializationRepository.reconcile_document
      pattern: reconcile_document
    - from: scripts/rebuild_from_okf_e2b.py
      to: llamaindex_runtime/config.py
      via: rag_entity_extractor switch
      pattern: rag_entity_extractor
---

<objective>
Build the E2b CLI runner that wires the composed pipeline into a redacted, switch-gated entry point mirroring the E2a rebuild CLI.

Purpose: R-OKF-01 requires E2b to close the derived-layer rebuild from the OKF bundle; the runner is the executable surface the live acceptance gate will invoke.
Output: E2b runner CLI with default-off enforcement, redacted outcome formatting, E2a-shared per-document/version locking, plus CLI tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/16-raw-corpus-entity-layer/16-RESEARCH.md
@.planning/phases/16-raw-corpus-entity-layer/16-PATTERNS.md
@scripts/rebuild_from_okf_e2a.py
@scripts/_rebuild_database_connection.py
@llamaindex_runtime/config.py
@llamaindex_runtime/entity/extractor.py
@llamaindex_runtime/entity/materialization_repository.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Specify runner CLI behavior with tests</name>
  <read_first>@scripts/rebuild_from_okf_e2a.py</read_first>
  <files>tests/llamaindex_runtime/okf/test_rebuild_from_okf_e2b_cli.py</files>
  <action>Write tests for `scripts/rebuild_from_okf_e2b.py` before implementation. Assert: (1) the CLI rejects `--rebuild` when `RAG_ENTITY_EXTRACTOR` is not `raner` (fail-closed, never defaulting on); (2) `--verify-roundtrip` remains a no-DB path; (3) the formatted outcome contains only redacted counters (outcome, manifest_sha256, per-table DML counts, failure-audit outcomes) and never contains a DSN, credential, corpus body, or span text; (4) the runner's connection-support loading mirrors `rebuild_from_okf_e2a.py` by loading `_rebuild_database_connection.py` fresh and never passing a raw URI to psycopg.connect; (5) the runner wires `EntityExtractor`, `E2bMaterializationRepository`, and `E2bFailureAudit` from injected factories so tests use fakes; (6) the runner delegates per-document/version locking to the repository's reconcile transaction (the runner itself passes document_id/version_id through, and the repository acquires the shared advisory xact lock on `okf:e2a:parent:{document_id}:{version_id}`). No database or model execution occurs in these tests.</action>
  <acceptance_criteria>Tests specify default-off gating, redacted outcomes, fresh-connection-support loading, injectable pipeline wiring, and E2a-shared lock delegation.</acceptance_criteria>
  <verify><automated>rtk env -u DATABASE_URL -u FORMAL_RUNTIME_DATABASE_URL -u OKF_MIGRATION_TEST_DATABASE_DISPOSABLE -u OKF_REBUILD_EXPECTED_DATABASE -u OKF_FAILURE_AUDIT_ACCEPTANCE -u OKF_REBUILD_DOCKER_ACCEPTANCE -u OKF_E2A_DISPOSABLE_TEST_AUTHORIZED -u OKF_E2B_DISPOSABLE_TEST_AUTHORIZED -u OKF_E2B_MIGRATION_TEST_AUTHORIZED -u OKF_E2B_MODELSCOPE_MIRROR_AUTHORIZED -u OKF_E2B_RANER_SMOKE_AUTHORIZED -u OKF_E2B_C2_ENTRY_AUTHORIZED pytest tests/llamaindex_runtime/okf/test_rebuild_from_okf_e2b_cli.py -q</automated></verify>
  <done>Failing tests specify switch gating, redaction, connection-support isolation, injectable wiring, and E2a-shared lock delegation.</done>
</task>

<task type="auto">
  <name>Task 2: Implement the E2b runner CLI</name>
  <read_first>@scripts/rebuild_from_okf_e2a.py</read_first>
  <files>scripts/rebuild_from_okf_e2b.py, tests/llamaindex_runtime/okf/test_rebuild_from_okf_e2b_cli.py</files>
  <action>Implement `scripts/rebuild_from_okf_e2b.py` modeled on `rebuild_from_okf_e2a.py`: load the fresh `_rebuild_database_connection.py` support module (never trust `sys.modules`), resolve the bundle root from CLI/`OKF_BUNDLE_ROOT`, require `RAG_ENTITY_EXTRACTOR=raner` for `--rebuild` (fail closed otherwise), build the composed pipeline from injected factories (EntityExtractor with adapter/segmenter/merger/resolver, E2bMaterializationRepository, E2bFailureAudit), admit the OKF bundle through the established `OKFParser`/`read_raw_pair` path, and produce a redacted `E2bRebuildOutcome` (outcome, manifest_sha256, primary_dml_by_table, failure_audit_outcome, post_rollback_failure_audit_outcome). The runner iterates documents deterministically, wrapping each document/version in the E2b repository's reconcile transaction with the FIXED E2a/E2b lock order: acquire the SHARED parent advisory xact lock using the EXACT E2a key string `okf:e2a:parent:{document_id}:{version_id}` — the SAME key E2a uses for the same document/version, NEVER `okf:e2b:parent` — then lock the document_versions row FOR UPDATE, then lock/load existing E2b-owned state (mentions, okf_e2b_node_link_ownership ledger, bridges) → full preflight → stable upsert desired → delete stale E2b-owned → commit; on failure roll back ALL insert/update/delete restoring the original committed state, close the primary connection, append okf_e2b_failure_audit on a fresh connection, and continue to the next document. Never print/log DATABASE_URL, credentials, raw corpus text, or disposable targets. Do NOT run the live materialization here; the disposable full-corpus acceptance is a separate gate. Keep the runner consistent with the E2a denylist so E2b never writes `chunk_entity_links` or `okf_rebuild_failure_audit`.</action>
  <acceptance_criteria>Running the CLI test selector passes after implementation; the tests prove default-off switch gating, redacted outcome formatting with no DSN/credential/corpus leakage, fresh-connection-support loading, injectable pipeline wiring, and E2a-shared lock delegation; the runner never executes a live database or model on its own.</acceptance_criteria>
  <verify><automated>rtk env -u DATABASE_URL -u FORMAL_RUNTIME_DATABASE_URL -u OKF_MIGRATION_TEST_DATABASE_DISPOSABLE -u OKF_REBUILD_EXPECTED_DATABASE -u OKF_FAILURE_AUDIT_ACCEPTANCE -u OKF_REBUILD_DOCKER_ACCEPTANCE -u OKF_E2A_DISPOSABLE_TEST_AUTHORIZED -u OKF_E2B_DISPOSABLE_TEST_AUTHORIZED -u OKF_E2B_MIGRATION_TEST_AUTHORIZED -u OKF_E2B_MODELSCOPE_MIRROR_AUTHORIZED -u OKF_E2B_RANER_SMOKE_AUTHORIZED -u OKF_E2B_C2_ENTRY_AUTHORIZED pytest tests/llamaindex_runtime/okf/test_rebuild_from_okf_e2b_cli.py -q</automated></verify>
  <done>Runner CLI tests pass; the runner is switch-gated, redacted, injectable, uses the E2a-shared advisory key, and never executes a live database or model on its own.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| CLI args/env → runner | A misconfigured switch could enable live extraction or materialization without authorization. |
| Runner → stdout/logs | Outcome formatting could leak connection targets or corpus content. |
| Runner → DB | The runner must never implicitly connect without the disposable gate. |
| Runner transaction → E2a | Cross-layer lock coordination must reuse E2a's exact advisory key string `okf:e2a:parent:{document_id}:{version_id}` so E2b and E2a serialize on the same document/version. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-16-37 | T | switch gating | mitigate | `--rebuild` fails closed unless RAG_ENTITY_EXTRACTOR=raner; default off. |
| T-16-38 | I | outcome redaction | mitigate | Typed redacted outcome only; never DSN/credential/corpus body/span text. |
| T-16-39 | E | implicit connection | mitigate | Fresh-connection-support loading mirrors E2a; no raw URI passthrough; live execution gated separately. |
| T-16-40 | D | E2b write boundary + cross-layer lock | mitigate | Runner never writes chunk_entity_links or okf_rebuild_failure_audit (D4/D5 denylist parity); the repository acquires the SAME advisory key `okf:e2a:parent:{document_id}:{version_id}` E2a uses (never `okf:e2b:parent`) in the fixed order (advisory xact lock → document_versions FOR UPDATE → ownership/mention/link row locks → DML). |
</threat_model>

<verification>
Run only non-DB CLI tests with injected fakes. No database, model, or network execution occurs in this plan; live E2b materialization is the separate full-corpus acceptance gate.
</verification>

<success_criteria>
- The E2b runner is switch-gated (off by default) and wired from injectable pipeline components.
- Outcomes are redacted typed summaries with no connection or corpus leakage.
- Per-document/version locking reuses E2a's exact advisory key `okf:e2a:parent:{document_id}:{version_id}` in the fixed lock order.
- No live database or model execution happens here.
</success_criteria>

<output>
After completion, create `.planning/phases/16-raw-corpus-entity-layer/16-11-SUMMARY.md`.
</output>
