---
phase: 16-raw-corpus-entity-layer
plan: 07
type: execute
wave: 1
macro_wave: W2
depends_on: [16-02]
files_modified:
  - llamaindex_runtime/entity/offline_mirror.py
  - tests/llamaindex_runtime/entity/test_offline_mirror.py
autonomous: true
requirements: [R-OKF-06]
must_haves:
  truths:
    - The loader reads model files only from a read-only offline mirror root and verifies every file against the frozen SHA-256 manifest before loading.
    - Importing the loader never imports modelscope/torch/sentencepiece; attempting to load when the ner extra is missing raises a typed error.
    - A runtime compatibility identity is produced only after a separately-authorized smoke; before that the loader exposes compatibility as unresolved.
  artifacts:
    - path: llamaindex_runtime/entity/offline_mirror.py
      provides: SHA-256-verified read-only offline mirror loader.
    - path: tests/llamaindex_runtime/entity/test_offline_mirror.py
      provides: Tests for manifest verification, dependency-absence errors, and mirror-path safety.
  key_links:
    - from: llamaindex_runtime/entity/offline_mirror.py
      to: llamaindex_runtime/entity/raner_adapter.py
      via: Loaded model/tokenizer consumed by the adapter
      pattern: model_id|artifact_digest
    - from: tests/llamaindex_runtime/entity/test_offline_mirror.py
      to: llamaindex_runtime/entity/offline_mirror.py
      via: SHA-256 manifest gate
      pattern: artifact_digest
---

<objective>
Implement the read-only offline mirror loader that freezes the RaNER model artifact by SHA-256 manifest without any network or default import surface.

Purpose: R-OKF-06 and D8 require offline loading from a SHA-256-verified read-only mirror; the `ner` extra must remain optional and the default path must never import ModelScope/PyTorch.
Output: A loader that verifies and loads the frozen mirror, plus dependency-absence and path-safety tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/16-raw-corpus-entity-layer/16-RESEARCH.md
@.planning/phases/16-raw-corpus-entity-layer/16-PATTERNS.md
@.planning/research/PHASE16-CHINESE-NER-MODEL-SELECTION-2026-07-13.md
@llamaindex_runtime/entity/contracts.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Specify loader behavior with tests</name>
  <read_first>@llamaindex_runtime/entity/contracts.py</read_first>
  <files>tests/llamaindex_runtime/entity/test_offline_mirror.py</files>
  <action>Write tests for `llamaindex_runtime/entity/offline_mirror.py` before implementation. The tests assert: (1) `load_mirror(root: Path) -> LoadedMirror` verifies every file listed in the frozen manifest (pytorch_model.bin, config.json, configuration.json, sentencepiece.bpe.model, tokenizer.json, special_tokens_map.json, tokenizer_config.json) against its SHA-256 from the frozen model-selection manifest, and rejects a single mismatched byte; (2) the module-level import does NOT import modelscope/torch/sentencepiece (assert via `sys.modules` that these names stay absent after import in a subprocess or with import-stubbing); (3) attempting to instantiate the loader when `importlib.util.find_spec("modelscope") is None` raises a typed `RaNERDependencyError` mentioning the `ner` extra; (4) the mirror root is resolved through a rooted read-only path (no symlink escape, no absolute-relative confusion, no traversal); (5) `runtime_compatibility_id` is None (unresolved) until a separately-authorized smoke freezes it. Provide a small fixture mirror directory with correct hashes for the test. No download and no network.</action>
  <acceptance_criteria>Tests prove manifest-verified loading, dependency-absence typed error, zero default imports, path safety, and unresolved compatibility before smoke.</acceptance_criteria>
  <verify><automated>rtk env -u DATABASE_URL -u FORMAL_RUNTIME_DATABASE_URL -u OKF_MIGRATION_TEST_DATABASE_DISPOSABLE -u OKF_REBUILD_EXPECTED_DATABASE -u OKF_FAILURE_AUDIT_ACCEPTANCE -u OKF_REBUILD_DOCKER_ACCEPTANCE -u OKF_E2A_DISPOSABLE_TEST_AUTHORIZED -u OKF_E2B_DISPOSABLE_TEST_AUTHORIZED -u OKF_E2B_MIGRATION_TEST_AUTHORIZED -u OKF_E2B_MODELSCOPE_MIRROR_AUTHORIZED -u OKF_E2B_RANER_SMOKE_AUTHORIZED -u OKF_E2B_C2_ENTRY_AUTHORIZED pytest tests/llamaindex_runtime/entity/test_offline_mirror.py -q</automated></verify>
  <done>Failing tests specify manifest verification, dependency-absence error, zero default imports, path safety, and unresolved compatibility.</done>
</task>

<task type="auto">
  <name>Task 2: Implement the offline mirror loader</name>
  <read_first>@llamaindex_runtime/entity/contracts.py</read_first>
  <files>llamaindex_runtime/entity/offline_mirror.py, tests/llamaindex_runtime/entity/test_offline_mirror.py</files>
  <action>Implement `llamaindex_runtime/entity/offline_mirror.py` with a module-scoped lazy import guard so that importing the module never imports modelscope/torch/sentencepiece. Define `RAINER_MIRROR_MANIFEST` (frozen file → SHA-256 map from the model-selection doc §1.3), `LoadedMirror` (frozen: root, resolved files dict, manifest digest, runtime_compatibility_id=None), `load_mirror(root)` (rooted read-only path resolution, verify each file hash streaming SHA-256, collect file paths), and a `load_model()` helper that raises `RaNERDependencyError` when `importlib.util.find_spec("modelscope") is None` and otherwise lazily imports modelscope to load the verified mirror read-only. The mirror root must default to `RAG_NER_MIRROR_ROOT` env with a fail-closed fallback. Set `runtime_compatibility_id` to None and never fabricate a compatibility value (D8: only the separately-authorized smoke may freeze it). Do NOT create or download any mirror; do NOT touch the database; never read/print DATABASE_URL or credentials.</action>
  <acceptance_criteria>Loader verifies the full frozen manifest, stays import-safe without the ner extra, raises typed errors when deps are missing, and leaves compatibility unresolved until smoke.</acceptance_criteria>
  <verify><automated>rtk env -u DATABASE_URL -u FORMAL_RUNTIME_DATABASE_URL -u OKF_MIGRATION_TEST_DATABASE_DISPOSABLE -u OKF_REBUILD_EXPECTED_DATABASE -u OKF_FAILURE_AUDIT_ACCEPTANCE -u OKF_REBUILD_DOCKER_ACCEPTANCE -u OKF_E2A_DISPOSABLE_TEST_AUTHORIZED -u OKF_E2B_DISPOSABLE_TEST_AUTHORIZED -u OKF_E2B_MIGRATION_TEST_AUTHORIZED -u OKF_E2B_MODELSCOPE_MIRROR_AUTHORIZED -u OKF_E2B_RANER_SMOKE_AUTHORIZED -u OKF_E2B_C2_ENTRY_AUTHORIZED pytest tests/llamaindex_runtime/entity/test_offline_mirror.py -q</automated></verify>
  <done>All offline-mirror tests pass; manifest verification and dependency guards are enforced with zero default imports.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Local mirror filesystem → model load | An unverified or escaped path could load a tampered model or arbitrary file. |
| Optional dependency → runtime | Importing the loader on the default path could pull ModelScope/PyTorch transitively. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-16-21 | T | artifact integrity | mitigate | Streaming SHA-256 verification of every manifest file before load; single mismatched byte rejected. |
| T-16-22 | E | path traversal | mitigate | Rooted read-only resolution with no symlink escape or traversal. |
| T-16-23 | D | default import surface | mitigate | Module import performs no heavy imports; dependency-absence raises typed RaNERDependencyError. |
| T-16-24 | R | compatibility | mitigate | runtime_compatibility_id stays None until the separately-authorized smoke freezes it (D8). |
</threat_model>

<verification>
Run only unit selectors using a small fixture mirror. No download, no network, no database. The loader is exercised only against hashed fixture files.
</verification>

<success_criteria>
- The loader verifies the frozen SHA-256 manifest before any model load.
- The default import surface stays free of modelscope/torch/sentencepiece.
- runtime_compatibility_id is unresolved until the separate smoke gate.
</success_criteria>

<output>
After completion, create `.planning/phases/16-raw-corpus-entity-layer/16-07-SUMMARY.md`.
</output>
