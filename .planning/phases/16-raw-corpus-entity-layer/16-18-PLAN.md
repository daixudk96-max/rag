---
phase: 16-raw-corpus-entity-layer
plan: 18
type: execute
wave: 8
macro_wave: W7
depends_on: [16-11, 16-15, 16-16, 16-17]
files_modified:
  - verification/phase16-raw-corpus-entity-layer/run_phase16_verification.py
  - tests/llamaindex_runtime/okf/test_phase16_verification_result.py
  - .planning/phases/16-raw-corpus-entity-layer/16-VERIFICATION.md
autonomous: true
requirements: [R-OKF-01, R-OKF-04, R-OKF-06, R-OKF-09]
must_haves:
  truths:
    - Phase 16 verification consumes measured typed results (E2bReconcileResult, migration-gate outcome, smoke evidence, C2 entry record) and never fabricates JSON evidence.
    - R-OKF-04 is evidenced by both a static AST/import proof and a runtime LLM spy proof that the enabled baseline performs zero generative-LLM calls.
    - The verification record defines both C2 branches: "C2 entered and passed the readiness gate" and "C2 not entered, default-off + reason recorded"; skipped C2 is never described as R-OKF-09 tested.
    - The aggregate acceptance record derives from the C1/C2 branch results: changed-set convergence (equivalent rerun zero DML AND selected-set-shrink deterministic deletion DML), ownership-safe bridge deletion (a bridge is deleted only when E2b ledger ownership is explicit AND no other owner remains, else fail-closed preserved), manual/legacy preservation (preloaded same-pair bridges and legacy mentions never claimed), and E2a/E2b shared-lock evidence (same document/version serialized on okf:e2a:parent:{document_id}:{version_id}).
    - All live selectors are listed separately, default denied, and blocked_not_executed is preserved for any unexecuted gate; no planned test is claimed to have run or passed unless it actually did.
  artifacts:
    - path: verification/phase16-raw-corpus-entity-layer/run_phase16_verification.py
      provides: Redacting result consumer and aggregate verification orchestrator (non-DB by default).
    - path: tests/llamaindex_runtime/okf/test_phase16_verification_result.py
      provides: Measured-result evidence and no-fabrication contract tests.
    - path: .planning/phases/16-raw-corpus-entity-layer/16-VERIFICATION.md
      provides: Factual Phase 16 acceptance matrix and non-claims.
  key_links:
    - from: verification/phase16-raw-corpus-entity-layer/run_phase16_verification.py
      to: llamaindex_runtime/entity/materialization_repository.py
      via: measured E2b materialization result
      pattern: E2bMaterializationResult
    - from: verification/phase16-raw-corpus-entity-layer/run_phase16_verification.py
      to: llamaindex_runtime/entity/extractor.py
      via: zero-LLM runtime proof
      pattern: EntityExtractor
---

<objective>
Create the aggregate Phase 16 verification consumer and acceptance record that distinguishes measured evidence from blocked or unexecuted gates.

Purpose: R-OKF-01/R-OKF-04/R-OKF-06/R-OKF-09 traceability and the phase boundary (PLANNED / EXECUTION NOT AUTHORIZED until live gates authorize) must be preserved honestly. As the last scheduler wave, 16-18 aggregates the C1 and C2 branch results into one factual acceptance record.
Output: Redacting non-DB verification consumer, result-shape tests, and a factual 16-VERIFICATION.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/16-raw-corpus-entity-layer/16-RESEARCH.md
@.planning/phases/16-raw-corpus-entity-layer/16-PATTERNS.md
@.planning/phases/15-okf-ingestion-pipeline/15-VERIFICATION.md
@.planning/v2.0-MILESTONE-OKF-MULTIROUTE.md
@llamaindex_runtime/entity/materialization_repository.py
@llamaindex_runtime/entity/extractor.py
</context>

<boundaries>
D4 frozen until Phase 19.
Phase 20 needs separate pilot authorization + human G6 Go/No-Go.
DATABASE_URL and credentials must never be read/printed/logged/persisted into .planning artifacts.
</boundaries>

<tasks>

<tdd_sequence>Task 1 = RED → Task 2 = GREEN → Task 2 = REFACTOR; each selector result recorded separately.</tdd_sequence>

<task type="auto" tdd="true">
  <name>Task 1 (RED): Specify measured-result-only verification and redaction</name>
  <read_first>@llamaindex_runtime/entity/materialization_repository.py</read_first>
  <files>tests/llamaindex_runtime/okf/test_phase16_verification_result.py</files>
  <behavior>
    - Evidence emission rejects fabricated JSON and accepts only typed measured results (materialization outcome + DML counts, migration-gate outcome, smoke evidence, C2 entry record).
    - Redacted output excludes connection strings, environment values, corpus text, model weights, and invented authorization receipts.
    - Any unexecuted live selector is represented as blocked_not_executed or skipped_not_entered (C2), never as a pass.
    - R-OKF-04 evidence combines a static AST/import proof (no generative-LLM import on the enabled baseline) with a runtime LLM spy proof (zero .complete()/.acomplete() calls).
    - The aggregate record aggregates the C1/C2 branch results: changed-set convergence, ownership-safe bridge deletion, manual/legacy preservation, and E2a/E2b shared-lock evidence, each attributed to the branch that produced it.
  </behavior>
  <action>Write failing tests for `verification/phase16-raw-corpus-entity-layer/run_phase16_verification.py` before implementation. Assert typed-result-only acceptance, whitelist/redact behavior, blocked_not_executed preservation, the zero-LLM static+runtime proof shape, and that a skipped C2 entry cannot be labeled as R-OKF-09 tested. Assert the aggregate consumer accepts and attributes each of the four C1/C2 branch evidence items (changed-set convergence from 16-15 materialization results, ownership-safe bridge deletion, manual/legacy preservation, E2a/E2b shared-lock serialization) from typed results only, and rejects any fabricated or unattributed JSON. No database, model, or network access. Run the selector in `<verify>` and record the expected RED result: the tests FAIL with ImportError/NameError because `verification/phase16-raw-corpus-entity-layer/run_phase16_verification.py` does not exist yet. Do NOT write any implementation or fabricate any evidence in this RED task.</action>
  <acceptance_criteria>RED verification-result tests exist; running the selector fails with ImportError/NameError for the missing `run_phase16_verification.py` module (recorded RED); no verification consumer implementation or fabricated evidence file was created in this task.</acceptance_criteria>
  <verify><automated>rtk env -u DATABASE_URL -u FORMAL_RUNTIME_DATABASE_URL -u OKF_MIGRATION_TEST_DATABASE_DISPOSABLE -u OKF_REBUILD_EXPECTED_DATABASE -u OKF_FAILURE_AUDIT_ACCEPTANCE -u OKF_REBUILD_DOCKER_ACCEPTANCE -u OKF_E2A_DISPOSABLE_TEST_AUTHORIZED -u OKF_E2B_DISPOSABLE_TEST_AUTHORIZED -u OKF_E2B_MIGRATION_TEST_AUTHORIZED -u OKF_E2B_MODELSCOPE_MIRROR_AUTHORIZED -u OKF_E2B_RANER_SMOKE_AUTHORIZED -u OKF_E2B_C2_ENTRY_AUTHORIZED pytest tests/llamaindex_runtime/okf/test_phase16_verification_result.py -q</automated></verify>
  <done>Tests reject fabricated evidence, secrets, implicit authority, and C2 overclaims; they specify both C2 branches and the four aggregated C1/C2 branch evidence items.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 (GREEN → REFACTOR): Implement the verification consumer and preserve factual state</name>
  <read_first>@.planning/phases/15-okf-ingestion-pipeline/15-VERIFICATION.md</read_first>
  <files>verification/phase16-raw-corpus-entity-layer/run_phase16_verification.py, tests/llamaindex_runtime/okf/test_phase16_verification_result.py, .planning/phases/16-raw-corpus-entity-layer/16-VERIFICATION.md</files>
  <behavior>
    - The verification consumer accepts only typed measured results and rejects fabricated JSON; output is whitelist-redacted.
    - Unexecuted live selectors serialize as blocked_not_executed / skipped_not_entered (C2), never as pass.
    - The R-OKF-04 zero-LLM proof combines a static AST/import check and a runtime LLM spy count of zero.
    - 16-VERIFICATION.md defines both C2 branches, aggregates the C1/C2 branch results, and preserves the verbatim boundary sentences.
  </behavior>
  <action>GREEN: write the minimal implementation so the SAME selector passes; record the GREEN selector result. REFACTOR: then apply only behavior-preserving cleanup and re-run the SAME selector; record the REFACTOR selector result separately. Do not alter test assertions between GREEN and REFACTOR. Implement a small typed result consumer (not a second E2b engine and not an evidence fabricator). It runs only non-DB selectors by default, serializes measured results with a whitelist/redactor, and preserves `blocked_not_executed` for any live selector without separate authority. The aggregate acceptance record must attribute each C1/C2 branch result from typed inputs only: (a) CHANGED-SET CONVERGENCE — equivalent rerun zero DML and selected-set-shrink deterministic deletion DML from 16-15 materialization results; (b) OWNERSHIP-SAFE BRIDGE DELETION — a bridge is deleted only when E2b ledger ownership is explicit AND no other owner remains, otherwise fail-closed preserved; (c) MANUAL/LEGACY PRESERVATION — preloaded same-pair bridges and legacy mentions preserved and never claimed; (d) E2A/E2B SHARED-LOCK EVIDENCE — same document/version serialized on okf:e2a:parent:{document_id}:{version_id}. Update `.planning/phases/16-raw-corpus-entity-layer/16-VERIFICATION.md` from actual results only: full acceptance matrix (config switch default-off, contracts/offsets, merger determinism, migration 020 provenance columns + okf_e2b_failure_audit + e2b_owner_scope stable owner identity + okf_e2b_node_link_ownership ledger + no destructive DML/no legacy claim, segmentation, label map + RaNER adapter, offline mirror, supplements + D3/D1 resolution, per-document/version atomic materialization + idempotence + changed-set convergence + ownership-safe bridge deletion + manual/legacy preservation + E2a/E2b shared-lock serialization, query persistence guard, E2b runner, and any executed live gates), a zero-generative-LLM proof section, and BOTH C2 branches ("C2 entered and passed readiness gate" OR "C2 not entered, default-off + reason recorded"). Preserve the execution-unstarted / blocked status for any gate that did not actually run. Preserve verbatim boundaries: `D4 frozen until Phase 19.`, `Phase 20 needs separate pilot authorization + human G6 Go/No-Go.`, and `DATABASE_URL and credentials must never be read/printed/logged/persisted into .planning artifacts.`</action>
  <acceptance_criteria>The same selector passes twice: once after minimal GREEN implementation and once after behavior-preserving REFACTOR; both selector results are recorded separately; the assertions in test_phase16_verification_result.py are unchanged between the GREEN and REFACTOR runs; 16-VERIFICATION.md contains only typed measured results, both C2 branches, the four aggregated C1/C2 branch evidence items, and the three verbatim boundary sentences.</acceptance_criteria>
  <verify><automated>rtk env -u DATABASE_URL -u FORMAL_RUNTIME_DATABASE_URL -u OKF_MIGRATION_TEST_DATABASE_DISPOSABLE -u OKF_REBUILD_EXPECTED_DATABASE -u OKF_FAILURE_AUDIT_ACCEPTANCE -u OKF_REBUILD_DOCKER_ACCEPTANCE -u OKF_E2A_DISPOSABLE_TEST_AUTHORIZED -u OKF_E2B_DISPOSABLE_TEST_AUTHORIZED -u OKF_E2B_MIGRATION_TEST_AUTHORIZED -u OKF_E2B_MODELSCOPE_MIRROR_AUTHORIZED -u OKF_E2B_RANER_SMOKE_AUTHORIZED -u OKF_E2B_C2_ENTRY_AUTHORIZED pytest tests/llamaindex_runtime/okf/test_phase16_verification_result.py -q</automated></verify>
  <done>Evidence derives from typed results only; blocked gates stay blocked; both C2 branches are defined; the aggregate record covers changed-set convergence, ownership-safe bridge deletion, manual/legacy preservation, and E2a/E2b shared-lock evidence; boundaries preserved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Runtime results → evidence | Serialization can overstate results or disclose sensitive material. |
| Verification runner → live gates | A default runner must never initiate live downloads, model loads, or DB connections. |
| C2 record → requirement claim | A skipped C2 must never be presented as R-OKF-09 tested. |
| C1/C2 branch results → aggregate record | Aggregate evidence must be attributed to the branch that produced it; unattributed or fabricated evidence is rejected. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-16-65 | I | result serializer | mitigate | Whitelist typed fields; exclude raw text, URLs, env values, model weights, credentials. |
| T-16-66 | E | live gates | mitigate | Default non-DB; blocked_not_executed / skipped_not_entered preserved without authority. |
| T-16-67 | R | zero-LLM proof | mitigate | Static AST/import proof + runtime LLM spy proof combined (R-OKF-04). |
| T-16-68 | S | C2 claim | mitigate | Both C2 branches defined; skipped C2 never labeled R-OKF-09 tested. |
| T-16-80 | I | aggregate evidence attribution | mitigate | Each aggregated item (changed-set convergence, ownership-safe bridge deletion, manual/legacy preservation, E2a/E2b shared-lock) is accepted from typed branch results only and rejected as fabricated/unattributed. |
| T-16-81 | D | ownership-safe bridge deletion claim | mitigate | Record deletion only when E2b ledger ownership is explicit AND no other owner remains; otherwise record fail-closed preservation. |
</threat_model>

<verification>
The default selector is non-DB. No evidence fixture is treated as a generated PASS artifact. Every live gate remains separately listed and default denied.
</verification>

<success_criteria>
- Measured typed results are the only verification data source.
- R-OKF-04 zero-LLM evidence is proven statically and at runtime.
- Both C2 branches are defined; blocked/skipped gates stay honest.
- The aggregate record covers changed-set convergence, ownership-safe bridge deletion, manual/legacy preservation, and E2a/E2b shared-lock evidence, each attributed to its C1/C2 branch result.
- Phase 16 remains PLANNED until live gates actually authorize and execute.
</success_criteria>

<output>
After completion, create `.planning/phases/16-raw-corpus-entity-layer/16-18-SUMMARY.md`.
</output>
