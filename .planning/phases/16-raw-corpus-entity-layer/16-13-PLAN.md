---
phase: 16-raw-corpus-entity-layer
plan: 13
type: execute
wave: 3
macro_wave: W4
depends_on: [16-12]
files_modified:
  - verification/phase16-raw-corpus-entity-layer/run_raner_smoke.py
  - verification/phase16-raw-corpus-entity-layer/raner_smoke_evidence.md
  - verification/phase16-raw-corpus-entity-layer/raner_resource_report.json
  - llamaindex_runtime/entity/compatibility.py
  - tests/llamaindex_runtime/entity/test_compatibility.py
autonomous: false
requirements: [R-OKF-04, R-OKF-06]
must_haves:
  truths:
    - A real local RaNER offline smoke runs against the verified mirror on the local host (and WSL when available), producing exact code-point mention coordinates and confidence=None/kind=unavailable.
    - Windows/WSL CPU cold start, peak memory, throughput, and p95 latency are measured and reported redacted; any budget exceedance is reported, never silently swapped for a smaller model.
    - runtime_compatibility_id is frozen only from the measured smoke result; without an authorized per-attempt Gate 3 attempt it stays unresolved and blocked_not_executed.
  artifacts:
    - path: verification/phase16-raw-corpus-entity-layer/run_raner_smoke.py
      provides: Per-attempt-authorized local RaNER offline smoke + resource measurement.
    - path: verification/phase16-raw-corpus-entity-layer/raner_smoke_evidence.md, verification/phase16-raw-corpus-entity-layer/raner_resource_report.json
      provides: Redacted smoke and resource measurement evidence.
    - path: llamaindex_runtime/entity/compatibility.py
      provides: Frozen runtime_compatibility_id after smoke.
  key_links:
    - from: verification/phase16-raw-corpus-entity-layer/run_raner_smoke.py
      to: llamaindex_runtime/entity/offline_mirror.py
      via: load_mirror verified mirror
      pattern: load_mirror
    - from: llamaindex_runtime/entity/compatibility.py
      to: llamaindex_runtime/entity/raner_adapter.py
      via: runtime_compatibility_id provenance on every candidate
      pattern: runtime_compatibility_id
---

<objective>
Run the real local RaNER offline smoke and Windows/WSL resource measurement under a per-attempt, separately-authorized gate (future gate 3).

Purpose: R-OKF-06 requires a runnable RaNER adapter for C1; D8 freezes runtime_compatibility_id only after a measured smoke. Resource measurement is required so Large Generic is never silently replaced by Base News.
Output: Per-attempt smoke + resource measurement with redacted evidence and a frozen compatibility identity (or blocked_not_executed).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/16-raw-corpus-entity-layer/16-RESEARCH.md
@.planning/research/PHASE16-CHINESE-NER-MODEL-SELECTION-2026-07-13.md
@llamaindex_runtime/entity/offline_mirror.py
@llamaindex_runtime/entity/raner_adapter.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the per-attempt smoke + resource measurement runner</name>
  <read_first>@llamaindex_runtime/entity/offline_mirror.py</read_first>
  <files>verification/phase16-raw-corpus-entity-layer/run_raner_smoke.py, tests/llamaindex_runtime/entity/test_raner_smoke_runner.py</files>
  <action>Create `run_raner_smoke.py` implementing future gate 3. It REQUIRES `OKF_E2B_RANER_SMOKE_AUTHORIZED=1` (per-attempt authorization) AND an existing verified mirror (from gate 2); otherwise emits exactly `blocked_not_executed` with zero model load. When authorized, it: (1) loads the model via the offline loader, (2) runs the adapter over a small fixed Chinese fixture set asserting exact code-point mention coordinates and confidence=None/confidence_kind="unavailable", (3) measures CPU cold-start time, peak memory, throughput (mentions/sec), and p95 latency on the local host and under WSL when `wsl.exe` is available, (4) writes `raner_resource_report.json` (redacted numbers only) and `raner_smoke_evidence.md`. If the model would exceed the resource envelope, the runner reports the exceedance and does NOT substitute Base News or any smaller model silently. Create `tests/llamaindex_runtime/entity/test_raner_smoke_runner.py` proving the default blocked path with zero model load. Never read/log DATABASE_URL or external DB targets.</action>
  <acceptance_criteria>Default denied with zero model load; authorized smoke measures coordinates, confidence stripping, and Windows/WSL resource envelope with redacted evidence.</acceptance_criteria>
  <verify><automated>rtk env -u DATABASE_URL -u FORMAL_RUNTIME_DATABASE_URL -u OKF_MIGRATION_TEST_DATABASE_DISPOSABLE -u OKF_REBUILD_EXPECTED_DATABASE -u OKF_FAILURE_AUDIT_ACCEPTANCE -u OKF_REBUILD_DOCKER_ACCEPTANCE -u OKF_E2A_DISPOSABLE_TEST_AUTHORIZED -u OKF_E2B_DISPOSABLE_TEST_AUTHORIZED -u OKF_E2B_MIGRATION_TEST_AUTHORIZED -u OKF_E2B_MODELSCOPE_MIRROR_AUTHORIZED -u OKF_E2B_RANER_SMOKE_AUTHORIZED -u OKF_E2B_C2_ENTRY_AUTHORIZED pytest tests/llamaindex_runtime/entity/test_raner_smoke_runner.py -q</automated></verify>
  <done>Unauthorized invocation emits blocked_not_executed with zero model load; the authorized smoke path is ready for a per-attempt gate launch.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 2 (decision): Authorize a per-attempt real local RaNER offline smoke + Windows/WSL resource measurement (future gate 3)?</name>
  <read_first>@llamaindex_runtime/entity/offline_mirror.py</read_first>
  <files>none — authorization decision only; no file changes before consent. Evidence (raner_smoke_evidence.md, raner_resource_report.json) and compatibility.py are produced only by an authorized smoke run, never by this decision.</files>
  <decision>Authorize a per-attempt real local RaNER offline smoke + Windows/WSL resource measurement (future gate 3)?</decision>
  <context>This loads the ~2.26 GB RaNER model locally from the verified mirror and runs resource measurement (cold start, peak memory, throughput, p95 latency) on Windows and WSL. Requires the mirror from gate 2. Default denied, per-attempt, no automatic retry.</context>
  <options>
    <option id="authorize">
      <name>Authorize smoke + measurement</name>
      <pros>Proves the runnable RaNER adapter, freezes runtime_compatibility_id, and closes C1's live-model readiness.</pros>
      <cons>Loads a large model locally and consumes CPU/memory; per-attempt only.</cons>
    </option>
    <option id="defer">
      <name>Keep blocked_not_executed</name>
      <pros>No model load; phase stays PLANNED.</pros>
      <cons>runtime_compatibility_id stays unresolved; live acceptance cannot run.</cons>
    </option>
  </options>
  <action>Default denied. Without the user's independent per-attempt authorization (OKF_E2B_RANER_SMOKE_AUTHORIZED=1) and a verified gate-2 mirror, record blocked_not_executed with zero model load; no automatic retry, no autonomous loop, no concurrency, must not inherit any other authorization, and must never read or fall back to DATABASE_URL. Only after user consent may the coordinator explicitly initiate an attempt: each attempt is a separate launch with a fresh immutable attempt_id, durably recorded as launch_committed before any model loader begins (supervised-launch protocol); the attempt is terminal on success, failure, timeout, interruption, or crash; a later attempt is a new attempt, never a retry of a failed attempt_id, and may be initiated only after the previous attempt has a terminal event; the three timed inference passes inside one attempt are measurement repetitions, not retries. runtime_compatibility_id is frozen only from measured values.</action>
  <acceptance_criteria>The un-authorized path records blocked_not_executed with zero model load and no evidence file; the authorized path runs one attempt per explicit coordinator initiation, measurement repetitions permitted within the attempt, measures the resource envelope, and freezes runtime_compatibility_id from measured values; the default blocked state is proven by the gate-runner unit selector.</acceptance_criteria>
  <verify><automated>rtk env -u DATABASE_URL -u FORMAL_RUNTIME_DATABASE_URL -u OKF_MIGRATION_TEST_DATABASE_DISPOSABLE -u OKF_REBUILD_EXPECTED_DATABASE -u OKF_FAILURE_AUDIT_ACCEPTANCE -u OKF_REBUILD_DOCKER_ACCEPTANCE -u OKF_E2A_DISPOSABLE_TEST_AUTHORIZED -u OKF_E2B_DISPOSABLE_TEST_AUTHORIZED -u OKF_E2B_MIGRATION_TEST_AUTHORIZED -u OKF_E2B_MODELSCOPE_MIRROR_AUTHORIZED -u OKF_E2B_RANER_SMOKE_AUTHORIZED -u OKF_E2B_C2_ENTRY_AUTHORIZED pytest tests/llamaindex_runtime/entity/test_raner_smoke_runner.py -q</automated></verify>
  <done>blocked_not_executed recorded with zero live side effect; if authorized, one attempt ran under explicit coordinator initiation, measured the envelope, and produced redacted evidence.</done>
  <resume-signal>Select "authorize" to initiate one smoke attempt, or "defer" to record blocked_not_executed.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Freeze runtime_compatibility_id from the measured smoke</name>
  <read_first>@llamaindex_runtime/entity/offline_mirror.py</read_first>
  <files>llamaindex_runtime/entity/compatibility.py, tests/llamaindex_runtime/entity/test_compatibility.py, verification/phase16-raw-corpus-entity-layer/raner_smoke_evidence.md</files>
  <action>After an authorized attempt produces measured results, create `llamaindex_runtime/entity/compatibility.py` holding the frozen `runtime_compatibility_id` (a stable tuple-hash over extractor id/version, model id/revision, artifact digest, label map digest, schema/normalization/segmentation versions, and the measured resource envelope summary). Before the smoke, compatibility stays unresolved (None) and any candidate construction with unresolved compatibility fails closed. Add `tests/llamaindex_runtime/entity/test_compatibility.py` asserting the tuple is stable and that a compatibility tuple cannot be fabricated without measured evidence (i.e., the id must match the recomputed tuple over the recorded smoke values). If the smoke was deferred, record blocked_not_executed in the evidence instead and leave compatibility unresolved.</action>
  <acceptance_criteria>runtime_compatibility_id is either frozen from measured smoke values or remains unresolved with blocked_not_executed recorded.</acceptance_criteria>
  <verify><automated>rtk env -u DATABASE_URL -u FORMAL_RUNTIME_DATABASE_URL -u OKF_MIGRATION_TEST_DATABASE_DISPOSABLE -u OKF_REBUILD_EXPECTED_DATABASE -u OKF_FAILURE_AUDIT_ACCEPTANCE -u OKF_REBUILD_DOCKER_ACCEPTANCE -u OKF_E2A_DISPOSABLE_TEST_AUTHORIZED -u OKF_E2B_DISPOSABLE_TEST_AUTHORIZED -u OKF_E2B_MIGRATION_TEST_AUTHORIZED -u OKF_E2B_MODELSCOPE_MIRROR_AUTHORIZED -u OKF_E2B_RANER_SMOKE_AUTHORIZED -u OKF_E2B_C2_ENTRY_AUTHORIZED pytest tests/llamaindex_runtime/entity/test_compatibility.py -q</automated></verify>
  <done>Compatibility is frozen only from measured smoke evidence (or recorded unresolved with blocked_not_executed).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Local model load → runtime | Loading a large model consumes resources and must not occur without authorization. |
| Smoke evidence → compatibility | Compatibility identity must derive from measured facts, not fabricated values. |
| Resource report → acceptance | A budget exceedance must not be silently papered over with a smaller model. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-16-44 | E | unauthorized model load | mitigate | Per-attempt OKF_E2B_RANER_SMOKE_AUTHORIZED gate; default blocked_not_executed, no automatic retry. |
| T-16-45 | I | compatibility fabrication | mitigate | runtime_compatibility_id recomputed from measured smoke values; unresolved fails closed before smoke. |
| T-16-46 | R | model substitution | mitigate | Any envelope exceedance is reported; Large Generic is never silently replaced by Base News. |
| T-16-47 | T | evidence disclosure | mitigate | Resource report holds redacted numbers only. |
</threat_model>

<verification>
The default selector is the blocked_not_executed gate test. The authorized smoke is a per-attempt gate; each attempt requires explicit coordinator initiation and must not run during ordinary execution.
</verification>

<success_criteria>
- A real local RaNER offline smoke runs only under per-attempt authorization and proves exact coordinates + confidence stripping.
- Windows/WSL resource metrics are measured and redacted; envelope exceedance is reported, not hidden.
- runtime_compatibility_id is frozen from measured smoke evidence (or stays unresolved with blocked_not_executed).
</success_criteria>

<output>
After completion, create `.planning/phases/16-raw-corpus-entity-layer/16-13-SUMMARY.md`.
</output>

---

## Additive Reconciliation Notes — Task #222 (2026-08-12)

> These notes are ADDITIVE to the frozen Plan 16-13 content above. They do not rewrite any historical truth or any task/contract text above; they record current-authority reconciliation per Task #222. A separate TDD-governed contract amendment of this plan's Task 2 wording is REQUIRED before the first Gate 3 launch.

### 1. Plan 16-13 still encodes obsolete single-use/no-retry Gate 3 semantics

- Task 2 wording above ("single-use", "no auto-retry", "the gate must not auto-retry", "runs exactly once") and the objective/must-haves describing a single-use gate reflect the pre-Task-#222 authorization model.
- Current authority (2026-08-11 user message `行，我授权继续吧然后 gate3 可以多次运行`; encoded 2026-08-12 by Task #222 in 16-GATE3-ATTEMPT-CONTRACT-2026-08-12.json): Gate 3 is multi-attempt-by-explicit-launch. Each attempt is a separate explicit coordinator-initiated launch with a fresh immutable attempt_id, durably recorded (launch_committed) BEFORE any model loader begins, terminal for that attempt on success/failure/timeout/interruption/crash, and permanently retained if failed. No automatic retry, no autonomous loop, no concurrency. A later attempt is a new explicit attempt, never a retry of a failed attempt_id. The three timed inference passes inside one attempt are measurement repetitions, not retries.
- The /goal skill/tests also still encode the obsolete single-use/no-retry model; both this plan and /goal require a separate TDD-governed contract amendment before the first Gate 3 launch. Task #222 is planning-only and does not amend production/test/skill code.
- The historical single-use materialization ledger (16-LIVE-GATE-TOKENS.json) is preserved byte-for-byte and grants no current runtime authority.

### 2. Task #221 isolated runtime preparation is authorized

- Preparation (official Python package access solely for repository-local isolated preparation; build/verify an isolated runtime so modelscope.pipelines imports; close the WSL measurement gap) is AUTHORIZED.
- NOT authorized: preparation-time model loading, RaNER inference, real WSL model benchmarking, live evidence publication, external DB access, DATABASE_URL read/fallback, C2, commit, push, mutation of global C:\Python311.

### 3. Downstream rules and invariants (recorded, not implemented)

- Task #221 implementation blockers and the Phase 16 safety invariants are recorded in the Task #222 contract (16-GATE3-ATTEMPT-CONTRACT-2026-08-12.json).
- Compatibility freezing remains a separate downstream action (binds to named attempt_id + evidence hashes + mirror digest + measured artifacts) and the live runner/launcher must not freeze automatically. Only an independently verified successful attempt may later be selected as canonical evidence.
- Gate 4 remains unexecuted and NOT authorized; C2 OFF/skipped_not_entered; Task #181 pending; no commit/push.

### 4. Contract amendment to per-attempt semantics (TDD-governed, 2026-08-29)

- This amendment executes the `pre_launch_contract_amendment_required` from 16-GATE3-ATTEMPT-CONTRACT-2026-08-12.json (Task #222); the single-use/no-retry wording in Task 2 and the objective/must-haves/success criteria above is replaced with per-attempt semantics matching the current authorization model.
- The /goal skill and its tests were amended in the same TDD-governed pass (88 passed).
- This plan's Task 2 wording now matches the per-attempt authorization model: each attempt is a separate explicit coordinator-initiated launch with a fresh immutable attempt_id, durably recorded as launch_committed before any model loader begins (supervised-launch protocol), terminal on success/failure/timeout/interruption/crash, no automatic retry, no autonomous loop, no concurrency; a later attempt is a new attempt, never a retry of a failed attempt_id, initiated only after the previous attempt has a terminal event; the three timed inference passes inside one attempt are measurement repetitions, not retries.
- The frozen lines above remain historical where marked; they are preserved and superseded per this amendment.
- No attempt has been launched (attempt_count_at_reconciliation: 0).
