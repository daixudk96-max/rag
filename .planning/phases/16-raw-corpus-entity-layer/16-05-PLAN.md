---
phase: 16-raw-corpus-entity-layer
plan: 05
type: tdd
wave: 1
macro_wave: W2
depends_on: [16-02]
files_modified:
  - llamaindex_runtime/entity/segmenter.py
  - tests/llamaindex_runtime/entity/test_segmenter.py
autonomous: true
requirements: [R-OKF-06]
must_haves:
  truths:
    - Long normalized spans are split into deterministic token-aware segments that jointly cover the entire span with zero overlap and zero gap, each carrying segment_id and parent interval.
    - Segment coordinates are Python Unicode code-point half-open intervals with segmentation_version recorded on every candidate.
    - Segmentation never relies on ModelScope split_max_length or on substring coordinate recovery; the fast-tokenizer offset path is preserved by construction.
  artifacts:
    - path: llamaindex_runtime/entity/segmenter.py
      provides: Deterministic token-aware segmentation with segment_id/parent-interval/segmentation_version.
    - path: tests/llamaindex_runtime/entity/test_segmenter.py
      provides: RED tests for full-span coverage, determinism, and code-point interval correctness.
  key_links:
    - from: llamaindex_runtime/entity/segmenter.py
      to: llamaindex_runtime/entity/contracts.py
      via: CorpusSpanInput normalized_text and MentionCandidate segmentation provenance
      pattern: segmentation_version|segment_id
    - from: tests/llamaindex_runtime/entity/test_segmenter.py
      to: llamaindex_runtime/entity/segmenter.py
      via: Shuffle-equivalent deterministic segmentation
      pattern: segment_id
---

<objective>
Implement the project-owned deterministic token-aware segmentation that protects against 512-token truncation without depending on ModelScope character splitting.

Purpose: R-OKF-06 requires exact normalized-text coordinates and segmentation provenance; RESEARCH confirms slow-tokenizer/truncation risk requires an owned segmenter.
Output: TDD-tested segmenter producing deterministic, fully-covering, versioned segments.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/16-raw-corpus-entity-layer/16-RESEARCH.md
@.planning/phases/16-raw-corpus-entity-layer/16-PATTERNS.md
@.planning/research/PHASE16-CHINESE-NER-MODEL-SELECTION-2026-07-13.md
@llamaindex_runtime/entity/contracts.py
</context>

<tasks>

<tdd_sequence>Task 1 = RED → Task 2 = GREEN → Task 2 = REFACTOR; each selector result recorded separately.</tdd_sequence>

<task type="auto" tdd="true">
  <name>Task 1 (RED): Write RED segmentation tests</name>
  <read_first>@llamaindex_runtime/entity/contracts.py</read_first>
  <files>tests/llamaindex_runtime/entity/test_segmenter.py</files>
  <behavior>
    - A span longer than the configured token budget yields multiple segments whose concatenation equals the normalized_text exactly (no overlap, no gap), each with a unique deterministic segment_id and the parent interval recorded.
    - Shuffled token order or repeated identical calls produce byte-identical segment lists (determinism).
    - Segment boundaries never split a single code point (no half surrogate pair) and never rely on `text.index`/`find`/substring recovery.
    - segmentation_version is recorded on each segment and propagated to every candidate derived from it.
  </behavior>
  <action>Write failing tests for `segment_text(span: CorpusSpanInput, *, max_tokens: int, tokenizer: object, segmentation_version: str) -> tuple[Segment, ...]` where Segment is a frozen dataclass (segment_id, parent_span_id, start, end, text, segmentation_version, token_count). Use a deterministic fake tokenizer (e.g. a callable counting tokens with stable boundaries) so tests never require modelscope/torch/sentencepiece. Assert full-span coverage, code-point safety (build inputs containing multi-byte CJK and emoji), shuffle determinism, and that the segmenter never calls `str.index`/`find` or ModelScope split APIs. Test that `max_tokens=0` and `max_tokens<0` fail closed. Run the selector in `<verify>` and record the expected RED result: the tests FAIL with ImportError/NameError because `llamaindex_runtime/entity/segmenter.py` does not exist yet. Do NOT write any implementation in this RED task.</action>
  <acceptance_criteria>RED segmenter tests exist under tests/llamaindex_runtime/entity/test_segmenter.py; running the selector fails with ImportError/NameError for the missing `llamaindex_runtime/entity/segmenter.py` (recorded RED); no segmenter implementation file was created in this task.</acceptance_criteria>
  <verify><automated>rtk env -u DATABASE_URL -u FORMAL_RUNTIME_DATABASE_URL -u OKF_MIGRATION_TEST_DATABASE_DISPOSABLE -u OKF_REBUILD_EXPECTED_DATABASE -u OKF_FAILURE_AUDIT_ACCEPTANCE -u OKF_REBUILD_DOCKER_ACCEPTANCE -u OKF_E2A_DISPOSABLE_TEST_AUTHORIZED -u OKF_E2B_DISPOSABLE_TEST_AUTHORIZED -u OKF_E2B_MIGRATION_TEST_AUTHORIZED -u OKF_E2B_MODELSCOPE_MIRROR_AUTHORIZED -u OKF_E2B_RANER_SMOKE_AUTHORIZED -u OKF_E2B_C2_ENTRY_AUTHORIZED pytest tests/llamaindex_runtime/entity/test_segmenter.py -q</automated></verify>
  <done>Failing tests specify full coverage, determinism, code-point safety, and fail-closed budgets without any model import.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 (GREEN → REFACTOR): Implement the deterministic segmenter</name>
  <read_first>@llamaindex_runtime/entity/contracts.py</read_first>
  <files>llamaindex_runtime/entity/segmenter.py, tests/llamaindex_runtime/entity/test_segmenter.py</files>
  <behavior>
    - segment_text returns segments whose concatenation equals the normalized span exactly (no overlap, no gap), each with a unique deterministic segment_id, parent interval, and segmentation_version.
    - Segments never split a surrogate pair and never call `str.index`/`find` or ModelScope split APIs.
    - max_tokens <= 0 and empty normalized_text fail closed.
  </behavior>
  <action>GREEN: write the minimal implementation so the SAME selector passes; record the GREEN selector result. REFACTOR: then apply only behavior-preserving cleanup and re-run the SAME selector; record the REFACTOR selector result separately. Do not alter test assertions between GREEN and REFACTOR. Implement `llamaindex_runtime/entity/segmenter.py` to make the RED tests pass. Build segments by advancing through the normalized_text by code point, never splitting a surrogate pair, accumulating tokens via the injected deterministic tokenizer until the budget would be exceeded, then closing the segment at the last whole token boundary. Guarantee the concatenation of all segment texts equals the normalized span and segment intervals are half-open `[start, end)`. Derive segment_id via the E2b `deterministic_id` helper over (parent_span_id, start, end, segmentation_version). Record segmentation_version on every segment. Reject `max_tokens <= 0` and any span whose normalized_text is empty. Do NOT call any ModelScope/tokenizer `split_max_length` or substring search; the fast-tokenizer offset path is preserved by construction. Export `segment_text` and `Segment` from `llamaindex_runtime/entity/__init__.py`.</action>
  <acceptance_criteria>The same selector passes twice: once after minimal GREEN implementation and once after behavior-preserving REFACTOR; both selector results are recorded separately; the assertions in test_segmenter.py are unchanged between the GREEN and REFACTOR runs.</acceptance_criteria>
  <verify><automated>rtk env -u DATABASE_URL -u FORMAL_RUNTIME_DATABASE_URL -u OKF_MIGRATION_TEST_DATABASE_DISPOSABLE -u OKF_REBUILD_EXPECTED_DATABASE -u OKF_FAILURE_AUDIT_ACCEPTANCE -u OKF_REBUILD_DOCKER_ACCEPTANCE -u OKF_E2A_DISPOSABLE_TEST_AUTHORIZED -u OKF_E2B_DISPOSABLE_TEST_AUTHORIZED -u OKF_E2B_MIGRATION_TEST_AUTHORIZED -u OKF_E2B_MODELSCOPE_MIRROR_AUTHORIZED -u OKF_E2B_RANER_SMOKE_AUTHORIZED -u OKF_E2B_C2_ENTRY_AUTHORIZED pytest tests/llamaindex_runtime/entity/test_segmenter.py -q</automated></verify>
  <done>All segmentation tests pass; the segmenter is deterministic, code-point safe, fully covering, and has zero model dependency.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Raw normalized text → segments | Truncation or offset drift corrupts entity coordinates downstream. |
| Segmenter → tokenizer | The fast-tokenizer offset path is authoritative; slow/substring recovery must never be used. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-16-14 | T | segment boundaries | mitigate | Code-point-safe half-open intervals with exact full-span coverage; no surrogate splitting. |
| T-16-15 | I | determinism | mitigate | segment_id derived deterministically; shuffle-equivalent inputs produce identical segments. |
| T-16-16 | D | offset recovery | mitigate | No `str.index`/`find`/substring recovery anywhere in the segmenter; fast-tokenizer path preserved by construction. |
</threat_model>

<verification>
Run only pure unit selectors with a deterministic fake tokenizer. No modelscope/torch/sentencepiece import, no DB, no network.
</verification>

<success_criteria>
- Long spans are split into deterministic, fully-covering, non-overlapping segments.
- Segment coordinates are code-point-safe half-open intervals with segmentation_version.
- ModelScope truncation/split APIs and substring recovery are never used.
</success_criteria>

<output>
After completion, create `.planning/phases/16-raw-corpus-entity-layer/16-05-SUMMARY.md`.
</output>
