---
phase: 16-raw-corpus-entity-layer
plan: 14
type: execute
wave: 5
macro_wave: W4
depends_on: [16-04]
files_modified:
  - verification/phase16-raw-corpus-entity-layer/run_e2b_migration_gate.py
  - verification/phase16-raw-corpus-entity-layer/e2b_migration_gate_evidence.md
  - tests/llamaindex_runtime/okf/test_e2b_migration_gate_runner.py
autonomous: false
requirements: [R-OKF-01, R-OKF-06]
must_haves:
  truths:
    - A disposable PostgreSQL fresh schema runs the full root catalog and proves 020 applies exactly once after 019 with all constraints/indexes present.
    - Re-running migration 020 on the same disposable schema issues zero DDL (idempotence), proving the catalog/migration contract.
    - Without the single-use migration authorization the gate emits exactly blocked_not_executed with zero connection attempt.
  artifacts:
    - path: verification/phase16-raw-corpus-entity-layer/run_e2b_migration_gate.py
      provides: Single-use authorized disposable-PostgreSQL migration gate with blocked_not_executed default.
    - path: verification/phase16-raw-corpus-entity-layer/e2b_migration_gate_evidence.md
      provides: Redacted migration-gate evidence record.
  key_links:
    - from: verification/phase16-raw-corpus-entity-layer/run_e2b_migration_gate.py
      to: llamaindex_runtime/registry/migration_catalog.py
      via: full root catalog incl. 020
      pattern: 020_ner_entity_mentions
    - from: verification/phase16-raw-corpus-entity-layer/run_e2b_migration_gate.py
      to: scripts/_rebuild_database_connection.py
      via: disposable loopback-only target parsing
      pattern: parse_disposable_postgresql_target
---

<objective>
Validate migration 020 against a disposable local PostgreSQL under a single-use, separately-authorized gate (future gate 1).

Purpose: R-OKF-06 requires provenance schema enforcement; R-OKF-01 requires a fully rebuildable derived layer. ADR T5 requires re-verifying the migration catalog and numbering at execution time.
Output: A migration gate whose default state is blocked_not_executed, plus redacted evidence proving 020 applies exactly once and idempotently.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/16-raw-corpus-entity-layer/16-RESEARCH.md
@.planning/ADR-OKF-PHASE-A-TECH-DECISIONS-2026-07-12.md
@scripts/_rebuild_database_connection.py
@llamaindex_runtime/registry/migration_catalog.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the single-use migration gate runner</name>
  <read_first>@scripts/_rebuild_database_connection.py</read_first>
  <files>verification/phase16-raw-corpus-entity-layer/run_e2b_migration_gate.py, tests/llamaindex_runtime/okf/test_e2b_migration_gate_runner.py</files>
  <action>Create `run_e2b_migration_gate.py` implementing future gate 1. It REQUIRES `OKF_MIGRATION_TEST_DATABASE_DISPOSABLE=1`, `OKF_REBUILD_EXPECTED_DATABASE` (disposable name), and `OKF_E2B_MIGRATION_TEST_AUTHORIZED=1` (single-use); otherwise emits exactly `blocked_not_executed` with zero connection attempt. When authorized: (1) parse only the strict loopback disposable target via the `_rebuild_database_connection` helper (never a raw URI to psycopg.connect, never DATABASE_URL fallback), (2) apply the full root catalog on a fresh disposable schema, (3) assert 020 applies exactly once after 019, all entity_mentions provenance columns and okf_e2b_failure_audit exist, the `chk_okf_e2b_*` constraints/indexes are present, and the okf_e2b_node_link_ownership ledger exists with its UNIQUE (node_id, entity_id, version_id), (4) re-run 020 and assert zero DDL (idempotence), (5) write redacted evidence (catalog tail, constraint/index inventory, DDL counts) with no connection string or credential. Create `tests/llamaindex_runtime/okf/test_e2b_migration_gate_runner.py` proving the default blocked path with zero connection attempt. Never read/log DATABASE_URL or any production target.</action>
  <acceptance_criteria>Default denied with zero connection; authorized path proves 020 applies exactly once, idempotently, on a disposable schema.</acceptance_criteria>
  <verify><automated>rtk env -u DATABASE_URL -u FORMAL_RUNTIME_DATABASE_URL -u OKF_MIGRATION_TEST_DATABASE_DISPOSABLE -u OKF_REBUILD_EXPECTED_DATABASE -u OKF_FAILURE_AUDIT_ACCEPTANCE -u OKF_REBUILD_DOCKER_ACCEPTANCE -u OKF_E2A_DISPOSABLE_TEST_AUTHORIZED -u OKF_E2B_DISPOSABLE_TEST_AUTHORIZED -u OKF_E2B_MIGRATION_TEST_AUTHORIZED -u OKF_E2B_MODELSCOPE_MIRROR_AUTHORIZED -u OKF_E2B_RANER_SMOKE_AUTHORIZED -u OKF_E2B_C2_ENTRY_AUTHORIZED pytest tests/llamaindex_runtime/okf/test_e2b_migration_gate_runner.py -q</automated></verify>
  <done>Unauthorized invocation emits blocked_not_executed with zero connection; the authorized migration-gate path is ready for the single-use gate.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 2 (decision): Authorize single-use disposable-PostgreSQL migration testing (future gate 1)?</name>
  <read_first>@scripts/_rebuild_database_connection.py</read_first>
  <files>none — authorization decision only; no file changes before consent. Evidence (e2b_migration_gate_evidence.md) is created only by an authorized gate run, never by this decision.</files>
  <decision>Authorize the single-use disposable-PostgreSQL migration testing gate (future gate 1)?</decision>
  <context>This spins up a local disposable PostgreSQL/Docker target, applies the full migration catalog, and validates 020 idempotence. Default denied, single-use, local disposable targets only, never DATABASE_URL or external targets.</context>
  <options>
    <option id="authorize">
      <name>Authorize migration gate</name>
      <pros>Proves migration 020 applies exactly once with correct constraints; unblocks full-corpus acceptance.</pros>
      <cons>Requires local Docker/PostgreSQL disposable target; single-use only.</cons>
    </option>
    <option id="defer">
      <name>Keep blocked_not_executed</name>
      <pros>No database work; phase stays PLANNED.</pros>
      <cons>Migration schema correctness stays unproven live.</cons>
    </option>
  </options>
  <action>Default denied. Without the user's independent single-use authorization (OKF_E2B_MIGRATION_TEST_AUTHORIZED=1) plus the disposable-target variables, record blocked_not_executed with zero connection attempt; the gate must not auto-retry, must not inherit any other authorization, and must never read or fall back to DATABASE_URL. Only after user consent does the gate run exactly once against a strict loopback disposable target and record redacted evidence.</action>
  <acceptance_criteria>The un-authorized path records blocked_not_executed with zero connection attempt and no evidence file; the authorized path runs once on a disposable target and proves exactly-once + idempotent 020; the default blocked state is proven by the gate-runner unit selector.</acceptance_criteria>
  <verify><automated>rtk env -u DATABASE_URL -u FORMAL_RUNTIME_DATABASE_URL -u OKF_MIGRATION_TEST_DATABASE_DISPOSABLE -u OKF_REBUILD_EXPECTED_DATABASE -u OKF_FAILURE_AUDIT_ACCEPTANCE -u OKF_REBUILD_DOCKER_ACCEPTANCE -u OKF_E2A_DISPOSABLE_TEST_AUTHORIZED -u OKF_E2B_DISPOSABLE_TEST_AUTHORIZED -u OKF_E2B_MIGRATION_TEST_AUTHORIZED -u OKF_E2B_MODELSCOPE_MIRROR_AUTHORIZED -u OKF_E2B_RANER_SMOKE_AUTHORIZED -u OKF_E2B_C2_ENTRY_AUTHORIZED pytest tests/llamaindex_runtime/okf/test_e2b_migration_gate_runner.py -q</automated></verify>
  <done>blocked_not_executed recorded with zero connection; if authorized, the gate ran once and produced redacted evidence.</done>
  <resume-signal>Select "authorize" to run the migration gate once, or "defer" to record blocked_not_executed.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Migration catalog → disposable DB | A mis-ordered or drifted catalog breaks fresh-schema rebuild. |
| Gate runner → DB | Only a strict loopback disposable target may be connected. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-16-48 | T | catalog drift | mitigate | Exactly-once 020 after 019 assertion on a fresh disposable schema. |
| T-16-49 | E | unauthorized connection | mitigate | Single-use OKF_E2B_MIGRATION_TEST_AUTHORIZED gate + strict loopback target parse; default blocked_not_executed. |
| T-16-50 | D | idempotence | mitigate | Re-run 020 asserts zero DDL; no accidental repeat migration. |
| T-16-51 | R | evidence redaction | mitigate | Evidence holds catalog tail + inventory only; never connection string/credential. |
</threat_model>

<verification>
The default selector is the blocked_not_executed gate test. The authorized migration gate runs once on a disposable local target only and must never auto-retry or touch DATABASE_URL.
</verification>

<success_criteria>
- 020 applies exactly once after 019 on a fresh disposable schema with all constraints/indexes.
- Re-running 020 issues zero DDL.
- Default state is blocked_not_executed with zero connection; evidence is redacted.
</success_criteria>

<output>
After completion, create `.planning/phases/16-raw-corpus-entity-layer/16-14-SUMMARY.md`.
</output>
