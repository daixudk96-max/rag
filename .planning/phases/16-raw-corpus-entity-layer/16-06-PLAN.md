---
phase: 16-raw-corpus-entity-layer
plan: 06
type: tdd
wave: 2
macro_wave: W2
depends_on: [16-02, 16-05]
files_modified:
  - llamaindex_runtime/entity/label_map.py
  - llamaindex_runtime/entity/raner_adapter.py
  - tests/llamaindex_runtime/entity/test_label_map.py
  - tests/llamaindex_runtime/entity/test_raner_adapter.py
autonomous: true
requirements: [R-OKF-04, R-OKF-06]
must_haves:
  truths:
    - RaNER raw labels CORP/CW/GRP/LOC/PER/PROD map deterministically to canonical labels Person/Location/Organization/CreativeWork/Product while raw_label is always preserved; validation uses a versioned label map, not a DB enum.
    - The adapter forces the fast-tokenizer offset path and fails closed if slow-tokenizer/substring coordinate recovery would be required; it drops the uncalibrated ModelScope `prob` and stores confidence=None, confidence_kind="unavailable".
    - The adapter produces full-provenance MentionCandidates (extractor/model/artifact/schema/normalization/segmentation/label-map/runtime) for each segment, and never calls a generative LLM.
  artifacts:
    - path: llamaindex_runtime/entity/label_map.py
      provides: Versioned RaNER label map with raw/canonical label validation.
    - path: llamaindex_runtime/entity/raner_adapter.py
      provides: ModelScope RaNER adapter returning MentionCandidate list with frozen provenance.
    - path: tests/llamaindex_runtime/entity/test_label_map.py, tests/llamaindex_runtime/entity/test_raner_adapter.py
      provides: RED tests for label mapping and adapter offset/provenance behavior.
  key_links:
    - from: llamaindex_runtime/entity/raner_adapter.py
      to: llamaindex_runtime/entity/segmenter.py
      via: Segment input → MentionCandidate coordinate projection
      pattern: segment_id|char_start|char_end
    - from: llamaindex_runtime/entity/raner_adapter.py
      to: llamaindex_runtime/entity/contracts.py
      via: MentionCandidate provenance completeness
      pattern: confidence_kind="unavailable"
---

<objective>
Implement the versioned RaNER label map and the ModelScope RaNER adapter that turns segmented corpus spans into full-provenance MentionCandidates.

Purpose: R-OKF-06 requires a runnable RaNER Large Generic adapter for C1; the adapter must use fast-tokenizer offsets, drop uncalibrated probabilities, and preserve every provenance field.
Output: TDD-tested label map and adapter (adapter logic tested against a fake tokenizer/model pipeline, never a live model).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/16-raw-corpus-entity-layer/16-RESEARCH.md
@.planning/phases/16-raw-corpus-entity-layer/16-PATTERNS.md
@.planning/research/PHASE16-CHINESE-NER-MODEL-SELECTION-2026-07-13.md
@llamaindex_runtime/entity/contracts.py
@llamaindex_runtime/entity/segmenter.py
</context>

<tasks>

<tdd_sequence>Task 1 = RED → Task 2 = GREEN → Task 2 = REFACTOR; each selector result recorded separately.</tdd_sequence>

<task type="auto" tdd="true">
  <name>Task 1 (RED): Write RED label-map and adapter tests</name>
  <read_first>@llamaindex_runtime/entity/contracts.py</read_first>
  <files>tests/llamaindex_runtime/entity/test_label_map.py, tests/llamaindex_runtime/entity/test_raner_adapter.py</files>
  <behavior>
    - Each raw label in {CORP, CW, GRP, LOC, PER, PROD} maps to its canonical label and raw_label is preserved; an unknown raw label raises a typed error carrying the label-map version.
    - An adapter fed segment + fake model output returns MentionCandidates whose char_start/char_end slice back to the mention text exactly, with confidence None and confidence_kind "unavailable", and never exposes a `prob` value.
    - If the underlying model path would require slow-tokenizer substring recovery (offset lookup fails or returns via `text[offset:].index`), the adapter raises a typed error instead of silently recovering coordinates.
    - The adapter never imports or invokes a generative LLM client; provenance fields (extractor_id/version, model_id/revision, artifact_digest, schema/normalization/segmentation versions, label_map_digest, runtime_compatibility_id) are all populated on every candidate.
  </behavior>
  <action>Write failing tests for `llamaindex_runtime/entity/label_map.py` and `llamaindex_runtime/entity/raner_adapter.py`. Specify `RAINER_LABEL_MAP_VERSION` and a frozen `RAINER_RAW_TO_CANONICAL` mapping per the frozen model selection doc §1.4; unknown labels raise `LabelMapError`. For the adapter, define `RaNERAdapter.extract_segment(segment, *, model, tokenizer, model_id, model_revision, artifact_digest, label_map_digest, runtime_compatibility_id, extractor_id, extractor_version, schema_version, normalization_version, segmentation_version) -> list[MentionCandidate]`. Test the offset projection against a deterministic fake model that returns label+char spans in fast-tokenizer space, and a fake that signals slow-path-only recovery to prove fail-closed behavior. Do NOT instantiate a real model, download artifacts, or open any network/DB connection. Run the selector in `<verify>` and record the expected RED result: the tests FAIL with ImportError/NameError because `llamaindex_runtime/entity/label_map.py` and `llamaindex_runtime/entity/raner_adapter.py` do not exist yet. Do NOT write any implementation in this RED task.</action>
  <acceptance_criteria>RED label-map and adapter tests exist; running the selector fails with ImportError/NameError for the missing `label_map.py`/`raner_adapter.py` modules (recorded RED); no implementation file was created in this task.</acceptance_criteria>
  <verify><automated>rtk env -u DATABASE_URL -u FORMAL_RUNTIME_DATABASE_URL -u OKF_MIGRATION_TEST_DATABASE_DISPOSABLE -u OKF_REBUILD_EXPECTED_DATABASE -u OKF_FAILURE_AUDIT_ACCEPTANCE -u OKF_REBUILD_DOCKER_ACCEPTANCE -u OKF_E2A_DISPOSABLE_TEST_AUTHORIZED -u OKF_E2B_DISPOSABLE_TEST_AUTHORIZED -u OKF_E2B_MIGRATION_TEST_AUTHORIZED -u OKF_E2B_MODELSCOPE_MIRROR_AUTHORIZED -u OKF_E2B_RANER_SMOKE_AUTHORIZED -u OKF_E2B_C2_ENTRY_AUTHORIZED pytest tests/llamaindex_runtime/entity/test_label_map.py tests/llamaindex_runtime/entity/test_raner_adapter.py -q</automated></verify>
  <done>Failing tests specify label-map versioning, canonical mapping, probability stripping, fast-path-only offsets, and zero generative-LLM calls.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 (GREEN → REFACTOR): Implement the label map and RaNER adapter</name>
  <read_first>@llamaindex_runtime/entity/contracts.py</read_first>
  <files>llamaindex_runtime/entity/label_map.py, llamaindex_runtime/entity/raner_adapter.py, tests/llamaindex_runtime/entity/test_label_map.py, tests/llamaindex_runtime/entity/test_raner_adapter.py</files>
  <behavior>
    - RAINER_RAW_TO_CANONICAL maps PER→Person, LOC→Location, CORP/GRP→Organization, CW→CreativeWork, PROD→Product with raw_label preserved; unknown labels raise LabelMapError carrying the label-map version.
    - RaNERAdapter.extract_segment returns MentionCandidates with confidence=None/confidence_kind="unavailable", exact code-point offsets, and the full provenance set; a slow-path-only model raises RaNEROffsetError.
    - The adapter never imports or invokes a generative LLM.
  </behavior>
  <action>GREEN: write the minimal implementation so the SAME selector passes; record the GREEN selector result. REFACTOR: then apply only behavior-preserving cleanup and re-run the SAME selector; record the REFACTOR selector result separately. Do not alter test assertions between GREEN and REFACTOR. Implement `label_map.py` with `RAINER_RAW_TO_CANONICAL = {"PER": "Person", "LOC": "Location", "CORP": "Organization", "GRP": "Organization", "CW": "CreativeWork", "PROD": "Product"}` (CORP/GRP share the canonical Organization but preserve distinct raw_label) plus a versioned lookup that raises on unknown labels. Implement `raner_adapter.py`: accept segment + injected model/tokenizer + frozen provenance; call the model once per segment; project each raw span through the fast-tokenizer offset path into Python code-point half-open `[char_start, char_end)` on the segment text; construct MentionCandidates with `confidence=None`, `confidence_kind="unavailable"`, raw_label preserved, canonical_label from the map, entity_type = canonical label (free-form TEXT per research, no enum), and the full provenance set. If the offset path cannot be satisfied by fast-tokenizer mapping (i.e. the only available recovery is slow-tokenizer substring search), raise a typed `RaNEROffsetError`; never silently recover coordinates. Never call a generative LLM; never persist. Do NOT download a model here — the adapter consumes an injected loaded model; the offline mirror is a separate plan.</action>
  <acceptance_criteria>The same selector passes twice: once after minimal GREEN implementation and once after behavior-preserving REFACTOR; both selector results are recorded separately; the assertions in test_label_map.py and test_raner_adapter.py are unchanged between the GREEN and REFACTOR runs.</acceptance_criteria>
  <verify><automated>rtk env -u DATABASE_URL -u FORMAL_RUNTIME_DATABASE_URL -u OKF_MIGRATION_TEST_DATABASE_DISPOSABLE -u OKF_REBUILD_EXPECTED_DATABASE -u OKF_FAILURE_AUDIT_ACCEPTANCE -u OKF_REBUILD_DOCKER_ACCEPTANCE -u OKF_E2A_DISPOSABLE_TEST_AUTHORIZED -u OKF_E2B_DISPOSABLE_TEST_AUTHORIZED -u OKF_E2B_MIGRATION_TEST_AUTHORIZED -u OKF_E2B_MODELSCOPE_MIRROR_AUTHORIZED -u OKF_E2B_RANER_SMOKE_AUTHORIZED -u OKF_E2B_C2_ENTRY_AUTHORIZED pytest tests/llamaindex_runtime/entity/test_label_map.py tests/llamaindex_runtime/entity/test_raner_adapter.py -q</automated></verify>
  <done>All label-map and adapter tests pass; candidates carry confidence=None/kind=unavailable, exact code-point offsets, full provenance, and no generative-LLM call.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Model raw output → mention coordinates | Uncalibrated probabilities and slow-path offsets must not leak into durable candidates. |
| Adapter → persistence | Query-derived adapter output must never be persistable. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-16-17 | T | offset projection | mitigate | Force fast-tokenizer path; typed RaNEROffsetError instead of substring recovery. |
| T-16-18 | I | probability stripping | mitigate | Drop ModelScope `prob`; store confidence=None with confidence_kind="unavailable" (D3 semantics). |
| T-16-19 | E | generative-LLM leakage | mitigate | Adapter has no LLM client import/call path (R-OKF-04). |
| T-16-20 | R | label map | mitigate | Versioned label map with canonical/raw preservation; unknown labels fail closed. |
</threat_model>

<verification>
Run only pure unit selectors with fake model/tokenizer objects. No real RaNER model, no download, no DB, no network. Live RaNER smoke is a separately-authorized gate.
</verification>

<success_criteria>
- RaNER raw labels map deterministically with raw_label preserved; unknown labels raise a versioned error.
- Candidates carry exact code-point offsets, stripped probabilities, and full provenance.
- Slow-tokenizer/substring recovery fails closed; no generative-LLM call occurs.
</success_criteria>

<output>
After completion, create `.planning/phases/16-raw-corpus-entity-layer/16-06-SUMMARY.md`.
</output>
