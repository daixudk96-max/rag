---
phase: 16-raw-corpus-entity-layer
plan: 10
type: tdd
wave: 3
macro_wave: W3
depends_on: [16-02, 16-06, 16-08]
files_modified:
  - llamaindex_runtime/entity/extractor.py
  - tests/llamaindex_runtime/entity/test_extractor.py
autonomous: true
requirements: [R-OKF-04, R-OKF-06]
must_haves:
  truths:
    - EntityExtractor.extract accepts a Sequence[ExtractionInput] (CorpusSpanInput | QueryTextInput), dispatches corpus and query variants through the same adapter/supplement/merge pipeline, and returns list[MentionCandidate].
    - Corpus-derived candidates are persistence-eligible with input_revision=document_revision; query-derived candidates use input_revision=query_revision and their audit/merger artifacts are strictly request-scoped with hard-zero persistence into durable mention/merge/alias/link/evidence stores.
    - The enabled baseline performs zero generative-LLM calls (proven by an injected LLM spy).
  artifacts:
    - path: llamaindex_runtime/entity/extractor.py
      provides: EntityExtractor composition + query persistence guard.
    - path: tests/llamaindex_runtime/entity/test_extractor.py
      provides: RED tests for dispatch, query non-persistence, and zero-LLM behavior.
  key_links:
    - from: llamaindex_runtime/entity/extractor.py
      to: llamaindex_runtime/entity/raner_adapter.py
      via: adapter dispatch for both input variants
      pattern: extract_segment
    - from: llamaindex_runtime/entity/extractor.py
      to: llamaindex_runtime/entity/resolution.py
      via: resolved decisions
      pattern: ResolutionResult
    - from: llamaindex_runtime/entity/extractor.py
      to: llamaindex_runtime/entity/merger.py
      via: merged candidates
      pattern: MergedMentions
---

<objective>
Implement the EntityExtractor composition that dispatches both corpus and query inputs through one pipeline and enforces the query request-scoped hard-zero-persistence guard.

Purpose: R-OKF-06 fixes the `EntityExtractor.extract(inputs: Sequence[ExtractionInput]) -> list[MentionCandidate]` contract and R-OKF-04 requires zero generative-LLM tokens on the enabled baseline.
Output: TDD-tested extractor with the query persistence guard.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/16-raw-corpus-entity-layer/16-RESEARCH.md
@.planning/phases/16-raw-corpus-entity-layer/16-PATTERNS.md
@llamaindex_runtime/entity/contracts.py
@llamaindex_runtime/entity/merger.py
@llamaindex_runtime/entity/raner_adapter.py
@llamaindex_runtime/entity/resolution.py
</context>

<tasks>

<tdd_sequence>Task 1 = RED → Task 2 = GREEN → Task 2 = REFACTOR; each selector result recorded separately.</tdd_sequence>

<task type="auto" tdd="true">
  <name>Task 1 (RED): Write RED tests for extractor dispatch and the query guard</name>
  <read_first>@llamaindex_runtime/entity/contracts.py</read_first>
  <files>tests/llamaindex_runtime/entity/test_extractor.py</files>
  <behavior>
    - Given a mixed Sequence[ExtractionInput], extract() returns one MentionCandidate list with corpus candidates tagged input_kind="corpus"/input_revision=document_revision and query candidates tagged input_kind="query"/input_revision=query_revision.
    - Query-derived candidates and their audit/merger artifacts expose no method that can write to entity_mentions, entity_aliases, entity_merge_log, node_entity_links, chunk_entity_links, or corpus-evidence stores (hard-zero persistence).
    - Injecting a generative-LLM spy at every seam (adapter, supplement, merge, resolve) records zero calls when RAG_ENTITY_EXTRACTOR=raner and the offline RaNER adapter is present.
    - The extractor validates its input sequence (rejects non-ExtractionInput entries and empty input) before any pipeline work.
  </behavior>
  <action>Write failing tests for `llamaindex_runtime/entity/extractor.py`. Specify `EntityExtractor(adapter, segmenter, merger, resolver, *, extractor_id, extractor_version, schema_version)` with `extract(inputs: Sequence[ExtractionInput]) -> list[MentionCandidate]`. Assert corpus vs query identity tagging, query-result request-scoping (the returned candidates for query inputs carry query_revision and the extractor returns an immutable tuple; there is no persistence callback or repository handle on the extractor or its query result), zero generative-LLM spy calls, and input validation. Use fake adapter/segmenter/merger/resolver objects; no real model or DB. Run the selector in `<verify>` and record the expected RED result: the tests FAIL with ImportError/NameError because `llamaindex_runtime/entity/extractor.py` does not exist yet. Do NOT write any implementation in this RED task.</action>
  <acceptance_criteria>RED extractor tests exist under tests/llamaindex_runtime/entity/test_extractor.py; running the selector fails with ImportError/NameError for the missing `llamaindex_runtime/entity/extractor.py` (recorded RED); no extractor implementation file was created in this task.</acceptance_criteria>
  <verify><automated>rtk env -u DATABASE_URL -u FORMAL_RUNTIME_DATABASE_URL -u OKF_MIGRATION_TEST_DATABASE_DISPOSABLE -u OKF_REBUILD_EXPECTED_DATABASE -u OKF_FAILURE_AUDIT_ACCEPTANCE -u OKF_REBUILD_DOCKER_ACCEPTANCE -u OKF_E2A_DISPOSABLE_TEST_AUTHORIZED -u OKF_E2B_DISPOSABLE_TEST_AUTHORIZED -u OKF_E2B_MIGRATION_TEST_AUTHORIZED -u OKF_E2B_MODELSCOPE_MIRROR_AUTHORIZED -u OKF_E2B_RANER_SMOKE_AUTHORIZED -u OKF_E2B_C2_ENTRY_AUTHORIZED pytest tests/llamaindex_runtime/entity/test_extractor.py -q</automated></verify>
  <done>Failing tests specify corpus/query dispatch, query hard-zero persistence, zero generative-LLM calls, and input validation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 (GREEN → REFACTOR): Implement the EntityExtractor composition</name>
  <read_first>@llamaindex_runtime/entity/contracts.py</read_first>
  <files>llamaindex_runtime/entity/extractor.py, tests/llamaindex_runtime/entity/test_extractor.py</files>
  <behavior>
    - extract() dispatches corpus inputs to input_kind="corpus"/input_revision=document_revision tagging and query inputs to input_kind="query"/input_revision=query_revision tagging through the same pipeline.
    - Query-derived candidates and audit/merger artifacts expose no persistence surface; the extractor holds no cursor/connection/repository.
    - Injecting a generative-LLM spy records zero calls; non-ExtractionInput and empty inputs are rejected before pipeline work.
  </behavior>
  <action>GREEN: write the minimal implementation so the SAME selector passes; record the GREEN selector result. REFACTOR: then apply only behavior-preserving cleanup and re-run the SAME selector; record the REFACTOR selector result separately. Do not alter test assertions between GREEN and REFACTOR. Implement `llamaindex_runtime/entity/extractor.py`: `EntityExtractor` (frozen-ish composed object) whose `extract(inputs)` validates the sequence, dispatches each input by type: CorpusSpanInput goes through segmentation → adapter/supplements → merge → resolve and tags candidates input_kind="corpus"/input_revision=document_revision; QueryTextInput goes through the same adapter/supplement/merge path but tags input_kind="query"/input_revision=query_revision and returns candidates inside an immutable request-scoped result with zero persistence surface. The extractor holds no cursor, connection, repository, or DML capability (hard-zero persistence for query artifacts per D2). It accepts injected adapter/segmenter/merger/resolver and fixed extractor provenance; it never imports or calls a generative LLM. Wire `RAG_ENTITY_EXTRACTOR=raner` selection so the composition is only built when the switch is enabled; default off builds nothing. Export `EntityExtractor` from `llamaindex_runtime/entity/__init__.py`. Do NOT add persistence or the runner here.</action>
  <acceptance_criteria>The same selector passes twice: once after minimal GREEN implementation and once after behavior-preserving REFACTOR; both selector results are recorded separately; the assertions in test_extractor.py are unchanged between the GREEN and REFACTOR runs.</acceptance_criteria>
  <verify><automated>rtk env -u DATABASE_URL -u FORMAL_RUNTIME_DATABASE_URL -u OKF_MIGRATION_TEST_DATABASE_DISPOSABLE -u OKF_REBUILD_EXPECTED_DATABASE -u OKF_FAILURE_AUDIT_ACCEPTANCE -u OKF_REBUILD_DOCKER_ACCEPTANCE -u OKF_E2A_DISPOSABLE_TEST_AUTHORIZED -u OKF_E2B_DISPOSABLE_TEST_AUTHORIZED -u OKF_E2B_MIGRATION_TEST_AUTHORIZED -u OKF_E2B_MODELSCOPE_MIRROR_AUTHORIZED -u OKF_E2B_RANER_SMOKE_AUTHORIZED -u OKF_E2B_C2_ENTRY_AUTHORIZED pytest tests/llamaindex_runtime/entity/test_extractor.py -q</automated></verify>
  <done>All extractor tests pass; corpus/query dispatch, request-scoped query results, and zero generative-LLM behavior are enforced.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Input union → pipeline | A query input must never become persistence-eligible. |
| Extract result → durability | Query audit/merger artifacts must have no durable-write surface. |
| Enabled baseline → LLM | The RaNER path must not fall back to a generative LLM. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-16-33 | T | input dispatch | mitigate | Type-based dispatch; corpus tagged document_revision, query tagged query_revision. |
| T-16-34 | D | query persistence | mitigate | Extractor holds no cursor/connection/repository; query results are immutable request-scoped (D2). |
| T-16-35 | E | LLM leakage | mitigate | No generative-LLM import/call; injected spy test proves zero calls (R-OKF-04). |
| T-16-36 | I | input validation | mitigate | Rejects non-ExtractionInput and empty inputs before pipeline work. |
</threat_model>

<verification>
Run only pure unit selectors with fake pipeline objects. No model, DB, network, or generative-LLM call occurs.
</verification>

<success_criteria>
- Corpus and query inputs share one pipeline with correct input_kind/input_revision tagging.
- Query-derived artifacts are request-scoped with hard-zero persistence.
- The enabled RaNER baseline performs zero generative-LLM calls.
</success_criteria>

<output>
After completion, create `.planning/phases/16-raw-corpus-entity-layer/16-10-SUMMARY.md`.
</output>
