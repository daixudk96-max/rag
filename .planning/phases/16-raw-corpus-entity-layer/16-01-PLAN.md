---
phase: 16-raw-corpus-entity-layer
plan: 01
type: execute
wave: 0
macro_wave: W0
depends_on: []
files_modified:
  - llamaindex_runtime/config.py
  - llamaindex_runtime/pyproject.toml
  - tests/conftest.py
  - tests/llamaindex_runtime/test_runtime_settings_phase16.py
autonomous: true
requirements: [R-OKF-04, R-OKF-06]
must_haves:
  truths:
    - The extraction switch is off by default, so the enabled baseline consumes zero generative-LLM tokens until explicitly enabled.
    - RAG_ENTITY_EXTRACTOR accepts exactly the frozen values off|raner and rejects anything else at construction time.
    - The optional ner dependency extra and the live_ner/disposable_db pytest markers exist so model and disposable-DB surfaces stay non-imported and non-collected by default.
  artifacts:
    - path: llamaindex_runtime/config.py
      provides: VALID_ENTITY_EXTRACTORS + rag_entity_extractor field with fail-fast validation and from_env read.
    - path: llamaindex_runtime/pyproject.toml
      provides: [project.optional-dependencies] ner extra + pytest live_ner/disposable_db markers.
    - path: tests/conftest.py
      provides: live_ner and disposable_db marker registration.
  key_links:
    - from: llamaindex_runtime/config.py
      to: llamaindex_runtime/entity/extractor.py
      via: rag_entity_extractor switch consumed by the C1 extractor composition
      pattern: rag_entity_extractor
    - from: tests/llamaindex_runtime/test_runtime_settings_phase16.py
      to: llamaindex_runtime/config.py
      via: RuntimeSettings fail-fast validation
      pattern: VALID_ENTITY_EXTRACTORS
---

<objective>
Wire the C1 default-off control plane and the test-infrastructure markers that keep Phase 16 non-importing and non-collected by default.

Purpose: R-OKF-04 and R-OKF-06 require the extraction feature to remain OFF until explicitly enabled, with zero ModelScope/PyTorch import and zero generative-LLM token consumption on the default path.
Output: Config switch, optional `ner` extra, and pytest markers `live_ner` and `disposable_db`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/16-raw-corpus-entity-layer/16-RESEARCH.md
@.planning/phases/16-raw-corpus-entity-layer/16-PATTERNS.md
@.planning/phases/16-raw-corpus-entity-layer/16-BOUNDARY.md
@llamaindex_runtime/config.py
@llamaindex_runtime/pyproject.toml
@tests/conftest.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the RAG_ENTITY_EXTRACTOR switch to RuntimeSettings</name>
  <read_first>@llamaindex_runtime/config.py</read_first>
  <files>llamaindex_runtime/config.py, tests/llamaindex_runtime/test_runtime_settings_phase16.py</files>
  <action>Before editing, run GitNexus impact on `RuntimeSettings` (direction: upstream) and report the exact caller/process blast radius plus all HIGH or CRITICAL warnings; proceed after recording them, stopping only if a new scope expansion requires a user decision. Per D8 and the frozen switch contract, add `VALID_ENTITY_EXTRACTORS: ClassVar[frozenset[str]] = frozenset({"off", "raner"})` alongside the existing VALID_* ClassVars, add field `rag_entity_extractor: str = "off"` with default `"off"`, add `__post_init__` validation `if self.rag_entity_extractor not in self.VALID_ENTITY_EXTRACTORS: raise ValueError(...)`, and read it in `from_env()` as `rag_entity_extractor=os.getenv("RAG_ENTITY_EXTRACTOR", "off")`. Preserve the existing `.env` fallback precedence (process env wins). Do NOT add a C2 `RAG_COREF_RESOLVER` switch here; that is scoped to the C2 entry plan. Create `tests/llamaindex_runtime/test_runtime_settings_phase16.py` asserting: default is "off"; "off" and "raner" are accepted; any other value (e.g. "uie", "hanlp", "deepke", "") raises ValueError; `from_env()` with `RAG_ENTITY_EXTRACTOR=raner` returns "raner"; the switch never triggers any model import at config construction.</action>
  <acceptance_criteria>RuntimeSettings accepts exactly off|raner, defaults to off, rejects invalid values before any entity code is imported, and never imports modelscope/torch.</acceptance_criteria>
  <verify><automated>rtk env -u DATABASE_URL -u FORMAL_RUNTIME_DATABASE_URL -u OKF_MIGRATION_TEST_DATABASE_DISPOSABLE -u OKF_REBUILD_EXPECTED_DATABASE -u OKF_FAILURE_AUDIT_ACCEPTANCE -u OKF_REBUILD_DOCKER_ACCEPTANCE -u OKF_E2A_DISPOSABLE_TEST_AUTHORIZED -u OKF_E2B_DISPOSABLE_TEST_AUTHORIZED -u OKF_E2B_MIGRATION_TEST_AUTHORIZED -u OKF_E2B_MODELSCOPE_MIRROR_AUTHORIZED -u OKF_E2B_RANER_SMOKE_AUTHORIZED -u OKF_E2B_C2_ENTRY_AUTHORIZED pytest tests/llamaindex_runtime/test_runtime_settings_phase16.py -q</automated></verify>
  <done>Config switch enforced by tests; invalid values fail fast; default path has zero model imports.</done>
</task>

<task type="auto">
  <name>Task 2: Add the optional ner extra and pytest markers</name>
  <read_first>@llamaindex_runtime/pyproject.toml</read_first>
  <files>llamaindex_runtime/pyproject.toml, tests/conftest.py</files>
  <action>Per D8, add `ner = ["modelscope>=1.39,<2", "sentencepiece"]` to `[project.optional-dependencies]` in `llamaindex_runtime/pyproject.toml` — the bare minimal surface confirmed by RESEARCH (do not add torch/transformers as direct deps; they arrive transitively via modelscope and are intentionally not imported unless the extra is installed). Extend `[tool.pytest.ini_options] markers` with `"live_ner: live ModelScope RaNER integration tests (model mirror download required)"` and `"disposable_db: tests that require a separately authorized disposable PostgreSQL/Docker target"`. In `tests/conftest.py`, register the same two markers via `config.addinivalue_line` (matching the existing `live_st`/`integration` pattern). Do not add an authorization helper module here; each live plan names its own gate. Assert marker presence in `tests/llamaindex_runtime/test_runtime_settings_phase16.py` (or a sibling `test_phase16_markers.py`) by reading `pyproject.toml` markers and conftest registrations so a marker typo is caught statically.</action>
  <acceptance_criteria>The ner extra and both markers exist in pyproject.toml and conftest.py; default pytest collection still excludes live_ner and disposable_db tests because none are created yet.</acceptance_criteria>
  <verify><automated>rtk env -u DATABASE_URL -u FORMAL_RUNTIME_DATABASE_URL -u OKF_MIGRATION_TEST_DATABASE_DISPOSABLE -u OKF_REBUILD_EXPECTED_DATABASE -u OKF_FAILURE_AUDIT_ACCEPTANCE -u OKF_REBUILD_DOCKER_ACCEPTANCE -u OKF_E2A_DISPOSABLE_TEST_AUTHORIZED -u OKF_E2B_DISPOSABLE_TEST_AUTHORIZED -u OKF_E2B_MIGRATION_TEST_AUTHORIZED -u OKF_E2B_MODELSCOPE_MIRROR_AUTHORIZED -u OKF_E2B_RANER_SMOKE_AUTHORIZED -u OKF_E2B_C2_ENTRY_AUTHORIZED pytest tests/llamaindex_runtime/test_runtime_settings_phase16.py tests/llamaindex_runtime/test_phase16_markers.py -q</automated></verify>
  <done>Optional extra and markers present and statically asserted; default collection unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Env/`.env` → RuntimeSettings | Untrusted configuration values become validated runtime switches. |
| Optional dependency extra → import surface | A misplaced dependency silently imports ModelScope/PyTorch on the default path. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-16-01 | T | config switch | mitigate | Frozen VALID_ENTITY_EXTRACTORS with fail-fast `__post_init__`; invalid values rejected before any entity import. |
| T-16-02 | D | optional extra | mitigate | `ner` extra only under `[project.optional-dependencies]`; default install does not include modelscope/torch. |
| T-16-03 | E | default path | mitigate | Default `off` switch guarantees zero generative-LLM/model import on the enabled baseline until explicit enablement (R-OKF-04). |
</threat_model>

<verification>
Run only non-DB pytest selectors during ordinary execution. No modelscope, sentencepiece, torch, network, or database access occurs in these selectors. The marker/extra assertions are static reads of pyproject.toml and conftest registrations.
</verification>

<success_criteria>
- The extraction switch is default OFF and accepts exactly off|raner.
- The `ner` optional extra exists and is not installed by default.
- `live_ner` and `disposable_db` markers are registered and excluded from default collection.
</success_criteria>

<output>
After completion, create `.planning/phases/16-raw-corpus-entity-layer/16-01-SUMMARY.md`.
</output>
