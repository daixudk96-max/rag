---
phase: 16-raw-corpus-entity-layer
plan: 12
type: execute
wave: 2
macro_wave: W4
depends_on: [16-07]
files_modified:
  - verification/phase16-raw-corpus-entity-layer/build_raner_mirror.py
  - verification/phase16-raw-corpus-entity-layer/raner_mirror_manifest.json
  - verification/phase16-raw-corpus-entity-layer/raner_mirror_evidence.md
  - tests/llamaindex_runtime/entity/test_raner_mirror_builder.py
autonomous: false
requirements: [R-OKF-06]
must_haves:
  truths:
    - A read-only RaNER offline mirror is created locally with every frozen manifest file verified by SHA-256; no external production database or service is touched.
    - Without the single-use ModelScope authorization, the mirror builder emits exactly blocked_not_executed with zero download attempts and no model load.
    - The mirror creation consumes no generative-LLM tokens and records redacted evidence only.
  artifacts:
    - path: verification/phase16-raw-corpus-entity-layer/build_raner_mirror.py
      provides: Single-use authorized ModelScope read-only mirror builder with blocked_not_executed default.
    - path: verification/phase16-raw-corpus-entity-layer/raner_mirror_manifest.json
      provides: Frozen file→SHA-256 manifest of the downloaded mirror.
    - path: verification/phase16-raw-corpus-entity-layer/raner_mirror_evidence.md
      provides: Redacted mirror-creation evidence record.
  key_links:
    - from: verification/phase16-raw-corpus-entity-layer/build_raner_mirror.py
      to: llamaindex_runtime/entity/offline_mirror.py
      via: load_mirror consumes the created mirror
      pattern: RAINER_MIRROR_MANIFEST
    - from: verification/phase16-raw-corpus-entity-layer/build_raner_mirror.py
      to: llamaindex_runtime/config.py
      via: RAG_NER_MIRROR_ROOT
      pattern: RAG_NER_MIRROR_ROOT
---

<objective>
Create the read-only RaNER offline mirror under a single-use, separately-authorized ModelScope gate (future gate 2).

Purpose: R-OKF-06 requires offline loading from a SHA-256-verified mirror; the ~2.26 GB artifact download is a live resource operation that must be individually authorized and never auto-retried.
Output: A single-use mirror builder whose default state is blocked_not_executed, plus a frozen manifest and redacted evidence.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/16-raw-corpus-entity-layer/16-RESEARCH.md
@.planning/research/PHASE16-CHINESE-NER-MODEL-SELECTION-2026-07-13.md
@llamaindex_runtime/entity/offline_mirror.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the single-use mirror builder with blocked_not_executed default</name>
  <read_first>@llamaindex_runtime/entity/offline_mirror.py</read_first>
  <files>verification/phase16-raw-corpus-entity-layer/build_raner_mirror.py, tests/llamaindex_runtime/entity/test_raner_mirror_builder.py</files>
  <action>Create `build_raner_mirror.py` implementing future gate 2. The builder REQUIRES `OKF_E2B_MODELSCOPE_MIRROR_AUTHORIZED=1` (single-use, independent authorization) and a target `RAG_NER_MIRROR_ROOT`; when absent, it emits exactly `blocked_not_executed` with zero ModelScope download and zero model load and exits non-zero. When authorized, it downloads the frozen `iic/nlp_raner_named-entity-recognition_chinese-large-generic` files (pytorch_model.bin, config.json, configuration.json, sentencepiece.bpe.model, tokenizer.json, special_tokens_map.json, tokenizer_config.json) into a read-only mirror directory, verifies each against the frozen §1.3 SHA-256 manifest, writes `raner_mirror_manifest.json`, and records redacted evidence (file list + digests only; never any URL query, credential, or connection string). It consumes zero generative-LLM tokens. The builder never reads or logs DATABASE_URL or any external DB target. Create `tests/llamaindex_runtime/entity/test_raner_mirror_builder.py` proving the default `blocked_not_executed` path and that no download/mirror exists when unauthorized.</action>
  <acceptance_criteria>Default denied with zero downloads; authorized path produces a SHA-256-verified read-only mirror and redacted evidence.</acceptance_criteria>
  <verify><automated>rtk env -u DATABASE_URL -u FORMAL_RUNTIME_DATABASE_URL -u OKF_MIGRATION_TEST_DATABASE_DISPOSABLE -u OKF_REBUILD_EXPECTED_DATABASE -u OKF_FAILURE_AUDIT_ACCEPTANCE -u OKF_REBUILD_DOCKER_ACCEPTANCE -u OKF_E2A_DISPOSABLE_TEST_AUTHORIZED -u OKF_E2B_DISPOSABLE_TEST_AUTHORIZED -u OKF_E2B_MIGRATION_TEST_AUTHORIZED -u OKF_E2B_MODELSCOPE_MIRROR_AUTHORIZED -u OKF_E2B_RANER_SMOKE_AUTHORIZED -u OKF_E2B_C2_ENTRY_AUTHORIZED pytest tests/llamaindex_runtime/entity/test_raner_mirror_builder.py -q</automated></verify>
  <done>Unauthorized invocation emits blocked_not_executed with zero download; the authorized path is ready for the single-use gate.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 2 (decision): Authorize single-use ModelScope mirror creation (future gate 2)?</name>
  <read_first>@llamaindex_runtime/entity/offline_mirror.py</read_first>
  <files>none — authorization decision only; no file changes before consent. Evidence (raner_mirror_manifest.json, raner_mirror_evidence.md) is created only by an authorized gate run, never by this decision.</files>
  <decision>Authorize the single-use ModelScope mirror download (future gate 2)?</decision>
  <context>This downloads ~2.26 GB from ModelScope into a local read-only mirror and requires network access. It is default denied. Evidence must be redacted. No auto-retry.</context>
  <options>
    <option id="authorize">
      <name>Authorize single-use mirror creation</name>
      <pros>Unblocks the RaNER offline smoke and full-corpus acceptance gates.</pros>
      <cons>Consumes ~2.26 GB disk and network; single-use only.</cons>
    </option>
    <option id="defer">
      <name>Keep blocked_not_executed</name>
      <pros>No download, no network, no disk cost.</pros>
      <cons>C1 cannot complete live RaNER smoke; phase remains PLANNED.</cons>
    </option>
  </options>
  <action>Default denied. Without the user's independent single-use authorization (OKF_E2B_MODELSCOPE_MIRROR_AUTHORIZED=1) record blocked_not_executed with zero ModelScope download and zero model load; the gate must not auto-retry, must not inherit any other authorization, and must never read or fall back to DATABASE_URL. Only after user consent does the gate run exactly once and record redacted evidence.</action>
  <acceptance_criteria>The un-authorized path records blocked_not_executed with zero download and no evidence file; the authorized path runs once and produces a SHA-256-verified mirror manifest plus redacted evidence; the default blocked state is proven by the gate-runner unit selector.</acceptance_criteria>
  <verify><automated>rtk env -u DATABASE_URL -u FORMAL_RUNTIME_DATABASE_URL -u OKF_MIGRATION_TEST_DATABASE_DISPOSABLE -u OKF_REBUILD_EXPECTED_DATABASE -u OKF_FAILURE_AUDIT_ACCEPTANCE -u OKF_REBUILD_DOCKER_ACCEPTANCE -u OKF_E2A_DISPOSABLE_TEST_AUTHORIZED -u OKF_E2B_DISPOSABLE_TEST_AUTHORIZED -u OKF_E2B_MIGRATION_TEST_AUTHORIZED -u OKF_E2B_MODELSCOPE_MIRROR_AUTHORIZED -u OKF_E2B_RANER_SMOKE_AUTHORIZED -u OKF_E2B_C2_ENTRY_AUTHORIZED pytest tests/llamaindex_runtime/entity/test_raner_mirror_builder.py -q</automated></verify>
  <done>blocked_not_executed recorded with zero live side effect; if authorized, the gate ran once and produced redacted evidence.</done>
  <resume-signal>Select "authorize" to run the builder once, or "defer" to record blocked_not_executed.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Network → local mirror | Downloaded artifacts become trusted model inputs; tampering breaks provenance. |
| Mirror builder → evidence | Evidence must never disclose URLs, credentials, or the mirror path beyond safe names. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-16-41 | T | artifact download | mitigate | Frozen SHA-256 manifest verification before mirror finalization. |
| T-16-42 | E | unauthorized download | mitigate | Single-use OKF_E2B_MODELSCOPE_MIRROR_AUTHORIZED gate; default blocked_not_executed, no auto-retry. |
| T-16-43 | I | evidence redaction | mitigate | Evidence records file list + digests only; never URL query, credential, or connection string. |
</threat_model>

<verification>
The default selector is the blocked_not_executed gate test. The authorized mirror creation is a separate single-use gate; it must not run during ordinary execution and must not auto-retry.
</verification>

<success_criteria>
- The mirror builder is default-denied with zero downloads.
- Authorized creation produces a SHA-256-verified read-only mirror and redacted evidence.
- No external production database, credential, or connection target is touched.
</success_criteria>

<output>
After completion, create `.planning/phases/16-raw-corpus-entity-layer/16-12-SUMMARY.md`.
</output>
