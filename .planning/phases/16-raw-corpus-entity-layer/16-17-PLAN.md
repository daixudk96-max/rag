---
phase: 16-raw-corpus-entity-layer
plan: 17
type: execute
wave: 7
macro_wave: W6
depends_on: [16-15, 16-16]
files_modified:
  - llamaindex_runtime/registry/migrations/021_ner_coref_clusters.sql
  - llamaindex_runtime/registry/migration_catalog.py
  - llamaindex_runtime/config.py
  - tests/llamaindex_runtime/test_runtime_settings_phase16.py
  - verification/phase16-raw-corpus-entity-layer/run_c2_entry_acceptance.py
  - verification/phase16-raw-corpus-entity-layer/c2_entry_evidence.md
autonomous: false
requirements: [R-OKF-09]
must_haves:
  truths:
    - C2 is a conditional sub-wave: it is entered only after C1 acceptance is CLOSED and a separate single-use C2 entry/live authorization is granted; otherwise it is recorded as skipped_not_entered without blocking C1-based closure.
    - Migration 021 creates coref_clusters + coref_cluster_mentions (normalized membership) per the supplement handoff, ordered after the C1 schema (ADR T5); no entity_merge_log and no canonical merge table is created.
    - RAG_COREF_RESOLVER=off|rules defaults to off; with off, C2 behavior is byte-identical to C1-only and the rules engine produces nothing.
  artifacts:
    - path: llamaindex_runtime/registry/migrations/021_ner_coref_clusters.sql
      provides: coref_clusters + coref_cluster_mentions normalized membership schema.
    - path: llamaindex_runtime/registry/migration_catalog.py
      provides: exactly-once 021 entry after 020 (provisional number re-verified at execution per ADR T5).
    - path: llamaindex_runtime/config.py
      provides: VALID_COREF_RESOLVERS + rag_coref_resolver switch default off.
    - path: verification/phase16-raw-corpus-entity-layer/run_c2_entry_acceptance.py
      provides: Conditional C2 entry + live acceptance gate with skipped_not_entered default.
  key_links:
    - from: llamaindex_runtime/entity/coref_rules.py
      to: llamaindex_runtime/registry/migrations/021_ner_coref_clusters.sql
      via: cluster membership persistence
      pattern: coref_cluster_mentions
    - from: llamaindex_runtime/config.py
      to: llamaindex_runtime/entity/coref_rules.py
      via: RAG_COREF_RESOLVER switch
      pattern: rag_coref_resolver
---

<objective>
Deliver the conditional C2 sub-wave: coref schema (migration 021), the RAG_COREF_RESOLVER switch, and a single-use entry/live acceptance gate that can also record skipped_not_entered.

Purpose: R-OKF-09 requires coref_clusters as a conservative, disable-able, default-off capability with a functional readiness gate in Phase 16. C2 must not block C1-based closure.
Output: Conditional C2 schema/config wiring and the entry gate, defaulted to not-entered.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/16-raw-corpus-entity-layer/16-RESEARCH.md
@.planning/OKF-LOW-TOKEN-GAPS-SUPPLEMENT-HANDOFF-2026-07-12.md
@.planning/v2.0-MILESTONE-OKF-MULTIROUTE.md
@llamaindex_runtime/config.py
@llamaindex_runtime/registry/migration_catalog.py
@llamaindex_runtime/entity/coref_rules.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire the C2 schema and default-off switch</name>
  <read_first>@llamaindex_runtime/config.py</read_first>
  <files>llamaindex_runtime/registry/migrations/021_ner_coref_clusters.sql, llamaindex_runtime/registry/migration_catalog.py, llamaindex_runtime/config.py, tests/llamaindex_runtime/test_runtime_settings_phase16.py</files>
  <action>Before editing, run GitNexus impact on `RuntimeSettings` and `FULL_MIGRATION_CATALOG` (direction: upstream) and report the blast radius; proceed after recording, stopping only on a new scope expansion requiring a user decision. Create `021_ner_coref_clusters.sql` defining `coref_clusters` (cluster_id UUID PK, version_id UUID NOT NULL REFERENCES document_versions ON DELETE CASCADE, coref_rules_version TEXT NOT NULL, tombstoned BOOLEAN NOT NULL DEFAULT false, created_at TIMESTAMPTZ DEFAULT now()) and `coref_cluster_mentions` (cluster_id UUID NOT NULL REFERENCES coref_clusters ON DELETE CASCADE, mention_id UUID NOT NULL REFERENCES entity_mentions ON DELETE CASCADE, ordinal_no INTEGER NOT NULL DEFAULT 0, PRIMARY KEY (cluster_id, mention_id)) per the supplement handoff §4 normalized-membership contract. Do NOT create entity_merge_log or any canonical-merge table (D7). Re-verify at execution time that 021 is the next free slot after 020 per ADR T5, then append exactly once to `FULL_MIGRATION_CATALOG`. Add to `config.py`: `VALID_COREF_RESOLVERS = frozenset({"off", "rules"})`, field `rag_coref_resolver: str = "off"`, `__post_init__` validation, and `from_env()` read of `RAG_COREF_RESOLVER`. Extend `tests/llamaindex_runtime/test_runtime_settings_phase16.py` with default-off and validation assertions. Add a non-DB static migration-spec test asserting 021 defines the two tables, PK shape, tombstone column, and no merge-log table. Do NOT run any live database; C2 acceptance is the separate entry gate. This plan's depends_on includes 16-15 (C1 full-corpus acceptance) and 16-16 (C2 rules) so the C2 entry gate cannot precede C1 acceptance CLOSED.</action>
  <acceptance_criteria>Migration 021 (provisional) defines coref_clusters + coref_cluster_mentions with normalized membership and no merge table; RAG_COREF_RESOLVER defaults to off; catalog has exactly one 021 after 020.</acceptance_criteria>
  <verify><automated>rtk env -u DATABASE_URL -u FORMAL_RUNTIME_DATABASE_URL -u OKF_MIGRATION_TEST_DATABASE_DISPOSABLE -u OKF_REBUILD_EXPECTED_DATABASE -u OKF_FAILURE_AUDIT_ACCEPTANCE -u OKF_REBUILD_DOCKER_ACCEPTANCE -u OKF_E2A_DISPOSABLE_TEST_AUTHORIZED -u OKF_E2B_DISPOSABLE_TEST_AUTHORIZED -u OKF_E2B_MIGRATION_TEST_AUTHORIZED -u OKF_E2B_MODELSCOPE_MIRROR_AUTHORIZED -u OKF_E2B_RANER_SMOKE_AUTHORIZED -u OKF_E2B_C2_ENTRY_AUTHORIZED pytest tests/llamaindex_runtime/test_runtime_settings_phase16.py tests/llamaindex_runtime/entity/test_coref_rules.py -q</automated></verify>
  <done>C2 schema/config wired with default off; static tests pass; no live database touched.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 2 (decision): Enter the C2 sub-wave and authorize its single-use live acceptance (future gate 5)?</name>
  <read_first>@llamaindex_runtime/entity/coref_rules.py</read_first>
  <files>none — authorization decision only; no file changes before consent. Evidence (c2_entry_evidence.md) is created only by an authorized gate run, never by this decision.</files>
  <decision>Enter the C2 sub-wave and authorize its single-use live acceptance (future gate 5)?</decision>
  <context>C2 enters only after C1 acceptance is CLOSED and this separate single-use entry/live authorization is granted. It runs migration 021 + coref rules against a disposable local PostgreSQL and measures the C2 functional readiness gate. Default is skipped_not_entered, which does NOT block C1-based closure.</context>
  <options>
    <option id="enter">
      <name>Enter C2 and authorize live acceptance</name>
      <pros>Closes the R-OKF-09 functional readiness gate with real evidence.</pros>
      <cons>Runs C2 migration + rules live against a disposable target; single-use only; net benefit still adjudicated at Phase 19 G3.</cons>
    </option>
    <option id="skip">
      <name>Record skipped_not_entered</name>
      <pros>No C2 live work; C1-based closure proceeds; default-off behavior stays byte-identical to C1-only.</pros>
      <cons>R-OKF-09 readiness gate remains unproven; Phase 19 G3 will decide net benefit later.</cons>
    </option>
  </options>
  <action>Default skipped_not_entered. Without the user's independent single-use authorization (OKF_E2B_C2_ENTRY_AUTHORIZED=1) AND the C1 CLOSED precondition, record skipped_not_entered with zero connection/model load and a redacted reason; the gate must not auto-retry, must not inherit any other authorization, and must never read or fall back to DATABASE_URL. Only after user consent and C1 CLOSED does the gate run exactly once against a strict loopback disposable target and record redacted evidence.</action>
  <acceptance_criteria>The un-authorized / C1-not-CLOSED path records skipped_not_entered with zero connection and a redacted reason, never described as R-OKF-09 tested; the authorized path runs once and proves migration 021 + rules readiness, default-off parity, and tombstone behavior; the default skipped state is proven by the gate-runner unit selector.</acceptance_criteria>
  <verify><automated>rtk env -u DATABASE_URL -u FORMAL_RUNTIME_DATABASE_URL -u OKF_MIGRATION_TEST_DATABASE_DISPOSABLE -u OKF_REBUILD_EXPECTED_DATABASE -u OKF_FAILURE_AUDIT_ACCEPTANCE -u OKF_REBUILD_DOCKER_ACCEPTANCE -u OKF_E2A_DISPOSABLE_TEST_AUTHORIZED -u OKF_E2B_DISPOSABLE_TEST_AUTHORIZED -u OKF_E2B_MIGRATION_TEST_AUTHORIZED -u OKF_E2B_MODELSCOPE_MIRROR_AUTHORIZED -u OKF_E2B_RANER_SMOKE_AUTHORIZED -u OKF_E2B_C2_ENTRY_AUTHORIZED pytest tests/llamaindex_runtime/entity/test_coref_rules.py -q</automated></verify>
  <done>skipped_not_entered recorded with redacted reason and zero connection; if authorized and C1 CLOSED, the gate ran once and produced redacted evidence.</done>
  <resume-signal>Select "enter" to run the C2 acceptance once, or "skip" to record skipped_not_entered.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Implement the C2 entry acceptance runner</name>
  <read_first>@llamaindex_runtime/entity/coref_rules.py</read_first>
  <files>verification/phase16-raw-corpus-entity-layer/run_c2_entry_acceptance.py, verification/phase16-raw-corpus-entity-layer/c2_entry_evidence.md</files>
  <action>Create `run_c2_entry_acceptance.py` implementing the C2 entry gate. Its default state records `skipped_not_entered` with zero connections/model loads. If the single-use authorization (`OKF_E2B_C2_ENTRY_AUTHORIZED=1`) and the C1 CLOSED precondition are both met, it (1) applies migration 021 on a disposable schema after 020, (2) runs the coref rules over selected resolved mentions from a C1 materialized corpus, (3) asserts default-off (RAG_COREF_RESOLVER=off) behavior is byte-identical to C1-only (zero clusters, zero C2 DML), (4) writes normalized cluster membership rows and applies a per-cluster tombstone soft delete, and (5) records redacted evidence. If skipped, write `c2_entry_evidence.md` recording the skipped_not_entered reason without claiming R-OKF-09 as tested. Never read/log DATABASE_URL or external targets; no generative-LLM token.</action>
  <acceptance_criteria>The gate defaults to skipped_not_entered; under authorization it proves migration 021 + rules readiness, default-off parity, and tombstone behavior with redacted evidence.</acceptance_criteria>
  <verify><automated>rtk env -u DATABASE_URL -u FORMAL_RUNTIME_DATABASE_URL -u OKF_MIGRATION_TEST_DATABASE_DISPOSABLE -u OKF_REBUILD_EXPECTED_DATABASE -u OKF_FAILURE_AUDIT_ACCEPTANCE -u OKF_REBUILD_DOCKER_ACCEPTANCE -u OKF_E2A_DISPOSABLE_TEST_AUTHORIZED -u OKF_E2B_DISPOSABLE_TEST_AUTHORIZED -u OKF_E2B_MIGRATION_TEST_AUTHORIZED -u OKF_E2B_MODELSCOPE_MIRROR_AUTHORIZED -u OKF_E2B_RANER_SMOKE_AUTHORIZED -u OKF_E2B_C2_ENTRY_AUTHORIZED pytest tests/llamaindex_runtime/entity/test_coref_rules.py -q</automated></verify>
  <done>Default is skipped_not_entered with redacted reason; authorized path proves C2 functional readiness without claiming net benefit.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| C2 schema → existing schema | Migration 021 must not alter C1 tables or create a canonical merge path. |
| C2 rules → durable clusters | Cluster writes must be local, normalized, tombstonable, and default-off. |
| C2 entry → live DB | Entry requires C1 CLOSED + single-use authorization; default skipped_not_entered. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-16-61 | T | C2 schema isolation | mitigate | coref_clusters + coref_cluster_mentions only; no entity_merge_log / canonical-merge table (D7). |
| T-16-62 | E | unauthorized entry | mitigate | skipped_not_entered default; single-use OKF_E2B_C2_ENTRY_AUTHORIZED + C1 CLOSED precondition. |
| T-16-63 | I | default-off parity | mitigate | RAG_COREF_RESOLVER=off yields byte-identical behavior to C1-only (R-OKF-09). |
| T-16-64 | R | evidence honesty | mitigate | skipped_not_entered never described as R-OKF-09 tested. |
</threat_model>

<verification>
The default is skipped_not_entered with redacted reason. Authorized C2 acceptance runs once on a disposable local target and must never auto-retry. C1-based closure is not blocked by a skipped C2.
</verification>

<success_criteria>
- Migration 021 defines coref_clusters + coref_cluster_mentions with normalized membership and no merge table.
- RAG_COREF_RESOLVER defaults to off; off behavior is byte-identical to C1-only.
- The entry gate defaults to skipped_not_entered and, only under single-use authorization + C1 CLOSED, proves the readiness gate with redacted evidence.
- The gate cannot precede C1 acceptance CLOSED (depends_on includes 16-15 and 16-16).
</success_criteria>

<output>
After completion, create `.planning/phases/16-raw-corpus-entity-layer/16-17-SUMMARY.md`.
</output>
