---
phase: 16-raw-corpus-entity-layer
plan: 02
type: tdd
wave: 0
macro_wave: W0
depends_on: []
files_modified:
  - llamaindex_runtime/entity/__init__.py
  - llamaindex_runtime/entity/contracts.py
  - tests/llamaindex_runtime/entity/__init__.py
  - tests/llamaindex_runtime/entity/test_contracts.py
autonomous: true
requirements: [R-OKF-06]
must_haves:
  truths:
    - A discriminated CorpusSpanInput | QueryTextInput union shares input_id/input_revision, normalized-text coordinates, and provenance fields; corpus carries input_revision equal to its document_revision FIELD VALUE and a document/sidecar projection; query carries input_revision equal to its query_revision FIELD VALUE. input_revision is never a fixed literal "document_revision" or "query_revision".
    - MentionCandidate carries the full frozen provenance set (extractor/model/artifact/schema/normalization/segmentation/label-map/runtime) and the invariant 0 <= char_start < char_end <= len(normalized_text) with normalized_text[char_start:char_end] == mention_text.
    - The E2b identity namespace and canonical JSON/hash helpers produce deterministic identities reusable by the merger, resolver, and materialization repository.
  artifacts:
    - path: llamaindex_runtime/entity/contracts.py
      provides: E2b immutable DTOs (ExtractionInput union, CorpusSpanInput, QueryTextInput, MentionCandidate) with provenance and offset invariants.
    - path: tests/llamaindex_runtime/entity/test_contracts.py
      provides: RED tests for the discriminated union, provenance completeness, and code-point offset invariant.
  key_links:
    - from: llamaindex_runtime/entity/contracts.py
      to: llamaindex_runtime/okf/e2a_contracts.py
      via: Reused canonical_json / deterministic_id / E2a namespace pattern
      pattern: deterministic_id|canonical_json
    - from: tests/llamaindex_runtime/entity/test_contracts.py
      to: llamaindex_runtime/entity/contracts.py
      via: MentionCandidate.__post_init__ invariant
      pattern: normalized_text\[char_start:char_end\]
---

<objective>
Define the frozen E2b immutable contract layer that every later adapter, merger, resolver, and repository implements against.

Purpose: R-OKF-06 requires a pluggable, schema-independent extractor interface with a discriminated input union, shared provenance, and exact normalized-text coordinate semantics that fail closed before persistence.
Output: TDD-tested `entity/contracts.py` DTOs with full provenance and offset invariants.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/16-raw-corpus-entity-layer/16-RESEARCH.md
@.planning/phases/16-raw-corpus-entity-layer/16-PATTERNS.md
@.planning/research/PHASE16-CHINESE-NER-MODEL-SELECTION-2026-07-13.md
@llamaindex_runtime/okf/e2a_contracts.py
@llamaindex_runtime/registry/migrations/016_entity_mentions.sql
</context>

<tasks>

<tdd_sequence>Task 1 = RED → Task 2 = GREEN → Task 2 = REFACTOR; each selector result recorded separately.</tdd_sequence>

<task type="auto" tdd="true">
  <name>Task 1 (RED): Write RED contract tests for the E2b union and provenance</name>
  <read_first>@llamaindex_runtime/okf/e2a_contracts.py</read_first>
  <files>tests/llamaindex_runtime/entity/test_contracts.py</files>
  <behavior>
    - A corpus input requires document_id, version_id, span_id, normalized_text, input_revision equal to its document_revision FIELD VALUE, normalization_version, and a document/sidecar projection; a query input requires normalized_text and input_revision equal to its query_revision FIELD VALUE and no document/sidecar projection. The input_revision equality is to the corresponding field VALUE — never a fixed literal "document_revision" / "query_revision".
    - Constructing a MentionCandidate with start >= end, start < 0, end > len(normalized_text), or a mention that does not slice back from normalized_text fails closed.
    - Every provenance field (extractor_id/version, model_id/revision, artifact_digest, schema_version, normalization_version, segmentation_version, label_map_digest, runtime_compatibility_id, source, confidence_kind) is mandatory where the contract requires it; confidence may be None only when confidence_kind == "unavailable".
    - deterministic_id(kind, natural_key) under the E2b namespace is stable and namespace-scoped, and canonical_json_sha256 produces the same digest for equal immutable inputs.
  </behavior>
  <action>Per the frozen C1 coordinate/provenance contract, write failing tests for `llamaindex_runtime/entity/contracts.py` before any implementation. Specify the `E2B_NAMESPACE` derived from `uuid5(NAMESPACE_URL, "https://gitnexus.local/okf/e2b")`, a `MentionCandidate` frozen dataclass (input_id, input_kind, input_revision, span_id, segment_id, char_start, char_end, mention_text, raw_label, canonical_label, entity_type, confidence, confidence_kind, source, extractor_id, extractor_version, model_id, model_revision, artifact_digest, schema_version, normalization_version, segmentation_version, label_map_digest, runtime_compatibility_id, and corpus-only document/sidecar projection fields), a `CorpusSpanInput`, a `QueryTextInput`, and an `ExtractionInput` Literal union. Encode the invariant `0 <= char_start < char_end <= len(normalized_text)` and `normalized_text[char_start:char_end] == mention_text` in `__post_init__`. Encode the revision invariant as FIELD-VALUE equality: `CorpusSpanInput.input_revision == CorpusSpanInput.document_revision` and `QueryTextInput.input_revision == QueryTextInput.query_revision` — the test must reject an implementation that hard-codes the literal strings "document_revision" or "query_revision" (e.g. a corpus input whose document_revision field is "abc-v2" must carry input_revision == "abc-v2", never the constant "document_revision"). Use the existing `canonical_json`/`deterministic_id` pattern from `e2a_contracts.py` (shared constants may be imported, never copied logic). No model, DB, or network access. Run the selector in `<verify>` and record the expected RED result: the tests FAIL with ImportError/NameError because `llamaindex_runtime/entity/contracts.py` does not exist yet. Do NOT write any implementation in this RED task.</action>
  <acceptance_criteria>RED contract tests exist under tests/llamaindex_runtime/entity/test_contracts.py; running the selector fails with ImportError/NameError for the missing `llamaindex_runtime/entity/contracts.py` (recorded RED); no contracts.py, entity/__init__.py, or any implementation file was created in this task.</acceptance_criteria>
  <verify><automated>rtk env -u DATABASE_URL -u FORMAL_RUNTIME_DATABASE_URL -u OKF_MIGRATION_TEST_DATABASE_DISPOSABLE -u OKF_REBUILD_EXPECTED_DATABASE -u OKF_FAILURE_AUDIT_ACCEPTANCE -u OKF_REBUILD_DOCKER_ACCEPTANCE -u OKF_E2A_DISPOSABLE_TEST_AUTHORIZED -u OKF_E2B_DISPOSABLE_TEST_AUTHORIZED -u OKF_E2B_MIGRATION_TEST_AUTHORIZED -u OKF_E2B_MODELSCOPE_MIRROR_AUTHORIZED -u OKF_E2B_RANER_SMOKE_AUTHORIZED -u OKF_E2B_C2_ENTRY_AUTHORIZED pytest tests/llamaindex_runtime/entity/test_contracts.py -q</automated></verify>
  <done>Failing tests specify the discriminated union, provenance completeness, code-point offset invariant, and deterministic identity without any model/DB/network dependency.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 (GREEN → REFACTOR): Implement the E2b contracts module</name>
  <read_first>@llamaindex_runtime/okf/e2a_contracts.py</read_first>
  <files>llamaindex_runtime/entity/__init__.py, llamaindex_runtime/entity/contracts.py, tests/llamaindex_runtime/entity/__init__.py, tests/llamaindex_runtime/entity/test_contracts.py</files>
  <behavior>
    - The module exports the discriminated CorpusSpanInput | QueryTextInput union, a frozen MentionCandidate with the full frozen provenance set, and E2b identity helpers (E2B_NAMESPACE, canonical_json, canonical_json_sha256, deterministic_id).
    - Constructing a MentionCandidate with out-of-range or non-slice-back coordinates raises; no substring recovery is attempted.
    - CorpusSpanInput enforces input_revision == document_revision (field-VALUE equality, never the literal string) with a document/sidecar projection; QueryTextInput enforces input_revision == query_revision (field-VALUE equality, never the literal string) with no projection.
    - Importing llamaindex_runtime.entity requires zero modelscope/torch/jieba imports.
  </behavior>
  <action>GREEN: write the minimal implementation so the SAME selector passes; record the GREEN selector result. REFACTOR: then apply only behavior-preserving cleanup (naming/docs/private helpers) and re-run the SAME selector; record the REFACTOR selector result separately. Do not alter test assertions between GREEN and REFACTOR. Implement `llamaindex_runtime/entity/contracts.py` to make the RED tests pass. Use frozen dataclasses with `__post_init__` re-validation for every DTO, matching the E2a style (no setters, no mutation). Define `E2B_NAMESPACE`, `canonical_json` (re-export or thin wrap of `e2a_contracts.canonical_json`), `canonical_json_sha256`, and `deterministic_id` bound to the E2b namespace. `MentionCandidate.__post_init__` MUST validate the Unicode code-point slice invariant with the exact half-open semantics from R-OKF-06 and MUST NOT attempt substring recovery. CorpusSpanInput enforces the presence of document/sidecar projection and the invariant `input_revision == document_revision` where `document_revision` is the FIELD VALUE carried by the corpus input — NOT the fixed string "document_revision"; QueryTextInput enforces `input_revision == query_revision` where `query_revision` is the FIELD VALUE carried by the query input — NOT the fixed string "query_revision" — and absence of projection. Expose an `ExtractionInput` union type. Create package `__init__.py` exporting the public surface (contracts only; do not import modelscope/torch/jieba). Do NOT create any persistence, merger, adapter, or runner code in this plan.</action>
  <acceptance_criteria>The same selector passes twice: once after minimal GREEN implementation and once after behavior-preserving REFACTOR; both selector results are recorded separately; the assertions in test_contracts.py are unchanged between the GREEN and REFACTOR runs.</acceptance_criteria>
  <verify><automated>rtk env -u DATABASE_URL -u FORMAL_RUNTIME_DATABASE_URL -u OKF_MIGRATION_TEST_DATABASE_DISPOSABLE -u OKF_REBUILD_EXPECTED_DATABASE -u OKF_FAILURE_AUDIT_ACCEPTANCE -u OKF_REBUILD_DOCKER_ACCEPTANCE -u OKF_E2A_DISPOSABLE_TEST_AUTHORIZED -u OKF_E2B_DISPOSABLE_TEST_AUTHORIZED -u OKF_E2B_MIGRATION_TEST_AUTHORIZED -u OKF_E2B_MODELSCOPE_MIRROR_AUTHORIZED -u OKF_E2B_RANER_SMOKE_AUTHORIZED -u OKF_E2B_C2_ENTRY_AUTHORIZED pytest tests/llamaindex_runtime/entity/test_contracts.py -q</automated></verify>
  <done>All contract tests pass; the union and provenance DTOs are immutable, namespace-bound, and importable without any heavy dependency.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Untrusted raw corpus text → DTO | Malformed coordinate claims cross into validated candidates. |
| Contract layer → downstream persistence | A corrupted provenance field could be persisted as durable evidence. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-16-04 | T | MentionCandidate offsets | mitigate | `__post_init__` enforces 0 <= start < end <= len and exact slice-back; no substring search recovery. |
| T-16-05 | I | provenance fields | mitigate | Mandatory provenance DTO fields with typed validation; confidence None only when confidence_kind == "unavailable". |
| T-16-06 | R | input identity | mitigate | Deterministic uuid5 under E2B namespace for input/mention identity, preventing identity collisions across sources. |
</threat_model>

<verification>
Run only the pure unit selectors. No model download, no database connection, no network. The slice invariant is enforced entirely in Python on the frozen DTO.
</verification>

<success_criteria>
- The discriminated CorpusSpanInput | QueryTextInput union and full-provenance MentionCandidate exist and are immutable.
- The exact Unicode code-point slice invariant fails closed before any downstream persistence.
- E2b deterministic identity helpers are namespace-bound and stable.
- CorpusSpanInput.input_revision equals CorpusSpanInput.document_revision and QueryTextInput.input_revision equals QueryTextInput.query_revision as field-VALUE equality; the literal strings "document_revision"/"query_revision" are never hard-coded.
</success_criteria>

<output>
After completion, create `.planning/phases/16-raw-corpus-entity-layer/16-02-SUMMARY.md`.
</output>
