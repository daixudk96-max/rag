---
phase: 16-raw-corpus-entity-layer
plan: 03
type: tdd
wave: 1
macro_wave: W0
depends_on: [16-02]
files_modified:
  - llamaindex_runtime/entity/merger.py
  - tests/llamaindex_runtime/entity/test_merger.py
autonomous: true
requirements: [R-OKF-06]
must_haves:
  truths:
    - Every raw overlapping candidate is retained before a deterministic, versioned merger marks selected/suppressed/duplicate/overlap/nested/grouped outcomes.
    - Merger state lives only in memory and is deterministically reconstructable from the raw candidate set plus the merger version.
    - Query-origin candidate sets never produce any durable audit/mention/merge/alias/link/evidence artifact.
  artifacts:
    - path: llamaindex_runtime/entity/merger.py
      provides: Deterministic, versioned in-memory candidate merger with outcome attribution.
    - path: tests/llamaindex_runtime/entity/test_merger.py
      provides: RED tests for overlap/nested/duplicate/selected/suppressed/grouped outcomes and query-scoped non-persistence.
  key_links:
    - from: llamaindex_runtime/entity/merger.py
      to: llamaindex_runtime/entity/contracts.py
      via: MentionCandidate input and output
      pattern: MentionCandidate
    - from: tests/llamaindex_runtime/entity/test_merger.py
      to: llamaindex_runtime/entity/merger.py
      via: Deterministic merge reconstruction
      pattern: merge_version|selected|suppressed
---

<objective>
Implement the deterministic candidate merger that resolves raw overlapping candidates into a single selected mention set without any durable candidate audit.

Purpose: R-OKF-06 and D2 require every raw candidate retained before a deterministic, versioned merger marks outcomes, and forbid durable candidate-audit storage.
Output: TDD-tested in-memory merger with versioned selection semantics.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/16-raw-corpus-entity-layer/16-RESEARCH.md
@.planning/phases/16-raw-corpus-entity-layer/16-PATTERNS.md
@.planning/phases/16-raw-corpus-entity-layer/16-02-SUMMARY.md
@llamaindex_runtime/entity/contracts.py
</context>

<tasks>

<tdd_sequence>Task 1 = RED → Task 2 = GREEN → Task 2 = REFACTOR; each selector result recorded separately.</tdd_sequence>

<task type="auto" tdd="true">
  <name>Task 1 (RED): Write RED merger tests for outcome attribution and determinism</name>
  <read_first>@llamaindex_runtime/entity/contracts.py</read_first>
  <files>tests/llamaindex_runtime/entity/test_merger.py</files>
  <behavior>
    - Given raw candidates with overlaps/nesting/duplicates, the merger returns a merged result that marks exactly one selected candidate per resolved mention and attributes every non-selected candidate to duplicate/overlap/nested/suppressed/grouped with a reason.
    - Identical raw candidate sets in different input orders produce byte-identical merged results (determinism under shuffle).
    - The merger is a pure function of (raw_candidates, merger_version); no persistence layer is touched and no database/cursor/connection is referenced.
    - Candidates whose input_kind == "query" are merged in a request-scoped result that offers no path to durable mention/merge/alias/link/evidence writes.
  </behavior>
  <action>Write failing tests for a `merge_mentions(raw_candidates: Sequence[MentionCandidate], *, merger_version: str) -> MergedMentions` function (frozen result with selected tuple, suppressed outcome map, and merger_version). Encode the versioned selection semantics: exact duplicate retention, containment/nested suppression, partial-overlap resolution, and grouped outcomes; assert the raw candidates are never mutated and the result is reconstructable from the raw set plus version. Assert query-origin candidates stay inside a request-scoped result with no durable-write surface (the result type offers no repository/DML method). No model, DB, or network access. Run the selector in `<verify>` and record the expected RED result: the tests FAIL with ImportError/NameError because `llamaindex_runtime/entity/merger.py` does not exist yet. Do NOT write any implementation in this RED task.</action>
  <acceptance_criteria>RED merger tests exist under tests/llamaindex_runtime/entity/test_merger.py; running the selector fails with ImportError/NameError for the missing `llamaindex_runtime/entity/merger.py` (recorded RED); no merger implementation file was created in this task.</acceptance_criteria>
  <verify><automated>rtk env -u DATABASE_URL -u FORMAL_RUNTIME_DATABASE_URL -u OKF_MIGRATION_TEST_DATABASE_DISPOSABLE -u OKF_REBUILD_EXPECTED_DATABASE -u OKF_FAILURE_AUDIT_ACCEPTANCE -u OKF_REBUILD_DOCKER_ACCEPTANCE -u OKF_E2A_DISPOSABLE_TEST_AUTHORIZED -u OKF_E2B_DISPOSABLE_TEST_AUTHORIZED -u OKF_E2B_MIGRATION_TEST_AUTHORIZED -u OKF_E2B_MODELSCOPE_MIRROR_AUTHORIZED -u OKF_E2B_RANER_SMOKE_AUTHORIZED -u OKF_E2B_C2_ENTRY_AUTHORIZED pytest tests/llamaindex_runtime/entity/test_merger.py -q</automated></verify>
  <done>Failing tests specify overlap/nested/duplicate/selected/suppressed/grouped outcome attribution, shuffle determinism, and query-scoped non-persistence.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 (GREEN → REFACTOR): Implement the deterministic in-memory merger</name>
  <read_first>@llamaindex_runtime/entity/contracts.py</read_first>
  <files>llamaindex_runtime/entity/merger.py, tests/llamaindex_runtime/entity/test_merger.py</files>
  <behavior>
    - merge_mentions returns an immutable MergedMentions with a selected tuple, outcome attribution for every raw candidate, and merger_version.
    - Shuffle-equivalent raw candidate sets produce byte-identical merged results.
    - The merger never imports persistence/DB/cursor/connection symbols; query-origin results expose no durable-write method.
  </behavior>
  <action>GREEN: write the minimal implementation so the SAME selector passes; record the GREEN selector result. REFACTOR: then apply only behavior-preserving cleanup and re-run the SAME selector; record the REFACTOR selector result separately. Do not alter test assertions between GREEN and REFACTOR. Implement `llamaindex_runtime/entity/merger.py` to make the RED tests pass. Provide `merge_mentions(...)` that: (1) sorts raw candidates deterministically by (input_id, span_id, char_start, char_end, source), (2) classifies each against the already-selected set into selected/duplicate/overlap/nested/suppressed/grouped, (3) returns an immutable `MergedMentions` carrying the selected tuple, an outcome map, and merger_version, and (4) never imports or touches persistence, DB, cursor, or connection symbols. Treat "unavailable" confidence candidates as retainable (never dropped for low confidence; D3 semantics are resolved later by the resolver, not the merger). Add `merge_mentions` and `MergedMentions` to `llamaindex_runtime/entity/__init__.py`. Do NOT create durable candidate-audit tables or any merge-log writes; D2 keeps all candidate state in memory.</action>
  <acceptance_criteria>The same selector passes twice: once after minimal GREEN implementation and once after behavior-preserving REFACTOR; both selector results are recorded separately; the assertions in test_merger.py are unchanged between the GREEN and REFACTOR runs.</acceptance_criteria>
  <verify><automated>rtk env -u DATABASE_URL -u FORMAL_RUNTIME_DATABASE_URL -u OKF_MIGRATION_TEST_DATABASE_DISPOSABLE -u OKF_REBUILD_EXPECTED_DATABASE -u OKF_FAILURE_AUDIT_ACCEPTANCE -u OKF_REBUILD_DOCKER_ACCEPTANCE -u OKF_E2A_DISPOSABLE_TEST_AUTHORIZED -u OKF_E2B_DISPOSABLE_TEST_AUTHORIZED -u OKF_E2B_MIGRATION_TEST_AUTHORIZED -u OKF_E2B_MODELSCOPE_MIRROR_AUTHORIZED -u OKF_E2B_RANER_SMOKE_AUTHORIZED -u OKF_E2B_C2_ENTRY_AUTHORIZED pytest tests/llamaindex_runtime/entity/test_merger.py -q</automated></verify>
  <done>All merger tests pass; the merger is pure, deterministic, versioned, and has zero persistence surface.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Raw candidates → merged set | A malformed overlapping candidate set could silently drop or duplicate a selected mention. |
| Merged result → persistence | The merged result must not be coercible into a durable candidate-audit write. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-16-07 | T | merger selection | mitigate | Deterministic sort-then-classify with explicit outcome attribution for every candidate; no partial loss. |
| T-16-08 | D | durable candidate audit | mitigate | Merger is pure and returns an immutable result with no DML/repository method; query results are request-scoped. |
| T-16-09 | I | deterministic reconstruction | mitigate | MergedMentions is a pure function of (raw candidates, merger_version); shuffle-equivalent inputs produce identical results. |
</threat_model>

<verification>
Run only pure unit selectors. No persistence, no model, no network. Determinism is verified under shuffled input order.
</verification>

<success_criteria>
- Every raw overlapping candidate is retained and outcome-attributed before selection.
- The merger is deterministic, versioned, and purely in-memory.
- Query-origin candidate sets expose no durable-write path.
</success_criteria>

<output>
After completion, create `.planning/phases/16-raw-corpus-entity-layer/16-03-SUMMARY.md`.
</output>
