---
phase: 16-raw-corpus-entity-layer
plan: 08
type: tdd
wave: 2
macro_wave: W3
depends_on: [16-02, 16-03]
files_modified:
  - llamaindex_runtime/entity/dictionary_loader.py
  - llamaindex_runtime/entity/frontmatter_supplement.py
  - llamaindex_runtime/entity/resolution.py
  - tests/llamaindex_runtime/entity/test_supplementary_sources.py
  - tests/llamaindex_runtime/entity/test_resolution.py
autonomous: true
requirements: [R-OKF-06]
must_haves:
  truths:
    - Domain dictionaries and OKF frontmatter declarations produce high-confidence supplementary candidates with source and confidence_kind attribution; a dictionary-only pipeline never satisfies C1 acceptance.
    - The versioned non-numeric conflict priority is frontmatter_declared > dictionary_exact > rule_weight > model_probability > unavailable, and "unavailable" is never treated as low confidence.
    - Canonical resolution attaches an existing entity only on an exact compatible canonical-name or an exact OKF alias/dictionary match; otherwise entity_id stays NULL (pending) with no canonical entity creation.
  artifacts:
    - path: llamaindex_runtime/entity/dictionary_loader.py
      provides: Dictionary-exact supplementary candidate loader.
    - path: llamaindex_runtime/entity/frontmatter_supplement.py
      provides: OKF frontmatter-declared candidate supplement.
    - path: llamaindex_runtime/entity/resolution.py
      provides: D3 conflict priority + D1 canonical resolution decision.
    - path: tests/llamaindex_runtime/entity/test_supplementary_sources.py, tests/llamaindex_runtime/entity/test_resolution.py
      provides: RED tests for source supplementation, priority, and canonical attach/pending behavior.
  key_links:
    - from: llamaindex_runtime/entity/resolution.py
      to: llamaindex_runtime/entity/contracts.py
      via: MentionCandidate.source/confidence_kind
      pattern: confidence_kind
    - from: llamaindex_runtime/entity/dictionary_loader.py
      to: llamaindex_runtime/entity/contracts.py
      via: Candidate source attribution
      pattern: dictionary_exact
---

<objective>
Implement the deterministic supplementary sources and the D3/D1 resolution rules that decide which candidates attach to existing entities and which stay pending.

Purpose: R-OKF-06 requires dictionaries/frontmatter/rules to supplement (never replace) the RaNER model, and D1/D3 freeze how priority and canonical attachment behave without creating canonical entities from NER.
Output: TDD-tested dictionary/frontmatter supplements and the resolution engine.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/16-raw-corpus-entity-layer/16-RESEARCH.md
@.planning/phases/16-raw-corpus-entity-layer/16-PATTERNS.md
@llamaindex_runtime/entity/contracts.py
@llamaindex_runtime/entity/merger.py
</context>

<tasks>

<tdd_sequence>Task 1 = RED → Task 2 = GREEN → Task 2 = REFACTOR; each selector result recorded separately.</tdd_sequence>

<task type="auto" tdd="true">
  <name>Task 1 (RED): Write RED tests for supplementary sources and resolution</name>
  <read_first>@llamaindex_runtime/entity/contracts.py</read_first>
  <files>tests/llamaindex_runtime/entity/test_supplementary_sources.py, tests/llamaindex_runtime/entity/test_resolution.py</files>
  <behavior>
    - A domain dictionary yields candidates tagged source="dictionary_exact" with exact code-point offsets; an OKF frontmatter declaration yields candidates tagged source="frontmatter_declared"; neither invokes the model and neither alone satisfies acceptance.
    - Given conflicting candidates for the same mention, the resolver chooses frontmatter_declared over dictionary_exact over rule_weight over model_probability over unavailable, and does not drop a candidate merely because confidence_kind == "unavailable".
    - Resolution attaches entity_id only when the candidate's canonical name exactly matches an existing entity canonical_name, or an OKF alias/dictionary exact match resolves; otherwise entity_id is None (pending) and no entities row is created.
    - Resolution is a pure function returning an immutable decision; it never writes to a database.
  </behavior>
  <action>Write failing tests for `dictionary_loader.py` (`load_dictionary_candidates(dictionary, span) -> list[MentionCandidate]`), `frontmatter_supplement.py` (`frontmatter_declared_candidates(frontmatter, span) -> list[MentionCandidate]`), and `resolution.py` (`resolve_candidates(candidates, *, entities, aliases, priority_version) -> ResolutionResult` with per-candidate entity_id or None and a chosen-priority record). Encode the frozen D3 priority and the D1 attach rule (exact compatible canonical-name OR exact OKF alias/dictionary match only). Assert dictionary/frontmatter candidates never consult the model, and resolution never creates entities. No model, DB, or network access. Run the selector in `<verify>` and record the expected RED result: the tests FAIL with ImportError/NameError because `dictionary_loader.py`, `frontmatter_supplement.py`, and `resolution.py` do not exist yet. Do NOT write any implementation in this RED task.</action>
  <acceptance_criteria>RED supplement/resolution tests exist; running the selector fails with ImportError/NameError for the missing `dictionary_loader.py`/`frontmatter_supplement.py`/`resolution.py` modules (recorded RED); no implementation file was created in this task.</acceptance_criteria>
  <verify><automated>rtk env -u DATABASE_URL -u FORMAL_RUNTIME_DATABASE_URL -u OKF_MIGRATION_TEST_DATABASE_DISPOSABLE -u OKF_REBUILD_EXPECTED_DATABASE -u OKF_FAILURE_AUDIT_ACCEPTANCE -u OKF_REBUILD_DOCKER_ACCEPTANCE -u OKF_E2A_DISPOSABLE_TEST_AUTHORIZED -u OKF_E2B_DISPOSABLE_TEST_AUTHORIZED -u OKF_E2B_MIGRATION_TEST_AUTHORIZED -u OKF_E2B_MODELSCOPE_MIRROR_AUTHORIZED -u OKF_E2B_RANER_SMOKE_AUTHORIZED -u OKF_E2B_C2_ENTRY_AUTHORIZED pytest tests/llamaindex_runtime/entity/test_supplementary_sources.py tests/llamaindex_runtime/entity/test_resolution.py -q</automated></verify>
  <done>Failing tests specify dictionary/frontmatter attribution, the exact D3 priority order, the D1 attach/pending rule, and pure non-persisting resolution.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 (GREEN → REFACTOR): Implement the supplements and resolution engine</name>
  <read_first>@llamaindex_runtime/entity/contracts.py</read_first>
  <files>llamaindex_runtime/entity/dictionary_loader.py, llamaindex_runtime/entity/frontmatter_supplement.py, llamaindex_runtime/entity/resolution.py, tests/llamaindex_runtime/entity/test_supplementary_sources.py, tests/llamaindex_runtime/entity/test_resolution.py</files>
  <behavior>
    - Dictionary/frontmatter supplements emit MentionCandidates with source/confidence_kind attribution and exact code-point offsets, never invoking the model.
    - resolve_candidates selects per mention by the frozen D3 priority (frontmatter_declared > dictionary_exact > rule_weight > model_probability > unavailable) and attaches entity_id only on an exact compatible canonical-name or exact OKF alias/dictionary match; otherwise entity_id is None (pending) with no entities DML.
    - Resolution returns an immutable ResolutionResult and never writes to a database.
  </behavior>
  <action>GREEN: write the minimal implementation so the SAME selector passes; record the GREEN selector result. REFACTOR: then apply only behavior-preserving cleanup and re-run the SAME selector; record the REFACTOR selector result separately. Do not alter test assertions between GREEN and REFACTOR. Implement `dictionary_loader.py` and `frontmatter_supplement.py` to produce MentionCandidates with source="dictionary_exact"/"frontmatter_declared", confidence_kind matching the source, exact code-point offsets, and the required provenance (extractor_id="supplementary", model_id=None, schema/normalization/segmentation versions inherited from the input). Implement `resolution.py`: define the frozen `PRIORITY_ORDER = ("frontmatter_declared", "dictionary_exact", "rule_weight", "model_probability", "unavailable")`; select the winning candidate per mention by the highest priority, treating "unavailable" as the lowest priority but never discarding it for low confidence; attach entity_id only for an exact compatible canonical-name match or an exact OKF alias/dictionary match against the injected existing-entities/aliases view; otherwise entity_id=None (pending) and never create an entities row. Return an immutable `ResolutionResult` (resolved tuple, pending tuple, chosen-priority map). Export all three modules' public symbols from `llamaindex_runtime/entity/__init__.py`. Do not add any persistence, canonical entity creation, or relation/evidence writes.</action>
  <acceptance_criteria>The same selector passes twice: once after minimal GREEN implementation and once after behavior-preserving REFACTOR; both selector results are recorded separately; the assertions in test_supplementary_sources.py and test_resolution.py are unchanged between the GREEN and REFACTOR runs.</acceptance_criteria>
  <verify><automated>rtk env -u DATABASE_URL -u FORMAL_RUNTIME_DATABASE_URL -u OKF_MIGRATION_TEST_DATABASE_DISPOSABLE -u OKF_REBUILD_EXPECTED_DATABASE -u OKF_FAILURE_AUDIT_ACCEPTANCE -u OKF_REBUILD_DOCKER_ACCEPTANCE -u OKF_E2A_DISPOSABLE_TEST_AUTHORIZED -u OKF_E2B_DISPOSABLE_TEST_AUTHORIZED -u OKF_E2B_MIGRATION_TEST_AUTHORIZED -u OKF_E2B_MODELSCOPE_MIRROR_AUTHORIZED -u OKF_E2B_RANER_SMOKE_AUTHORIZED -u OKF_E2B_C2_ENTRY_AUTHORIZED pytest tests/llamaindex_runtime/entity/test_supplementary_sources.py tests/llamaindex_runtime/entity/test_resolution.py -q</automated></verify>
  <done>All supplement and resolution tests pass; D3 priority and D1 attach/pending are deterministic, pure, and non-persisting.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Dictionary/frontmatter input → candidates | Uncontrolled supplements could dominate the model or create false high-confidence mentions. |
| Candidate decision → entity attachment | An over-eager attach could permanently link mentions to the wrong canonical entity. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-16-25 | T | supplement attribution | mitigate | source/confidence_kind fixed per source; dictionary-only never satisfies C1 acceptance. |
| T-16-26 | I | conflict priority | mitigate | Frozen versioned PRIORITY_ORDER (D3); "unavailable" never treated as low confidence. |
| T-16-27 | E | canonical creation | mitigate | D1 exact-match-only attach; otherwise entity_id None with zero entities DML. |
| T-16-28 | R | resolution purity | mitigate | Pure function returning immutable decisions; no DB/entity writes. |
</threat_model>

<verification>
Run only pure unit selectors. No model, no database, no network. Resolution is exercised against in-memory entities/aliases views.
</verification>

<success_criteria>
- Dictionary/frontmatter supplements carry exact attribution and never replace the model.
- The D3 priority order and the D1 exact-match attach/pending rule are deterministic and pure.
- Resolution never creates canonical entities.
</success_criteria>

<output>
After completion, create `.planning/phases/16-raw-corpus-entity-layer/16-08-SUMMARY.md`.
</output>
