---
phase: 08-matched-validation-rerun-and-level-assessment
plan: 04
type: execute
wave: 4
depends_on:
  - 08-03-PLAN
files_modified:
  - verification/phase8-matched-validation-rerun/calculate_phase8_metrics.py
  - verification/phase8-matched-validation-rerun/quality_metrics.json
  - verification/phase8-matched-validation-rerun/level_assessment.json
  - .planning/phases/08-matched-validation-rerun-and-level-assessment/08-SUMMARY.md
  - .planning/phases/08-matched-validation-rerun-and-level-assessment/08-VERIFICATION.md
requirements:
  - REQ-P8-MATCHED-VALIDATION-RERUN
autonomous: true
---

<objective>
Calculate Phase 8 quality metrics and publish a valid Phase 8 `level_assessment.json` only after matched retrieval, completed judgments, and evidence-chain integrity pass. If any gate is blocked, preserve Level 2 as authoritative and write a summary that routes safely to remediation instead of advancing the baseline.
</objective>

<must_haves>
  <truths>
    <truth>`calculate_phase8_metrics.py` refuses to calculate metrics unless `validation_integrity_report.json` has `next_allowed_action: "calculate_metrics"`.</truth>
    <truth>Plan 08-04 must stop without writing `level_assessment.json` when Plan 08-03 has not produced `"next_allowed_action": "calculate_metrics"`.</truth>
    <truth>Frozen thresholds remain `hit_rate >= 0.80`, `top1_relevance >= 0.90`, and `stability >= 0.85`.</truth>
    <truth>Phase 8 `level_assessment.json` is published only from Phase 8 `retrieval_results.json` and Phase 8 `judgment_completed.csv`.</truth>
    <truth>STATE/ROADMAP baseline can be updated only when Phase 8 assessment is valid; otherwise Level 2 remains authoritative.</truth>
    <truth>REQ-P8-MATCHED-VALIDATION-RERUN is closed either as `MATCHED_VALIDATION_COMPLETE` or as a blocked handoff with exact blockers.</truth>
  </truths>
  <key_links>
    <link from="verification/phase8-matched-validation-rerun/validation_integrity_report.json" to="verification/phase8-matched-validation-rerun/calculate_phase8_metrics.py" via="metrics entry gate" pattern="next_allowed_action|calculate_metrics" />
    <link from="verification/phase8-matched-validation-rerun/judgment_completed.csv" to="verification/phase8-matched-validation-rerun/quality_metrics.json" via="manual relevance judgments" pattern="is_relevant|relevance_score|judgment_category" />
    <link from="verification/phase8-matched-validation-rerun/quality_metrics.json" to="verification/phase8-matched-validation-rerun/level_assessment.json" via="frozen Level thresholds" pattern="hit_rate|top1_relevance|stability|Level_2|Level_3|Level_4" />
    <link from="verification/phase8-matched-validation-rerun/level_assessment.json" to=".planning/phases/08-matched-validation-rerun-and-level-assessment/08-SUMMARY.md" via="baseline routing decision" pattern="MATCHED_VALIDATION_COMPLETE|Level 2 remains authoritative" />
  </key_links>
</must_haves>

<threat_model>
  <threat id="T-08-07" severity="high" stride="Tampering">
    Metrics could be calculated from mismatched retrieval/judgment artifacts or placeholder judgments.
    Mitigation: `calculate_phase8_metrics.py` must require `validation_integrity_report.json.next_allowed_action == "calculate_metrics"`, then load only Phase 8 artifacts and reject placeholders.
  </threat>
  <threat id="T-08-08" severity="high" stride="Repudiation">
    A blocked or partial Phase 8 run could be mistaken for a valid baseline promotion.
    Mitigation: `08-SUMMARY.md`, `08-VERIFICATION.md`, and `level_assessment.json` must distinguish `MATCHED_VALIDATION_COMPLETE` from `MATCHED_VALIDATION_BLOCKED`, and state whether Level 2 remains authoritative.
  </threat>
</threat_model>

<tasks>
  <task type="auto">
    <name>Task 04-01: Create Phase 8 metrics and Level assessment calculator</name>
    <read_first>
      - E:/github/rag/verification/phase3-real-validation/calculate_metrics.py
      - E:/github/rag/verification/phase8-matched-validation-rerun/validation_integrity_report.json
      - E:/github/rag/verification/phase8-matched-validation-rerun/retrieval_results.json
      - E:/github/rag/verification/phase8-matched-validation-rerun/judgment_completed.csv
      - E:/github/rag/tests/verification/test_validation_integrity.py
    </read_first>
    <action>
      Prefer creating a Phase 8 calculator instead of modifying Phase 3 `calculate_metrics.py`. If existing metric functions must be edited, first run GitNexus impact analysis for `load_completed_judgments`, `calculate_hit_rate`, `calculate_top1_relevance`, `calculate_stability`, and `assess_level`, and stop on HIGH or CRITICAL risk.

      Create `verification/phase8-matched-validation-rerun/calculate_phase8_metrics.py` with type annotations on all functions.

      Required constants:
      - `MIN_HIT_RATE = 0.80`
      - `MIN_TOP1_RELEVANCE = 0.90`
      - `MIN_STABILITY = 0.85`
      - `PHASE8_DIR = Path(__file__).parent`
      - `INTEGRITY_REPORT_FILE = PHASE8_DIR / "validation_integrity_report.json"`
      - `RETRIEVAL_FILE = PHASE8_DIR / "retrieval_results.json"`
      - `JUDGMENT_FILE = PHASE8_DIR / "judgment_completed.csv"`
      - `METRICS_FILE = PHASE8_DIR / "quality_metrics.json"`
      - `LEVEL_FILE = PHASE8_DIR / "level_assessment.json"`

      Required functions:
      - `load_integrity_report() -> dict[str, object]`
      - `assert_metrics_allowed(report: dict[str, object]) -> None`
      - `load_completed_judgments(csv_path: Path) -> list[dict[str, object]]`
      - `calculate_hit_rate(judgments: list[dict[str, object]], retrieval_results: dict[str, object]) -> float`
      - `calculate_top1_relevance(judgments: list[dict[str, object]]) -> float`
      - `calculate_stability(judgments: list[dict[str, object]], retrieval_results: dict[str, object]) -> float`
      - `assess_level(hit_rate: float, top1_relevance: float, stability: float) -> dict[str, object]`
      - `main() -> int`

      Required behavior:
      - If `validation_integrity_report.json` is missing or `next_allowed_action` is not `calculate_metrics`, write no `level_assessment.json`, print `[ERROR] Phase 8 integrity gate has not allowed metric calculation`, and exit 2.
      - Reject `judgment_completed.csv` if any row contains `True/False`, `0.00` with placeholder category, `<category>`, or `<notes>`.
      - Load only `verification/phase8-matched-validation-rerun/retrieval_results.json` and `verification/phase8-matched-validation-rerun/judgment_completed.csv`.
      - Calculate `hit_rate`, `top1_relevance`, and `stability` using the same formulas and frozen thresholds as Phase 3.
      - Write `quality_metrics.json` with `metrics`, `thresholds`, `judgment_count`, `query_count`, and `integrity_gate_passed: true`.
      - Write `level_assessment.json` with `assessment_date`, `validation_date`, `version_id`, `query_count`, `judgment_count`, `metrics`, `thresholds`, `level`, `baseline_update_allowed: true`, and `source_artifacts` pointing to Phase 8 retrieval/judgment/integrity files.
    </action>
    <acceptance_criteria>
      - `verification/phase8-matched-validation-rerun/calculate_phase8_metrics.py` exists.
      - File contains `MIN_HIT_RATE = 0.80`.
      - File contains `MIN_TOP1_RELEVANCE = 0.90`.
      - File contains `MIN_STABILITY = 0.85`.
      - File contains `calculate_metrics`.
      - File contains `baseline_update_allowed`.
      - File contains `integrity_gate_passed`.
      - File contains `Phase 8 integrity gate has not allowed metric calculation`.
      - `$env:PYTHONPATH='E:/github/rag'; rtk proxy python -m compileall -q verification/phase8-matched-validation-rerun/calculate_phase8_metrics.py` exits 0.
    </acceptance_criteria>
    <verify>
      <automated>`$env:PYTHONPATH='E:/github/rag'; rtk proxy python -m compileall -q verification/phase8-matched-validation-rerun/calculate_phase8_metrics.py`</automated>
      <automated>`rtk grep "MIN_HIT_RATE = 0.80" verification/phase8-matched-validation-rerun/calculate_phase8_metrics.py`</automated>
      <automated>`rtk grep "baseline_update_allowed" verification/phase8-matched-validation-rerun/calculate_phase8_metrics.py`</automated>
    </verify>
  </task>

  <task type="auto">
    <name>Task 04-02: Run metrics gate and write Phase 8 closure summary</name>
    <read_first>
      - E:/github/rag/verification/phase8-matched-validation-rerun/calculate_phase8_metrics.py
      - E:/github/rag/verification/phase8-matched-validation-rerun/validation_integrity_report.json
      - E:/github/rag/verification/phase8-matched-validation-rerun/judgment_collection_status.json
      - E:/github/rag/.planning/ROADMAP.md
      - E:/github/rag/.planning/STATE.md
      - E:/github/rag/.planning/REQUIREMENTS.md
    </read_first>
    <action>
      Run:
      `$env:PYTHONPATH='E:/github/rag'; rtk proxy python verification/phase8-matched-validation-rerun/calculate_phase8_metrics.py`

      If the command exits 2 because gates are not ready, do not create `level_assessment.json`. Instead, write `.planning/phases/08-matched-validation-rerun-and-level-assessment/08-SUMMARY.md` with these exact strings:
      - `REQ-P8-MATCHED-VALIDATION-RERUN`
      - `MATCHED_VALIDATION_BLOCKED`
      - `Level 2 remains authoritative`
      - `No baseline update performed`
      - `Next route: evidence-chain remediation or rerun Phase 8 after gates pass`

      If the command exits 0 and `level_assessment.json` exists, write `08-SUMMARY.md` with these exact strings:
      - `REQ-P8-MATCHED-VALIDATION-RERUN`
      - `MATCHED_VALIDATION_COMPLETE`
      - `validation_integrity_report.json passed`
      - `level_assessment.json`
      - `baseline_update_allowed: true`

      Always create `.planning/phases/08-matched-validation-rerun-and-level-assessment/08-VERIFICATION.md` with a table verifying:
      - retrieval/judgment artifacts are matched or explicitly blocked
      - integrity gate status
      - metric calculation status
      - Level baseline decision
      - no stale Phase 3/4 judgments were reused
      - no raw `DATABASE_URL` was written
    </action>
    <acceptance_criteria>
      - `.planning/phases/08-matched-validation-rerun-and-level-assessment/08-SUMMARY.md` exists.
      - Summary contains `REQ-P8-MATCHED-VALIDATION-RERUN`.
      - Summary contains either `MATCHED_VALIDATION_COMPLETE` or `MATCHED_VALIDATION_BLOCKED`.
      - If blocked, summary contains `Level 2 remains authoritative` and `No baseline update performed`.
      - `.planning/phases/08-matched-validation-rerun-and-level-assessment/08-VERIFICATION.md` exists.
      - Verification contains `No stale Phase 3/4 judgments were reused`.
      - Verification contains `No raw DATABASE_URL was written`.
    </acceptance_criteria>
    <verify>
      <automated>`rtk grep "REQ-P8-MATCHED-VALIDATION-RERUN" .planning/phases/08-matched-validation-rerun-and-level-assessment/08-SUMMARY.md`</automated>
      <automated>`rtk grep "MATCHED_VALIDATION_COMPLETE\|MATCHED_VALIDATION_BLOCKED" .planning/phases/08-matched-validation-rerun-and-level-assessment/08-SUMMARY.md`</automated>
      <automated>`rtk grep "No stale Phase 3/4 judgments were reused" .planning/phases/08-matched-validation-rerun-and-level-assessment/08-VERIFICATION.md`</automated>
    </verify>
  </task>
</tasks>

<verification>
- `$env:PYTHONPATH='E:/github/rag'; rtk proxy python -m compileall -q verification/phase8-matched-validation-rerun/calculate_phase8_metrics.py`
- `rtk grep "REQ-P8-MATCHED-VALIDATION-RERUN" .planning/phases/08-matched-validation-rerun-and-level-assessment/08-SUMMARY.md`
- `rtk grep "MATCHED_VALIDATION_COMPLETE\|MATCHED_VALIDATION_BLOCKED" .planning/phases/08-matched-validation-rerun-and-level-assessment/08-SUMMARY.md`
</verification>

<success_criteria>
- Phase 8 metrics and Level assessment are calculated only from matched Phase 8 artifacts after integrity passes.
- Blocked Phase 8 runs preserve Level 2 and explain exactly why no baseline update occurred.
- Complete Phase 8 runs publish a valid `level_assessment.json` and clear verification evidence for Phase 9 milestone close.
</success_criteria>
