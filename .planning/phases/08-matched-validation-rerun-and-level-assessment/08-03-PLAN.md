---
phase: 08-matched-validation-rerun-and-level-assessment
plan: 03
type: execute
wave: 3
depends_on:
  - 08-02-PLAN
files_modified:
  - verification/phase8-matched-validation-rerun/run_phase8_integrity_gate.py
  - verification/phase8-matched-validation-rerun/validation_integrity_report.json
  - verification/phase8-matched-validation-rerun/judgment_collection_status.json
  - verification/phase8-matched-validation-rerun/judgment_completed.csv
requirements:
  - REQ-P8-MATCHED-VALIDATION-RERUN
autonomous: false
---

<objective>
Run a Phase 8-scoped integrity gate over matched retrieval, the Phase 7/8 evidence-chain status, and Phase 8 judgment artifacts. This plan creates a hard checkpoint for human judgment collection: it may prepare the handoff, but `judgment_completed.csv` can be created only after the gate returns `next_allowed_action: "collect_judgments"`.
</objective>

<must_haves>
  <truths>
    <truth>Human judgment collection starts only after `validation_integrity_report.json` has `next_allowed_action: "collect_judgments"`.</truth>
    <truth>If retrieval is blocked, missing, mismatched, or evidence-chain readiness is missing, `judgment_collection_status.json` says `BLOCKED` and names exact blockers.</truth>
    <truth>Phase 8 judgments, when collected, must be saved in `verification/phase8-matched-validation-rerun/judgment_completed.csv`, not in Phase 3 or Phase 4 directories.</truth>
    <truth>Level 2 remains authoritative throughout this plan; no metrics or Level assessment is calculated here.</truth>
  </truths>
  <key_links>
    <link from="verification/phase8-matched-validation-rerun/retrieval_results.json" to="verification/phase8-matched-validation-rerun/judgment_template.csv" via="retrieval rows become judgment rows" pattern="query_id|hit_rank|node_id" />
    <link from="verification/phase5-evidence-chain-verification/validation_integrity_gate.py" to="verification/phase8-matched-validation-rerun/run_phase8_integrity_gate.py" via="reuse fail-closed retrieval/judgment/evidence gate" pattern="build_validation_integrity_report|next_allowed_action" />
    <link from="verification/phase8-matched-validation-rerun/validation_integrity_report.json" to="verification/phase8-matched-validation-rerun/judgment_collection_status.json" via="manual judgment checkpoint" pattern="collect_judgments|BLOCKED|READY_FOR_HUMAN_JUDGMENT" />
  </key_links>
</must_haves>

<threat_model>
  <threat id="T-08-05" severity="high" stride="Tampering">
    A user or automation could fill judgments for stale rows or a partial retrieval set and then calculate metrics from mismatched data.
    Mitigation: run the integrity gate before and after judgment collection; require exact `(query_id, hit_rank, node_id)` matching and complete row coverage before metrics.
  </threat>
  <threat id="T-08-06" severity="medium" stride="Repudiation">
    It may be unclear whether judgment collection was authorized or skipped.
    Mitigation: write `judgment_collection_status.json` with `status`, `next_allowed_action`, `blocking_reasons`, `template_file`, `completed_file`, and `human_action_required`.
  </threat>
</threat_model>

<tasks>
  <task type="auto">
    <name>Task 03-01: Create Phase 8 integrity gate wrapper</name>
    <read_first>
      - E:/github/rag/verification/phase5-evidence-chain-verification/validation_integrity_gate.py
      - E:/github/rag/verification/phase8-matched-validation-rerun/validation_status.json
      - E:/github/rag/verification/phase8-matched-validation-rerun/retrieval_results.json
      - E:/github/rag/verification/phase8-matched-validation-rerun/judgment_template.csv
      - E:/github/rag/verification/phase7-db-backed-evidence-chain-rerun/evidence_chain_delta.json
      - E:/github/rag/verification/phase7-db-backed-evidence-chain-rerun/active_version_counts.after.json
      - E:/github/rag/tests/verification/test_validation_integrity.py
    </read_first>
    <action>
      Prefer creating a Phase 8 wrapper instead of modifying the Phase 5 gate. If existing Phase 5 gate functions must be edited, first run GitNexus impact analysis for `build_validation_integrity_report`, `validate_retrieval_judgment_integrity`, and `validate_evidence_chain_gate`, and stop on HIGH or CRITICAL risk.

      Create `verification/phase8-matched-validation-rerun/run_phase8_integrity_gate.py` with type annotations on all functions.

      Required constants:
      - `PHASE8_DIR = Path(__file__).parent`
      - `PHASE5_GATE_PATH = PHASE8_DIR.parent / "phase5-evidence-chain-verification" / "validation_integrity_gate.py"`
      - `RETRIEVAL_FILE = PHASE8_DIR / "retrieval_results.json"`
      - `JUDGMENT_TEMPLATE_FILE = PHASE8_DIR / "judgment_template.csv"`
      - `JUDGMENT_COMPLETED_FILE = PHASE8_DIR / "judgment_completed.csv"`
      - `EVIDENCE_COUNTS_FILE = PHASE8_DIR.parent / "phase7-db-backed-evidence-chain-rerun" / "active_version_counts.after.json"`
      - `OUTPUT_FILE = PHASE8_DIR / "validation_integrity_report.json"`
      - `JUDGMENT_STATUS_FILE = PHASE8_DIR / "judgment_collection_status.json"`

      Required functions:
      - `load_phase5_gate_module() -> ModuleType`
      - `build_phase8_integrity_report(judgment_file: Path) -> dict[str, object]`
      - `build_judgment_collection_status(report: dict[str, object]) -> dict[str, object]`
      - `write_reports(report: dict[str, object], status: dict[str, object]) -> None`
      - `main() -> int`

      Required behavior:
      - Default mode uses `judgment_template.csv` as the judgment file only to check row shape and should return `next_allowed_action: "collect_judgments"` when retrieval/evidence are ready but completed judgments are not present.
      - `--completed` mode uses `judgment_completed.csv` and must fail unless all required rows are completed with non-placeholder `is_relevant`, `relevance_score`, and `judgment_category` values.
      - If Phase 8 retrieval artifacts are missing or blocked, write `validation_integrity_report.json` with `passed: false`, `blocking_reasons` including `matched_retrieval_missing` or `matched_retrieval_blocked`, and `next_allowed_action: "rerun_retrieval"`.
      - If evidence-chain counts are blocked or missing, write `passed: false`, `blocking_reasons` including `evidence_chain_not_ready`, and `next_allowed_action: "fix_evidence_chain"`.
      - If retrieval and evidence are ready but completed judgments are absent, write `passed: false`, `next_allowed_action: "collect_judgments"`, and `judgment_collection_status.json` with `status: "READY_FOR_HUMAN_JUDGMENT"`.
      - If completed judgments are present and valid, write `passed: true`, `next_allowed_action: "calculate_metrics"`, and `judgment_collection_status.json` with `status: "JUDGMENTS_COMPLETE"`.
    </action>
    <acceptance_criteria>
      - `verification/phase8-matched-validation-rerun/run_phase8_integrity_gate.py` exists.
      - File contains `READY_FOR_HUMAN_JUDGMENT`.
      - File contains `JUDGMENTS_COMPLETE`.
      - File contains `collect_judgments`.
      - File contains `calculate_metrics`.
      - File contains `evidence_chain_not_ready`.
      - File contains `judgment_completed.csv`.
      - `$env:PYTHONPATH='E:/github/rag'; rtk proxy python -m compileall -q verification/phase8-matched-validation-rerun/run_phase8_integrity_gate.py` exits 0.
    </acceptance_criteria>
    <verify>
      <automated>`$env:PYTHONPATH='E:/github/rag'; rtk proxy python -m compileall -q verification/phase8-matched-validation-rerun/run_phase8_integrity_gate.py`</automated>
      <automated>`rtk grep "collect_judgments" verification/phase8-matched-validation-rerun/run_phase8_integrity_gate.py`</automated>
      <automated>`rtk grep "READY_FOR_HUMAN_JUDGMENT" verification/phase8-matched-validation-rerun/run_phase8_integrity_gate.py`</automated>
    </verify>
  </task>

  <task type="manual">
    <name>Task 03-02: Gate and collect Phase 8 human judgments only when authorized</name>
    <read_first>
      - E:/github/rag/verification/phase8-matched-validation-rerun/run_phase8_integrity_gate.py
      - E:/github/rag/verification/phase8-matched-validation-rerun/judgment_template.csv
      - E:/github/rag/verification/phase8-matched-validation-rerun/validation_integrity_report.json
      - E:/github/rag/verification/phase8-matched-validation-rerun/judgment_collection_status.json
    </read_first>
    <action>
      Run:
      `$env:PYTHONPATH='E:/github/rag'; rtk proxy python verification/phase8-matched-validation-rerun/run_phase8_integrity_gate.py`

      If `judgment_collection_status.json` contains `"status": "BLOCKED"`, stop this plan and report blockers. Do not create `judgment_completed.csv`.

      If `judgment_collection_status.json` contains `"status": "READY_FOR_HUMAN_JUDGMENT"` and `validation_integrity_report.json` contains `"next_allowed_action": "collect_judgments"`, perform the manual step:
      - Copy `verification/phase8-matched-validation-rerun/judgment_template.csv` to `verification/phase8-matched-validation-rerun/judgment_completed.csv`.
      - Fill every row manually: replace `True/False`, `0.00`, `<category>`, and `<notes>` with completed judgment values.
      - Preserve `query_id`, `hit_rank`, `node_id`, `heading_path`, `page_no`, and `text_preview` exactly from the template.

      After manual completion, run:
      `$env:PYTHONPATH='E:/github/rag'; rtk proxy python verification/phase8-matched-validation-rerun/run_phase8_integrity_gate.py --completed`

      Proceed to Plan 08-04 only if `judgment_collection_status.json` contains `"status": "JUDGMENTS_COMPLETE"` and `validation_integrity_report.json` contains `"next_allowed_action": "calculate_metrics"`.
    </action>
    <acceptance_criteria>
      - If blocked, `judgment_collection_status.json` contains `BLOCKED` and `blocking_reasons`.
      - If ready for manual judgment, `judgment_collection_status.json` contains `READY_FOR_HUMAN_JUDGMENT` and `collect_judgments`.
      - If judgments are collected, `verification/phase8-matched-validation-rerun/judgment_completed.csv` exists.
      - If judgments are collected, `judgment_completed.csv` contains no `True/False` placeholders.
      - If judgments are collected, `judgment_completed.csv` contains no `<category>` placeholders.
      - If judgments are collected, `validation_integrity_report.json` contains `"next_allowed_action": "calculate_metrics"` before Plan 08-04 can execute.
      - If judgments are collected, `validation_integrity_report.json` contains `calculate_metrics`.
    </acceptance_criteria>
    <verify>
      <automated>`rtk grep "next_allowed_action" verification/phase8-matched-validation-rerun/validation_integrity_report.json`</automated>
      <automated>`rtk grep "True/False" verification/phase8-matched-validation-rerun/judgment_completed.csv` must return no matches when `judgment_completed.csv` exists.</automated>
      <automated>`rtk grep "<category>" verification/phase8-matched-validation-rerun/judgment_completed.csv` must return no matches when `judgment_completed.csv` exists.</automated>
    </verify>
  </task>
</tasks>

<verification>
- `$env:PYTHONPATH='E:/github/rag'; rtk proxy python -m compileall -q verification/phase8-matched-validation-rerun/run_phase8_integrity_gate.py`
- `rtk grep "next_allowed_action" verification/phase8-matched-validation-rerun/validation_integrity_report.json`
- `rtk grep "READY_FOR_HUMAN_JUDGMENT\|JUDGMENTS_COMPLETE\|BLOCKED" verification/phase8-matched-validation-rerun/judgment_collection_status.json`
</verification>

<success_criteria>
- Judgment collection is blocked unless the Phase 8 integrity gate explicitly allows `collect_judgments`.
- Completed judgments, when present, are matched to the Phase 8 retrieval rows and contain no placeholders.
- The next plan cannot calculate metrics unless integrity status advances to `calculate_metrics`.
</success_criteria>
