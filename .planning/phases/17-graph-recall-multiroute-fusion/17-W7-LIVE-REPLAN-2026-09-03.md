# W7 Live Gates Replan — 2026-09-03

## Authorization
- User '1' (2026-09-03): live acceptance = disposable PG container + real LLM 127.0.0.1:8317 + DashScope embedding + LightRAG PG storages. Covered by master-plan §24 live-gate scope.

## Recon findings (LightRAG 1.5.6, .tmp/lightrag/extracted/lightrag/kg/postgres_impl.py)
- PG env names (ClientManager.get_config :2433-2588): POSTGRES_HOST / POSTGRES_PORT / POSTGRES_USER / POSTGRES_PASSWORD / POSTGRES_DATABASE / POSTGRES_WORKSPACE / POSTGRES_MAX_CONNECTIONS; fallback config.ini read from cwd (we do NOT ship one).
- ClientManager (:2425-2664): process-wide singleton pool; first caller fixes vector signature; incompatible second init → RuntimeError 'pool is process-wide and already initialized'. get_client(vector_storage=...) → PostgreSQLDB(config).initdb() (bootstrap conn creates pgvector extension first when enable_vector, :598-626) → check_tables().
- enable_vector = (vector_storage == 'PGVectorStorage') :2499 — PGTableGraphStorage passes sentinel so no pgvector demand; our set uses PGVectorStorage → pgvector required → pgvector/pgvector:pg16 image OK.
- Workspace priority (:2690-2702): POSTGRES_WORKSPACE env > instance workspace > 'default'. Live runs must NOT set POSTGRES_WORKSPACE so per-instance workspace (LightragRuntimeConfig.workspace) wins.
- Storages: kv='PGKVStorage' vector='PGVectorStorage' graph='PGTableGraphStorage' doc_status='PGDocStatusStorage' (all in STORAGES registry kg/__init__.py; pgtable_impl.py:267 class PGTableGraphStorage).
- EmbeddingFunc (utils.py:540): embedding_dim int + func(texts)->list[list[float]] (async ok) + model_name. Wrap DashScopeEmbedding.embed via async def _embed(texts): return [list(v) for v in embedding.embed(list(texts)).vectors].
- LightRAG dataclass kwargs: working_dir/kv_storage/vector_storage/graph_storage/doc_status_storage/workspace/embedding_func/llm_model_func(None ok, zero-LLM path)/llm_model_name.

## Live-chain gaps (4) and fixes
- G1 lightrag-hku not installed → NEW .cache/lightrag-venv offline install from .cache/lightrag-wheelhouse (79 wheels) + .cache/lightrag-lock/requirements.lock (79 lines, --no-index). Coordinator infra task.
- G2 asyncpg/pgvector not installed → included in offline-storage extra wheels (asyncpg>=0.31,<1.0 + pgvector>=0.4.2,<1.0).
- G3 UIE worker script missing → T1 NEW verification/phase17-graph-recall-multiroute-fusion/uie_worker.py (runs under .cache/uie2-venv; stdin {'texts':[...]}; stdout {'documents':[...]} with runner-DTO-compatible entities/relations; 4 entity labels 人名/地名/公司名/产品名 + 8 predicates; deterministic text.index offsets; model dir default E:/github/rag/.cache/uie2-models/aistudio/PP-UIE-0.5B).
- G4 runner _LiveExtractor stub (run_graph_recall_acceptance.py:465-471) → T2 EDIT build_deps: inline subprocess extractor driven by PHASE17_UIE_PYTHON + PHASE17_UIE_WORKER env (absolute+exist checked; must NOT import uie_batch — uie_batch._load_runner execs runner source = circular). T3 EDIT run_phase17_demo.py build_deps: same factory wiring (demo may use uie_batch.SubprocessUieBatchExtractor); factory inlined in both files (~35 lines dup, merge follow-up registered).

## Design: shared lightrag factory (inline in both runners)
- Env inputs: DASHSCOPE_API_KEY (required non-blank), DASHSCOPE_EMBEDDING_BASE_URL (default https://dashscope.aliyuncs.com/compatible-mode/v1), DASHSCOPE_EMBEDDING_MODEL (default text-embedding-v4), DASHSCOPE_EMBEDDING_DIMENSIONS (default 1024).
- factory(config, **lightrag_kwargs) -> LightRAG instance with kv/vector/graph/doc_status PG names + workspace + embedding_func + working_dir (explicit temp dir, never default './rag_storage').
- Testability: tests inject fake sys.modules['lightrag'] + fake 'lightrag.utils' recording kwargs; factory must not import lightrag at module top (lazy import inside factory body).

## TDD + review + live-run plan
- Implementer (single writer): T1+T2+T3, RED→GREEN, statics, combined 3 failed/1433+new passed/1 skipped; no frozen-file edits; no network; no git.
- 4-path read-only review → fix round if C/H → coordinator fresh verify.
- Live run (coordinator .tmp script): docker pgvector/pgvector:pg16 --tmpfs + POSTGRES_* env + DASHSCOPE_* env + PHASE17_UIE_* env + OKF_GRAPH_RECALL_ACCEPTANCE_AUTHORIZED=1 + OKF_PHASE17_DEMO_AUTHORIZED=1 + three gate envs → lightrag-venv python with PYTHONPATH=E:/github/rag → runner main() programmatic target_uri → archive evidence/report → docker stop --rm → credential cleanup.

## GitNexus impact
- build_deps / run_pipeline in run_graph_recall_acceptance.py + run_phase17_demo.py build_deps: not in GitNexus index (verification scripts, stale index) → manual blast radius LOW (live-only call sites, tests already cover contract). Recorded per AGENTS.md rule.