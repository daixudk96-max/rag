{"schemaVersion":1,"nodes":[{"key":"prepare","kind":"prepare","kindVersion":"1","config":{"repo":"E:/github/rag","tests":{"llamaindex_runtime/okf/test_apply.py":"\"\"\"Phase 18 W4: apply executor (approved proposal -> bundle body, journal-gated).\"\"\"\n\nfrom __future__ import annotations\n\nimport hashlib\nfrom pathlib import Path\nfrom typing import Any, cast\n\nimport pytest\n\nfrom llamaindex_runtime.okf.writeback import journal as journal\nfrom llamaindex_runtime.okf.writeback.apply import apply_proposal\nfrom llamaindex_runtime.okf.writeback.proposal import (\n    WritebackProposal,\n    write_proposal,\n)\n\n\ndef _proposal() -> WritebackProposal:\n    return WritebackProposal(\n        target_file='entities/org/huawei.md',\n        patch='华为是一家公司',\n        reason='evidence-backed add',\n        evidence=('span-1',),\n        proposed_by='agent-1',\n        created_at='2026-09-03T00:00:00Z',\n    )\n\n\ndef _setup(\n    tmp_path: Path,\n    *,\n    approved: bool = True,\n    existing: str | None = '---\\ntype: entity\\nname: 华为\\n---\\nold body\\n',\n) -> tuple[Path, Path, WritebackProposal]:\n    staging = tmp_path / '.staging'\n    bundle = tmp_path / 'bundle'\n    (bundle / 'entities' / 'org').mkdir(parents=True)\n    proposal = _proposal()\n    write_proposal(staging, proposal)\n    proposal_path = staging / (proposal.proposal_id + '.json')\n    digest = hashlib.sha256(proposal_path.read_bytes()).hexdigest()\n    journal.record_creation(\n        staging / 'journal.jsonl',\n        proposal.proposal_id,\n        actor='agent-1',\n        at='2026-09-03T00:00:00Z',\n        payload_digest=digest,\n    )\n    if approved:\n        journal.approve(\n            staging / 'journal.jsonl',\n            proposal.proposal_id,\n            actor='human-1',\n            at='2026-09-04T00:00:00Z',\n            payload_digest=digest,\n        )\n    if existing is not None:\n        (bundle / 'entities' / 'org' / 'huawei.md').write_text(existing, encoding='utf-8')\n    return staging, bundle, proposal\n\n\ndef test_happy_path_replaces_body_preserving_frontmatter(tmp_path: Path) -> None:\n    staging, bundle, proposal = _setup(tmp_path)\n    returned = apply_proposal(\n        proposal,\n        bundle_root=bundle,\n        staging_dir=staging,\n        actor='human-1',\n        at='2026-09-05T00:00:00Z',\n    )\n    content = (bundle / 'entities' / 'org' / 'huawei.md').read_text(encoding='utf-8')\n    assert content == '---\\ntype: entity\\nname: 华为\\n---\\n华为是一家公司\\n'\n    events = journal.read_events(staging / 'journal.jsonl')\n    assert events[-1].event_type == 'merged'\n    assert journal.derive_status_for(events, proposal.proposal_id) == 'merged'\n    assert returned == bundle / 'entities' / 'org' / 'huawei.md'\n\n\ndef test_gate_rejects_created(tmp_path: Path) -> None:\n    staging, bundle, proposal = _setup(tmp_path, approved=False)\n    with pytest.raises(ValueError, match='proposal must be approved before merge'):\n        apply_proposal(proposal, bundle_root=bundle, staging_dir=staging, actor='human-1', at='2026-09-05T00:00:00Z')\n\n\ndef test_gate_rejects_remerged(tmp_path: Path) -> None:\n    staging, bundle, proposal = _setup(tmp_path)\n    apply_proposal(proposal, bundle_root=bundle, staging_dir=staging, actor='human-1', at='2026-09-05T00:00:00Z')\n    with pytest.raises(ValueError, match='proposal must be approved before merge'):\n        apply_proposal(proposal, bundle_root=bundle, staging_dir=staging, actor='human-1', at='2026-09-06T00:00:00Z')\n\n\ndef test_gate_rejects_rejected(tmp_path: Path) -> None:\n    staging, bundle, proposal = _setup(tmp_path, approved=False)\n    digest = hashlib.sha256((staging / (proposal.proposal_id + '.json')).read_bytes()).hexdigest()\n    journal.reject(\n        staging / 'journal.jsonl',\n        proposal.proposal_id,\n        actor='human-1',\n        at='2026-09-04T00:00:00Z',\n        payload_digest=digest,\n        reason='low-confidence',\n    )\n    with pytest.raises(ValueError, match='proposal must be approved before merge'):\n        apply_proposal(proposal, bundle_root=bundle, staging_dir=staging, actor='human-1', at='2026-09-05T00:00:00Z')\n\n\ndef test_tampered_proposal_file_rejected_before_write(tmp_path: Path) -> None:\n    staging, bundle, proposal = _setup(tmp_path)\n    target = bundle / 'entities' / 'org' / 'huawei.md'\n    before = target.read_text(encoding='utf-8')\n    path = staging / (proposal.proposal_id + '.json')\n    path.write_text(path.read_text(encoding='utf-8').replace('evidence-backed add', 'evidence-backed HACK'), encoding='utf-8')\n    with pytest.raises(ValueError, match='tamper suspected'):\n        apply_proposal(proposal, bundle_root=bundle, staging_dir=staging, actor='human-1', at='2026-09-05T00:00:00Z')\n    assert target.read_text(encoding='utf-8') == before\n\n\ndef test_existing_file_without_frontmatter_replaced(tmp_path: Path) -> None:\n    staging, bundle, proposal = _setup(tmp_path, existing='plain old body')\n    apply_proposal(proposal, bundle_root=bundle, staging_dir=staging, actor='human-1', at='2026-09-05T00:00:00Z')\n    content = (bundle / 'entities' / 'org' / 'huawei.md').read_text(encoding='utf-8')\n    assert content == '华为是一家公司\\n'\n\n\ndef test_new_file_created_when_parent_exists(tmp_path: Path) -> None:\n    staging, bundle, proposal = _setup(tmp_path, existing=None)\n    apply_proposal(proposal, bundle_root=bundle, staging_dir=staging, actor='human-1', at='2026-09-05T00:00:00Z')\n    content = (bundle / 'entities' / 'org' / 'huawei.md').read_text(encoding='utf-8')\n    assert content == '华为是一家公司\\n'\n\n\ndef test_missing_parent_directory_rejected(tmp_path: Path) -> None:\n    staging, bundle, proposal = _setup(tmp_path, existing=None)\n    (bundle / 'entities' / 'org').rmdir()\n    with pytest.raises(ValueError, match='target parent directory does not exist'):\n        apply_proposal(proposal, bundle_root=bundle, staging_dir=staging, actor='human-1', at='2026-09-05T00:00:00Z')\n\n\ndef test_no_tmp_leftovers_after_success(tmp_path: Path) -> None:\n    staging, bundle, proposal = _setup(tmp_path)\n    apply_proposal(proposal, bundle_root=bundle, staging_dir=staging, actor='human-1', at='2026-09-05T00:00:00Z')\n    leftovers = [item.name for item in bundle.rglob('*.tmp')] + [item.name for item in staging.glob('*.tmp')]\n    assert leftovers == []\n\n\ndef test_sync_seam_invoked_with_bundle_root(tmp_path: Path) -> None:\n    staging, bundle, proposal = _setup(tmp_path)\n    calls: list[Path] = []\n    apply_proposal(\n        proposal,\n        bundle_root=bundle,\n        staging_dir=staging,\n        actor='human-1',\n        at='2026-09-05T00:00:00Z',\n        sync=cast(Any, calls.append),\n    )\n    assert len(calls) == 1\n    assert calls[0] == bundle.resolve()\n\n\ndef test_sync_omitted_runs_clean(tmp_path: Path) -> None:\n    staging, bundle, proposal = _setup(tmp_path)\n    returned = apply_proposal(proposal, bundle_root=bundle, staging_dir=staging, actor='human-1', at='2026-09-05T00:00:00Z')\n    assert returned == bundle / 'entities' / 'org' / 'huawei.md'\n\n\ndef test_sync_failure_propagates_after_merge_recorded(tmp_path: Path) -> None:\n    staging, bundle, proposal = _setup(tmp_path)\n\n    def boom(root: Path) -> int:\n        raise RuntimeError('sync down')\n\n    with pytest.raises(RuntimeError, match='sync down'):\n        apply_proposal(\n            proposal,\n            bundle_root=bundle,\n            staging_dir=staging,\n            actor='human-1',\n            at='2026-09-05T00:00:00Z',\n            sync=boom,\n        )\n    events = journal.read_events(staging / 'journal.jsonl')\n    assert journal.derive_status_for(events, proposal.proposal_id) == 'merged'\n\n\ndef test_missing_bundle_root_rejected(tmp_path: Path) -> None:\n    staging, bundle, proposal = _setup(tmp_path)\n    with pytest.raises(ValueError, match='bundle_root must be an existing directory'):\n        apply_proposal(proposal, bundle_root=tmp_path / 'absent', staging_dir=staging, actor='human-1', at='2026-09-05T00:00:00Z')\n\n\ndef test_proposal_file_missing_rejected(tmp_path: Path) -> None:\n    staging, bundle, proposal = _setup(tmp_path)\n    (staging / (proposal.proposal_id + '.json')).unlink()\n    with pytest.raises(ValueError, match='proposal not found'):\n        apply_proposal(proposal, bundle_root=bundle, staging_dir=staging, actor='human-1', at='2026-09-05T00:00:00Z')\n\n\ndef test_ast_purity_and_no_wall_clock() -> None:\n    import llamaindex_runtime.okf.writeback.apply as apply_module\n\n    source = Path(apply_module.__file__).read_text(encoding='utf-8')\n    assert 'datetime.now' not in source\n    assert 'uuid1' not in source"},"testsRoot":"tests","allowTestsUpdate":false,"allowScratchInit":true},"retry":{"maxAttempts":3,"backoffMs":100,"maxBackoffMs":5000},"budget":{"maxSpend":8192,"attemptBudget":3},"effect":"external-idempotent"},{"key":"w4b-apply:workspace","kind":"workspace","kindVersion":"1","config":{"testPaths":["llamaindex_runtime/okf/test_apply.py"]},"retry":{"maxAttempts":3,"backoffMs":100,"maxBackoffMs":5000},"budget":{"maxSpend":8192,"attemptBudget":3},"effect":"external-idempotent"},{"key":"w4b-apply:writer","kind":"writer","kindVersion":"1","config":{"spec":"Implement the merge executor: NEW llamaindex_runtime/okf/writeback/apply.py, public apply_proposal(proposal: WritebackProposal, *, bundle_root: Path | str, staging_dir: Path | str, actor: str, at: str, sync: Callable[[Path], object] | None = None) -> Path.\nSemantics: (1) staging self-check identical to W1 (resolve().name == '.staging', no 'raw' component); (2) proposal file must exist at staging_dir/<proposal_id>.json else ValueError 'proposal not found: <id>'; (3) journal gate via read_events + derive_status_for: status must be 'approved' else ValueError 'proposal must be approved before merge; current status: <status>'; (4) tamper check BEFORE any write: sha256(proposal file bytes) must equal the latest event's payload_digest for this proposal else ValueError containing 'tamper suspected'; (5) whitelist re-check (defense in depth, reuse proposal.TARGET_ROOTS): PurePosixPath first component in TARGET_ROOTS, no absolute, no '..'; (6) bundle_root must exist and be a directory else ValueError 'bundle_root must be an existing directory'; target = bundle_root resolved + PurePosixPath parts; target parent must already be a directory (NO mkdir) else ValueError 'target parent directory does not exist'; (7) body replacement preserving frontmatter: read existing text (empty when file absent); if text starts with a line '---' find the first later line whose stripped text is '---' (head = lines[0..i] verbatim + trailing newline); unterminated frontmatter -> ValueError 'existing frontmatter block is unterminated'; new content = head (verbatim) + patch (append '\\n' when missing) when head exists, else patch with '\\n'; (8) atomic write: uuid4-hex tmp sibling + write_text utf-8 + os.replace; no leftover *.tmp on success; (9) AFTER the body write, journal.record_merge(journal_path=<staging>/journal.jsonl, proposal_id, actor=actor, at=at, payload_digest=digest) — journal failure after a successful body write is recoverable by re-running apply (same content, idempotent); (10) sync seam: when provided, sync(bundle_root.resolve()) is called AFTER the merged event is durable; sync exceptions propagate (documented); when None, nothing is called; (11) return the written target Path.\nPurity: stdlib only (collections.abc, hashlib, os, pathlib, uuid) + relative imports of .journal and .proposal; no datetime.now, no uuid1, no network, no raw/ writes."},"retry":{"maxAttempts":3,"backoffMs":100,"maxBackoffMs":5000},"budget":{"maxSpend":8192,"attemptBudget":3,"loopBudget":6},"effect":"non-idempotent"},{"key":"w4b-apply:candidate","kind":"candidate","kindVersion":"1","config":{},"retry":{"maxAttempts":3,"backoffMs":100,"maxBackoffMs":5000},"budget":{"maxSpend":8192,"attemptBudget":3},"effect":"external-idempotent"},{"key":"w4b-apply:test","kind":"test","kindVersion":"1","config":{"testPaths":["llamaindex_runtime/okf/test_apply.py"]},"retry":{"maxAttempts":3,"backoffMs":100,"maxBackoffMs":5000},"budget":{"maxSpend":8192,"attemptBudget":3},"effect":"pure"},{"key":"w4b-apply:reviewer","kind":"reviewer","kindVersion":"1","config":{"spec":"Implement the merge executor: NEW llamaindex_runtime/okf/writeback/apply.py, public apply_proposal(proposal: WritebackProposal, *, bundle_root: Path | str, staging_dir: Path | str, actor: str, at: str, sync: Callable[[Path], object] | None = None) -> Path.\nSemantics: (1) staging self-check identical to W1 (resolve().name == '.staging', no 'raw' component); (2) proposal file must exist at staging_dir/<proposal_id>.json else ValueError 'proposal not found: <id>'; (3) journal gate via read_events + derive_status_for: status must be 'approved' else ValueError 'proposal must be approved before merge; current status: <status>'; (4) tamper check BEFORE any write: sha256(proposal file bytes) must equal the latest event's payload_digest for this proposal else ValueError containing 'tamper suspected'; (5) whitelist re-check (defense in depth, reuse proposal.TARGET_ROOTS): PurePosixPath first component in TARGET_ROOTS, no absolute, no '..'; (6) bundle_root must exist and be a directory else ValueError 'bundle_root must be an existing directory'; target = bundle_root resolved + PurePosixPath parts; target parent must already be a directory (NO mkdir) else ValueError 'target parent directory does not exist'; (7) body replacement preserving frontmatter: read existing text (empty when file absent); if text starts with a line '---' find the first later line whose stripped text is '---' (head = lines[0..i] verbatim + trailing newline); unterminated frontmatter -> ValueError 'existing frontmatter block is unterminated'; new content = head (verbatim) + patch (append '\\n' when missing) when head exists, else patch with '\\n'; (8) atomic write: uuid4-hex tmp sibling + write_text utf-8 + os.replace; no leftover *.tmp on success; (9) AFTER the body write, journal.record_merge(journal_path=<staging>/journal.jsonl, proposal_id, actor=actor, at=at, payload_digest=digest) — journal failure after a successful body write is recoverable by re-running apply (same content, idempotent); (10) sync seam: when provided, sync(bundle_root.resolve()) is called AFTER the merged event is durable; sync exceptions propagate (documented); when None, nothing is called; (11) return the written target Path.\nPurity: stdlib only (collections.abc, hashlib, os, pathlib, uuid) + relative imports of .journal and .proposal; no datetime.now, no uuid1, no network, no raw/ writes.","acceptanceCriteria":[{"id":"B01","scenario":"approved proposal merges: body replaced, frontmatter block preserved verbatim, merged journal event recorded, returns target Path"},{"id":"B02","scenario":"created/rejected/already-merged proposals raise ValueError 'proposal must be approved before merge'"},{"id":"B03","scenario":"proposal file bytes changed after approval raise ValueError containing 'tamper suspected' and the bundle body is unchanged"},{"id":"B04","scenario":"file without frontmatter is replaced wholesale; missing target creates the file when the parent exists; missing parent raises 'target parent directory does not exist'"},{"id":"B05","scenario":"sync seam receives the resolved bundle root exactly once when provided; omitted sync writes cleanly; sync exception propagates after the merged event is recorded"},{"id":"B06","scenario":"no *.tmp leftovers after success; missing bundle_root raises; missing proposal file raises 'proposal not found'"}]},"retry":{"maxAttempts":3,"backoffMs":100,"maxBackoffMs":5000},"budget":{"maxSpend":8192,"attemptBudget":3},"effect":"non-idempotent"},{"key":"w4b-apply:mutation","kind":"mutation","kindVersion":"1","config":{"testPaths":["llamaindex_runtime/okf/test_apply.py"]},"retry":{"maxAttempts":3,"backoffMs":100,"maxBackoffMs":5000},"budget":{"maxSpend":8192,"attemptBudget":3},"effect":"pure"},{"key":"w4b-apply:certificate","kind":"certificate","kindVersion":"1","config":{"acceptanceCriteria":[{"id":"B01","scenario":"approved proposal merges: body replaced, frontmatter block preserved verbatim, merged journal event recorded, returns target Path"},{"id":"B02","scenario":"created/rejected/already-merged proposals raise ValueError 'proposal must be approved before merge'"},{"id":"B03","scenario":"proposal file bytes changed after approval raise ValueError containing 'tamper suspected' and the bundle body is unchanged"},{"id":"B04","scenario":"file without frontmatter is replaced wholesale; missing target creates the file when the parent exists; missing parent raises 'target parent directory does not exist'"},{"id":"B05","scenario":"sync seam receives the resolved bundle root exactly once when provided; omitted sync writes cleanly; sync exception propagates after the merged event is recorded"},{"id":"B06","scenario":"no *.tmp leftovers after success; missing bundle_root raises; missing proposal file raises 'proposal not found'"}]},"retry":{"maxAttempts":3,"backoffMs":100,"maxBackoffMs":5000},"budget":{"maxSpend":8192,"attemptBudget":3},"effect":"pure"},{"key":"merge","kind":"merge","kindVersion":"1","config":{},"retry":{"maxAttempts":3,"backoffMs":100,"maxBackoffMs":5000},"budget":{"maxSpend":8192,"attemptBudget":3},"effect":"external-idempotent"}],"edges":[{"from":"prepare","fromPort":"out","to":"w4b-apply:workspace","toPort":"in","label":"data"},{"from":"prepare","fromPort":"out","to":"w4b-apply:writer","toPort":"in","label":"data"},{"from":"prepare","fromPort":"out","to":"w4b-apply:test","toPort":"in","label":"data"},{"from":"prepare","fromPort":"out","to":"w4b-apply:reviewer","toPort":"in","label":"data"},{"from":"prepare","fromPort":"out","to":"w4b-apply:mutation","toPort":"in","label":"data"},{"from":"prepare","fromPort":"out","to":"w4b-apply:certificate","toPort":"in","label":"data"},{"from":"w4b-apply:workspace","fromPort":"out","to":"w4b-apply:writer","toPort":"in","label":"data"},{"from":"w4b-apply:workspace","fromPort":"out","to":"w4b-apply:candidate","toPort":"in","label":"data"},{"from":"w4b-apply:workspace","fromPort":"out","to":"w4b-apply:test","toPort":"in","label":"data"},{"from":"w4b-apply:workspace","fromPort":"out","to":"w4b-apply:reviewer","toPort":"in","label":"data"},{"from":"w4b-apply:workspace","fromPort":"out","to":"w4b-apply:mutation","toPort":"in","label":"data"},{"from":"w4b-apply:workspace","fromPort":"out","to":"w4b-apply:certificate","toPort":"in","label":"data"},{"from":"w4b-apply:writer","fromPort":"out","to":"w4b-apply:candidate","toPort":"in","label":"data+invalidate"},{"from":"w4b-apply:writer","fromPort":"out","to":"w4b-apply:certificate","toPort":"in","label":"invalidate"},{"from":"w4b-apply:writer","fromPort":"out","to":"w4b-apply:test","toPort":"in","label":"rework-auth"},{"from":"w4b-apply:writer","fromPort":"out","to":"w4b-apply:reviewer","toPort":"in","label":"rework-auth"},{"from":"w4b-apply:writer","fromPort":"out","to":"w4b-apply:mutation","toPort":"in","label":"rework-auth"},{"from":"w4b-apply:candidate","fromPort":"out","to":"w4b-apply:test","toPort":"in","label":"data"},{"from":"w4b-apply:candidate","fromPort":"out","to":"w4b-apply:reviewer","toPort":"in","label":"data"},{"from":"w4b-apply:candidate","fromPort":"out","to":"w4b-apply:mutation","toPort":"in","label":"data"},{"from":"w4b-apply:candidate","fromPort":"out","to":"w4b-apply:certificate","toPort":"in","label":"data"},{"from":"w4b-apply:test","fromPort":"out","to":"w4b-apply:reviewer","toPort":"in","label":"data"},{"from":"w4b-apply:test","fromPort":"out","to":"w4b-apply:mutation","toPort":"in","label":"data"},{"from":"w4b-apply:test","fromPort":"out","to":"w4b-apply:certificate","toPort":"in","label":"data"},{"from":"w4b-apply:reviewer","fromPort":"out","to":"w4b-apply:mutation","toPort":"in","label":"data"},{"from":"w4b-apply:reviewer","fromPort":"out","to":"w4b-apply:certificate","toPort":"in","label":"data"},{"from":"w4b-apply:mutation","fromPort":"out","to":"w4b-apply:certificate","toPort":"in","label":"data"},{"from":"w4b-apply:certificate","fromPort":"out","to":"merge","toPort":"in","label":"data"},{"from":"prepare","fromPort":"out","to":"merge","toPort":"in","label":"data"}],"policy":{"reserveBeforeDispatch":true,"defaultRetry":{"maxAttempts":3,"backoffMs":100,"maxBackoffMs":5000},"concurrentDispatch":true,"maxSteps":200}}