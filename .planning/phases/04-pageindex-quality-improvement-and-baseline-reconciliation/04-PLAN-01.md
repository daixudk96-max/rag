---
phase: 04-pageindex-quality-improvement-and-baseline-reconciliation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/ROADMAP.md
  - .planning/STATE.md
  - .planning/workspace-memory.json
  - verification/phase3-real-validation/validation_status.json
requirements: []
must_haves:
  truths:
    - 项目对当前真实 Level 只有一个权威口径，不再同时存在 Level 2 与 Level 3 双真相。
    - Phase 4 使用的工作记忆与验证快照一致，不再出现 STATE 写 Level 2、validation_status 写 Level 3 的冲突。
  key_links:
    - from: .planning/STATE.md
      to: verification/phase3-real-validation/validation_status.json
      via: baseline reconciliation
      pattern: Level_2|current_level|phase_3_status
autonomous: true
---

<objective>
统一项目基线，消除 ROADMAP/STATE/workspace-memory/validation_artifacts 之间的 Level 分歧，确立 Level 2 为 Phase 4 的保守规划基线。
</objective>

<context>
Phase 3 完成后，STATE/workspace-memory 记录 Level 2，而 level_assessment.json 报告 Level 3。这种分裂意味着基线未对账，规划必须取保守解释，直到报告链统一。
</context>

<tasks>
<task type="auto">
  <name>Task 01-01: 对账 STATE 与 validation_status.json 的 Level 叙述</name>
  <read_first>
    - E:/github/rag/.planning/STATE.md
    - E:/github/rag/.planning/workspace-memory.json
    - E:/github/rag/verification/phase3-real-validation/validation_status.json
    - E:/github/rag/verification/phase3-real-validation/level_assessment.json
    - E:/github/rag/verification/phase3-real-validation/FINAL-REPORT.md
  </read_first>
  <action>
读取 STATE.md 的 `## Current Position` section，提取 `Phase: 4`、`Status`、`Last activity` 字段。
读取 workspace-memory.json 的 `accumulated_context` section，提取 `phase_3_level` 或等价字段。
读取 validation_status.json 的 `level_assessment` 或 `current_level` 字段。
读取 level_assessment.json 的 `final_level` 字段。
读取 FINAL-REPORT.md 的 `## Level Determination` section。

对比五个来源的 Level 叙述：
- STATE.md 应写 "Phase 3 COMPLETED: Level 2 determined"
- workspace-memory.json 应在 accumulated_context.decisions 中记录 "Phase 3 real validation determined Level 2"
- validation_status.json 的 current_level 应为 "Level_2" 或等价值
- level_assessment.json 的 final_level 应为 "Level_2"（若不一致，需在下一步修正）
- FINAL-REPORT.md 应明确写 "Final Level: Level 2"

若发现不一致（例如 level_assessment.json 写 Level_3），记录分歧清单：
{
  "source": "level_assessment.json",
  "reported_level": "Level_3",
  "conflicts_with": ["STATE.md", "workspace-memory.json", "FINAL-REPORT.md"],
  "consensus_level": "Level_2"
}

输出一份 baseline_reconciliation_report.md（临时诊断文件），包含：
- 五个来源的 Level 叙述对比表
- 分歧点列表（若有）
- 推荐的权威基线：Level 2（保守解释）
  </action>
  <acceptance_criteria>
baseline_reconciliation_report.md 存在且包含五个来源的 Level 叙述对比表。
对比表包含：STATE.md、workspace-memory.json、validation_status.json、level_assessment.json、FINAL-REPORT.md 的 Level 字段。
若存在分歧，baseline_reconciliation_report.md 明确记录分歧点并推荐 Level 2 作为权威基线。
  </acceptance_criteria>
  <verify>
    <automated>grep -E "Level_[0-9]|Level [0-9]" baseline_reconciliation_report.md 应返回至少 5 行（五个来源的 Level 叙述）</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 01-02: 修正 level_assessment.json 以匹配权威基线</name>
  <read_first>
    - E:/github/rag/verification/phase3-real-validation/level_assessment.json
    - E:/github/rag/.planning/phases/04-pageindex-quality-improvement-and-baseline-reconciliation/baseline_reconciliation_report.md
    - E:/github/rag/verification/phase3-real-validation/FINAL-REPORT.md
  </read_first>
  <action>
读取 baseline_reconciliation_report.md 的分歧清单和推荐基线。
若 level_assessment.json 的 final_level 字段与推荐基线不一致：
- 将 final_level 从 "Level_3" 修改为 "Level_2"
- 在 level_assessment.json 中添加 reconciled 字段：true
- 在 level_assessment.json 中添加 reconciliation_note 字段："Aligned with STATE.md and FINAL-REPORT.md consensus - Level 2 is the conservative planning baseline for Phase 4"
- 保留原有的 threshold_scores、manual_judgment_summary 等字段不变

若 level_assessment.json 已与推荐基线一致：
- 添加 reconciled 字段：true
- 添加 reconciliation_note："Already aligned with STATE.md and FINAL-REPORT.md - Level 2 baseline confirmed"
  </action>
  <acceptance_criteria>
level_assessment.json 包含 reconciled 字段且值为 true。
level_assessment.json 的 final_level 字段为 "Level_2"。
level_assessment.json 包含 reconciliation_note 字段，内容明确说明已与 STATE.md/FINAL-REPORT.md 对账。
  </acceptance_criteria>
  <verify>
    <automated>grep '"final_level": "Level_2"' verification/phase3-real-validation/level_assessment.json 应返回 1 行</automated>
    <automated>grep '"reconciled": true' verification/phase3-real-validation/level_assessment.json 应返回 1 行</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 01-03: 更新 STATE.md 以正式切换到 Phase 4</name>
  <read_first>
    - E:/github/rag/.planning/STATE.md
    - E:/github/rag/verification/phase3-real-validation/level_assessment.json
    - E:/github/rag/.planning/ROADMAP.md
  </read_first>
  <action>
读取 STATE.md 的 frontmatter 和 `## Current Position` section。
将 Phase 字段从 "4 of 4" 保持不变（Phase 4 已打开）。
将 Status 字段更新为："Phase 4 baseline reconciliation complete - Level 2 authoritative baseline established"。
将 Last activity 字段更新为："Baseline reconciliation completed - STATE/workspace-memory/validation artifacts now aligned on Level 2"。
在 `## Accumulated Context` → `### Decisions` section 添加新决策：
- "Phase 3 real validation final Level: Level 2 (conservative baseline, reconciled across STATE/workspace-memory/validation_status/level_assessment/FINAL-REPORT)"
- "Phase 4 planning baseline: Level 2 (authoritative)"

在 `### Blockers/Concerns` section：
- 移除 "Phase 3 COMPLETED: All core goals achieved, real Level determined (Level 2)"（已迁移到 Decisions）
- 更新 blocker 列表为 Phase 4 的新 blockers：
  - "Quality gaps remain: hit_rate 5%, top1_relevance 10%, stability 0% (need ≥80%/≥90%/≥85%)"
  - "Tree depth too shallow: max_level 1 (need ≥3)"
  - "Evidence-chain metrics incomplete: chunks/mapped_chunks/heading_path need restoration"
  - "Document/query mismatch: validation corpus (竞品分析) doesn't match business queries (技术系统)"

读取 ROADMAP.md 的 Phase 4 section，确认其 Status: Active 与 STATE.md 一致。
  </action>
  <acceptance_criteria>
STATE.md 的 Status 字段包含 "Phase 4 baseline reconciliation complete"。
STATE.md 的 Last activity 字段包含 "Baseline reconciliation completed"。
STATE.md 的 Decisions section 包含 "Phase 3 real validation final Level: Level 2"。
STATE.md 的 Blockers/Concerns section 列出 Phase 4 的四个新 blockers。
  </acceptance_criteria>
  <verify>
    <automated>grep "Phase 4 baseline reconciliation complete" .planning/STATE.md 应返回 1 行</automated>
    <automated>grep "Phase 3 real validation final Level: Level 2" .planning/STATE.md 应返回 1 行</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 01-04: 更新 workspace-memory.json 以匹配 STATE.md</name>
  <read_first>
    - E:/github/rag/.planning/workspace-memory.json
    - E:/github/rag/.planning/STATE.md
    - E:/github/rag/verification/phase3-real-validation/level_assessment.json
  </read_first>
  <action>
读取 workspace-memory.json 的 accumulated_context section。
在 decisions 数组中添加：
- {"decision": "Phase 3 real validation final Level: Level 2 (reconciled baseline)", "timestamp": "2026-05-31", "source": "baseline reconciliation"}
- {"decision": "Phase 4 planning baseline: Level 2 (authoritative)", "timestamp": "2026-05-31", "source": "STATE.md update"}

在 pending_todos 数组中：
- 移除 "Phase 4 planning: Quality improvement strategy"（已在本 plan 执行）
- 添加新的 Phase 4 todos：
  - "Align validation corpus with business query domain"
  - "Improve tree depth to >= 3 levels"
  - "Restore evidence-chain metrics (chunks/mapped_chunks/heading_path)"
  - "Rerun real validation with frozen thresholds"

在 blockers 数组中更新：
- 移除 "Phase 3 COMPLETED" 相关 blocker
- 添加四个新的 Phase 4 blockers（与 STATE.md 一致）：
  - "Quality gaps: hit_rate 5%, top1_relevance 10%, stability 0%"
  - "Tree depth: max_level 1 (need ≥3)"
  - "Evidence-chain incomplete"
  - "Document/query mismatch"

更新 last_activity 字段为："Baseline reconciliation completed - Level 2 authoritative baseline established"。
  </action>
  <acceptance_criteria>
workspace-memory.json 的 decisions 数组包含 "Phase 3 real validation final Level: Level 2"。
workspace-memory.json 的 pending_todos 数组包含 "Align validation corpus with business query domain"。
workspace-memory.json 的 blockers 数组包含 "Quality gaps: hit_rate 5%"。
workspace-memory.json 的 last_activity 字段包含 "Baseline reconciliation completed"。
  </acceptance_criteria>
  <verify>
    <automated>grep "Phase 3 real validation final Level: Level 2" .planning/workspace-memory.json 应返回 1 行</automated>
    <automated>grep "Align validation corpus" .planning/workspace-memory.json 应返回 1 行</automated>
  </verify>
</task>
</tasks>

<verification>
STATE.md、workspace-memory.json、validation_status.json、level_assessment.json、FINAL-REPORT.md 的 Level 叙述一致（均为 Level 2）。
STATE.md 正式切换到 Phase 4 并记录新的 blockers。
workspace-memory.json 的 decisions/todos/blockers 与 STATE.md 同步。
level_assessment.json 包含 reconciled: true 字段。
</verification>

<success_criteria>
- 五个来源的 Level 叙述完全一致
- STATE.md 的 Phase 4 status 正确反映基线对账完成
- workspace-memory.json 与 STATE.md 同步
- level_assessment.json 标记已对账
- baseline_reconciliation_report.md 诊断文件生成
</success_criteria>