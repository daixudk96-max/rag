---
phase: 04-pageindex-quality-improvement-and-baseline-reconciliation
plan: 03
type: execute
wave: 3
depends_on:
  - 04-PLAN-01
  - 04-PLAN-02
files_modified:
  - verification/phase3-real-validation/validation_status.json
  - verification/phase3-real-validation/level_assessment.json
  - verification/phase3-real-validation/judgment_completed.csv
  - verification/phase3-real-validation/FINAL-REPORT.md
  - llamaindex_runtime/tree/reasoning_backend.py
  - llamaindex_runtime/tree/pageindex_adapter.py
  - tests/llamaindex_runtime/test_evidence_chain_completeness.py
  - tests/llamaindex_runtime/test_level_assessment.py
requirements: []
must_haves:
  truths:
    - Phase 4 真实复验使用修复后的文档/查询域和树结构，不再重复 Phase 3 的 mismatch 问题。
    - 证据链统计恢复为可解释真实值，不再出现 chunks=0、mapped_chunks=0 的空快照。
    - 最终 Level 判断严格沿用 Phase 2 冻结阈值，所有 gates 通过才能判定 Level 4。
  key_links:
    - from: verification/phase3-real-validation/validation_status.json
      to: tests/llamaindex_runtime/test_evidence_chain_completeness.py
      via: evidence-chain metrics restoration
      pattern: chunks|mapped_chunks|heading_path_rate
    - from: verification/phase3-real-validation/level_assessment.json
      to: tests/llamaindex_runtime/test_level_assessment.py
      via: frozen threshold enforcement
      pattern: hit_rate|top1_relevance|stability|tree_depth|threshold_gate
autonomous: true
---

<objective>
恢复真实 chunk/mapping/heading 统计口径，重新运行真实验证，生成最终 Level 判断并确保所有阈值 gates 严格通过。
</objective>

<context>
Phase 3 真实验证结果：Level 2（未达标），证据链统计 chunks/mapped_chunks/heading_path_rate 可能为空或不完整。Phase 4 修复 corpus/tree 后，需恢复证据链统计，重新验证并判断是否达到 Level 3/4。
</context>

<tasks>
<task type="auto">
  <name>Task 03-01: 恢复 evidence-chain 统计口径</name>
  <read_first>
    - E:/github/rag/verification/phase3-real-validation/validation_status.json
    - E:/github/rag/llamaindex_runtime/tree/pageindex_adapter.py
    - E:/github/rag/llamaindex_runtime/tree/reasoning_backend.py
    - E:/github/rag/tests/llamaindex_runtime/test_evidence_chain_completeness.py
  </read_first>
  <action>
读取 validation_status.json 的当前证据链字段：
- chunks（当前值可能为 0 或空）
- mapped_chunks（当前值可能为 0 或空）
- heading_path_rate（当前值可能为 0 或空）

分析统计口径恢复点：
读取 pageindex_adapter.py 的 chunk extraction 逻辑：
- 定位 extract_chunks 或等价方法
- 检查 chunk 计数如何从 donor 输出提取
- 查看是否有 flatten/merge 操作丢失 chunk 信息

读取 reasoning_backend.py 的 mapping 逻辑：
- 定位 map_chunks_to_nodes 或等价方法
- 检查 node-chunk mapping 如何生成
- 查看是否有逻辑导致 mapped_chunks 为 0

读取 test_evidence_chain_completeness.py 的验证逻辑：
- 提取阈值要求：node_chunk_mapping >= 80%、heading_path >= 95%
- 分析测试如何验证证据链完整性

执行统计口径恢复：
若 chunks 为 0：修复 pageindex_adapter.py 的 chunk extraction 逻辑，确保从 donor 输出正确提取 chunk 计数。
若 mapped_chunks 为 0：修复 reasoning_backend.py 的 mapping 逻辑，确保生成完整的 node-chunk mapping。
若 heading_path_rate 为 0：修复 heading_path 提取逻辑，确保从树结构正确生成 heading_path。

修复后重新运行 ingestion phase：
执行命令：cd E:/github/rag && python verification/phase3-real-validation/run_validation.py --phase ingestion
生成新的 validation_status.json（包含恢复后的证据链统计）。

验证修复结果：
运行 pytest tests/llamaindex_runtime/test_evidence_chain_completeness.py -q
检查测试输出是否通过 evidence-chain thresholds 验证。
若测试失败，记录失败原因并调整修复逻辑。
若测试通过，确认 validation_status.json 的 chunks、mapped_chunks、heading_path_rate 均为非零值。
  </action>
  <acceptance_criteria>
validation_status.json 的 chunks 字段 > 0（非空快照）。
validation_status.json 的 mapped_chunks 字段 > 0。
validation_status.json 的 heading_path_rate 字段 > 0 且 >= 0.95（或接近阈值）。
test_evidence_chain_completeness.py 测试通过（或接近阈值，允许 Phase 4 优化后达到）。
  </acceptance_criteria>
  <verify>
    <automated>grep "chunks.*[1-9]" verification/phase3-real-validation/validation_status.json 应返回 1 行</automated>
    <automated>grep "mapped_chunks.*[1-9]" verification/phase3-real-validation/validation_status.json 应返回 1 行</automated>
    <automated>pytest tests/llamaindex_runtime/test_evidence_chain_completeness.py -q 应 exit 0 或接近阈值</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 03-02: 重新运行真实验证（使用修复后的 corpus/tree/evidence-chain）</name>
  <read_first>
    - E:/github/rag/verification/phase3-real-validation/run_validation.py
    - E:/github/rag/verification/phase3-real-validation/validation_status.json
    - E:/github/rag/llamaindex_runtime/tree/reasoning_backend.py
    - E:/github/rag/.planning/phases/04-pageindex-quality-improvement-and-baseline-reconciliation/preparation_summary.md
  </read_first>
  <action>
读取 preparation_summary.md 确认 Phase 4 准备状态：
- Corpus aligned: true（或确认 corpus_not_found 状态并跳过本 task）
- Tree depth fixed: true（或确认 fix required 状态并跳过本 task）
- Ready for validation rerun: true（或确认 incomplete 状态并跳过本 task）

若 preparation_status 为 incomplete：
输出警告：Phase 4 preparation incomplete - skip real validation rerun
记录 blockers 并建议先完成 04-PLAN-02 的修复任务。

若 preparation_status 为 ready：
运行完整真实验证流程：
执行命令：cd E:/github/rag && python verification/phase3-real-validation/run_validation.py --phase full

验证流程包含：
1. Ingestion phase（使用新 corpus）
2. Retrieval phase（使用修复后的树结构）
3. Judgment phase（人工判断 top1 relevance）

等待验证完成，生成新的 judgment_completed.csv、validation_status.json。

检查验证输出：
- hit_rate：应 >= 0.80（Phase 2 冻结阈值）
- top1_relevance：应 >= 0.90（Phase 2 冻结阈值）
- stability：应 >= 0.85（Phase 2 冻结阈值）
- tree_max_level：应 >= 3（Phase 2 冻结阈值）
- node_chunk_mapping：应 >= 0.80（Phase 2 冻结阈值）
- heading_path_rate：应 >= 0.95（Phase 2 冻结阈值）

若验证失败（某些指标未达到阈值）：
记录失败指标和 gap analysis。
建议进一步优化（如调整 retrieval 策略、改进 corpus coverage）。

若验证成功（所有指标达到阈值）：
准备生成最终 Level assessment。
  </action>
  <acceptance_criteria>
若 preparation incomplete：跳过验证，记录 blockers。
若 preparation ready：judgment_completed.csv 包含 20 个查询的 judgment 结果（至少部分 relevant）。
validation_status.json 包含新的 hit_rate、top1_relevance、stability、tree_max_level 等字段。
validation_status.json 的 tree_max_level >= 3。
validation_status.json 的证据链字段为非零值。
  </acceptance_criteria>
  <verify>
    <automated>若 preparation ready：grep "judgment_completed" verification/phase3-real-validation/ 应返回至少 1 行（judgment_completed.csv 存在）</automated>
    <automated>若 preparation ready：grep "tree_max_level.*[3-9]" verification/phase3-real-validation/validation_status.json 应返回 1 行</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 03-03: 生成最终 Level assessment（严格使用冻结阈值）</name>
  <read_first>
    - E:/github/rag/verification/phase3-real-validation/validation_status.json
    - E:/github/rag/verification/phase3-real-validation/judgment_completed.csv
    - E:/github/rag/verification/phase3-real-validation/level_assessment.json
    - E:/github/rag/.planning/phases/02-pageindex-main-function-quality-validation-and-closure/02-SUMMARY.md
    - E:/github/rag/tests/llamaindex_runtime/test_level_assessment.py
  </read_first>
  <action>
读取 Phase 2 SUMMARY.md 的冻结阈值：
- hit_rate >= 80%
- top1_relevance >= 90%
- stability >= 85%
- tree_depth >= 3
- node_chunk_mapping >= 80%
- heading_path >= 95%

读取 validation_status.json 的新验证结果：
提取 hit_rate、top1_relevance、stability、tree_max_level、node_chunk_mapping_rate、heading_path_rate。

读取 judgment_completed.csv：
统计 relevant 查询数量，计算 manual judgment pass rate。

执行 Level assessment 逻辑：
**Level 4 判定条件（ALL thresholds must pass）：**
- hit_rate >= 80%
- top1_relevance >= 90%
- stability >= 85%
- tree_max_level >= 3
- node_chunk_mapping_rate >= 80%
- heading_path_rate >= 95%
若所有指标通过 → Level 4

**Level 3 判定条件：**
- hit_rate >= 80%
- top1_relevance、stability、tree_depth、evidence-chain 可部分未达标
若 hit_rate 通过但其他指标部分未达标 → Level 3

**Level 2 判定条件：**
- hit_rate < 80%
若 hit_rate 未达标 → Level 2（未验证）

生成 level_assessment.json：
{
  "final_level": "Level_2|Level_3|Level_4",
  "threshold_scores": {
    "hit_rate": <value>,
    "top1_relevance": <value>,
    "stability": <value>,
    "tree_max_level": <value>,
    "node_chunk_mapping_rate": <value>,
    "heading_path_rate": <value>
  },
  "threshold_gates": {
    "hit_rate_gate": "PASS|FAIL",
    "top1_relevance_gate": "PASS|FAIL",
    "stability_gate": "PASS|FAIL",
    "tree_depth_gate": "PASS|FAIL",
    "node_chunk_mapping_gate": "PASS|FAIL",
    "heading_path_gate": "PASS|FAIL"
  },
  "manual_judgment_summary": {
    "total_queries": 20,
    "relevant_queries": <count>,
    "pass_rate": <value>
  },
  "assessment_timestamp": "2026-05-31",
  "phase": "Phase_4_real_validation_rerun",
  "thresholds_frozen_from": "Phase_2_VALIDATION.md",
  "reconciled": true
}

运行 test_level_assessment.py 验证：
执行 pytest tests/llamaindex_runtime/test_level_assessment.py -q
检查测试是否通过 Level assessment logic 验证。
  </action>
  <acceptance_criteria>
level_assessment.json 包含 final_level 字段（Level_2、Level_3 或 Level_4）。
level_assessment.json 包含所有 threshold_scores 字段（hit_rate、top1_relevance、stability、tree_max_level、node_chunk_mapping_rate、heading_path_rate）。
level_assessment.json 包含 threshold_gates 字段（每个 gate 为 PASS 或 FAIL）。
level_assessment.json 包含 reconciled: true 字段。
level_assessment.json 包含 thresholds_frozen_from: "Phase_2_VALIDATION.md" 字段。
test_level_assessment.py 测试通过（验证 Level assessment logic 正确）。
  </acceptance_criteria>
  <verify>
    <automated>grep "final_level.*Level_[2-4]" verification/phase3-real-validation/level_assessment.json 应返回 1 行</automated>
    <automated>grep "reconciled.*true" verification/phase3-real-validation/level_assessment.json 应返回 1 行</automated>
    <automated>grep "thresholds_frozen_from.*Phase_2" verification/phase3-real-validation/level_assessment.json 应返回 1 行</automated>
    <automated>pytest tests/llamaindex_runtime/test_level_assessment.py -q 应 exit 0</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 03-04: 生成 FINAL-REPORT.md 并更新 STATE/workspace-memory</name>
  <read_first>
    - E:/github/rag/verification/phase3-real-validation/FINAL-REPORT.md
    - E:/github/rag/verification/phase3-real-validation/level_assessment.json
    - E:/github/rag/verification/phase3-real-validation/validation_status.json
    - E:/github/rag/.planning/STATE.md
    - E:/github/rag/.planning/workspace-memory.json
  </read_first>
  <action>
读取 level_assessment.json 的 final_level 和 threshold_gates。
读取 validation_status.json 的完整验证结果。

生成新的 FINAL-REPORT.md：
包含以下 sections：

**## Phase 4 Validation Summary**
- Corpus: <技术系统文档路径>（或竞品分析，若 corpus_not_found）
- Tree depth: <tree_max_level value>
- Evidence-chain: chunks=<value>, mapped_chunks=<value>, heading_path_rate=<value>

**## Quality Metrics**
| Metric | Value | Threshold | Status |
|--------|-------|-----------|--------|
| hit_rate | <value> | >=80% | PASS/FAIL |
| top1_relevance | <value> | >=90% | PASS/FAIL |
| stability | <value> | >=85% | PASS/FAIL |
| tree_depth | <value> | >=3 | PASS/FAIL |
| node_chunk_mapping | <value> | >=80% | PASS/FAIL |
| heading_path | <value> | >=95% | PASS/FAIL |

**## Level Determination**
- Final Level: <Level_2|Level_3|Level_4>
- Reasoning: <解释哪些 threshold gates 通过/失败>
- Frozen thresholds enforced: Phase 2 validation gates (no relaxation)

**## Key Improvements from Phase 3**
- Corpus/query domain alignment（若 corpus_aligned）
- Tree depth improvement（若 tree_depth >= 3）
- Evidence-chain restoration（若 chunks/mapped_chunks/heading_path > 0）

**## Remaining Gaps（若未达到 Level 4）**
- <列出未通过的 threshold gates 和 gap 值>
- Recommendation: <下一步优化建议>

更新 STATE.md：
在 `## Current Position` section：
- Status 更新为："Phase 4 real validation rerun complete - Level <X> determined"
- Last activity 更新为："Phase 4 validation completed with corpus/tree/evidence-chain fixes"

在 `## Accumulated Context` → `### Decisions` section：
- 添加："Phase 4 real validation final Level: <Level_X> (thresholds enforced from Phase 2)"

在 `### Blockers/Concerns` section：
- 若达到 Level 4：移除所有 blockers，添加 "Phase 4 validation passed all frozen thresholds"
- 若未达到 Level 4：保留未通过的 threshold gates 作为 blockers

更新 workspace-memory.json：
在 decisions 数组添加：
- {"decision": "Phase 4 real validation final Level: <Level_X>", "timestamp": "2026-05-31", "source": "FINAL-REPORT.md"}

在 pending_todos 数组：
- 若达到 Level 4：清空 todos，添加 "Project readiness achieved - Level 4"
- 若未达到 Level 4：添加优化建议 todos

在 blockers 数组：
- 同步 STATE.md 的 blockers 更新。
  </action>
  <acceptance_criteria>
FINAL-REPORT.md 包含 Phase 4 Validation Summary section（corpus/tree/evidence-chain 状态）。
FINAL-REPORT.md 包含 Quality Metrics 表格（所有 threshold gates 的 PASS/FAIL 状态）。
FINAL-REPORT.md 包含 Level Determination section（明确 final_level 和 reasoning）。
STATE.md 的 Status 包含 "Phase 4 real validation rerun complete - Level <X>"。
STATE.md 的 Decisions section 包含 "Phase 4 real validation final Level: <Level_X>"。
workspace-memory.json 的 decisions 数组包含 "Phase 4 real validation final Level: <Level_X>"。
若达到 Level 4：STATE.md 和 workspace-memory.json 的 blockers 已清空或标记为 passed。
  </acceptance_criteria>
  <verify>
    <automated>grep "## Phase 4 Validation Summary" verification/phase3-real-validation/FINAL-REPORT.md 应返回 1 行</automated>
    <automated>grep "Final Level.*Level_[2-4]" verification/phase3-real-validation/FINAL-REPORT.md 应返回 1 行</automated>
    <automated>grep "Phase 4 real validation rerun complete" .planning/STATE.md 应返回 1 行</automated>
  </verify>
</task>
</tasks>

<verification>
validation_status.json 的证据链字段恢复为非零值。
judgment_completed.csv 包含新验证结果（若 preparation ready）。
level_assessment.json 包含 reconciled: true 和 thresholds_frozen_from: "Phase_2_VALIDATION.md"。
test_evidence_chain_completeness.py 和 test_level_assessment.py 测试通过。
FINAL-REPORT.md 包含完整的 Phase 4 validation summary 和 Level determination。
STATE.md 和 workspace-memory.json 更新为 Phase 4 validation complete 状态。
</verification>

<success_criteria>
- 证据链统计恢复为可解释真实值（chunks、mapped_chunks、heading_path_rate > 0）
- 真实复验完成（或明确记录 preparation incomplete blockers）
- 最终 Level 判断与所有 threshold gates 一致
- 冻结阈值未被放松
- STATE/workspace-memory/FINAL-REPORT 叙述一致
</success_criteria>