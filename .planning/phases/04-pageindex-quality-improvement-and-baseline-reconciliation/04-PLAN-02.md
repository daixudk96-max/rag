---
phase: 04-pageindex-quality-improvement-and-baseline-reconciliation
plan: 02
type: execute
wave: 2
depends_on:
  - 04-PLAN-01
files_modified:
  - verification/phase3-real-validation/run_validation.py
  - verification/phase3-real-validation/judgment_completed.csv
  - llamaindex_runtime/tree/reasoning_backend.py
  - llamaindex_runtime/tree/pageindex_adapter.py
  - llamaindex_runtime/client/pageindex_client.py
  - tests/llamaindex_runtime/test_tree_structure_quality.py
requirements: []
must_haves:
  truths:
    - Phase 4 使用的验证文档域与业务查询域明确匹配，不再用竞品分析文档回答技术系统问题。
    - 真实树结构达到至少 3 层，减少 root/大章节主导 top1 的情况。
    - 文档/查询对齐和树深修复后，验证输入集已准备完成。
  key_links:
    - from: verification/phase3-real-validation/run_validation.py
      to: llamaindex_runtime/tree/reasoning_backend.py
      via: corpus/query alignment
      pattern: document_path|query_set|validation_corpus
    - from: llamaindex_runtime/tree/pageindex_adapter.py
      to: tests/llamaindex_runtime/test_tree_structure_quality.py
      via: tree depth verification
      pattern: tree_max_level|level_no|depth_threshold
autonomous: true
---

<objective>
对齐验证文档域与业务查询域，修复树结构深度使真实树达到至少 3 层，准备 Phase 4 的真实验证输入集。
</objective>

<context>
Phase 3 真实验证暴露：验证文档为竞品分析，而业务查询针对技术系统，导致 90% 查询无相关结果。同时真实树结构 max_level 为 1，无法支撑精确小节命中。本 plan 解决这两个 P0/P1 瓶颈。
</context>

<tasks>
<task type="auto">
  <name>Task 02-01: 识别并选择与技术系统查询匹配的验证文档</name>
  <read_first>
    - E:/github/rag/verification/phase3-real-validation/run_validation.py
    - E:/github/rag/verification/phase3-real-validation/judgment_completed.csv
    - E:/github/rag/.planning/STATE.md
    - E:/github/rag/.planning/workspace-memory.json
  </read_first>
  <action>
读取 run_validation.py 的 validation_corpus 配置 section，提取当前文档路径（如 "第二阶段-竞品分析-代旭.md"）。
读取 judgment_completed.csv 的 query 列，提取 20 个业务查询文本。
分析查询主题分布：
- 统计查询关键词：PageIndex、检索、树结构、LLM、rag、embedding、文档、markdown、 PostgreSQL、pgvector
- 确认查询域为"技术系统实现与架构"

搜索项目中与技术系统匹配的文档：
在项目根目录执行 Glob 搜索：
- E:/github/rag/**/*.md（排除 verification/ 目录）
- 优先查找：PageIndex*.md、rag*.md、技术*.md、系统*.md、架构*.md
- 读取候选文档的 frontmatter 和前 50 行，判断是否包含：
  - PageIndex 实现细节
  - 树结构设计
  - LLM/retrieval 架构
  - PostgreSQL/pgvector 使用

选择最匹配的文档（如存在）：
- 文档标题包含 "PageIndex" 或 "rag" 或 "技术系统"
- 文档内容覆盖树结构、检索流程、数据库设计
- 文档长度 >= 5000 字（足够支撑真实验证）

若找不到匹配文档，记录诊断结果：
{
  "status": "corpus_not_found",
  "current_corpus": "竞品分析文档",
  "query_domain": "技术系统",
  "recommendation": "需新建或导入技术系统文档，或修改查询集以匹配竞品分析域"
}

若找到匹配文档，记录选择结果：
{
  "status": "corpus_aligned",
  "selected_corpus": "<文档路径>",
  "match_score": "<关键词覆盖率>",
  "coverage": ["PageIndex", "树结构", "LLM", "PostgreSQL"]
}

输出 corpus_alignment_report.md（临时诊断文件），包含：
- 当前验证文档与查询域的 mismatch 分析
- 新文档选择结果（若有）
- corpus/query alignment 状态
  </action>
  <acceptance_criteria>
corpus_alignment_report.md 存在且包含当前 corpus/query mismatch 分析。
corpus_alignment_report.md 包含 query 关键词统计（PageIndex、树结构、LLM、PostgreSQL 等）。
若找到匹配文档，corpus_alignment_report.md 记录 selected_corpus 路径和 match_score。
若未找到，corpus_alignment_report.md 记录 corpus_not_found 状态和 recommendation。
  </acceptance_criteria>
  <verify>
    <automated>grep -E "mismatch|corpus_aligned|corpus_not_found" corpus_alignment_report.md 应返回至少 1 行</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 02-02: 更新 run_validation.py 的验证文档配置</name>
  <read_first>
    - E:/github/rag/verification/phase3-real-validation/run_validation.py
    - E:/github/rag/.planning/phases/04-pageindex-quality-improvement-and-baseline-reconciliation/corpus_alignment_report.md
  </read_first>
  <action>
读取 corpus_alignment_report.md 的 selected_corpus 路径（若 corpus_aligned）。
若 corpus_not_found：
- 保持 run_validation.py 的文档路径不变
- 在 run_validation.py 顶部添加注释：
  # TODO: Phase 4 requires corpus/query alignment
  # Current corpus (竞品分析) mismatches business query domain (技术系统)
  # Recommendation: import or create 技术系统 document, OR modify query set
- 输出警告：corpus_not_found 状态下无法完成 domain alignment

若 corpus_aligned：
读取 run_validation.py 的配置 section（通常在文件开头的 VALIDATION_CONFIG 或等价变量）。
定位文档路径配置字段（如 document_path、corpus_file、markdown_path）。
将文档路径从竞品分析文档修改为新选定的技术系统文档路径：
- 旧路径："第二阶段-竞品分析-代旭.md" 或等价值
- 新路径：<selected_corpus>

在 run_validation.py 添加注释：
  # Phase 4: Corpus aligned with business query domain
  # Selected corpus covers: PageIndex, 树结构, LLM, PostgreSQL
  # Alignment score: <match_score>

在 run_validation.py 的 query_set 配置 section 添加验证：
  # Queries validated for corpus alignment:
  # - All queries target 技术系统 domain
  # - Corpus coverage verified via corpus_alignment_report.md
  </action>
  <acceptance_criteria>
若 corpus_aligned：run_validation.py 包含新文档路径（非竞品分析文档）。
run_validation.py 包含 Phase 4 corpus alignment 注释。
若 corpus_not_found：run_validation.py 包含 TODO 注释说明 mismatch 状态。
  </acceptance_criteria>
  <verify>
    <automated>若 corpus_aligned：grep -v "竞品分析" verification/phase3-real-validation/run_validation.py | grep "document_path|corpus_file" 应返回新路径</automated>
    <automated>grep "Phase 4: Corpus aligned" verification/phase3-real-validation/run_validation.py 应返回 1 行（若 corpus_aligned）</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 02-03: 分析当前树结构深度瓶颈</name>
  <read_first>
    - E:/github/rag/llamaindex_runtime/tree/pageindex_adapter.py
    - E:/github/rag/llamaindex_runtime/tree/reasoning_backend.py
    - E:/github/rag/verification/phase3-real-validation/validation_status.json
    - E:/github/rag/tests/llamaindex_runtime/test_tree_structure_quality.py
  </read_first>
  <action>
读取 validation_status.json 的 tree_max_level 字段（当前值应为 1）。
读取 pageindex_adapter.py 的 _flatten_embedded_tree 方法实现：
- 分析 level_no 计算逻辑
- 检查是否正确传递树深度信息
- 查看是否有 flatten 操作丢失层级

读取 reasoning_backend.py 的 retrieve_tree_hits 方法：
- 分析如何使用树结构进行检索
- 检查是否有逻辑限制树深度使用
- 查看是否有 max_level 配置参数

读取 test_tree_structure_quality.py 的测试用例：
- 提取 tree_depth_threshold（应为 >= 3）
- 分析测试如何验证树深度

输出 tree_depth_analysis_report.md（临时诊断文件），包含：
- 当前 tree_max_level 状态：1（从 validation_status.json）
- 适配器层（pageindex_adapter.py）的树深度处理逻辑分析
- 运行层（reasoning_backend.py）的树深度使用逻辑分析
- 根因诊断：
  - 若适配器 flatten 丢失层级：记录具体代码位置和逻辑缺陷
  - 若运行层限制深度：记录配置参数或逻辑限制
  - 若上游数据本身浅：记录 donor 输出或 markdown 结构问题
- 修复建议：明确需要修改的具体函数、参数、逻辑
  </action>
  <acceptance_criteria>
tree_depth_analysis_report.md 存在且包含 tree_max_level 状态（值为 1）。
tree_depth_analysis_report.md 包含 pageindex_adapter.py 和 reasoning_backend.py 的树深度逻辑分析。
tree_depth_analysis_report.md 包含根因诊断和修复建议（明确到具体函数/参数）。
  </acceptance_criteria>
  <verify>
    <automated>grep "tree_max_level: 1" tree_depth_analysis_report.md 应返回 1 行</automated>
    <automated>grep "pageindex_adapter|reasoning_backend" tree_depth_analysis_report.md 应返回至少 2 行</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 02-04: 修复树结构深度以达到 >= 3 层</name>
  <read_first>
    - E:/github/rag/llamaindex_runtime/tree/pageindex_adapter.py
    - E:/github/rag/llamaindex_runtime/tree/reasoning_backend.py
    - E:/github/rag/.planning/phases/04-pageindex-quality-improvement-and-baseline-reconciliation/tree_depth_analysis_report.md
    - C:/Users/daixu/Downloads/rag-upstreams/PageIndex/pageindex/page_index_md.py
  </read_first>
  <action>
读取 tree_depth_analysis_report.md 的根因诊断和修复建议。
根据诊断结果执行修复：

**Case A: 适配器 flatten 丢失层级**
若诊断显示 pageindex_adapter.py 的 _flatten_embedded_tree 方法丢失层级：
- 定位 _flatten_embedded_tree 方法
- 检查 level_no 计算逻辑（是否正确累加深度）
- 修复方案：
  - 确保 level_no 从 donor 输出正确继承
  - 若 flatten 逻辑简化了树结构，添加 preserve_depth 参数
  - 在 flatten 输出中保留完整 level_no 信息

**Case B: 运行层限制树深度使用**
若诊断显示 reasoning_backend.py 限制树深度：
- 定位 retrieve_tree_hits 方法
- 检查是否有 max_level 配置参数（值可能为 1）
- 修复方案：
  - 移除或提高 max_level 限制（设置为 >= 3）
  - 或添加配置参数允许动态设置树深度阈值
  - 确保检索逻辑能使用完整树深度

**Case C: 上游数据本身浅**
若诊断显示 donor 输出或 markdown 结构本身只有 1 层：
- 检查 C:/Users/daixu/Downloads/rag-upstreams/PageIndex/pageindex/page_index_md.py 的树构建逻辑
- 修复方案（需要上游配合，在本 task 中记录诊断）：
  - 在 tree_depth_analysis_report.md 添加 "Upstream Fix Required" section
  - 记录上游 page_index_md.py 的树构建逻辑缺陷
  - 标记本 task 为 partial completion（需要上游修复后才能达到 >= 3）

修复后验证：
运行 pytest tests/llamaindex_runtime/test_tree_structure_quality.py -q
检查测试输出是否通过 tree_depth >= 3 验证。
若测试失败，记录失败原因并在 tree_depth_analysis_report.md 添加 "Fix Verification Failed" section。
若测试通过，在 tree_depth_analysis_report.md 添加 "Fix Verification Passed" section。
  </action>
  <acceptance_criteria>
若 Case A/B：pageindex_adapter.py 或 reasoning_backend.py 包含树深度修复代码（level_no 累加逻辑或 max_level 参数移除）。
test_tree_structure_quality.py 测试通过（tree_depth >= 3 验证）。
tree_depth_analysis_report.md 包含 "Fix Verification Passed" section（或 "Upstream Fix Required"）。
若 upstream fix required：tree_depth_analysis_report.md 明确记录上游缺陷和需要的修复。
  </acceptance_criteria>
  <verify>
    <automated>pytest tests/llamaindex_runtime/test_tree_structure_quality.py -q 应 exit 0（若 Case A/B 成功）</automated>
    <automated>grep "Fix Verification Passed|Upstream Fix Required" tree_depth_analysis_report.md 应返回至少 1 行</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 02-05: 生成新的验证输入集快照</name>
  <read_first>
    - E:/github/rag/verification/phase3-real-validation/run_validation.py
    - E:/github/rag/llamaindex_runtime/tree/reasoning_backend.py
    - E:/github/rag/llamaindex_runtime/tree/pageindex_adapter.py
    - E:/github/rag/verification/phase3-real-validation/validation_status.json
  </read_first>
  <action>
运行 run_validation.py 的 ingestion phase（若 corpus_aligned）：
执行命令：cd E:/github/rag && python verification/phase3-real-validation/run_validation.py --phase ingestion
输出新的 validation_status.json（包含新 corpus 的 ingestion 结果）。

检查 validation_status.json 的关键字段：
- tree_max_level：应为 >= 3（若 tree depth fix 成功）
- chunks：应 > 0（非空快照）
- mapped_chunks：应 > 0
- heading_path_rate：应 > 0
- corpus_path：应为新选定的技术系统文档（若 corpus_aligned）

若 corpus_not_found 或 tree depth fix 未成功：
在 validation_status.json 添加注释字段：
{
  "phase_4_preparation_status": "incomplete",
  "blockers": ["corpus_not_found", "tree_depth_fix_required"],
  "recommendation": "resolve blockers before Phase 4 real validation rerun"
}

若 corpus_aligned 且 tree depth >= 3：
在 validation_status.json 添加注释字段：
{
  "phase_4_preparation_status": "ready",
  "corpus_aligned": true,
  "tree_depth_fixed": true,
  "ready_for_validation_rerun": true
}

生成 preparation_summary.md：
- Corpus alignment status（aligned 或 not_found）
- Tree depth status（>= 3 或 fix required）
- Validation input set readiness（ready 或 incomplete）
- Blockers list（若有）
  </action>
  <acceptance_criteria>
若 corpus_aligned：validation_status.json 的 corpus_path 为新文档路径（非竞品分析）。
若 tree depth fix 成功：validation_status.json 的 tree_max_level >= 3。
validation_status.json 包含 phase_4_preparation_status 字段（ready 或 incomplete）。
preparation_summary.md 存在且包含 corpus/tree/readiness 状态。
  </acceptance_criteria>
  <verify>
    <automated>若 corpus_aligned：grep -v "竞品分析" verification/phase3-real-validation/validation_status.json | grep "corpus_path" 应返回新路径</automated>
    <automated>若 tree depth fix 成功：grep "tree_max_level.*[3-9]" verification/phase3-real-validation/validation_status.json 应返回 1 行</automated>
  </verify>
</task>
</tasks>

<verification>
corpus_alignment_report.md 确认 corpus/query domain alignment 或记录 corpus_not_found 状态。
tree_depth_analysis_report.md 确认 tree depth 根因和修复方案。
若 corpus_aligned：run_validation.py 使用新文档路径。
若 tree depth fix 成功：test_tree_structure_quality.py 测试通过。
validation_status.json 包含 phase_4_preparation_status（ready 或 incomplete）。
preparation_summary.md 总结准备状态。
</verification>

<success_criteria>
- 验证文档域与业务查询域对齐（或明确记录 corpus_not_found 状态）
- 真实树结构达到 >= 3 层（或明确记录 upstream fix required）
- 新的 validation_status.json 反映 corpus/tree 修复结果
- Phase 4 验证输入集准备完成（或明确记录 blockers）
</success_criteria>