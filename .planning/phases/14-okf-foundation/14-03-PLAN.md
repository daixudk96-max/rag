---
phase: 14-okf-foundation
plan: 03
type: execute
wave: 2
depends_on: ["14-01", "14-02"]
files_modified:
  - llamaindex_runtime/okf/serializer.py
  - llamaindex_runtime/okf/parser.py
  - tests/llamaindex_runtime/okf/test_okf_serializer_*.py
  - tests/llamaindex_runtime/okf/test_okf_parser_completion.py
autonomous: false
requirements: [P14-05, P14-06, P14-07, P14-08, P14-12]
must_haves:
  truths:
    - "serialize_document(docling_output, *, doc_id, version_id, source_checksum, docling_version, bundle_root, name) writes the current zero-depth generated outputs `raw/<slug>.md` (contract frontmatter + readable body), `raw/<slug>.spans.json` (schema v1), and `raw/<slug>.pair.json` into the bundle root. The manifest is mandatory, adjacent, same-slug, plain-filename-bound, and published last; production parsing requires it. The serializer emits zero-depth output, while production readers also accept legal safe nested descendants under the logical `<bundle-root>/raw/` subtree through the authority/capability opened on the configured bundle root."
    - "The serializer obtains page_no/heading_path/offset/text by calling the SAME NormalizationContract.normalize() the direct chain uses — imported, never copied"
    - "Parser exposes sidecar span coordinates only after required shared M-D-S-M manifest admission; span identity is never re-derived from the Markdown body"
    - "Parser validates raw frontmatter fail-fast via RawFrontmatterContract (14-01)"
    - "Sync: unchanged canonical_hash → skip; changed → upsert; file gone → delete detection; all recorded in okf_sync_state"
    - "Format-only rewrite of a raw file (reordered YAML keys, reflowed body) triggers NO rebuild"
    - "Relation qualifier fields in entity-type frontmatter are carried through, never silently dropped (P14-12)"
    - "OKFParagraph existing fields/behavior preserved — additive extension only"
  artifacts:
    - path: "llamaindex_runtime/okf/serializer.py"
      provides: "docling→OKF serializer emitting the zero-depth raw Markdown, spans sidecar, and mandatory pair manifest"
      contains: "def serialize_document"
    - path: "llamaindex_runtime/okf/parser.py"
      provides: "M-D-S-M manifest-bound raw admission + fail-fast frontmatter validation + canonical-hash incremental sync + delete detection + qualifier passthrough"
      contains: "spans.json"
  key_links:
    - from: "llamaindex_runtime/okf/serializer.py"
      to: "NormalizationContract"
      via: "import from the ingestion module — single normalization source of truth"
      pattern: "NormalizationContract"
    - from: "llamaindex_runtime/okf/parser.py"
      to: "llamaindex_runtime/okf/canonical_hash.py"
      via: "sync comparison"
      pattern: "canonical_hash"
---

<objective>
Build both directions of the OKF raw layer: the docling→OKF serializer (F1 入库流's OKF leg) and the parser completion that makes OKF readable back into span-coordinate form with incremental sync (F3 同步流). This is the plan where the round-trip becomes POSSIBLE; 14-04 then proves it.

The single most important constraint (milestone risk #1): the serializer must import and reuse `NormalizationContract` — any reimplementation, however faithful-looking, eventually diverges and permanently breaks `S_direct == S_okf`.

Output: new `serializer.py`; extended `parser.py`; two test files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-okf-foundation/14-CONTEXT.md
@.planning/ADR-OKF-PHASE-A-TECH-DECISIONS-2026-07-12.md (T1/T2/T3, T6)
@.planning/research/UPSTREAM-SOURCE-ANALYSIS-2026-07-12.md (§1 保留文件义务, §2 ogham bundle.py/concept.py 移植模式)

<interfaces>
Direct-chain reference (docling_ingestor.py:40-119, READ-ONLY except imports):
- `_flatten_docling_metadata` extracts page_no/charspan from docling's nested `doc_items[0]['prov'][0]` — the serializer needs the same flattening; if it is private, either import it deliberately (documenting the private-import decision) or extract a shared helper WITHOUT changing the direct chain's behavior (impact analysis first).
- `NormalizationContract.normalize(raw_text, metadata, ordinal)` returns the normalized span (page_no, headings, offset, text) — six-element source.

Existing parser facts (historical planning snapshot; use current symbols rather than line numbers as implementation pointers):
- `llamaindex_runtime/okf/parser.py::OKFParagraph` has `id/content/heading/okf_file_path`; extend additively (e.g. optional span fields or a parallel OKFSpan type; executor's choice), never break existing constructors/callers.
- `OKFParser.parse_paragraphs` retains `body.split("\n\n")` for legacy non-raw files; current raw files use `OKFParser._parse_authorized` → `OKFParser._parse_admitted_raw` and `llamaindex_runtime/okf/raw_pair.py::read_raw_pair` for manifest-bound M-D-S-M admission and sidecar spans.
- `OKFParser.compute_hash` retains raw-byte SHA256 for source/version hashing; canonical hash is additive, not a replacement (T3 dual hash).

Sync contract (writes okf_sync_state from 14-02):
- status transitions: absent→synced (insert), hash-equal→skip (touch last_synced_at optional), hash-diff→resynced (upsert), file-missing→deleted (mark + downstream cleanup hook for Phase 15).
</interfaces>
</context>

<tasks>

<task type="checkpoint:manual" gate="blocking">
  <name>Task 1 [BLOCKING]: GitNexus impact analysis on parser + ingestion symbols</name>
  <read_first>
    - llamaindex_runtime/okf/parser.py (full — 328 lines)
    - llamaindex_runtime/ingestion/docling_ingestor.py:40-119
    - ./CLAUDE.md (impact-before-edit rule)
  </read_first>
  <action>
Run upstream impact analysis on every existing symbol this plan touches or imports across module boundaries:
- `gitnexus_impact({target: "parse_paragraphs", direction: "upstream"})`
- `gitnexus_impact({target: "compute_hash", direction: "upstream"})`
- `gitnexus_impact({target: "OKFParagraph", direction: "upstream"})`
- `gitnexus_impact({target: "NormalizationContract", direction: "upstream"})`
- plus `_flatten_docling_metadata` if the shared-helper extraction route is chosen
Record blast radius table in 14-03-SUMMARY.md; WARN the user before proceeding on any HIGH/CRITICAL. If the GitNexus index is stale, run `npx gitnexus analyze` first.
  </action>
  <verify>
    <manual>Blast-radius table for all analyzed symbols present in 14-03-SUMMARY.md.</manual>
  </verify>
  <acceptance_criteria>
    - Table rows for parse_paragraphs, compute_hash, OKFParagraph, NormalizationContract minimum; HIGH/CRITICAL rows carry explicit user-facing warnings.
  </acceptance_criteria>
  <resume-signal>Type "impact-recorded" once the table is written.</resume-signal>
  <done>Mandatory GitNexus gate satisfied before any source edit.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Serializer (RED tests → implementation)</name>
  <read_first>
    - llamaindex_runtime/okf/sidecar.py + contracts.py (14-01 outputs — emit through these, never hand-roll JSON/YAML)
    - llamaindex_runtime/ingestion/docling_ingestor.py:40-119
  </read_first>
  <action>
RED first (the split `test_okf_serializer_*.py` modules), using a small docling-output fixture (dict-shaped, mirroring `doc_items/prov` nesting — no live docling call needed at unit level):
- output files use the current zero-depth serializer layout at `OKF_BUNDLE_ROOT/raw/<slug>.md`, `OKF_BUNDLE_ROOT/raw/<slug>.spans.json`, and `OKF_BUNDLE_ROOT/raw/<slug>.pair.json`; the manifest is mandatory, adjacent to the members, binds their same-slug plain filenames, and is published last. Production parsing requires this manifest and admits safe nested descendants under the logical `<bundle-root>/raw/` subtree through the authority/capability opened on the configured bundle root; it has no direct-child-only restriction or sidecar-only fallback
- frontmatter passes RawFrontmatterContract; body is readable markdown with headings
- sidecar spans equal NormalizationContract output element-for-element; recorded span_id equals uuid5 recomputation over the six elements
- serializing the same input twice yields byte-identical outputs (determinism — prerequisite for canonical_hash stability)
Then implement `serialize_document(docling_output: Sequence[object], *, doc_id: str, version_id: str, source_checksum: str, docling_version: str, bundle_root: Path, name: str) -> SerializedRawFile` in serializer.py. It emits the three zero-depth generated outputs `raw/<slug>.md`, `raw/<slug>.spans.json`, and mandatory `raw/<slug>.pair.json`; the manifest is adjacent, same-slug, plain-filename-bound, and published last. Import NormalizationContract; do NOT copy its logic (module docstring must state this constraint).
  </action>
  <verify>
    <automated>python -m pytest tests/llamaindex_runtime/okf/test_okf_serializer_*.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `rtk grep -c "class Normalization\|def normalize" llamaindex_runtime/okf/serializer.py` == 0 (no reimplementation)
    - `rtk grep -c "NormalizationContract" llamaindex_runtime/okf/serializer.py` ≥ 1 (import + use)
    - Determinism test GREEN.
  </acceptance_criteria>
  <done>Serializer emits the contract-conformant zero-depth Markdown, sidecar, and mandatory manifest set while reusing the shared normalization.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Parser completion (RED tests → implementation)</name>
  <read_first>
    - llamaindex_runtime/okf/parser.py (full)
    - llamaindex_runtime/okf/canonical_hash.py (14-01)
  </read_first>
  <action>
RED first (test_okf_parser_completion.py) covering P14-06/07/08/12:
- raw file plus mandatory adjacent same-slug plain-filename-bound manifest and sidecar → parsed spans carry page_no/heading_path/offset/text from the sidecar only after shared M-D-S-M admission, even when the markdown body is reflowed differently
- raw file with invalid frontmatter → named-field error (fail-fast)
- sync: unchanged canonical_hash → skip; content change → resync; deleted file → detected (state transition surfaced; DB write may be faked at unit level, exercised for real in 14-04)
- entity-type frontmatter with relations carrying negation/condition/direction/confidence + unknown future qualifier → ALL retained on the parsed relation object (nothing silently dropped)
Then implement additively in parser.py: production raw parsing through the binding ADR's shared M-D-S-M manifest/Markdown/sidecar/manifest admission using the authority/capability opened on the configured bundle root and constrained to its logical `raw/` subtree; strict raw-frontmatter and sidecar validation follow admission. The reader accepts safe nested raw descendants as well as serializer-emitted zero-depth files, but has no legacy sidecar-only fallback. Add the sync-decision function comparing canonical_hash against okf_sync_state rows; preserve qualifiers in the parsed relation representation (spillover dict for unknown keys — 采用 ogham `concept.py` 的 `_RECOGNISED_FIELDS` 集合 + 未识别键落 metadata 再回吐模式，上游研究 §2). Existing OKFParagraph behavior and legacy split("\n\n") path stay intact for non-raw files.

Additional parser obligations（上游研究 §1 SPEC 保留文件义务 + §2 ogham bundle.py 模式）:
- bundle 扫描（rglob）时在所有层级跳过保留文件 `index.md`/`log.md`——不作 raw/entity/concept 摄入（RED 测试：bundle 内放置 index.md/log.md，断言零摄入）。
- malformed 文件语义：skip + 计数统计（不静默、不崩溃）——与上面单文件 fail-fast 的边界是：显式 parse 单文件 = fail-fast；bundle 批量扫描 = skip+stats，统计随 sync 结果返回。
  </action>
  <verify>
    <automated>python -m pytest tests/llamaindex_runtime/okf/ -q (whole OKF suite stays GREEN, including 14-01 tests)</automated>
  </verify>
  <acceptance_criteria>
    - Body-reflow test proves span identity is sidecar-sourced (P14-06/07 core).
    - Qualifier passthrough test covers a qualifier NOT in the known four (spillover works).
    - No existing parser test (if any pre-exist) or import breaks.
  </acceptance_criteria>
  <done>Parser reads back everything the serializer writes; sync semantics in place.</done>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation |
|---|---|---|---|---|
| T-14-05 | Tampering | normalization divergence serializer vs direct chain | mitigate | import-only rule + grep gate + 14-04 round-trip regression |
| T-14-06 | DoS/corruption | malformed manifest, sidecar, or frontmatter from disk | mitigate | shared M-D-S-M fail-fast admission at the raw boundary; never partially ingest a file that failed manifest, byte-hash, sidecar-v1, canonical-v1, or frontmatter validation |
</threat_model>

<verification>
- `python -m pytest tests/llamaindex_runtime/okf/ -q` exits 0, including checks that the serializer emits `raw/<slug>.md`, `raw/<slug>.spans.json`, and mandatory `raw/<slug>.pair.json`, and that production raw parsing requires shared M-D-S-M manifest admission
- Manual spot: open one generated zero-depth raw set — Markdown is human-readable; the adjacent same-slug plain-filename manifest binds the Markdown and sidecar and is published last; the reader also accepts safe nested descendants under logical `raw/`
</verification>

<success_criteria>
- P14-05..P14-08 + P14-12 GREEN under TDD.
- Direct ingestion chain byte-identical in behavior (no caller switched; NormalizationContract untouched).
- Round-trip is now mechanically possible; proof deferred to 14-04.
</success_criteria>

<output>
After completion, create `.planning/phases/14-okf-foundation/14-03-SUMMARY.md`.
</output>
