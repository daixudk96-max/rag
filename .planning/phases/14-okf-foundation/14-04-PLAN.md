---
phase: 14-okf-foundation
plan: 04
type: execute
wave: 3
depends_on: ["14-01", "14-02", "14-03"]
files_modified:
  - scripts/rebuild_from_okf.py
  - tests/llamaindex_runtime/okf/test_span_id_roundtrip.py
  - tests/fixtures/okf_roundtrip/
  - llamaindex_runtime/pyproject.toml
  - llamaindex_runtime/ingestion/docling_ingestor.py
  - tests/llamaindex_runtime/test_docling_2109_default_reader.py
autonomous: false
requirements: [P14-09, P14-10, P14-11]
must_haves:
  truths:
    - "E1 `rebuild_from_okf.py` admits raw OKF frontmatter plus sidecar data, recomputes and reconciles `canonical_spans` only, without original binaries or Docling calls, and logs to `okf_rebuild_log`; registered/protected `documents` and `document_versions` provenance parents already exist"
    - "S_direct == S_okf holds 100% for all three fixture classes: sectioned PDF, table/complex-layout PDF, DOCX"
    - "On mismatch, output names the FIRST mismatching coordinate: span index + which of page_no/heading_path/offset/text differs + both values"
    - "Running E1 reconciliation twice is a no-op for canonical-span DML on the second equivalent run (idempotence)"
    - "The regression is a pytest suite AND a deterministic CLI command producing the same verdict"
    - "docling is pinned to 2.109.0 and HeadingHierarchyModel is enabled in _build_default_reader (user-approved Phase 14 scope expansion)"
    - "PDF spans with multi-level headings produce heading_path depth >= 2 (T9 preserved)"
    - "span_id migration impact is documented: heading_path change from flat to hierarchical is an intentional migration boundary, not a regression"
  artifacts:
    - path: "scripts/rebuild_from_okf.py"
      provides: "transactional, scoped `canonical_spans` reconciliation from an admitted raw OKF bundle, with span_id verification and an `okf_rebuild_log` entry"
      contains: "okf_rebuild_log"
    - path: "tests/llamaindex_runtime/okf/test_span_id_roundtrip.py"
      provides: "the A→B hard gate: three fixture classes, 100% identity assertion, first-mismatch reporting assertion"
      contains: "S_direct"
    - path: "llamaindex_runtime/pyproject.toml"
      provides: "docling==2.109.0 pin (user-approved Phase 14 scope expansion)"
      contains: "docling"
    - path: "llamaindex_runtime/ingestion/docling_ingestor.py"
      provides: "_build_default_reader with HeadingHierarchyModel enabled (narrow authorized exception)"
      contains: "HeadingHierarchyOptions"
    - path: "tests/llamaindex_runtime/test_docling_2109_default_reader.py"
      provides: "6 tests verifying docling version, HeadingHierarchyOptions config, multi-level headings, span-id migration, non-PDF support"
      contains: "HeadingHierarchyModel"
  key_links:
    - from: "scripts/rebuild_from_okf.py"
      to: "llamaindex_runtime/okf/parser.py"
      via: "the ONLY read path during rebuild (SSOT proof: parser+sidecar suffice)"
      pattern: "from llamaindex_runtime.okf"
---

<objective>
Prove the E1 SSOT foundation for one admitted raw document/version: registered/protected `documents` and `document_versions` provenance parents already exist; `rebuild_from_okf.py` deterministically recomputes and transactionally reconciles `canonical_spans` from raw OKF frontmatter plus sidecar data alone; and the span_id round-trip regression demonstrates `S_direct == S_okf` 100% across three fixture classes with first-mismatch coordinate reporting. A second equivalent E1 run makes no canonical-span DML. E1 does **not** reconstruct vector chunks, tree nodes, entities, relations, evidence, or NER associations; full derived-association parity is the joint Phase 15 E2a / Phase 16 E2b gate. **Current-status note (2026-07-17):** [Artifact 13](../../../verification/phase14-okf-foundation/13-final-authority-evidence-closure-review.md) records **APPROVE_FOR_FORMAL_CLOSURE** with no CRITICAL/HIGH findings; Phase 14/E1 is **BOUNDED CLOSED/PASS** for A–F only. Gate F remains PASS only within declared non-goal scope. Artifacts 04–07 retain tracked-diff-only/HIGH-CRITICAL targeted-impact/non-exhaustive-untracked limits and do not confer clean-worktree, intended-commit-scope, security, production, or full-derived-database-rebuild PASS. **Historical/as-of-closeout:** Phase 15/E2a was unstarted and Gate 3 was unsatisfied. **Current routing (later):** the interactive selection `授权规划和实施（推荐）` satisfies Gate 3; Phase 15 is authorized/planned/execution-unstarted pending approved plan execution. This routing grants no later-phase, Git, production, or disposable-acceptance authority.

Output: rebuild script + fixtures + the regression suite (pytest + CLI).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-okf-foundation/14-CONTEXT.md
@.planning/ADR-OKF-PHASE-A-TECH-DECISIONS-2026-07-12.md (T9)
@.planning/OKF-MULTIROUTE-EXECUTION-HANDOFF-2026-07-12.md (§10-B, §10-E)
@.planning/research/UPSTREAM-SOURCE-ANALYSIS-2026-07-12.md (§1 示例 bundle fixture, §2 ogham 测试移植清单, §3 okf-lint 门禁)

<interfaces>
Round-trip definition:
- S_direct: span_id set from the EXISTING direct chain — binary → docling → NormalizationContract → uuid5 (docling_ingestor.py:78-81). Computed per fixture during fixture PREPARATION (docling runs once, offline) and frozen into the fixture as `expected_span_ids.json` — CI/pytest never needs docling installed.
- S_okf: span_id set recomputed by uuid5 over the six elements read back from frontmatter (doc_id/version_id) + sidecar (page_no/heading_path/offset/text) after serialize→parse.
- Gate: set equality AND per-span element equality, in document order.

Fixture classes (T9, minimum one document each):
1. sectioned PDF (multi-level headings — exercises heading_path)
2. table/complex-layout PDF (exercises docling prov/charspan edge cases)
3. DOCX (exercises the non-PDF docling path)
Fixture layout: `tests/fixtures/okf_roundtrip/<class>/` containing the source binary (small, license-safe), the frozen docling output JSON, and expected_span_ids.json. If repo policy forbids committing binaries, commit the frozen docling output + expected ids and document the regeneration command instead — the gate still runs fully offline either way.

First-mismatch report format (P14-11, both pytest failure message and CLI output):
`ROUNDTRIP MISMATCH at span[17]: field=offset direct=1284 okf=1291 (doc=<doc_id> file=raw/xxx.md)`

CLI (deterministic, T9): `python scripts/rebuild_from_okf.py --bundle <root> --verify-roundtrip [--fixture <class>]` → exit 0 all-identical / exit 1 with first mismatch; `--rebuild` performs E1 `canonical_spans` reconciliation for registered document/version scopes and writes `okf_rebuild_log`. It neither creates provenance parents nor rebuilds vector/tree/entity/relation/evidence/NER materializations.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Prepare the three fixture classes</name>
  <read_first>
    - llamaindex_runtime/ingestion/docling_ingestor.py:40-119 (what raw docling output shape the frozen JSON must preserve)
    - tests/fixtures/ (existing fixture conventions, if any)
  </read_first>
  <action>
For each class: pick/author a small source document; run docling ONCE (preparation step, requires docling locally); freeze raw docling output JSON; compute S_direct via the real direct-chain code path (import, don't reimplement) and freeze expected_span_ids.json; write a README.md in the fixture dir recording the exact regeneration commands + docling version used.
  </action>
  <verify>
    <automated>python -c fixture-load sanity: all three classes load, expected_span_ids nonempty, uuid-parseable</automated>
  </verify>
  <acceptance_criteria>
    - Three fixture dirs, each with frozen docling output + expected_span_ids.json + README regeneration note.
    - No pytest-time docling dependency.
  </acceptance_criteria>
  <done>Offline, deterministic fixtures in place.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Round-trip regression suite (RED → GREEN)</name>
  <read_first>
    - llamaindex_runtime/okf/serializer.py + parser.py (14-03 outputs)
  </read_first>
  <action>
Write test_span_id_roundtrip.py — parameterized over the three classes:
- serialize frozen docling output → tmp bundle → parse back → recompute S_okf → assert 100% identity vs expected_span_ids (set + order + per-element)
- mismatch-report test: corrupt one sidecar span's offset in a copy → assert failure message names span index, field=offset, both values (P14-11)
- canonical-hash interplay: reflow the raw .md body in a copy → round-trip STILL passes (identity is sidecar-sourced)
Additionally translate the ogham round-trip edge suite（上游研究 §2，源：`E:\github\rag-upstream\ogham-mcp\tests\test_okf_{roundtrip,serialization,concept,bundle}.py`）into our fixtures:
- **尾换行漂移（最高优先）**：serialize→parse→serialize N 轮后文件字节不漂移（换行不累积）
- 未知 frontmatter 键经 serialize→parse→re-serialize 存活（T6 泛化）
- CRLF 输入文件可读且往返归一
- unicode/中文/emoji 文本 span 往返身份保持
- bundle 扫描：保留文件（index.md/log.md）零摄入；malformed 文件 skip+计数
- SPEC 符合性 fixture：knowledge-catalog 三个示例 bundle（crypto_bitcoin/ga4/stackoverflow，复制入 `tests/fixtures/okf_spec_bundles/`，Apache-2.0 署名）能被 parser 无错扫描（不要求摄入为 raw——它们无 sidecar；断言不崩溃、保留文件被跳过、未知 type 不被拒绝）
Fix any 14-03 defects this surfaces (expected place for normalization-edge bugs to appear; that is the point of the gate).
  </action>
  <verify>
    <automated>python -m pytest tests/llamaindex_runtime/okf/test_span_id_roundtrip.py -q</automated>
  </verify>
  <acceptance_criteria>
    - All three classes GREEN at 100% identity; mismatch-report test GREEN; reflow test GREEN.
  </acceptance_criteria>
  <done>The A→B hard gate exists and passes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: rebuild_from_okf.py (E1 canonical-span reconciliation + CLI verdict + idempotence)</name>
  <read_first>
    - scripts/run_pageindex_real_retrieval_workflow.py (house style for DB-touching scripts: connection handling, arg parsing, logging)
    - llamaindex_runtime/registry/migrations/015_okf_sync_state.sql (okf_rebuild_log shape from 14-02)
  </read_first>
  <action>
Implement the script per the CLI contract above:
- `--rebuild`: within one whole-bundle transaction, require every `document_versions(doc_id, version_id)` provenance parent to be registered and reconcile only that version's `canonical_spans` from admitted parser/sidecar output. Do not delete/truncate/recreate `documents` or `document_versions`; do not materialize or rebuild `vector_chunks`, `vector_chunk_spans`, `tree_nodes`, `tree_node_spans`, entities, relations, evidence, aliases, mentions, or node/entity associations. FK cascade cleanup caused by removal of a stale canonical span is dependency cleanup, not reconstruction.
- `--verify-roundtrip`: run the same comparison the pytest gate runs; exit 0/1 with first-mismatch line.
- Idempotence: immediately re-running `--rebuild` performs zero canonical-span DML (assert in a disposable-DB integration test via row changes plus canonical re-select hash).
- Database URL is read from the environment only. Before any DB integration or rebuild operation, require `OKF_MIGRATION_TEST_DATABASE_DISPOSABLE=1`, verify the expected database identity against the connected target, and confirm that target is disposable. Never persist database credentials in plans, logs, fixtures, or audit records. Errors are explicit; partial E1 reconciliation rolls back and failure audit follows the documented separate-connection boundary.
  </action>
  <verify>
    <automated>python scripts/rebuild_from_okf.py --bundle tests-tmp-bundle --verify-roundtrip → exit 0; integration test for rebuild+idempotence GREEN against docker DB</automated>
  </verify>
  <acceptance_criteria>
    - CLI verdict matches pytest verdict on the same inputs (deterministic parity, T9).
    - okf_rebuild_log row written per run; the second equivalent E1 run performs no canonical-span DML (P14-09 + §10-E).
  </acceptance_criteria>
  <done>E1 canonical-span reconciliation proof delivered end-to-end; E2a/E2b materialization and full derived-association parity remain later-stage work.</done>
</task>

<task type="checkpoint:manual" gate="blocking">
  <name>Task 4 [BLOCKING]: Phase acceptance sweep (handoff §10 A-F) + detect_changes</name>
  <read_first>
    - .planning/OKF-MULTIROUTE-EXECUTION-HANDOFF-2026-07-12.md §10
  </read_first>
  <action>
Walk the §10 checklist A-F against the delivered artifacts and record verdicts in 14-04-SUMMARY.md. Run the full OKF suite + the pre-existing test suites touched phases usually guard (at minimum: full tests/llamaindex_runtime/ selection that CI runs).

okf-lint 符合性门（上游研究 §3，ADOPT 不 fork）：对生成的 `okf_bundle/` 运行 `pnpm dlx @thisismydesign/okf-lint ./okf_bundle --max-warnings 0`；预期 error 全过；若 warning 报缺 `description`，在 repo 根落 `.okflintrc.json` 将 description 规则降级 off 并记录理由（raw 为机器区无一句话摘要）。lint 结果进 SUMMARY。

Then `gitnexus_detect_changes()` and verify the affected scope is ONLY Phase-14 files — dirty-tree files remain unstaged. Do NOT commit; commit/push happens only on explicit user request per handoff §13.
  </action>
  <verify>
    <manual>§10 A-F verdict table in 14-04-SUMMARY.md; detect_changes scope recorded.</manual>
  </verify>
  <acceptance_criteria>
    - All six §10 items PASS; any non-PASS blocks phase closure and is surfaced to the user.
  </acceptance_criteria>
  <resume-signal>Type "phase-a-acceptance-recorded" once the verdict table is written.</resume-signal>
  <done>Phase 14 provably meets the handoff's hard acceptance; A→B gate green.</done>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation |
|---|---|---|---|---|
| T-14-07 | Tampering/data loss | E1 canonical-span reconciliation | mitigate | registered doc/version scope only; never delete parents; transactional reconciliation, canonical-DML idempotence test, and okf_rebuild_log audit trail. FK cascade cleanup is not reconstruction. |
| T-14-08 | Repudiation | "it round-trips" claimed without proof | mitigate | frozen expected_span_ids + deterministic CLI = independently re-runnable verdict |
</threat_model>

<verification>
- `python -m pytest tests/llamaindex_runtime/okf/ -q` exits 0 (entire phase suite)
- `python scripts/rebuild_from_okf.py --bundle <fixture bundle> --verify-roundtrip` exits 0
- §10 A-F verdict table complete in SUMMARY
</verification>

<success_criteria>
- P14-09/10/11 GREEN; S_direct == S_okf 100% on all three fixture classes.
- E1 reconciliation is idempotent for `canonical_spans`; `okf_rebuild_log` is populated; E2a/E2b full derived-association parity is explicitly not claimed; no commit performed without user request.
- Historical expected outcome: Phase 15 entry gate satisfied.
- **Historical traceability correction (2026-07-17):** The former frontmatter references to `P14-13` and `P14-14` were removed because those IDs were never defined in the Phase 14 requirements registry. This corrects metadata only; it changes neither scope nor recorded results.
- **Current-status correction (2026-07-17):** This plan remains a historical execution/checkpoint record. [Artifact 13](../../../verification/phase14-okf-foundation/13-final-authority-evidence-closure-review.md) records **APPROVE_FOR_FORMAL_CLOSURE** with no CRITICAL/HIGH findings, so Phase 14/E1 is **BOUNDED CLOSED/PASS** for A–F acceptance only. Gates A–E have PASS execution evidence and APPROVE independent reviews; Gate F is PASS only within declared non-goal scope, with an APPROVE independent review. Artifacts 04–07 retain tracked-diff-only/HIGH-CRITICAL targeted-impact/non-exhaustive-untracked limits, so the closure is not a clean-worktree, intended-commit-scope, security, production, or full-derived-database-rebuild PASS. Canonical-path ratification remains complete: `OKF_BUNDLE_ROOT` defaults to `okf_bundle`, templates are `okf_bundle/templates/`, and `scripts/rebuild_from_okf.py` is the sole supported E1 CLI; `okf-bundles/main/entities/e001-内蒙古自治区卫健委.md` is a legacy sample/test fixture only. E1 remains limited to admitted raw-sidecar-to-`canonical_spans` reconciliation for registered/protected parents. **Historical/as-of-closeout:** Phase 15/E2a was unstarted and Gate 3 authorization was unsatisfied. **Current routing (later):** the interactive selection `授权规划和实施（推荐）` satisfies Gate 3; Phase 15 is authorized/planned/execution-unstarted pending approved plan execution. This routing grants no later-phase, Git, production, or disposable-acceptance authority. Current Phase 14 closeout/verification artifacts contain no credential values; protected variables were isolated as recorded.
</success_criteria>

<output>
After completion, create `.planning/phases/14-okf-foundation/14-04-SUMMARY.md`.
</output>
