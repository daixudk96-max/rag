---
phase: 14-okf-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - llamaindex_runtime/okf/sidecar.py
  - llamaindex_runtime/okf/canonical_hash.py
  - llamaindex_runtime/okf/contracts.py
  - llamaindex_runtime/config.py
  - okf_bundle/AGENT.md
  - okf_bundle/templates/
  - tests/llamaindex_runtime/okf/test_okf_sidecar.py
  - tests/llamaindex_runtime/okf/test_okf_canonical_hash.py
  - tests/llamaindex_runtime/okf/test_okf_contracts.py
autonomous: false
requirements: [P14-01, P14-02, P14-03]
must_haves:
  truths:
    - "SpanSidecar loads/dumps *.spans.json with schema_version=1; unknown schema_version raises fail-fast"
    - "Each sidecar span carries page_no, heading_path, offset, text (normalized), span_id (recorded)"
    - "RawFrontmatterContract validates doc_id, version_id, source_checksum, docling_version, type: raw; missing/malformed field raises with the field named"
    - "canonical_hash(frontmatter, sidecar) is invariant under YAML key reordering, trailing whitespace, and Markdown body reflow"
    - "canonical_hash changes when any span text/offset/heading_path/page_no or any frontmatter value changes"
    - "config exposes OKF_BUNDLE_ROOT (env-overridable, default okf_bundle/) — verified absent today"
  artifacts:
    - path: "llamaindex_runtime/okf/sidecar.py"
      provides: "SpanSidecar dataclass + load/dump + schema_version gate"
      contains: "schema_version"
    - path: "llamaindex_runtime/okf/canonical_hash.py"
      provides: "canonical_serialize + canonical_hash (T3 structural hash)"
      contains: "def canonical_hash"
    - path: "llamaindex_runtime/okf/contracts.py"
      provides: "RawFrontmatterContract validation (fail-fast, named-field errors)"
      contains: "class RawFrontmatterContract"
    - path: "okf_bundle/AGENT.md"
      provides: "bundle-root declaration: raw/ machine-generated, no hand edits; human editing goes to entities/, concepts/, synthesis/"
      contains: "raw/"
  key_links:
    - from: "llamaindex_runtime/okf/canonical_hash.py"
      to: "llamaindex_runtime/okf/sidecar.py"
      via: "deterministic serialization of sidecar spans feeds the hash"
      pattern: "canonical_serialize"
    - from: "llamaindex_runtime/config.py"
      to: "okf_bundle default"
      via: "OKF_BUNDLE_ROOT setting"
      pattern: "OKF_BUNDLE_ROOT"
---

<objective>
Define the OKF format contracts everything else in Phases 14-15 builds on: the spans sidecar schema v1 (T1/T2 — the ONLY source of span identity), the raw-file frontmatter contract, the canonical structural hash (T3 — sync comparison that ignores formatting), the `OKF_BUNDLE_ROOT` config key, and the bundle skeleton (`okf_bundle/` with `raw/`, `entities/`, `concepts/`, `synthesis/`, `AGENT.md`, templates).

Purpose: 14-03's serializer emits these formats and 14-04's rebuild consumes them; locking the contract first (TDD) prevents serializer/parser drift. All new files — no existing symbol is edited except an additive settings field in `config.py`.

Output: three new `llamaindex_runtime/okf/` modules + config key + bundle skeleton + three RED→GREEN test files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-okf-foundation/14-CONTEXT.md
@.planning/ADR-OKF-PHASE-A-TECH-DECISIONS-2026-07-12.md
@.planning/OKF-MULTIROUTE-EXECUTION-HANDOFF-2026-07-12.md (§5.1, §10-A)
@.planning/research/UPSTREAM-SOURCE-ANALYSIS-2026-07-12.md (§1 规范符合性, §2 ogham 移植清单, §4 okf-wiki 模板 — 执行前必读；上游克隆在 E:\github\rag-upstream\)

<interfaces>
<!-- Extracted from live code this session. Use directly. -->

span_id identity (docling_ingestor.py:78-81) — the sidecar must carry every element not provided by frontmatter:
```python
span_id = uuid5(
    NAMESPACE_URL,
    f"{doc_id}|{version_id}|{normalized.page_no}|{'/'.join(normalized.headings)}|{normalized.offset}|{normalized.text}",
)
```
doc_id/version_id → file frontmatter; page_no/headings/offset/text → per-span sidecar entries.

Sidecar schema v1 (LOCKED shape; field order at executor's discretion):
```json
{
  "schema_version": 1,
  "doc_id": "<uuid>",
  "version_id": "<uuid>",
  "spans": [
    {
      "span_id": "<uuid, recorded for cross-check — identity is REBUILT from the six elements, then compared>",
      "page_no": 3,
      "heading_path": ["H1", "H2"],
      "offset": 128,
      "text": "<exact NormalizationContract.normalize() output text>"
    }
  ]
}
```

Raw frontmatter contract (LOCKED fields):
```yaml
type: raw
doc_id: <uuid>
version_id: <uuid>
source_checksum: <sha256 of original binary — audit-layer hash, T3>
docling_version: <string>
generated_by: <serializer id/version>
```

canonical_hash (T3): SHA256 over canonical_serialize(frontmatter_dict) + canonical_serialize(spans_list), where canonical_serialize is sorted-key, separator-stable, ensure_ascii-stable JSON. The Markdown BODY is deliberately excluded — body reflow must not trigger rebuilds; span identity lives in the sidecar.

Existing parser types to stay compatible with (parser.py:57-63): `OKFParagraph(id, content, heading, okf_file_path)` — do NOT modify in this plan (14-03 owns parser edits).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED tests for sidecar schema, frontmatter contract, canonical hash</name>
  <read_first>
    - llamaindex_runtime/okf/parser.py (frontmatter parsing style, error conventions)
    - tests/llamaindex_runtime/ (any existing test for import/fixture conventions; note: NO OKF tests exist yet — this creates the package)
  </read_first>
  <action>
Create `tests/llamaindex_runtime/okf/` (with __init__.py if the suite requires) and three test files covering, at minimum:
- test_okf_sidecar.py: round-trips a two-span sidecar through dump→load; asserts all five span fields survive; asserts `schema_version: 2` raises a named error; asserts missing `spans` key raises.
- test_okf_contracts.py: valid frontmatter passes; each required field removed one-at-a-time raises an error naming that field; `type: entity` on a raw file raises.
- test_okf_canonical_hash.py: (a) same logical content with reordered YAML keys + trailing whitespace + reflowed body → SAME hash; (b) single-character change in one span's text → DIFFERENT hash; (c) offset change → DIFFERENT hash; (d) hash is a 64-char lowercase hex string; (e) two consecutive calls are identical (determinism).
Run to confirm RED (import errors count as RED).
  </action>
  <verify>
    <automated>python -m pytest tests/llamaindex_runtime/okf/ -q; exit code nonzero expected at this task (RED)</automated>
  </verify>
  <acceptance_criteria>
    - Three test files exist and express P14-01/02/03 behaviors before any implementation exists.
  </acceptance_criteria>
  <done>RED suite in place defining the contract.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement sidecar.py, contracts.py, canonical_hash.py → GREEN</name>
  <read_first>
    - llamaindex_runtime/okf/parser.py (reuse its YAML loading approach; keep error style consistent)
  </read_first>
  <action>
Implement the three modules to the interface block above. Constraints:
- Pure stdlib + already-present dependencies (yaml is already used by parser.py); no new packages.
- Immutability: dataclasses frozen; load returns new objects; dump never mutates input.
- Fail fast with field-named messages (coding-style rule: validate at boundaries).
- canonical_serialize must be its own tested function (14-03/14-04 import it).
- **移植而非重写（上游研究 §2）：** frontmatter I/O 以 `E:\github\rag-upstream\ogham-mcp\src\ogham\okf\serialization.py` 为底稿改造——`yaml.safe_dump(fm, sort_keys=False, allow_unicode=True)` + 固定 dict 插入序实现定序；写恰好一个尾换行、读剥恰好一个（防往返换行累积）；CRLF 兼容读取；datetime→`.isoformat()` 归一（`concept.py` 模式）。被移植文件头注明来源仓库 + MIT 署名。
  </action>
  <verify>
    <automated>python -m pytest tests/llamaindex_runtime/okf/ -q</automated>
  </verify>
  <acceptance_criteria>
    - All Task 1 tests GREEN.
    - `python -c "from llamaindex_runtime.okf.canonical_hash import canonical_hash"` exits 0.
    - No edits outside `llamaindex_runtime/okf/` and `tests/llamaindex_runtime/okf/` in this task.
  </acceptance_criteria>
  <done>Contract modules implemented and GREEN.</done>
</task>

<task type="auto">
  <name>Task 3: OKF_BUNDLE_ROOT config key + bundle skeleton + AGENT.md</name>
  <read_first>
    - llamaindex_runtime/config.py (existing settings pattern + from_env precedence — recent commit 9c1922c made env vars take precedence over .env; follow it. FILE HAS UNRELATED DIRTY-TREE EDITS — add the new field surgically, do not reformat, do not touch other hunks)
  </read_first>
  <action>
1. Add `OKF_BUNDLE_ROOT` to the settings surface in config.py following the existing field/env pattern, default `okf_bundle` relative to repo root.
2. Create the bundle skeleton: `okf_bundle/raw/.gitkeep`, `okf_bundle/entities/.gitkeep`, `okf_bundle/concepts/.gitkeep`, `okf_bundle/synthesis/.gitkeep`.
3. Write `okf_bundle/AGENT.md` following the okf-wiki three-section boundary template（上游研究 §4：✅ Always do / ⚠️ Ask first / 🚫 Never — 参照 `E:\github\rag-upstream\okf-wiki\AGENT.md`）: raw/ is machine-generated by the docling→OKF serializer — never hand-edit (T1/T2 constraint 2); human knowledge editing goes to entities/, concepts/, synthesis/; sidecar files are the sole source of span identity; Never 区含 "Remove or alter the type field"。
4. Add `okf_bundle/index.md`（唯一带 frontmatter 的 index，内容仅 `okf_version: "0.1"` + 分区链接目录）与 `okf_bundle/log.md`（`## YYYY-MM-DD` 新在前，无 frontmatter）——SPEC 保留文件（上游研究 §1 义务 1-3；键名用 `okf_version`，不用 okf-wiki 的非标 `okf_wiki_version`）。
5. Add `okf_bundle/templates/raw.md` (frontmatter template matching the contract；另加 `title` 与 `timestamp` 两个 recommended 字段——okf-lint warning 对齐，validator 仍只强制 LOCKED 五字段) and `okf_bundle/templates/entity.md` (matching OKFFrontmatter's existing extension fields: aliases, canonical_entity_id, entity_type, relations, mentions).
  </action>
  <verify>
    <automated>python -c "from llamaindex_runtime.config import *" (module import stays clean); python -m pytest tests/llamaindex_runtime/okf/ -q</automated>
  </verify>
  <acceptance_criteria>
    - `rtk grep OKF_BUNDLE_ROOT llamaindex_runtime/config.py` returns ≥1 match.
    - AGENT.md + index.md（含 okf_version frontmatter）+ log.md + 4 subdirs + 2 templates exist under okf_bundle/.
    - `rtk git diff llamaindex_runtime/config.py` shows ONLY the additive OKF hunk beyond pre-existing dirty-tree changes.
  </acceptance_criteria>
  <done>Config key + bundle skeleton in place.</done>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation |
|---|---|---|---|---|
| T-14-01 | Tampering | sidecar files (span identity source) | mitigate | schema_version gate + recorded span_id cross-check in 14-04's round-trip gate; canonical_hash detects content drift |
| T-14-02 | Spoofing | hand-edited raw/*.md passed off as generated | accept | AGENT.md declares policy; enforcement is the round-trip gate (edited body without sidecar update → hash mismatch) |
</threat_model>

<verification>
- `python -m pytest tests/llamaindex_runtime/okf/ -q` exits 0
- Determinism spot-check: hashing the same fixture twice in separate processes yields identical output
</verification>

<success_criteria>
- P14-01, P14-02, P14-03 GREEN under TDD (RED first, then implementation).
- No existing symbol modified anywhere except the additive config field.
- Bundle skeleton + AGENT.md establish the T1/T2 editing policy on disk.
</success_criteria>

<output>
After completion, create `.planning/phases/14-okf-foundation/14-01-SUMMARY.md`.
</output>
