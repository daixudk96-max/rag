---
phase: 14-okf-foundation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - llamaindex_runtime/registry/migrations/015_okf_sync_state.sql
  - llamaindex_runtime/registry/migrations/016_entity_mentions.sql
  - llamaindex_runtime/registry/migrations/017_relation_qualifiers.sql
  - tests/llamaindex_runtime/okf/test_okf_migrations.py
autonomous: false
requirements: [P14-04, P14-12]
must_haves:
  truths:
    - "Migrations 015/016/017 apply cleanly on a FRESH initdb (whole-directory lexicographic mount) AND on an EXISTING 001-014 database (idempotent-safe ordering, no name collisions)"
    - "okf_sync_state stores per-OKF-file source_checksum + canonical_hash + status + last_synced_at"
    - "okf_rebuild_log stores per-rebuild-run status, span_count, mismatch_detail"
    - "entity_mentions has mention_id PK, entity_id FK, span_id, char_start, char_end, mention_text, confidence, source, okf provenance columns (T4)"
    - "relation table gains negation/condition/direction/confidence explicit columns + qualifiers JSONB (T6)"
    - "Both hardcoded migration lists were read and a written decision exists for each: added or explicitly not-added with reason"
  artifacts:
    - path: "llamaindex_runtime/registry/migrations/015_okf_sync_state.sql"
      provides: "okf_sync_state + okf_rebuild_log tables"
      contains: "okf_sync_state"
    - path: "llamaindex_runtime/registry/migrations/016_entity_mentions.sql"
      provides: "entity_aliases + entity_mentions + entity_merge_log tables (created EMPTY — Phase 16 populates)"
      contains: "entity_mentions"
    - path: "llamaindex_runtime/registry/migrations/017_relation_qualifiers.sql"
      provides: "ALTER TABLE … ADD COLUMN negation/condition/direction/confidence + qualifiers JSONB"
      contains: "qualifiers"
  key_links:
    - from: "016_entity_mentions.sql"
      to: "canonical_spans (or the span table 005 references)"
      via: "span_id column referencing the same coordinate space as evidence_links"
      pattern: "span_id"
---

<objective>
Land the Phase-A database schema: OKF sync/rebuild bookkeeping (015), the entity-mention layer tables Phase 16 will populate (016), and relation qualifier columns (017) — plus the explicit reconciliation of the two hardcoded migration lists that T5 mandates.

Purpose: 14-03's parser sync writes okf_sync_state; 14-04's rebuild writes okf_rebuild_log; Phase 16 needs entity_mentions to exist. Runs parallel with 14-01 (no shared files).

Output: three .sql files + a migration test + a written reconciliation decision in the SUMMARY.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-okf-foundation/14-CONTEXT.md
@.planning/ADR-OKF-PHASE-A-TECH-DECISIONS-2026-07-12.md (T4, T5, T6)

<interfaces>
Verified migration execution paths (ADR fact table):
1. `verification/docker-compose.yml:12` mounts the WHOLE migrations dir at `/docker-entrypoint-initdb.d` — PostgreSQL initdb runs ALL .sql lexicographically. 015-017 are picked up automatically; naming any file `001_*` is BANNED (collision with 001_initial.sql).
2. `scripts/run_pageindex_real_retrieval_workflow.py:21-26` — `MIGRATION_FILES: tuple[str, ...] = ("001_initial.sql", "002_version_lifecycle.sql", "003_tree_persistence.sql", "007_processing_status.sql")`, executed in list order by `apply_migrations()` (lines 183-203).
3. Root `run_migrations.py` — separate hardcoded `key_migrations` list.

`evidence_links` (005_kg_extension.sql) already has span_id + confidence but NO char offsets / mention text — that is exactly why entity_mentions is a NEW table, not columns on evidence_links (T4). Executor MUST read 005 first to get the exact entities/relations table names and the FK/span coordinate conventions before writing 016/017.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Read 005 + latest migrations; write 015/016/017</name>
  <read_first>
    - llamaindex_runtime/registry/migrations/005_kg_extension.sql (exact entities/relations/evidence_links table names, PK/FK style, span coordinate conventions)
    - llamaindex_runtime/registry/migrations/013_*.sql and 014_*.sql (house SQL style: IF NOT EXISTS usage, index naming, comments)
  </read_first>
  <action>
Write three migrations in house style, all guarded (CREATE TABLE IF NOT EXISTS / ADD COLUMN IF NOT EXISTS) so they are safe on both fresh initdb and existing DBs:

015_okf_sync_state.sql:
- `okf_sync_state(okf_file_path text PRIMARY KEY, doc_id uuid, version_id uuid, source_checksum text NOT NULL, canonical_hash text NOT NULL, status text NOT NULL, last_synced_at timestamptz NOT NULL DEFAULT now())`
- `okf_rebuild_log(rebuild_id uuid PRIMARY KEY, started_at timestamptz NOT NULL, finished_at timestamptz, status text NOT NULL, span_count integer, mismatch_detail jsonb)`

016_entity_mentions.sql (tables created EMPTY; Phase 16 populates):
- `entity_aliases(alias_id uuid PK, entity_id uuid FK→<entities table from 005>, alias text NOT NULL, source text)`
- `entity_mentions(mention_id uuid PK, entity_id uuid FK, span_id uuid NOT NULL, char_start integer NOT NULL, char_end integer NOT NULL, mention_text text NOT NULL, confidence numeric, source text NOT NULL, okf_file_path text, okf_paragraph_id text)` + index on (entity_id), index on (span_id)
- `entity_merge_log(merge_id uuid PK, from_entity_id uuid NOT NULL, into_entity_id uuid NOT NULL, reason text, merged_at timestamptz NOT NULL DEFAULT now())`

017_relation_qualifiers.sql:
- ALTER the relation table found in 005: ADD `negation boolean NOT NULL DEFAULT false`, `condition text`, `direction text`, `confidence numeric` (skip any column 005 already defines — verify), plus `qualifiers jsonb`.
  </action>
  <verify>
    <automated>rtk grep -c "IF NOT EXISTS" llamaindex_runtime/registry/migrations/015_okf_sync_state.sql (≥1; same for 016/017)</automated>
  </verify>
  <acceptance_criteria>
    - Three files exist with exact names 015_okf_sync_state.sql / 016_entity_mentions.sql / 017_relation_qualifiers.sql (three-digit zero-padded, T5).
    - No file named 001_*; no renumbering of 001-014.
    - 017 alters the REAL relation table name from 005, not a guessed one.
  </acceptance_criteria>
  <done>Schema files written in house style.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Migration application test (fresh + existing DB)</name>
  <read_first>
    - tests/conftest.py (DB fixture availability/markers; note file has unrelated dirty-tree edits — read, understand markers, do not modify unless a fixture is genuinely required)
    - verification/docker-compose.yml (how the test DB comes up)
  </read_first>
  <action>
Write `tests/llamaindex_runtime/okf/test_okf_migrations.py` (integration-marked, skipped when DATABASE_URL absent — follow existing conftest conventions):
- fresh-path test: apply 001-017 in lexicographic order on an empty schema → all succeed; okf_sync_state/okf_rebuild_log/entity_aliases/entity_mentions/entity_merge_log exist; relation table has the five new columns.
- existing-path test: apply 001-014, then 015-017 → same assertions (proves additive safety).
- re-apply test: run 015-017 twice → second run is a no-op (IF NOT EXISTS guards hold).
DATABASE_URL comes from env/.env only; never hardcode or echo it.
  </action>
  <verify>
    <automated>python -m pytest tests/llamaindex_runtime/okf/test_okf_migrations.py -q (against the docker-compose DB)</automated>
  </verify>
  <acceptance_criteria>
    - All three application scenarios GREEN with a live DB; suite auto-skips cleanly without one.
  </acceptance_criteria>
  <done>P14-04 proven by test, not by hand-running.</done>
</task>

<task type="checkpoint:manual" gate="blocking">
  <name>Task 3 [BLOCKING]: Reconcile the two hardcoded migration lists (T5)</name>
  <read_first>
    - scripts/run_pageindex_real_retrieval_workflow.py:21-26,183-203 (MIGRATION_FILES + apply_migrations)
    - run_migrations.py (root; key_migrations)
  </read_first>
  <action>
For EACH list decide and record in 14-02-SUMMARY.md: does this runner need OKF tables?
- `run_pageindex_real_retrieval_workflow.py` exercises the retrieval workflow only — expected decision: NOT added in Phase 14 (no OKF table is read by that workflow yet); revisit at Phase 15 when ingestion switches over. If the executor finds the workflow DOES touch OKF tables by then, add 015-017 to the tuple in list order.
- root `run_migrations.py` is the general-purpose migration runner — expected decision: ADD 015/016/017 to key_migrations so a manually-migrated dev DB matches initdb.
Either way the SUMMARY must contain a two-row decision table (list, decision, reason). Editing these files touches existing symbols → run `gitnexus_impact({target: "apply_migrations", direction: "upstream"})` first and record blast radius; warn on HIGH/CRITICAL.
  </action>
  <verify>
    <manual>Decision table present in 14-02-SUMMARY.md; any list edit preceded by recorded impact analysis.</manual>
  </verify>
  <acceptance_criteria>
    - Two-row decision table exists; no silent skips (T5 requires explicit reconciliation, not omission).
  </acceptance_criteria>
  <resume-signal>Type "lists-reconciled" once the decision table is recorded.</resume-signal>
  <done>T5 reconciliation complete and auditable.</done>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation |
|---|---|---|---|---|
| T-14-03 | Tampering | migration ordering across three execution paths | mitigate | zero-padded 015+ naming; explicit list reconciliation; re-apply idempotence test |
| T-14-04 | Information disclosure | DATABASE_URL in tests/artifacts | mitigate | env-only sourcing; never echoed; STATE.md precedent upheld |
</threat_model>

<verification>
- `python -m pytest tests/llamaindex_runtime/okf/test_okf_migrations.py -q` exits 0 against docker DB
- Fresh `docker compose up` in verification/ initializes without error (initdb path proof)
</verification>

<success_criteria>
- P14-04 GREEN on fresh + existing + re-apply paths; P14-12's schema prerequisite (qualifier columns) in place.
- Migration-list decisions written, impact-analyzed where code was edited.
</success_criteria>

<output>
After completion, create `.planning/phases/14-okf-foundation/14-02-SUMMARY.md`.
</output>
