---
phase: 15-okf-ingestion-pipeline
plan: 03
type: tdd
wave: 3
depends_on: [15-02]
files_modified:
  - llamaindex_runtime/ingestion/docling_ingestor.py
  - llamaindex_runtime/ingestion/pipeline.py
  - llamaindex_runtime/tree/pageindex_adapter.py
  - llamaindex_runtime/client/pageindex_client.py
  - tests/llamaindex_runtime/okf/test_e2a_caller_switchover.py
  - .planning/phases/15-okf-ingestion-pipeline/15-CALLER-INVENTORY.md
autonomous: true
requirements: [R-OKF-01, R-OKF-02]
must_haves:
  truths:
    - Normal ingestion performs exactly one Docling conversion, serializes the strict raw pair, admits it, and invokes E2a reconciliation.
    - Normal ingestion never calls legacy `write_spans`, a direct persistence mode, an automatic fallback, or an E2b callback.
    - PageIndex consumes canonical E2a tree state and cannot author primary tree rows on E2a routes.
    - Direct derivation can exist only as a no-persistence test or disposable shadow comparator.
  artifacts:
    - path: llamaindex_runtime/ingestion/pipeline.py
      provides: Sole normal ingress route to serializer, admission, and E2a reconciliation.
    - path: llamaindex_runtime/tree/pageindex_adapter.py
      provides: PageIndex cache/consumer behavior with no primary tree authority.
    - path: .planning/phases/15-okf-ingestion-pipeline/15-CALLER-INVENTORY.md
      provides: Current caller impact/disposition record.
  key_links:
    - from: llamaindex_runtime/ingestion/pipeline.py
      to: llamaindex_runtime/okf/serializer.py
      via: One conversion output handed to serialize_document
      pattern: serialize_document
    - from: llamaindex_runtime/ingestion/pipeline.py
      to: llamaindex_runtime/okf/e2a_reconciler.py
      via: Whole-corpus reconciliation after strict admission
      pattern: reconcile
---

<objective>
Make OKF admission and E2a reconciliation the sole normal ingestion writer path and retire PageIndex primary-tree authority.

Purpose: Enforce the SSOT ingress boundary without silently retaining direct persistence.
Output: One-conversion normal route, pure comparator-only direct derivation, consumer-only PageIndex, and caller-boundary tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/15-okf-ingestion-pipeline/15-CONTEXT.md
@.planning/phases/15-okf-ingestion-pipeline/15-CALLER-INVENTORY.md
@.planning/phases/15-okf-ingestion-pipeline/15-02-SUMMARY.md
@llamaindex_runtime/ingestion/pipeline.py
@llamaindex_runtime/ingestion/docling_ingestor.py
@llamaindex_runtime/okf/serializer.py
@llamaindex_runtime/tree/pageindex_adapter.py
@llamaindex_runtime/client/pageindex_client.py
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Write RED normal-route, pure-comparator, and PageIndex-boundary tests</name>
  <files>tests/llamaindex_runtime/okf/test_e2a_caller_switchover.py, .planning/phases/15-okf-ingestion-pipeline/15-CALLER-INVENTORY.md</files>
  <behavior>
    - A normal request registers provenance, converts once, passes captured nodes/metadata to `serialize_document`, strictly admits the published raw pair, and calls whole-corpus reconciliation.
    - Spies prove no normal `write_spans`, `write_tree`, direct persistence setting, automatic fallback, or E2b callback invocation.
    - A direct-vs-OKF comparator returns parity measurements only and has no connection/cursor/DML capability.
    - PageIndex E2a paths read/cache canonical tree IDs and mappings rather than allocate UUID4 IDs or write empty node-span mappings.
    - An executable deterministic caller-inventory completeness test fails if any required named persistence surface or callback lacks a row with exact symbol/path, entry route/condition, owner, disposition, and enforcement test.
  </behavior>
  <action>Before changing source, rerun and report GitNexus upstream impact for `IngestionPipeline.ingest`, `DoclingIngestor.ingest`, `PageIndexTreeAdapter.index_tree`, and `EnhancedPageIndexClient.index`; report the exact caller/process blast radius and all HIGH/CRITICAL warnings, then proceed within the already authorized Phase 15 scope after recording them. Stop only when a newly discovered scope expansion requires a user decision. Per D-15-06, D-15-07, and D-15-10, write failing spy tests around the exact source flow and update the caller inventory with fresh revision/index, callers, processes, risk, and migrate/comparator/cache/deferred disposition. In `tests/llamaindex_runtime/okf/test_e2a_caller_switchover.py`, add an executable deterministic caller-inventory completeness checker that fails when any named persistence surface or callback lacks a row with its exact symbol/path, entry route/condition, owner, disposition, and enforcement test. Its required inventory covers every writer (`write_spans`, `write_tree`, vector-chunk DML/`write_vector_chunks`, `write_entities`, `write_relations`, `write_evidence_links`, `write_chunk_entity_links`, and `write_node_entity_links`), `VectorLoader`, all PageIndex routes, and `build_tree_entity_extraction_callback`. Do not use `RAG_INGESTION_PATH=direct` or create a production compatibility feature.</action>
  <verify><automated>rtk env -u DATABASE_URL -u FORMAL_RUNTIME_DATABASE_URL -u OKF_MIGRATION_TEST_DATABASE_DISPOSABLE -u OKF_REBUILD_EXPECTED_DATABASE -u OKF_FAILURE_AUDIT_ACCEPTANCE -u OKF_REBUILD_DOCKER_ACCEPTANCE pytest tests/llamaindex_runtime/okf/test_e2a_caller_switchover.py -q</automated></verify>
  <done>Tests prove normal routing and primary tree authority cannot be bypassed, while the comparator remains pure and non-persisting. The deterministic checker validates a complete, exact-field inventory for every required writer, VectorLoader, PageIndex route, and `build_tree_entity_extraction_callback`.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Switch normal ingress and make PageIndex a canonical-tree consumer</name>
  <files>llamaindex_runtime/ingestion/docling_ingestor.py, llamaindex_runtime/ingestion/pipeline.py, llamaindex_runtime/tree/pageindex_adapter.py, llamaindex_runtime/client/pageindex_client.py, tests/llamaindex_runtime/okf/test_e2a_caller_switchover.py</files>
  <action>After recorded GitNexus impact analysis and required HIGH/CRITICAL warning, implement the exact order in the inventory: provenance registration, one Docling conversion/captured serializer-compatible nodes and metadata, `serialize_document`, strict raw admission, then E2a corpus reconciliation. Do not call legacy `write_spans`; do not duplicate conversion, serializer normalization, or span-ID mechanics. Remove E2a primary writer authority from PageIndex PDF and Markdown routes: PageIndex can read/cache canonical E2a trees but cannot use `registry.write_tree`, UUID4 tree identities, or empty node-span mappings. Keep E2b callbacks untouched but structurally absent from E2a composition. Implement any direct derivation exclusively as an injected pure comparator/test helper with no persistence imports or database handle; its result fills comparator-parity measurement only. External vector projection is excluded from E2a selected scope and requires separate future design.</action>
  <verify><automated>rtk env -u DATABASE_URL -u FORMAL_RUNTIME_DATABASE_URL -u OKF_MIGRATION_TEST_DATABASE_DISPOSABLE -u OKF_REBUILD_EXPECTED_DATABASE -u OKF_FAILURE_AUDIT_ACCEPTANCE -u OKF_REBUILD_DOCKER_ACCEPTANCE pytest tests/llamaindex_runtime/okf/test_e2a_caller_switchover.py -q</automated></verify>
  <done>The production ingress has one durable writer path, PageIndex has no primary-tree write path, and direct comparison is non-persisting/non-fallback-only.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Application caller → ingress | A legacy caller could bypass the durable-authority route. |
| E2a state → PageIndex cache | A consumer could create divergent primary tree state. |
| Comparator → materialization | Test-only derivation must not gain a persistence capability. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-15-09 | E | normal ingress | mitigate | Spy-tested single route; no direct mode/configuration and no fallback catch. |
| T-15-10 | T | PageIndex | mitigate | Read/cache canonical IDs only; prohibit registry tree writes and locally generated IDs. |
| T-15-11 | E | direct comparator | mitigate | Pure function/test fixture accepts no cursor/connection and emits parity measurements only. |
| T-15-12 | R | caller migration | mitigate | Fresh GitNexus inventory records affected callers/processes before each edit. |
</threat_model>

<verification>
Use non-DB spies and deterministic fixtures. The direct comparator cannot be used to create ordinary fact state or acceptance evidence by itself.
</verification>

<success_criteria>
- Normal ingress uses exactly one conversion and one durable E2a writer route.
- No persistence-capable direct path or fallback exists.
- PageIndex is non-authoritative and E2b is unreachable.
</success_criteria>

<output>
After completion, create `.planning/phases/15-okf-ingestion-pipeline/15-03-SUMMARY.md`.
</output>


## Binding remediation addendum

Task 1 first creates a fresh, exhaustive GitNexus plus source-search inventory before source edits. It must classify every production usage of `write_spans`, `write_tree`, vector-chunk DML/`write_vector_chunks`, `write_entities`, `write_relations`, `write_evidence_links`, `write_chunk_entity_links`, `write_node_entity_links`, `build_tree_entity_extraction_callback`, `IngestionPipeline`, `DoclingIngestor`, PageIndex adapter/client, and `VectorLoader`. Each row names source symbol/path, entry route/condition, owner (`E2a`, `E2b`, cache, test), disposition, and enforcement test. There must be zero unclassified production persistence callers. `build_tree_entity_extraction_callback` is Phase 16/E2b and each normal E2a route has a negative composition/call-graph test proving it is absent.

Task 2 adapts the live interfaces exactly: add immutable `DoclingConversionResult`; `convert_for_okf` has one reader and one node-parser invocation; serializer receives that node tuple; `read_raw_pair` receives `BundleAuthority` and manifest-bound `("raw", slug + ".md")` after manifest-last publication; then the full corpus is admitted and reconciled. Its spy order is registration, conversion once, serialization, stable strict raw-pair read, full-corpus admission/build, one transaction reconcile. Prohibit legacy writers, VectorLoader, direct persistence comparator, automatic fallback, UUID4 PageIndex tree authority, and external vector work. PageIndex only reads canonical IDs/mappings as a cache consumer.
