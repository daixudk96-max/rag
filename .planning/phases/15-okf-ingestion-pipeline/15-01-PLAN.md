---
phase: 15-okf-ingestion-pipeline
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - llamaindex_runtime/okf/e2a_contracts.py
  - llamaindex_runtime/okf/e2a_admission.py
  - llamaindex_runtime/registry/migrations/019_e2a_materialization_contract.sql
  - llamaindex_runtime/registry/migration_catalog.py
  - tests/llamaindex_runtime/okf/test_e2a_admission.py
  - tests/llamaindex_runtime/okf/test_e2a_contracts.py
  - tests/llamaindex_runtime/okf/test_e2a_migration_019_postgres.py
autonomous: true
requirements: [R-OKF-01, R-OKF-04]
must_haves:
  truths:
    - Valid strict raw pairs and manual OKF facts admit into one deterministic immutable desired state.
    - Invalid raw pairs, ambiguous facts, evidence without a unique document/version/span, and ownership collisions fail before primary DML.
    - Migration 019 makes manual E2a fact provenance and same-version evidence integrity enforceable.
  artifacts:
    - path: llamaindex_runtime/okf/e2a_contracts.py
      provides: Immutable admission, ownership, desired-state, and measured-result contracts.
    - path: llamaindex_runtime/okf/e2a_admission.py
      provides: Strict raw-pair and manual-fact corpus admission.
    - path: llamaindex_runtime/registry/migrations/019_e2a_materialization_contract.sql
      provides: E2a ownership and evidence integrity schema contract.
  key_links:
    - from: llamaindex_runtime/okf/e2a_admission.py
      to: llamaindex_runtime/okf/serializer.py
      via: Accepted strict raw-pair contract
      pattern: pair.json|M-D-S-M
    - from: run_migrations.py
      to: llamaindex_runtime/registry/migration_catalog.py
      via: Single full ordered migration catalog
      pattern: migration_catalog
---

<objective>
Define deterministic E2a admission and the schema contract that makes its primary materialization safe.

Purpose: Ensure only complete, validated OKF sources reach the later single transaction.
Output: TDD-tested immutable contracts, strict M-D-S-M admission, migration 019, and one migration catalog.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/15-okf-ingestion-pipeline/15-CONTEXT.md
@.planning/phases/15-okf-ingestion-pipeline/15-CALLER-INVENTORY.md
@.planning/ADR-OKF-RAW-PAIR-GENERATION-BINDING-2026-07-16.md
@llamaindex_runtime/okf/serializer.py
@llamaindex_runtime/registry/migrations/005_kg_extension.sql
@llamaindex_runtime/registry/migrations/008_evidence_object.sql
@llamaindex_runtime/registry/migrations/018_okf_rebuild_failure_audit.sql
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Write RED contracts for strict admission and deterministic ownership</name>
  <files>tests/llamaindex_runtime/okf/test_e2a_admission.py, tests/llamaindex_runtime/okf/test_e2a_contracts.py</files>
  <behavior>
    - A valid raw pair is read only through ADR M-D-S-M validation and equivalent shuffled input gives the same manifest hash.
    - Missing/unstable/legacy raw pairs, duplicate logical targets, and unsafe paths fail closed before any repository call.
    - Manual evidence requires normalized document/version/span plus exactly one entity or relation target; paragraph-only evidence fails.
    - Same natural key with divergent deterministic ID or incompatible ownership fails; equivalent facts retain UUID5 identities and relation qualifiers.
  </behavior>
  <action>Per D-15-06, D-15-08, and D-15-09, create isolated temporary-bundle tests against a no-DML spy. Require use of the accepted raw-pair ADR rather than an inferred sidecar convention. Specify frozen dataclasses and canonical JSON/hash behavior; derive span identity through the established `NormalizationContract.normalize`, never copied logic. Add fixture assertions for a future fresh-schema migration test without opening a database.</action>
  <verify><automated>rtk env -u DATABASE_URL -u FORMAL_RUNTIME_DATABASE_URL -u OKF_MIGRATION_TEST_DATABASE_DISPOSABLE -u OKF_REBUILD_EXPECTED_DATABASE -u OKF_FAILURE_AUDIT_ACCEPTANCE -u OKF_REBUILD_DOCKER_ACCEPTANCE -u OKF_E2A_DISPOSABLE_TEST_AUTHORIZED pytest tests/llamaindex_runtime/okf/test_e2a_admission.py tests/llamaindex_runtime/okf/test_e2a_contracts.py -q</automated></verify>
  <done>Failing tests specify complete admission, deterministic identities, required evidence, and ownership collision behavior without DB access.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement admission, migration 019, and the full migration catalog</name>
  <files>llamaindex_runtime/okf/e2a_contracts.py, llamaindex_runtime/okf/e2a_admission.py, llamaindex_runtime/registry/migrations/019_e2a_materialization_contract.sql, llamaindex_runtime/registry/migration_catalog.py, tests/llamaindex_runtime/okf/test_e2a_admission.py, tests/llamaindex_runtime/okf/test_e2a_contracts.py, tests/llamaindex_runtime/okf/test_e2a_migration_019_postgres.py</files>
  <action>Before source edits, run GitNexus impact for every modified symbol; report the exact caller/process blast radius and all HIGH or CRITICAL warnings, then proceed within the already authorized Phase 15 scope after recording them. Stop only when a newly discovered scope expansion requires a user decision. Implement frozen DTOs including `E2aDesiredState`, `DmlRecorder`, and `E2aReconciliationResult` with manifest hash, per-table DML, outcome, denylist counts, comparator parity, stale/cache counts, audit outcome, and post-rollback audit outcome. Implement strict raw-pair admission per the accepted ADR. Add `019_e2a_materialization_contract.sql`: ownership ledger keyed by safe relative OKF path/fact kind/deterministic ID, source digest, nullable scope, multi-owner global facts, composite `(version_id, span_id)` and `(version_id, evidence_id)` integrity, exactly-one link target, required evidence, constrained source kind/dedup, bounded E2a failure phases, append-only initial and final same-transaction attestation of observed function source, OID, and trigger catalog state without a `pg_proc` exclusion, and no projection outbox/status. The final re-attestation is a detection control only, not a lock or race prevention. Update and test `migration_catalog.py` for the complete ordered catalog and exactly-once 019 entry; `run_migrations.py` remains unchanged as the catalog consumer. Preserve the PageIndex curated subset as explicitly non-applicable with a regression. Create `tests/llamaindex_runtime/okf/test_e2a_migration_019_postgres.py`: a non-DB static migration-spec/gate test that validates migration SQL structure without database connection; it is NOT a live PostgreSQL acceptance selector. Runner connection policy, timeouts, role assumption, DDL lease, and production execution are excluded. Do not add `chunk_entity_links` or any E2b table to E2a.</action>
  <verify><automated>rtk env -u DATABASE_URL -u FORMAL_RUNTIME_DATABASE_URL -u OKF_MIGRATION_TEST_DATABASE_DISPOSABLE -u OKF_REBUILD_EXPECTED_DATABASE -u OKF_FAILURE_AUDIT_ACCEPTANCE -u OKF_REBUILD_DOCKER_ACCEPTANCE -u OKF_E2A_DISPOSABLE_TEST_AUTHORIZED pytest tests/llamaindex_runtime/okf/test_e2a_admission.py tests/llamaindex_runtime/okf/test_e2a_contracts.py tests/llamaindex_runtime/okf/test_e2a_migration_019_postgres.py -q</automated></verify>
  <done>Admission has no persistence fallback, migration 019 is the named materialization contract, and catalog tests define full-runner versus PageIndex-subset behavior. The non-DB selector validates the no-authority `blocked_not_executed` gate with zero connection attempt; actual disposable execution remains separately authorized.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| OKF filesystem - admission | Untrusted files become candidate durable derived state. |
| Manual fact - provenance schema | Human-authored references select database evidence and ownership. |
| Migration catalog - schema | A skipped migration weakens integrity guarantees. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-15-01 | T | raw-pair admission | mitigate | Apply accepted rooted M-D-S-M validation with three bounded attempts and no legacy fallback. |
| T-15-02 | E | manual evidence | mitigate | Require unique admitted document/version/span and exactly one typed target before DML. |
| T-15-03 | R | ownership facts | mitigate | Deterministic IDs, source digest, and ownership ledger make facts auditable. |
| T-15-04 | D | migration delivery | mitigate | One ordered catalog plus runner/subset regression prevents an accidental 019 omission. |
</threat_model>

<verification>
Run only non-DB pytest selectors during ordinary execution. Fresh-schema/disposable acceptance is specified here but remains separately authorized. The materialization-contract coverage referenced by Task 1 and Task 2 is exercised by `tests/llamaindex_runtime/okf/test_e2a_contracts.py` together with the distributed non-DB selectors (`test_e2a_admission.py`, `test_e2a_desired_state.py`, `test_e2a_reconciliation.py`, `test_e2a_materialization_dml_parity.py`, `test_e2a_wave1_fourth.py`); no single `test_e2a_materialization_contract.py` file exists in the repository. These selectors describe available static/unit/non-DB coverage only, not a fresh execution result or a live acceptance PASS. Live Wave 2 disposable PostgreSQL acceptance has NOT been executed and remains blocked pending the aggregate Wave 1 gate and separate disposable authorization.
</verification>

<success_criteria>
- All raw and manual inputs are deterministically admitted or rejected before DML.
- Migration 019 names and enforces the E2a materialization boundary.
- No E2b or `chunk_entity_links` capability enters E2a.
</success_criteria>

<output>
After completion, create `.planning/phases/15-okf-ingestion-pipeline/15-01-SUMMARY.md`.
</output>


## Binding remediation addendum

Task 1 RED cases additionally require `E2aCorpusAdmitter.admit_e2a_corpus(authority) -> E2aDesiredState`: recursive manifest-only `raw/**/*.pair.json` admission through `read_raw_pair`, fact discovery only below entities, relations, concepts, strict exclusions, safe POSIX paths, duplicate identity rejection, `(kind, path, identity)` ordering, canonical full-corpus manifest/SHA-256, and no result for empty/invalid corpus. Tests name every immutable desired-state collection and deterministic natural key.

Task 2 must update and test the exact root catalog while `run_migrations.py` remains its unchanged consumer, plus add `tests/llamaindex_runtime/okf/test_e2a_migration_019_postgres.py`. A non-DB source regression specifies initial and final same-transaction append-only attestation of observed function source, OID, and trigger state without a `pg_proc` exclusion; the final check is detection only, not a lock or race prevention. The gated actual-root-catalog PostgreSQL test validates that 019 applies exactly once after 018, valid inserts, named constraints/indexes, cross-version rejection, ownership collision, dual-target rejection, valid legacy schema, and legacy-mismatch failure without rewrite; it proves PageIndex limited migration workflow is intentionally non-applicable. The test `test_e2a_migration_019_postgres.py` is a non-DB static migration-spec/gate test that validates migration SQL structure; it is NOT a live PostgreSQL acceptance selector. Live acceptance remains `blocked_not_executed` without separate disposable authority. Runner connection policy, timeouts, role assumption, DDL lease, and production execution remain excluded. Migration 019 has no external projection/outbox/status.

All examples must begin with `rtk` and explicitly unset `DATABASE_URL`, `FORMAL_RUNTIME_DATABASE_URL`, `OKF_MIGRATION_TEST_DATABASE_DISPOSABLE`, `OKF_REBUILD_EXPECTED_DATABASE`, `OKF_FAILURE_AUDIT_ACCEPTANCE`, `OKF_REBUILD_DOCKER_ACCEPTANCE`, and `OKF_E2A_DISPOSABLE_TEST_AUTHORIZED` before a non-DB selector. The executor must inspect the configured quality tools: `llamaindex_runtime/pyproject.toml` currently configures pytest only, so use `ruff check` and `ruff format --check` only when Ruff is installed/configured and do not invent mypy, Black, or isort; public APIs remain typed.