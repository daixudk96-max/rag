---
phase: 15-okf-ingestion-pipeline
plan: 04
type: execute
wave: 4
depends_on: [15-01, 15-02, 15-03]
files_modified:
  - verification/phase15-okf-ingestion-pipeline/run_e2a_verification.py
  - tests/llamaindex_runtime/okf/test_e2a_verification_result.py
  - .planning/phases/15-okf-ingestion-pipeline/15-VERIFICATION.md
autonomous: true
requirements: [R-OKF-01, R-OKF-02, R-OKF-04]
must_haves:
  truths:
    - Phase 15 verification consumes measured typed reconciliation results rather than fabricating evidence JSON.
    - Unexecuted disposable PostgreSQL acceptance is visibly blocked, never represented as a pass.
    - Verification distinguishes pure comparator parity and retrieval smoke reachability from quality or Level claims.
  artifacts:
    - path: verification/phase15-okf-ingestion-pipeline/run_e2a_verification.py
      provides: Redacting result consumer and non-DB verification orchestrator.
    - path: tests/llamaindex_runtime/okf/test_e2a_verification_result.py
      provides: Measured-result evidence and no-fabrication contract tests.
    - path: .planning/phases/15-okf-ingestion-pipeline/15-VERIFICATION.md
      provides: Current acceptance matrix and execution-unstarted status.
  key_links:
    - from: verification/phase15-okf-ingestion-pipeline/run_e2a_verification.py
      to: llamaindex_runtime/okf/e2a_contracts.py
      via: E2aReconciliationResult input
      pattern: E2aReconciliationResult
---

<objective>
Make Phase 15 evidence an authenticated-by-behavior consumer of typed measured results without overstating unrun acceptance.

Purpose: Preserve the truth that verification follows execution and that database acceptance requires separate authority.
Output: Redacting non-DB evidence consumer, result-shape tests, and factual acceptance matrix.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/15-okf-ingestion-pipeline/15-CONTEXT.md
@.planning/phases/15-okf-ingestion-pipeline/15-VERIFICATION.md
@.planning/phases/15-okf-ingestion-pipeline/15-01-SUMMARY.md
@.planning/phases/15-okf-ingestion-pipeline/15-02-SUMMARY.md
@.planning/phases/15-okf-ingestion-pipeline/15-03-SUMMARY.md
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Specify measured-result-only and redaction verification behavior</name>
  <files>tests/llamaindex_runtime/okf/test_e2a_verification_result.py</files>
  <behavior>
    - Evidence emission rejects arbitrary table counts; it accepts an `E2aReconciliationResult` carrying manifest hash, DML, outcomes, denylist, comparator, stale/cache, audit, and post-rollback audit measurements.
    - It emits canonical redacted output without connection strings, environment values, raw corpus text, or invented authorization receipt metadata.
    - An unavailable disposable selector is represented as `blocked_not_executed`; a smoke/comparator record contains an explicit no-quality/no-Level label.
  </behavior>
  <action>Write failing unit tests for evidence serialization and status transitions. Per D-15-04, D-15-05, and D-15-10, prohibit standalone fabricated JSON as acceptance proof. Define only test fixtures and schema-level output expectations; do not launch a database, Docker, or an external service.</action>
  <verify><automated>rtk env -u DATABASE_URL -u FORMAL_RUNTIME_DATABASE_URL -u OKF_MIGRATION_TEST_DATABASE_DISPOSABLE -u OKF_REBUILD_EXPECTED_DATABASE -u OKF_FAILURE_AUDIT_ACCEPTANCE -u OKF_REBUILD_DOCKER_ACCEPTANCE pytest tests/llamaindex_runtime/okf/test_e2a_verification_result.py -q</automated></verify>
  <done>Tests reject fabricated evidence, secrets, implicit authority, and quality overclaims.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement result consumer and preserve factual verification state</name>
  <files>verification/phase15-okf-ingestion-pipeline/run_e2a_verification.py, tests/llamaindex_runtime/okf/test_e2a_verification_result.py, .planning/phases/15-okf-ingestion-pipeline/15-VERIFICATION.md</files>
  <action>Implement a small typed result consumer, not a second reconciler and not an evidence fabricator. It may run non-DB selectors and serialize returned measured results with a whitelist/redactor; it must not inspect/print environment values or open external services. Require separately authorized disposable acceptance before executing its DB selector, otherwise emit only `blocked_not_executed` with no connection attempt. Update verification status only from actual execution results. Document full acceptance matrix: first/second DML, locks/concurrency, late rollback/fresh audit, stale/cache effects, denylist, direct pure-comparator parity, fresh schema, migration catalog, and R1/R2a/R2b/p6 smoke. Preserve the execution-unstarted status until execution has actually occurred.</action>
  <verify><automated>rtk env -u DATABASE_URL -u FORMAL_RUNTIME_DATABASE_URL -u OKF_MIGRATION_TEST_DATABASE_DISPOSABLE -u OKF_REBUILD_EXPECTED_DATABASE -u OKF_FAILURE_AUDIT_ACCEPTANCE -u OKF_REBUILD_DOCKER_ACCEPTANCE pytest tests/llamaindex_runtime/okf/test_e2a_verification_result.py -q</automated></verify>
  <done>Evidence is derived from typed results, non-DB output is secret-safe, and blocked database acceptance remains honest.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Runtime result → evidence | Serialization can overstate results or disclose sensitive material. |
| Verification runner → DB acceptance | A default runner must not initiate destructive activity. |
| Evidence → phase status | Smoke or comparator results can be misrepresented as quality proof. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-15-13 | I | result serializer | mitigate | Whitelist typed fields and exclude raw text, URLs, environment values, and credentials. |
| T-15-14 | E | acceptance runner | mitigate | Default non-DB operation and explicit blocked state without a connection attempt. |
| T-15-15 | R | verification status | mitigate | Status follows measured result outcome only; preserve unexecuted state otherwise. |
| T-15-16 | S | smoke evidence | mitigate | Label comparator/retrieval outputs no-quality/no-Level and validate the label. |
</threat_model>

<verification>
The default selector is non-DB. No evidence fixture is treated as a generated PASS artifact.
</verification>

<success_criteria>
- Measured typed results are the only verification data source.
- Database acceptance cannot accidentally run or be claimed as passed.
- Phase 15 remains execution-unstarted until implementation has real results.
</success_criteria>

<output>
After completion, create `.planning/phases/15-okf-ingestion-pipeline/15-04-SUMMARY.md`.
</output>


## Binding remediation addendum

The result consumer has no projection/outbox field. The acceptance matrix must name migration preflight, each 019 constraint/index, first/second primary DML, locks including new parents, late failure at each reconciliation layer, failure audit after rollback/close/fresh connection, every cascade-closure rejection with zero mutation, E2b zero mutation, deterministic tree shuffle/no-DML rerun, relation qualifier equality, ingress negative routes, and PageIndex non-authority. Disposable PostgreSQL/Docker selectors emit exactly `blocked_not_executed` without connection when no separate authority exists. No standalone JSON may claim PASS.
