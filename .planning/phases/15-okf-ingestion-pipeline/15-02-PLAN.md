---
phase: 15-okf-ingestion-pipeline
plan: 02
type: tdd
wave: 2
depends_on: [15-01]
files_modified:
  - llamaindex_runtime/okf/e2a_materialization_repository.py
  - llamaindex_runtime/okf/e2a_reconciler.py
  - llamaindex_runtime/registry/tree_generator.py
  - tests/llamaindex_runtime/okf/test_e2a_desired_state.py
  - tests/llamaindex_runtime/okf/test_e2a_reconciliation.py
  - tests/llamaindex_runtime/okf/test_e2a_disposable_postgres.py
autonomous: true
requirements: [R-OKF-01, R-OKF-02, R-OKF-04]
must_haves:
  truths:
    - An admitted whole corpus commits all primary E2a state together or leaves its prior state untouched.
    - Equivalent re-admission performs zero DML in every primary table, including sync and ownership state.
    - Stale E2a state is removed safely while E2b data is never altered.
    - Shuffled equivalent spans yield the same tree IDs, mappings, and zero second-run primary DML.
  artifacts:
    - path: llamaindex_runtime/okf/e2a_materialization_repository.py
      provides: Cursor-only exact E2a repository and DML recording.
    - path: llamaindex_runtime/okf/e2a_reconciler.py
      provides: Caller-owned transaction orchestration, locks, rollback, and post-rollback audit.
    - path: tests/llamaindex_runtime/okf/test_e2a_reconciliation.py
      provides: Deterministic no-op, stale deletion, lock, and denylist behavior tests.
  key_links:
    - from: llamaindex_runtime/okf/e2a_reconciler.py
      to: llamaindex_runtime/okf/e2a_materialization_repository.py
      via: Caller-supplied cursor
      pattern: reconcile.*cursor
    - from: llamaindex_runtime/okf/e2a_reconciler.py
      to: llamaindex_runtime/registry/tree_generator.py
      via: Fully sorted canonical span tree derivation
      pattern: generate_tree
---

<objective>
Implement exact, atomic primary E2a reconciliation over an already admitted whole corpus.

Purpose: Replace insert-if-absent and component-owned transactions with deterministic desired-state reconciliation.
Output: Cursor-only repository, one caller-owned transaction, concurrency-safe reconciliation, and acceptance tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/15-okf-ingestion-pipeline/15-CONTEXT.md
@.planning/phases/15-okf-ingestion-pipeline/15-CALLER-INVENTORY.md
@.planning/phases/15-okf-ingestion-pipeline/15-01-SUMMARY.md
@llamaindex_runtime/registry/postgres_adapter.py
@llamaindex_runtime/vector/loader.py
@llamaindex_runtime/registry/tree_generator.py
@llamaindex_runtime/registry/migrations/003_tree_persistence.sql
@llamaindex_runtime/registry/migrations/004_vector_extension.sql
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Write RED exact-reconciliation, deterministic-tree, and concurrency specifications</name>
  <files>tests/llamaindex_runtime/okf/test_e2a_desired_state.py, tests/llamaindex_runtime/okf/test_e2a_reconciliation.py, tests/llamaindex_runtime/okf/test_e2a_disposable_postgres.py</files>
  <behavior>
    - A fake cursor shows ordered global, parent-advisory, and existing-parent row locks, then only allowlisted DML.
    - Equivalent reruns produce zero INSERT/UPDATE/DELETE for all primary tables, `okf_sync_state`, and ownership state; no success/no-op audit row is written.
    - A late failure rolls primary state back entirely; only after rollback/primary close can a fresh connection append bounded failure audit.
    - Stale tree deletion repoints chunks, invalidates summaries/node embeddings/semantic distribution, fails closed on `node_entity_links`, and never changes E2b tables.
    - Shuffled span order produces equal tree IDs/mappings/manifest; overlapping and new-parent disposable runs serialize correctly.
  </behavior>
  <action>Per D-15-03 through D-15-05 and D-15-07, write fake-cursor RED tests and separately gated disposable fixtures. Require immutable desired-state builders, finite confidence checks, canonical JSONB relation qualifiers, `IS DISTINCT FROM`, and deterministic sorted domain keys. Test a primary DML recorder against the full allowlist and denylist including `chunk_entity_links`. Fixtures must be inert unless separately authorized; do not connect to a database in ordinary selectors.</action>
  <verify><automated>rtk env -u DATABASE_URL -u FORMAL_RUNTIME_DATABASE_URL -u OKF_MIGRATION_TEST_DATABASE_DISPOSABLE -u OKF_REBUILD_EXPECTED_DATABASE -u OKF_FAILURE_AUDIT_ACCEPTANCE -u OKF_REBUILD_DOCKER_ACCEPTANCE pytest tests/llamaindex_runtime/okf/test_e2a_desired_state.py tests/llamaindex_runtime/okf/test_e2a_reconciliation.py -q</automated></verify>
  <done>RED selectors define atomicity, DML no-op, tree cache repair, exact relation comparison, lock order, and E2b exclusion without live DB access.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement cursor-only repository and caller-owned reconciliation orchestration</name>
  <files>llamaindex_runtime/okf/e2a_materialization_repository.py, llamaindex_runtime/okf/e2a_reconciler.py, llamaindex_runtime/registry/tree_generator.py, tests/llamaindex_runtime/okf/test_e2a_desired_state.py, tests/llamaindex_runtime/okf/test_e2a_reconciliation.py</files>
  <action>Before source edits, rerun GitNexus impact for every changed symbol; surface HIGH/CRITICAL findings. Implement `E2aMaterializationRepository.reconcile(cursor, desired, *, recorder) -> E2aReconciliationResult`; it must never commit, roll back, change autocommit, or open a connection. Do not make `PostgresRegistryWriter.write_*` or `VectorLoader.load()` E2a primitives. Derive/embedd desired state before locks, acquire the global transaction-scoped E2a advisory lock, sorted per-parent advisory locks including new parents, and sorted existing-parent `FOR UPDATE` locks. Upsert/diff with exact predicates and no unconditional conflict updates. Reconcile parent-first tree nodes and node spans, repoint chunks, then delete stale nodes after cache invalidation counts; fail closed on `node_entity_links`. Sort normalized semantic domains before `TreeGenerator.generate_tree`. Delete global manual entities/relations only when ownership is gone and no non-E2a dependency remains. Record all outcomes in the typed result; run failure audit solely via a fresh connection after rollback/close.</action>
  <verify><automated>rtk env -u DATABASE_URL -u FORMAL_RUNTIME_DATABASE_URL -u OKF_MIGRATION_TEST_DATABASE_DISPOSABLE -u OKF_REBUILD_EXPECTED_DATABASE -u OKF_FAILURE_AUDIT_ACCEPTANCE -u OKF_REBUILD_DOCKER_ACCEPTANCE pytest tests/llamaindex_runtime/okf/test_e2a_desired_state.py tests/llamaindex_runtime/okf/test_e2a_reconciliation.py -q</automated></verify>
  <done>The one transaction controls all primary E2a DML, a true equivalent run is database-silent, and no prohibited table can be reached.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Desired state → PostgreSQL | One derived-state attempt touches linked primary tables. |
| Concurrent corpus callers → lock protocol | Overlap can create duplicate/mixed materialization. |
| Primary rollback → failure audit | Audit must not commit residue from a failed transaction. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-15-05 | T | reconciliation SQL | mitigate | Parameterized scoped exact diffs, recorder, and denylist assertions. |
| T-15-06 | D | concurrent runs | mitigate | Global plus sorted parent locks and sorted row locks held to terminal transaction outcome. |
| T-15-07 | I | failure audit | mitigate | Fresh post-close connection, bounded phases, redacted diagnostics. |
| T-15-08 | E | stale global facts | mitigate | Ownership/non-E2a dependency checks before deletion; fail closed around E2b links. |
</threat_model>

<verification>
Run unit selectors normally. The database fixture is an acceptance specification only and cannot run without separate disposable-target authorization.
</verification>

<success_criteria>
- E2a primary materialization is atomic and concurrency-safe.
- Exact idempotence and cache invalidations are measured in `E2aReconciliationResult`.
- Tree identity is input-order independent and E2b remains unreachable.
</success_criteria>

<output>
After completion, create `.planning/phases/15-okf-ingestion-pipeline/15-02-SUMMARY.md`.
</output>


## Binding remediation addendum

Task 1 must include RED tests for stale span rejection with entity_mentions or non-E2a/legacy evidence, stale chunk rejection with chunk_entity_links, recursive stale-node descendant rejection with node_entity_links, repointing surviving chunks, and zero mutation after every rejected closure. It must also seed aliases, outside-scope relations, non-E2a evidence, node/chunk links, and ownership combinations before global entity/relation deletion tests.

Task 2 implements explicit desired-state builder, connection factory, `DmlRecorder`, and root orchestrator. Repository signature is exactly `E2aMaterializationRepository.reconcile(cursor, desired, *, recorder) -> E2aReconciliationResult`; it cannot call `commit`, `rollback`, alter autocommit, or open a connection. It records database cascade effects as well as issued DML. `PostgresRegistryWriter.write_spans`, `write_tree`, `write_vector_chunks`, `write_entities`, `write_relations`, `write_evidence_links`, `write_chunk_entity_links`, `write_node_entity_links`, and `VectorLoader.load` are forbidden E2a primitives. Relation equality includes negation, condition, direction, finite confidence, canonical JSONB qualifiers; the no-op test covers every primary, ownership, and sync row without durable success/no-op logging.

Before every actual symbol edit rerun GitNexus impact, report caller/process blast radius, warn on HIGH/CRITICAL, and stop only for discovered scope expansion requiring a user decision. The disposable test remains blocked until separately authorized.
