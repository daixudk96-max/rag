---
phase: 10-hotspot-semantic-retrieval-validation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: true
requirements: [REQ-P10-DB-BACKED-WHITEBOX]
user_setup: []

must_haves:
  truths:
    - "Demo script executes successfully with real PostgreSQL DATABASE_URL"
    - "Demo produces evidence-bearing hits with valid chunk_id (not all-zero UUID)"
    - "Navigation metadata (hotspot_node_id, navigation_node_ids, drill_depth) preserved in hits"
  artifacts:
    - path: "scripts/demo_hotspot_semantic_retrieval.py"
      provides: "DB-backed whitebox demo execution"
      min_lines: 250
    - path: "verification/hotspot-semantic-demo/"
      provides: "Demo fixture output directory"
      pattern: "hotspot-source-*.md"
  key_links:
    - from: "scripts/demo_hotspot_semantic_retrieval.py"
      to: "DATABASE_URL environment variable"
      via: "load_dotenv + psycopg.connect"
      pattern: "DATABASE_URL.*is not configured"
---

<objective>
Execute DB-backed whitebox demo to validate hotspot semantic retrieval works with real PostgreSQL data.

Purpose: Prove SubtreeHotspotSelector → RecursiveTreeTraversalRunner → EvidenceContentResolver pipeline works against real registry data (spans, chunks, tree_nodes, embeddings), not just fixtures.
Output: Demo execution log, evidence-chain integrity verification, navigation metadata presence confirmation.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-hotspot-semantic-retrieval-validation/10-RESEARCH.md

<interfaces>
From scripts/demo_hotspot_semantic_retrieval.py:
```python
def main() -> int:
    """CLI entry point returning 0 (success), 1 (integrity failure), 2 (config error)."""

# Exit codes:
# 0 = success: demo completed, evidence-chain integrity verified
# 1 = integrity failure: hits contain all-zero chunk_id
# 2 = config error: DATABASE_URL not configured
```

From verification/phase8-matched-validation-rerun/level_assessment.json:
```json
{
  "level": "Level_2",
  "metrics": {"hit_rate": 0.7, "top1_relevance": 0.59, "stability": 0.65},
  "thresholds": {"hit_rate": 0.8, "top1_relevance": 0.9, "stability": 0.85}
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify DATABASE_URL configuration</name>
  <files>.env</files>
  <action>
Check `.env` file exists and contains DATABASE_URL entry (non-empty, no placeholder values).
If missing or placeholder, report configuration blocker; do not expose raw DATABASE_URL value in any output artifact.
Use pattern: `grep -E "^DATABASE_URL=" .env` (exit 0 = found, non-zero = missing).
Do NOT print or log the actual DATABASE_URL string.
  </action>
  <verify>
    <automated>
PowerShell: `if (Select-String -Path .env -Pattern "^DATABASE_URL=" -Quiet) { echo "OK" } else { echo "FAIL" }`
    </automated>
  </verify>
  <done>
.env contains DATABASE_URL entry (non-placeholder); no raw DATABASE_URL exposed in output.
  </done>
</task>

<task type="auto">
  <name>Task 2: Execute demo script with DB-backed retrieval</name>
  <files>scripts/demo_hotspot_semantic_retrieval.py, verification/hotspot-semantic-demo/*.md</files>
  <action>
Run `python scripts/demo_hotspot_semantic_retrieval.py` from repo root.
Demo script:
1. Loads .env DATABASE_URL (via load_dotenv)
2. Connects to PostgreSQL (psycopg.connect)
3. Registers temporary document (doc_id, version_id)
4. Persists canonical_spans
5. Generates tree_nodes + tree_node_spans (TreeGenerator)
6. Materializes vector_chunks + embeddings (VectorLoader, DeterministicEmbedder dim=16)
7. Runs hotspot retrieval: SubtreeHotspotSelector.select_hotspots → RecursiveTreeTraversalRunner.traverse_tree_for_query
8. Prints hotspot node IDs, navigation metadata, evidence-bearing hits
9. Verifies evidence-chain integrity (chunk_id != all-zero UUID)
10. Returns exit code: 0 (success), 1 (integrity failure), 2 (config error)

Capture stdout/stderr output; verify exit code == 0.
Check output contains "[OK] parent hotspots were used as navigation; final hits are evidence-bearing chunks".
  </action>
  <verify>
    <automated>
PowerShell: `python scripts/demo_hotspot_semantic_retrieval.py; if ($LASTEXITCODE -eq 0) { echo "OK" } else { echo "FAIL exit=$LASTEXITCODE" }`
    </automated>
  </verify>
  <done>
Demo script exit code 0; output contains "[OK] parent hotspots were used as navigation"; no "[FAIL]" lines.
  </done>
</task>

<task type="auto">
  <name>Task 3: Verify evidence-chain integrity in demo output</name>
  <files>scripts/demo_hotspot_semantic_retrieval.py (output log)</files>
  <action>
Parse demo output log; verify:
- `zero_chunk_hits` count == 0 (or explicitly stated as zero)
- All final hits have `chunk_id` field present and not all-zero UUID (UUID(int=0))
- Navigation metadata present: `hotspot_node_id`, `navigation_path` (or `navigation_node_ids`), `drill_depth`

If output contains `[FAIL] expected evidence-bearing hits with normal chunk IDs`, integrity check failed.
  </action>
  <verify>
    <automated>
PowerShell demo output parsing: `python scripts/demo_hotspot_semantic_retrieval.py | Select-String -Pattern "zero_chunk_hits" -Context 0,2`
    </automated>
  </verify>
  <done>
Demo output shows zero_chunk_hits == 0; all hits have valid chunk_id; navigation metadata present.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Demo script → PostgreSQL | DATABASE_URL crosses environment boundary; must not be exposed in output |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-P10-01 | Information Disclosure | Demo output log | mitigate | Do not print raw DATABASE_URL; use existence check only |
| T-P10-02 | Tampering | Evidence-chain integrity | mitigate | Demo script asserts chunk_id != MISSING_CHUNK_ID (all-zero UUID) |
</threat_model>

<verification>
Demo execution completes with exit code 0; evidence-chain integrity verified; navigation metadata preserved.
</verification>

<success_criteria>
- Demo script runs successfully against real PostgreSQL (DATABASE_URL configured, no placeholder)
- Demo output contains "[OK]" success message
- Evidence-chain integrity verified: zero_chunk_hits == 0, all hits have valid chunk_id
- Navigation metadata present in hits: hotspot_node_id, navigation_node_ids, drill_depth
- No raw DATABASE_URL exposed in any output artifact
</success_criteria>

<output>
After completion, create `.planning/phases/10-hotspot-semantic-retrieval-validation/10-01-SUMMARY.md` capturing:
- Demo execution log (stdout/stderr)
- Exit code verification
- Evidence-chain integrity status
- Navigation metadata presence confirmation
- DATABASE_URL configuration status (found/not found, without exposing value)
</output>