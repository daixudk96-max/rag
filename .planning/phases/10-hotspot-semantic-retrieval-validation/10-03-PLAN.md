---
phase: 10-hotspot-semantic-retrieval-validation
plan: 03
type: execute
wave: 2
depends_on: [10-01, 10-02]
files_modified: []
autonomous: true
requirements: [REQ-P10-LEVEL-IMPACT]
user_setup: []

must_haves:
  truths:
    - "Hotspot semantic retrieval metrics can be compared against Phase 8 baseline"
    - "Quality comparison script produces delta report (improved/regressed/neutral)"
    - "Level assessment impact is documented (Level 2 baseline preserved or new Level proposed)"
  artifacts:
    - path: "verification/phase8-matched-validation-rerun/quality_metrics.json"
      provides: "Phase 8 baseline metrics (hit_rate 0.70, top1_relevance 0.59, stability 0.65)"
      contains: "metrics.*hit_rate.*top1_relevance.*stability"
    - path: "verification/phase8-matched-validation-rerun/level_assessment.json"
      provides: "Phase 8 Level_2 assessment, thresholds"
      contains: "level.*Level_2"
    - path: "verification/phase10-hotspot-quality-comparison/"
      provides: "Hotspot metrics comparison output"
      pattern: "phase10_quality_metrics.json, phase10_level_assessment.json"
  key_links:
    - from: "Phase 10 hotspot retrieval"
      to: "Phase 8 judgment corpus"
      via: "same queries/judgments (if reusable) or new judgment collection"
      pattern: "judgment_completed.*95 rows"
    - from: "Hotspot metrics calculation"
      to: "Phase 8 calculate_phase8_metrics.py pattern"
      via: "reuse integrity gate + metric calculation logic"
      pattern: "calculate_hit_rate.*calculate_top1_relevance.*calculate_stability"
---

<objective>
Compare hotspot semantic retrieval metrics against Phase 8 baseline to assess Level impact.

Purpose: Determine if hotspot routing improves, regresses, or maintains quality relative to Phase 8 baseline (hit_rate 0.70, top1_relevance 0.59, stability 0.65).
Output: Quality metrics comparison report, Level impact assessment, judgment reuse analysis.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-hotspot-semantic-retrieval-validation/10-RESEARCH.md

<interfaces>
From verification/phase8-matched-validation-rerun/calculate_phase8_metrics.py:
```python
def calculate_hit_rate(judgments, retrieval_results) -> float:
    """Queries with at least one relevant hit / total queries."""

def calculate_top1_relevance(judgments) -> float:
    """Average relevance score for rank-1 hits."""

def calculate_stability(judgments, retrieval_results) -> float:
    """Query share whose top-1 hit is relevant among queries with hits."""

def assess_level(hit_rate, top1_relevance, stability) -> dict:
    """Assess Level using frozen thresholds (0.80/0.90/0.85)."""
```

From verification/phase8-matched-validation-rerun/level_assessment.json:
```json
{
  "level": "Level_2",
  "blocking_factors": [
    "hit_rate: 70.00% (need ≥ 80.00%)",
    "top1_relevance: 59.00% (need ≥ 90.00%)",
    "stability: 65.00% (need ≥ 85.00%)"
  ]
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Assess judgment reuse feasibility</name>
  <files>verification/phase8-matched-validation-rerun/judgment_completed.csv, verification/phase8-matched-validation-rerun/retrieval_results.json</files>
  <action>
Read Phase 8 judgment_completed.csv (95 rows) and retrieval_results.json.
Assess whether Phase 8 judgments can be reused for hotspot retrieval hits:
- If hotspot hits use same canonical_spans.raw_text (preview matches Phase 8 text_preview), judgments may be reusable.
- If hotspot hits use different preview (EvidenceContentResolver fallback chain changes text), judgments invalid.

Check integrity gate pattern from Phase 5: retrieval/judgment hit_id matching (node_id + heading_path + text_preview consistency).

Report assessment:
- "REUSE_OK" if preview text matches Phase 8 hits (judgments reusable)
- "REUSE_BLOCKED" if preview differs significantly (need new judgment collection)
- "UNCLEAR" if preview consistency requires manual inspection

Do NOT proceed to metrics calculation if judgment reuse is blocked or unclear.
  </action>
  <verify>
    <automated>
PowerShell: `python -c "import csv; rows = list(csv.DictReader(open('verification/phase8-matched-validation-rerun/judgment_completed.csv'))); print(f'{len(rows)} judgment rows')"`
    </automated>
  </verify>
  <done>
Judgment reuse feasibility assessed; report contains REUSE_OK/REUSE_BLOCKED/UNCLEAR status with rationale.
  </done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <decision>Judgment reuse strategy for hotspot quality comparison</decision>
  <context>
Phase 8 has 95 judgment rows for matched corpus. Hotspot retrieval may produce different hits (different traversal path, different preview text). Need to determine if Phase 8 judgments are reusable or if new judgment collection is required.
  </context>
  <options>
    <option id="reuse-phase8-judgments">
      <name>Reuse Phase 8 judgments (if preview matches)</name>
      <pros>Fast metrics comparison; no human judgment workload; aligned corpus reuse</pros>
      <cons>Risk of invalid comparison if preview differs; integrity gate may block</cons>
    </option>
    <option id="collect-new-judgments">
      <name>Collect new judgments for hotspot hits</name>
      <pros>Valid comparison; accurate Level assessment; integrity guaranteed</pros>
      <cons>Human judgment workload (95 rows); time delay; not Phase 10 planning scope</cons>
    </option>
    <option id="skip-quality-comparison">
      <name>Skip quality comparison (defer to future phase)</name>
      <pros>Focus on integrity + code review; avoid judgment workload in Phase 10</pros>
      <cons>No Level impact assessment; baseline drift risk; incomplete validation</cons>
    </option>
  </options>
  <resume-signal>
Select: reuse-phase8-judgments, collect-new-judgments, or skip-quality-comparison.
If reuse-phase8-judgments, proceed to metrics calculation.
If collect-new-judgments, report blocker for next phase (not Phase 10 scope).
If skip-quality-comparison, proceed to code review gate without Level impact report.
  </resume-signal>
</task>

<task type="auto">
  <name>Task 3: Calculate hotspot metrics (if judgment reuse approved)</name>
  <files>verification/phase10-hotspot-quality-comparison/phase10_quality_metrics.json</files>
  <action>
If judgment reuse approved (user selected reuse-phase8-judgments):
1. Run hotspot retrieval on Phase 8 aligned corpus (PageIndex完整功能分析与集成方案.md)
2. Use same 20 queries from Phase 8 retrieval_results.json
3. Apply Phase 8 judgments (judgment_completed.csv) to hotspot hits
4. Calculate metrics using Phase 8 pattern: calculate_hit_rate, calculate_top1_relevance, calculate_stability
5. Produce phase10_quality_metrics.json with same structure as Phase 8

If judgment reuse blocked or skipped, report as blocker; do NOT calculate metrics.
  </action>
  <verify>
    <automated>
MISSING — requires hotspot retrieval execution script (not in working tree). Wave 0 must create verification/phase10_calculate_hotspot_metrics.py first.
    </automated>
  </verify>
  <done>
If judgment reuse approved: phase10_quality_metrics.json created with hotspot metrics.
If judgment reuse blocked: blocker reported for next phase.
  </done>
</task>

<task type="auto">
  <name>Task 4: Compare hotspot metrics vs Phase 8 baseline</name>
  <files>verification/phase10-hotspot-quality-comparison/phase10_metrics_comparison.json</files>
  <action>
If phase10_quality_metrics.json exists:
1. Load Phase 8 baseline metrics (hit_rate 0.70, top1_relevance 0.59, stability 0.65)
2. Load Phase 10 hotspot metrics
3. Calculate delta: hotspot - baseline for each metric
4. Assess improvement/regression/neutral for each metric
5. Produce comparison report with delta, threshold status, Level impact

Comparison structure:
```json
{
  "baseline": {"hit_rate": 0.70, "top1_relevance": 0.59, "stability": 0.65},
  "hotspot": {...},
  "delta": {"hit_rate": 0.XX, "top1_relevance": 0.XX, "stability": 0.XX},
  "improved": {"hit_rate": true/false, ...},
  "threshold_passed": {"hit_rate": true/false, ...},
  "level_impact": "Level_2 preserved" or "Level_3 candidate" or "Level regression risk"
}
```

If metrics calculation skipped, report comparison blocker.
  </action>
  <verify>
    <automated>
MISSING — requires comparison script creation. Wave 0 must create verification/phase10_compare_metrics.py first.
    </automated>
  </verify>
  <done>
If metrics exist: comparison report produced with delta and Level impact.
If metrics skipped: blocker reported.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Hotspot hits → Phase 8 judgments | Judgment reuse crosses validation boundary; integrity gate required |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-P10-05 | Tampering | Judgment reuse integrity | mitigate | Integrity gate checks retrieval/judgment hit_id match (node_id + heading_path + preview) |
| T-P10-06 | Information Disclosure | Level assessment exposure | accept | Level assessment is quality transparency, not PII |
</threat_model>

<verification>
Judgment reuse feasibility assessed; user decision on reuse strategy; metrics comparison report (if approved) or blocker documented.
</verification>

<success_criteria>
- Judgment reuse feasibility assessed (REUSE_OK/REUSE_BLOCKED/UNCLEAR)
- User decision captured on judgment reuse strategy
- If reuse approved: phase10_quality_metrics.json and comparison report produced
- If reuse blocked: blocker documented for next phase
- Comparison report shows delta vs Phase 8 baseline and Level impact assessment
</success_criteria>

<output>
After completion, create `.planning/phases/10-hotspot-semantic-retrieval-validation/10-03-SUMMARY.md` capturing:
- Judgment reuse feasibility assessment
- User decision on reuse strategy
- Metrics comparison results (if approved) or blocker documentation
- Level impact assessment (Level 2 preserved / Level 3 candidate / regression risk)
- Next phase recommendations (judgment collection if blocked)
</output>