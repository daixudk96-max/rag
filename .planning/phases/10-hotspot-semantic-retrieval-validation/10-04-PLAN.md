---
phase: 10-hotspot-semantic-retrieval-validation
plan: 04
type: execute
wave: 3
depends_on: [10-01, 10-02, 10-03]
files_modified: []
autonomous: false
requirements: [REQ-P10-CODE-REVIEW-GATE]
user_setup: []

must_haves:
  truths:
    - "GitNexus impact analysis executed on hotspot-related symbols before eventual commit"
    - "GitNexus detect_changes verifies affected scope is expected"
    - "Code review completed: architectural boundaries, performance, error handling"
    - "Phase 10 closure readiness assessed: validation artifacts, blockers, commit gates"
  artifacts:
    - path: ".planning/phases/10-hotspot-semantic-retrieval-validation/10-CODE-REVIEW.md"
      provides: "Code review findings (architectural, performance, error handling)"
      pattern: "architectural_boundaries.*performance.*error_handling"
    - path: ".planning/phases/10-hotspot-semantic-retrieval-validation/10-IMPACT-ANALYSIS.md"
      provides: "GitNexus impact analysis blast radius report"
      pattern: "affected_symbols.*callers.*risk_level"
    - path: ".planning/phases/10-hotspot-semantic-retrieval-validation/10-VALIDATION.md"
      provides: "Phase 10 validation checklist and closure readiness"
      pattern: "validation_complete.*blockers.*commit_gate"
  key_links:
    - from: "GitNexus impact analysis"
      to: "SubtreeHotspotSelector, RecursiveTreeTraversalRunner, QueryHit symbols"
      via: "gitnexus_impact({target: symbolName, direction: upstream})"
      pattern: "blast_radius.*direct_callers.*affected_processes"
    - from: "GitNexus detect_changes"
      to: "Uncommitted working tree changes"
      via: "gitnexus_detect_changes() or CLI equivalent"
      pattern: "affected_symbols.*execution_flows.*expected_scope"
---

<objective>
Complete code review gates and GitNexus impact analysis for eventual commit readiness.

Purpose: Ensure hotspot semantic retrieval changes pass architectural review, performance review, error handling review, and GitNexus impact gates before any eventual commit.
Output: Code review report, GitNexus impact analysis, Phase 10 validation checklist, closure readiness assessment.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-hotspot-semantic-retrieval-validation/10-RESEARCH.md
@.claude/skills/gitnexus/gitnexus-impact-analysis/SKILL.md

<interfaces>
From CLAUDE.md GitNexus directive:
```markdown
## Always Do

- **MUST run impact analysis before editing any symbol.**
- **MUST run gitnexus_detect_changes() before committing** to verify changes affect expected symbols.
- **MUST warn user** if impact analysis returns HIGH or CRITICAL risk.

## Never Do

- NEVER edit a function, class, or method without first running gitnexus_impact.
- NEVER ignore HIGH or CRITICAL risk warnings.
- NEVER commit changes without running gitnexus_detect_changes().
```

From llamaindex_runtime/tree/semantic_distribution.py hotspot symbols:
```python
class SubtreeHotspotSelector:
    """Select parent routing nodes whose subtree centroid is close to query embedding."""

class RecursiveTreeTraversalRunner:
    """Traverse from hotspot head to evidence-bearing children."""

class QueryHit:
    """Evidence-bearing hit with navigation metadata."""
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify GitNexus availability</name>
  <files>.claude/skills/gitnexus/gitnexus-impact-analysis/SKILL.md</files>
  <action>
Check GitNexus MCP availability in execution shell:
- If MCP tool `gitnexus_impact` available: proceed with MCP-based impact analysis
- If MCP unavailable: use CLI fallback `npx gitnexus analyze` (or document unavailability)

Probe availability:
PowerShell: `npx gitnexus --version` (exit 0 = available, non-zero = fallback or unavailability)

If unavailable, report GitNexus unavailability as blocker; impact analysis cannot proceed automatically.
Document CLI fallback requirement or manual call graph inspection alternative.
  </action>
  <verify>
    <automated>
PowerShell: `npx gitnexus --version; if ($LASTEXITCODE -eq 0) { echo "OK GitNexus available" } else { echo "WARN fallback required" }`
    </automated>
  </verify>
  <done>
GitNexus availability verified (MCP or CLI fallback); unavailability documented if applicable.
  </done>
</task>

<task type="auto">
  <name>Task 2: Run GitNexus impact analysis on hotspot symbols</name>
  <files>llamaindex_runtime/tree/semantic_distribution.py (symbols: SubtreeHotspotSelector, RecursiveTreeTraversalRunner, QueryHit)</files>
  <action>
Run GitNexus impact analysis on hotspot-related symbols (if available):
- `gitnexus_impact({target: "SubtreeHotspotSelector", direction: "upstream"})`
- `gitnexus_impact({target: "RecursiveTreeTraversalRunner", direction: "upstream"})`
- `gitnexus_impact({target: "QueryHit", direction: "upstream"})`

For each symbol, capture blast radius:
- Direct callers (functions/methods using this symbol)
- Affected processes (execution flows involving this symbol)
- Risk level (HIGH/CRITICAL if many callers, low if isolated)

If GitNexus unavailable:
- Document manual call graph inspection alternative
- Report unavailability as blocker for automated impact analysis
- Grep-based fallback: `grep -r "SubtreeHotspotSelector" llamaindex_runtime/` to identify callers

Report impact analysis findings in `.planning/phases/10-hotspot-semantic-retrieval-validation/10-IMPACT-ANALYSIS.md`.
  </action>
  <verify>
    <automated>
MISSING — requires GitNexus MCP execution or CLI fallback. If MCP unavailable, document manual inspection fallback.
    </automated>
  </verify>
  <done>
GitNexus impact analysis executed (MCP or CLI); blast radius report created; risk level assessed; unavailability documented if applicable.
  </done>
</task>

<task type="auto">
  <name>Task 3: Architectural code review</name>
  <files>llamaindex_runtime/tree/semantic_distribution.py, llamaindex_runtime/tree/runtime.py</files>
  <action>
Review architectural boundaries and responsibilities:
1. SubtreeHotspotSelector: Should reside in retrieval layer (per RESEARCH.md Architectural Responsibility Map)
   - Check: imports, dependencies, registry queries, distribution adapter usage
2. RecursiveTreeTraversalRunner: Should reside in retrieval layer
   - Check: traversal logic, policy decision, navigation tracking, depth limit
3. QueryHit: Dataclass contract (provenance fields, navigation metadata)
   - Check: frozen=True, field types, immutability, backend mapping
4. EvidenceContentResolver: Hydration layer (vector layer secondary)
   - Check: preview resolution, fallback chain (span → chunk → summary/title)

Architectural violations to flag:
- Retrieval logic in database layer (wrong tier)
- Direct database queries in traversal runner (should use adapter)
- Mutable QueryHit (should be frozen dataclass)
- Missing navigation metadata fields

Report findings in `.planning/phases/10-hotspot-semantic-retrieval-validation/10-CODE-REVIEW.md` under "Architectural Boundaries" section.
  </action>
  <verify>
    <automated>
PowerShell grep-based inspection: `Select-String -Path llamaindex_runtime/tree/semantic_distribution.py -Pattern "class SubtreeHotspotSelector|class RecursiveTreeTraversalRunner|class QueryHit" -Context 5,5`
    </automated>
  </verify>
  <done>
Architectural review completed; boundary violations documented; tier assignments verified against RESEARCH.md responsibility map.
  </done>
</task>

<task type="auto">
  <name>Task 4: Performance and error handling review</name>
  <files>llamaindex_runtime/tree/semantic_distribution.py</files>
  <action>
Review performance and error handling:
1. Performance concerns:
   - Recursion depth limit: `_MAX_TRAVERSAL_DEPTH = 256` (prevent infinite recursion)
   - Hotspot limit: `limit=3` (prevent excessive subtree selection)
   - Embedding dimension validation: `_coerce_embedding` validates dimension consistency
   - Centroid computation efficiency: check vector aggregation cost
2. Error handling:
   - Missing required fields: `_required_value` raises ValueError if key missing
   - Dimension mismatch: `_compute_centroid` raises ValueError on mismatch
   - Missing node_id: `_resolve_node_id` returns None, chunk skipped
   - Path traversal injection: `_heading_path_parts` sanitizes separator

Flag CRITICAL/HIGH issues:
- Infinite recursion risk (depth limit missing or too high)
- Unbounded hotspot selection (limit missing or too high)
- Missing error handling for registry failures
- Unvalidated embedding dimensions

Report findings in `.planning/phases/10-hotspot-semantic-retrieval-validation/10-CODE-REVIEW.md` under "Performance" and "Error Handling" sections.
  </action>
  <verify>
    <automated>
PowerShell grep-based inspection: `Select-String -Path llamaindex_runtime/tree/semantic_distribution.py -Pattern "_MAX_TRAVERSAL_DEPTH|limit|_coerce_embedding|_required_value" -Context 2,2`
    </automated>
  </verify>
  <done>
Performance review completed (depth limit, hotspot limit, dimension validation); error handling reviewed (missing fields, dimension mismatch); CRITICAL/HIGH issues documented.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>GitNexus impact analysis and code review completed</what-built>
  <how-to-verify>
Review `.planning/phases/10-hotspot-semantic-retrieval-validation/10-IMPACT-ANALYSIS.md` and `.planning/phases/10-hotspot-semantic-retrieval-validation/10-CODE-REVIEW.md`:

1. Impact analysis blast radius:
   - Affected symbols: SubtreeHotspotSelector, RecursiveTreeTraversalRunner, QueryHit
   - Direct callers: list of functions/methods
   - Risk level: HIGH/CRITICAL if many callers, LOW if isolated
   - Expected scope: retrieval layer only (no database layer callers)

2. Code review findings:
   - Architectural boundaries: tier assignments correct (retrieval layer for hotspot symbols)
   - Performance: depth limit, hotspot limit, dimension validation present
   - Error handling: ValueError for missing fields, dimension mismatch; sanitization for path traversal

Verify no CRITICAL/HIGH risk warnings that block eventual commit.
If warnings present, assess whether fixes are required before commit or deferred.
  </how-to-verify>
  <resume-signal>Type "approved" if impact analysis and code review pass, or describe issues requiring fixes</resume-signal>
</task>

<task type="auto">
  <name>Task 6: Create Phase 10 validation checklist</name>
  <files>.planning/phases/10-hotspot-semantic-retrieval-validation/10-VALIDATION.md</files>
  <action>
Create validation checklist summarizing Phase 10 status:

## Phase 10 Validation Checklist

### Evidence-Chain Integrity
- [ ] DB-backed whitebox demo executed (exit code 0)
- [ ] Evidence-chain integrity verified (zero_chunk_hits == 0)
- [ ] Navigation metadata preserved (hotspot_node_id, navigation_node_ids, drill_depth)

### Quality Impact Assessment
- [ ] Judgment reuse feasibility assessed
- [ ] Metrics comparison executed (if reuse approved) OR blocker documented (if reuse blocked)
- [ ] Level impact reported (Level 2 preserved / Level 3 candidate / regression risk)

### Code Review Gates
- [ ] GitNexus impact analysis executed (or unavailability documented)
- [ ] Architectural boundaries reviewed (tier assignments correct)
- [ ] Performance reviewed (depth limit, hotspot limit, dimension validation)
- [ ] Error handling reviewed (ValueError for missing fields, dimension mismatch)
- [ ] No CRITICAL/HIGH issues blocking eventual commit

### Closure Readiness
- [ ] Validation artifacts created (10-01-SUMMARY, 10-02-SUMMARY, 10-03-SUMMARY, 10-CODE-REVIEW, 10-IMPACT-ANALYSIS)
- [ ] Blockers documented for next phase (if any: judgment collection, GitNexus unavailability, Level regression)
- [ ] Commit gate: `gitnexus_detect_changes` required before eventual commit (or CLI fallback)

### Next Phase Recommendations
- If judgment reuse blocked: recommend Phase 11 for new judgment collection
- If metrics regressed: recommend tuning hotspot limit/policy thresholds
- If Level improvement candidate: recommend Phase 11 for Level 3/4 promotion validation
- If all validation passed: recommend commit readiness (after GitNexus detect_changes)

Write checklist to `.planning/phases/10-hotspot-semantic-retrieval-validation/10-VALIDATION.md`.
  </action>
  <verify>
    <automated>
MISSING — requires manual checklist creation. Wave 0 task: create validation template file.
    </automated>
  </verify>
  <done>
10-VALIDATION.md created with checklist; validation status summarized; blockers documented; next phase recommendations provided.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitNexus → Working tree changes | Impact analysis crosses code intelligence boundary; blast radius must be verified |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-P10-07 | Denial of Service | Infinite recursion in traversal | mitigate | `_MAX_TRAVERSAL_DEPTH = 256` prevents infinite recursion |
| T-P10-08 | Tampering | Unbounded hotspot selection | mitigate | `limit=3` in SubtreeHotspotSelector.select_hotspots prevents excessive selection |
| T-P10-09 | Tampering | Dimension mismatch in embeddings | mitigate | `_coerce_embedding` validates dimension consistency; `_compute_centroid` raises ValueError on mismatch |
</threat_model>

<verification>
GitNexus impact analysis executed (or unavailability documented); code review completed; validation checklist created; closure readiness assessed.
</verification>

<success_criteria>
- GitNexus availability verified (MCP or CLI fallback documented)
- Impact analysis executed on SubtreeHotspotSelector, RecursiveTreeTraversalRunner, QueryHit (blast radius report created)
- Architectural boundaries reviewed (tier assignments correct, no violations)
- Performance reviewed (depth limit, hotspot limit, dimension validation present)
- Error handling reviewed (ValueError for missing fields, sanitization present)
- No CRITICAL/HIGH issues blocking eventual commit
- Validation checklist created (10-VALIDATION.md)
- Closure readiness assessed with next phase recommendations
</success_criteria>

<output>
After completion, create `.planning/phases/10-hotspot-semantic-retrieval-validation/10-04-SUMMARY.md` capturing:
- GitNexus availability status (MCP available / CLI fallback / unavailability documented)
- Impact analysis blast radius report (affected symbols, callers, risk level)
- Code review findings (architectural, performance, error handling)
- Validation checklist status (passed/blocked)
- Closure readiness assessment (validation artifacts, blockers, commit gate)
- Next phase recommendations
</output>