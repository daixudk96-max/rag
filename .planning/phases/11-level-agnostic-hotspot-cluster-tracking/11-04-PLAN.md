---
phase: 11-level-agnostic-hotspot-cluster-tracking
plan: 04
type: execute
wave: 4
depends_on: [11-03]
files_modified:
  - verification/phase11-level-agnostic-hotspot-cluster-tracking/run_validation.py
  - verification/phase11-level-agnostic-hotspot-cluster-tracking/README.md
  - verification/phase11-level-agnostic-hotspot-cluster-tracking/validation_status.json
  - verification/phase11-level-agnostic-hotspot-cluster-tracking/evidence_chain_report.json
  - .planning/phases/11-level-agnostic-hotspot-cluster-tracking/11-VALIDATION.md
autonomous: false
requirements: [REQ-11-P6-REGRESSION, REQ-11-METADATA-RATE, REQ-11-GITNEXUS-GATE]
requirements_addressed: [D-05, D-07, D-08]
must_haves:
  truths:
    - "p6 DNA validation returns evidence containing 数据驱动, 非确定性, 持续性 per D-07."
    - "p6 DNA hotspot is 00:31 - 产品特性对比 or AI产品经理核心DNA, not unrelated sections, per D-07."
    - "Validation artifacts report hotspot_metadata_rate >= 0.90 and navigation_path_rate >= 0.90 per D-05."
    - "Code/security review gates and GitNexus detect-changes are completed before commit readiness per D-08."
  artifacts:
    - path: "verification/phase11-level-agnostic-hotspot-cluster-tracking/run_validation.py"
      provides: "Phase 11 p6 validation runner"
      contains: "AI产品经理的核心DNA是什么？"
    - path: "verification/phase11-level-agnostic-hotspot-cluster-tracking/validation_status.json"
      provides: "validation execution result"
      contains: "hotspot_metadata_rate"
    - path: "verification/phase11-level-agnostic-hotspot-cluster-tracking/evidence_chain_report.json"
      provides: "metadata/provenance rates"
      contains: "navigation_path_rate"
  key_links:
    - from: "run_validation.py"
      to: "RAG_TREE_HOTSPOT_SELECTOR=cluster"
      via: "environment setup before retrieval"
      pattern: "RAG_TREE_HOTSPOT_SELECTOR"
---

<objective>
Create and run Phase 11 p6 validation artifacts, then complete review/security/GitNexus gates for safe commit readiness.

Purpose: Prove the cluster selector fixes the p6 DNA hotspot behavior without weakening evidence-chain provenance or hiding CRITICAL runtime risk.
Output: Validation runner, JSON reports, updated validation checklist, and blocking human verification for review/security/GitNexus gate results.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/11-level-agnostic-hotspot-cluster-tracking/11-CONTEXT.md
@.planning/phases/11-level-agnostic-hotspot-cluster-tracking/11-DESIGN.md
@.planning/phases/11-level-agnostic-hotspot-cluster-tracking/11-RESEARCH.md
@.planning/phases/11-level-agnostic-hotspot-cluster-tracking/11-PATTERNS.md
@.planning/phases/11-level-agnostic-hotspot-cluster-tracking/11-VALIDATION.md
@.planning/phases/11-level-agnostic-hotspot-cluster-tracking/11-03-SUMMARY.md
@verification/p6_validation/p6_final_sample_structured.md
@verification/phase10-real-docx-retrieval-validation/run_validation.py
@CLAUDE.md

<interfaces>
Validation runner required query set includes exactly:
```text
AI产品经理的核心DNA是什么？
传统软件和AI产品的输出特点有什么区别？
AI产品的智能来源是什么？
什么是数据闭环飞轮？为什么它重要？
抖音如何利用用户行为数据优化推荐？
特斯拉如何收集自动驾驶数据？
AI产品落地时第一个要思考的问题是什么？
如何处理不干净的数据？
李菲菲在AI 1.0时代遇到了什么数据问题？
独家数据为什么是真正的护城河？
```
Functional pass criteria: `query_failures = 0`, `total_hits > 0`, `hotspot_metadata_rate >= 0.90`, `navigation_path_rate >= 0.90`, `empty_preview_hits = 0`.
Semantic pass criteria for DNA query: evidence contains `数据驱动`, `非确定性`, `持续性`; hotspot is `00:31 - 产品特性对比` or `AI产品经理核心DNA`; hotspot is not `05:40 - 抖音案例`, `04:40 - 数据工作重要性`, or `06:29 - 特斯拉案例`.
</interfaces>
</context>

<tasks>
<task type="auto">
  <name>Task 1: Create Phase 11 validation runner and README</name>
  <read_first>
    - verification/phase10-real-docx-retrieval-validation/run_validation.py
    - verification/p6_validation/p6_final_sample_structured.md
    - .planning/phases/11-level-agnostic-hotspot-cluster-tracking/11-DESIGN.md
    - .planning/phases/11-level-agnostic-hotspot-cluster-tracking/11-VALIDATION.md
  </read_first>
  <files>verification/phase11-level-agnostic-hotspot-cluster-tracking/run_validation.py, verification/phase11-level-agnostic-hotspot-cluster-tracking/README.md</files>
  <action>Create the Phase 11 validation directory if missing. Add `run_validation.py` by adapting the Phase 10 runner structure: set `os.environ["RAG_TREE_HOTSPOT_SELECTOR"] = "cluster"` before retrieval, use p6 corpus `verification/p6_validation/p6_final_sample_structured.md`, include the ten required query strings from this plan, write `validation_status.json` and `evidence_chain_report.json`, and fail closed with nonzero exit when functional or semantic pass criteria are missed. Add `README.md` documenting exact command `rtk test python verification/phase11-level-agnostic-hotspot-cluster-tracking/run_validation.py`, required env vars, selector value `cluster`, p6 DNA expected evidence strings, and excluded unrelated hotspot headings. Do not write raw `DATABASE_URL` or secrets to README or JSON artifacts.</action>
  <verify>
    <automated>rtk test python -m pytest tests/llamaindex_runtime/test_tree_semantic_cluster_hotspot.py tests/llamaindex_runtime/test_tree_runtime_traversal_integration.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `run_validation.py` contains exact string `RAG_TREE_HOTSPOT_SELECTOR` and exact value `cluster`.
    - `run_validation.py` contains exact query `AI产品经理的核心DNA是什么？`.
    - `run_validation.py` contains exact evidence strings `数据驱动`, `非确定性`, `持续性`.
    - `README.md` contains exact command `rtk test python verification/phase11-level-agnostic-hotspot-cluster-tracking/run_validation.py`.
    - Neither file contains a literal PostgreSQL URL such as `postgresql://`.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Run p6 validation and update validation artifacts</name>
  <read_first>
    - verification/phase11-level-agnostic-hotspot-cluster-tracking/run_validation.py
    - .planning/phases/11-level-agnostic-hotspot-cluster-tracking/11-VALIDATION.md
    - .planning/phases/11-level-agnostic-hotspot-cluster-tracking/11-CONTEXT.md
  </read_first>
  <files>verification/phase11-level-agnostic-hotspot-cluster-tracking/validation_status.json, verification/phase11-level-agnostic-hotspot-cluster-tracking/evidence_chain_report.json, .planning/phases/11-level-agnostic-hotspot-cluster-tracking/11-VALIDATION.md</files>
  <action>Run the validation runner with `RAG_TREE_HOTSPOT_SELECTOR=cluster`. Generate or update `validation_status.json` with fields `query_failures`, `total_hits`, `dna_query`, `dna_expected_terms_found`, `dna_primary_hotspot_heading`, `dna_forbidden_hotspot_selected`, and `passed`. Generate or update `evidence_chain_report.json` with fields `hotspot_metadata_rate`, `navigation_path_rate`, `drill_depth_rate`, `empty_preview_hits`, and `passed`. Update `11-VALIDATION.md` task rows from pending to green only for checks that actually passed. If DB/env is unavailable, write fail-closed JSON with `passed: false`, `blocked_reason`, and do not claim D-07 success.</action>
  <verify>
    <automated>rtk test python verification/phase11-level-agnostic-hotspot-cluster-tracking/run_validation.py</automated>
  </verify>
  <acceptance_criteria>
    - `validation_status.json` exists and contains exact key `dna_expected_terms_found`.
    - `validation_status.json` contains `数据驱动`, `非确定性`, and `持续性` when `passed` is true.
    - `validation_status.json` contains exact key `dna_forbidden_hotspot_selected` with value `false` when `passed` is true.
    - `evidence_chain_report.json` exists and contains exact keys `hotspot_metadata_rate` and `navigation_path_rate`.
    - When `passed` is true, `hotspot_metadata_rate >= 0.90`, `navigation_path_rate >= 0.90`, and `empty_preview_hits == 0`.
    - If validation is blocked, artifacts explicitly contain `passed: false` and `blocked_reason`; no Level promotion is claimed.
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Review, security, GitNexus detect-changes, and commit-readiness approval</name>
  <read_first>
    - CLAUDE.md
    - .planning/phases/11-level-agnostic-hotspot-cluster-tracking/11-CONTEXT.md
    - verification/phase11-level-agnostic-hotspot-cluster-tracking/validation_status.json
    - verification/phase11-level-agnostic-hotspot-cluster-tracking/evidence_chain_report.json
  </read_first>
  <files>.planning/phases/11-level-agnostic-hotspot-cluster-tracking/11-VALIDATION.md</files>
  <action>Run final automated gates: `rtk test python -m pytest tests/llamaindex_runtime/test_tree_semantic_cluster_hotspot.py tests/llamaindex_runtime/test_tree_semantic_hotspot.py tests/llamaindex_runtime/test_tree_runtime_traversal_integration.py -q`; run code review and security review according to project rules; run GitNexus detect changes using `rtk proxy npx gitnexus detect-changes --repo rag` or the available GitNexus MCP equivalent. Update `11-VALIDATION.md` with review status, security status, GitNexus affected-symbol/process summary, and explicit D-08 risk acceptance status. Do not run git add, commit, push, reset, clean, checkout, or stash. Pause for human verification of CRITICAL/HIGH risk acceptance and commit scope.</action>
  <verify>
    <automated>rtk test python -m pytest tests/llamaindex_runtime/test_tree_semantic_cluster_hotspot.py tests/llamaindex_runtime/test_tree_semantic_hotspot.py tests/llamaindex_runtime/test_tree_runtime_traversal_integration.py -q</automated>
    <automated>rtk proxy npx gitnexus detect-changes --repo rag</automated>
  </verify>
  <acceptance_criteria>
    - `11-VALIDATION.md` contains exact strings `code_review_status`, `security_review_status`, and `gitnexus_detect_changes_status`.
    - `11-VALIDATION.md` records 0 CRITICAL/HIGH code review findings before commit readiness, or records blocking findings without claiming readiness.
    - `11-VALIDATION.md` records 0 CRITICAL/HIGH security findings before commit readiness, or records blocking findings without claiming readiness.
    - GitNexus detect-changes output is summarized with affected files/symbols/processes.
    - No git staging or commit command is executed in this plan.
  </acceptance_criteria>
  <what-built>Phase 11 cluster selector, switch-gated runtime integration, and p6 validation artifacts.</what-built>
  <how-to-verify>
    1. Review `verification/phase11-level-agnostic-hotspot-cluster-tracking/validation_status.json` and confirm `passed: true` or a fail-closed blocker is explicit.
    2. Confirm DNA query evidence includes `数据驱动`, `非确定性`, and `持续性`.
    3. Confirm primary hotspot is `00:31 - 产品特性对比` or `AI产品经理核心DNA` when validation passed.
    4. Confirm `11-VALIDATION.md` records code review, security review, and GitNexus detect-changes status.
    5. Confirm no unrelated dirty-tree files are included in any proposed commit scope.
  </how-to-verify>
  <resume-signal>Type `approved` to accept Phase 11 validation/risk gate, or describe required fixes.</resume-signal>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| local environment -> validation runner | DB URL and runtime env influence validation but must not leak to artifacts. |
| retrieval output -> JSON artifacts | Evidence/provenance metrics become planning truth and must fail closed when incomplete. |
| GitNexus detect changes -> commit readiness | CRITICAL/HIGH risk must be surfaced before any commit readiness claim. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-14 | Information Disclosure | validation artifacts | mitigate | Never serialize raw `DATABASE_URL`; grep artifacts for `postgresql://` before readiness. |
| T-11-15 | Repudiation | validation status | mitigate | Write explicit pass/fail JSON with expected terms and forbidden hotspot status. |
| T-11-16 | Spoofing | evidence-chain provenance | mitigate | Require hotspot/navigation/drill rates and empty-preview counts in `evidence_chain_report.json`. |
| T-11-17 | Tampering | selector env during validation | mitigate | Force `RAG_TREE_HOTSPOT_SELECTOR=cluster` in runner and record it in artifacts. |
| T-11-18 | Elevation of Privilege | commit readiness | mitigate | Human checkpoint required for GitNexus CRITICAL/HIGH risk acceptance; no git staging/commit here. |
</threat_model>

<verification>
- `rtk test python verification/phase11-level-agnostic-hotspot-cluster-tracking/run_validation.py`
- `rtk test python -m pytest tests/llamaindex_runtime/test_tree_semantic_cluster_hotspot.py tests/llamaindex_runtime/test_tree_semantic_hotspot.py tests/llamaindex_runtime/test_tree_runtime_traversal_integration.py -q`
- `rtk proxy npx gitnexus detect-changes --repo rag`
</verification>

<success_criteria>
- p6 DNA validation passes or fails closed with explicit blocker.
- Passing validation contains all three required evidence strings and allowed hotspot region.
- Metadata rates meet `hotspot_metadata_rate >= 0.90` and `navigation_path_rate >= 0.90` when passed.
- Code review/security/GitNexus gates are recorded before commit readiness.
- No git add/commit/push/reset/clean/checkout/stash is performed.
</success_criteria>

<output>
After completion, create `.planning/phases/11-level-agnostic-hotspot-cluster-tracking/11-04-SUMMARY.md`.
</output>
