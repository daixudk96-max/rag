---
phase: 11-level-agnostic-hotspot-cluster-tracking
plan: 06
type: execute
wave: 2
depends_on: [11-05]
files_modified:
  - verification/phase11-level-agnostic-hotspot-cluster-tracking/validation_status.json
  - verification/phase11-level-agnostic-hotspot-cluster-tracking/06_evidence_chain_report.json
autonomous: true
requirements: []
gap_closure: true
gaps_addressed:
  - "Gap 2: DNA query returns expected evidence strings (数据驱动, 非确定性, 持续性)"

must_haves:
  truths:
    - "Validation runner executes DNA query with adjusted cluster weights"
    - "DNA query returns evidence containing 数据驱动, 非确定性, 持续性"
    - "Validation status shows dna_expected_terms_found=true"
  artifacts:
    - path: "verification/phase11-level-agnostic-hotspot-cluster-tracking/validation_status.json"
      provides: "DNA query semantic validation result"
      contains: "dna_expected_terms_found: true"
    - path: "verification/phase11-level-agnostic-hotspot-cluster-tracking/06_evidence_chain_report.json"
      provides: "Functional validation report"
      contains: "functional_status: PASS"
  key_links:
    - from: "Cluster scoring weight adjustment (11-05)"
      to: "Validation runner DNA query result"
      via: "RAG_TREE_HOTSPOT_SELECTOR=cluster environment"
      pattern: "dna_expected_terms_found"
---

<objective>
Rerun Phase 11 validation with adjusted cluster scoring weights to verify DNA query returns expected evidence strings and selects correct hotspot region.

Purpose: After weight adjustment in 11-05, validation runner must prove DNA query selects expected hotspot (产品特性对比) and returns evidence containing 数据驱动, 非确定性, 持续性.
Output: Updated validation_status.json showing dna_expected_terms_found=true and dna_forbidden_hotspot_selected=false.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-level-agnostic-hotspot-cluster-tracking/11-VERIFICATION.md
@.planning/debug/phase11-dna-hotspot-selection.md

<interfaces>
From verification/phase11-level-agnostic-hotspot-cluster-tracking/run_validation.py:
```python
# Line 27: Environment variable forces cluster selector
os.environ["RAG_TREE_HOTSPOT_SELECTOR"] = "cluster"

# QUERY_SET includes DNA query
QUERY_SET = [
    {"query_id": "Q01", "query_text": "AI产品经理的核心DNA是什么？"},
    # ... more queries
]

# Validation checks
expected_terms = ["数据驱动", "非确定性", "持续性"]
forbidden_regions = ["05:40 - 抖音案例", "04:40 - 数据工作重要性"]
```

From verification/phase11-level-agnostic-hotspot-cluster-tracking/validation_status.json (CURRENT FAILED STATE):
```json
{
  "dna_primary_hotspot_heading": "05:40 - 抖音案例 > AI产品经理的思考方向",
  "dna_forbidden_hotspot_selected": true,
  "dna_expected_terms_found": false
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rerun validation with adjusted cluster weights</name>
  <files>
    - verification/phase11-level-agnostic-hotspot-cluster-tracking/validation_status.json
    - verification/phase11-level-agnostic-hotspot-cluster-tracking/06_evidence_chain_report.json
  </files>
  <read_first>
    - verification/phase11-level-agnostic-hotspot-cluster-tracking/run_validation.py (validation runner logic)
    - .planning/phases/11-level-agnostic-hotspot-cluster-tracking/11-05-SUMMARY.md (weight adjustment confirmation)
    - llamaindex_runtime/tree/semantic_distribution.py (lines 903-921, adjusted _score_cluster)
  </read_first>
  <action>
    Execute validation runner to generate new validation artifacts with adjusted cluster weights.

    1. Run validation runner:
       ```bash
       cd E:\github\rag
       python verification/phase11-level-agnostic-hotspot-cluster-tracking/run_validation.py
       ```

    2. Verify expected outcomes after weight adjustment:
       - DNA query Q01 selects hotspot heading: "00:31 - 产品特性对比" (expected) not "05:40 - 抖音案例" (forbidden)
       - validation_status.json contains:
         - `dna_primary_hotspot_heading`: starts with "00:31 - 产品特性对比"
         - `dna_forbidden_hotspot_selected`: false
         - `dna_expected_terms_found`: true
       - Retrieved hits text_preview contains expected terms: "数据驱动", "非确定性", "持续性"
       - 06_evidence_chain_report.json contains:
         - `functional_status`: "PASS"
         - `rationale`: "DNA query returned expected evidence strings"

    3. If validation fails, troubleshoot:
       - Check that 11-05 weight adjustment is actually applied (semantic_distribution.py not reverted)
       - Check Docker Desktop PostgreSQL is running (validation requires DB-backed retrieval)
       - Check environment variable RAG_TREE_HOTSPOT_SELECTOR=cluster is set in runner
       - If weights are correct but still failing, further weight tuning may be needed (max_score < 0.20)

    **Validation runner environment:**
    - Runner forces `os.environ["RAG_TREE_HOTSPOT_SELECTOR"] = "cluster"` at import time
    - Runner uses p6 validation corpus (PageIndex完整功能分析与集成方案.md)
    - Runner expects PostgreSQL container healthy (Docker Desktop)

    Do NOT modify run_validation.py logic — only execute it and verify artifacts.
  </action>
  <acceptance_criteria>
    - validation_status.json contains `dna_forbidden_hotspot_selected: false`
    - validation_status.json contains `dna_expected_terms_found: true`
    - validation_status.json `dna_primary_hotspot_heading` starts with "00:31 - 产品特性对比" (not "05:40 - 抖音案例")
    - 06_evidence_chain_report.json contains `functional_status: PASS`
  </acceptance_criteria>
  <verify>
    <automated>grep -E "dna_forbidden_hotspot_selected.*false" verification/phase11-level-agnostic-hotspot-cluster-tracking/validation_status.json && grep -E "dna_expected_terms_found.*true" verification/phase11-level-agnostic-hotspot-cluster-tracking/validation_status.json && grep -E '"dna_primary_hotspot_heading": "00:31 - 产品特性对比' verification/phase11-level-agnostic-hotspot-cluster-tracking/validation_status.json</automated>
  </verify>
  <done>
    Validation runner executed successfully, DNA query returns expected evidence strings, validation_status.json shows semantic validation passing.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Validation rerun with adjusted cluster weights
    DNA query selects expected hotspot region
    Expected evidence strings found in retrieved hits
  </what-built>
  <how-to-verify>
    1. Read validation_status.json: `cat verification/phase11-level-agnostic-hotspot-cluster-tracking/validation_status.json`
    2. Verify dna_forbidden_hotspot_selected=false
    3. Verify dna_expected_terms_found=true
    4. Verify dna_primary_hotspot_heading contains "00:31 - 产品特性对比" (not "05:40 - 抖音案例")
    5. Read 06_evidence_chain_report.json: `cat verification/phase11-level-agnostic-hotspot-cluster-tracking/06_evidence_chain_report.json`
    6. Verify functional_status=PASS
  </how-to-verify>
  <resume-signal>Type "approved" to proceed to git commit, or describe issues if validation still fails</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Validation runner → PostgreSQL | DB-backed retrieval uses parameterized queries (no SQL injection) |
| Environment variable → Selector switch | RAG_TREE_HOTSPOT_SELECTOR validated against allowed values |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-06-01 | Tampering | validation_status.json output | accept | Validation artifacts are development outputs, no production impact |
| T-11-06-02 | Denial of Service | PostgreSQL container unavailable | mitigate | Docker Desktop health check in runner (Phase 10 pattern) |
</threat_model>

<verification>
- Validation runner exits 0
- validation_status.json shows dna_forbidden_hotspot_selected=false
- validation_status.json shows dna_expected_terms_found=true
- validation_status.json shows expected hotspot heading
- 06_evidence_chain_report.json shows functional_status=PASS
- grep verification confirms expected values in artifacts
</verification>

<success_criteria>
- [ ] Validation runner executed successfully
- [ ] DNA query selects expected hotspot (产品特性对比)
- [ ] Expected evidence strings found (数据驱动, 非确定性, 持续性)
- [ ] validation_status.json shows semantic validation passing
- [ ] 06_evidence_chain_report.json shows functional_status=PASS
</success_criteria>

<output>
After completion, create `.planning/phases/11-level-agnostic-hotspot-cluster-tracking/11-06-SUMMARY.md`
</output>