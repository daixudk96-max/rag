---
phase: 11-level-agnostic-hotspot-cluster-tracking
plan: 02
type: execute
wave: 2
depends_on: [11-01]
files_modified:
  - llamaindex_runtime/tree/semantic_distribution.py
autonomous: true
requirements: [REQ-11-NO-ROUTE-BONUS, REQ-11-DENSEST-ANCESTOR, REQ-11-ROOT-AVOIDANCE, REQ-11-SHORT-NODE]
requirements_addressed: [D-01, D-02, D-03, D-04, D-08]
must_haves:
  truths:
    - "ClusterHotspotSelector scores all eligible nodes by cosine similarity only before cluster inference per D-01."
    - "Selected hotspot is inferred from densest shared local region per D-02."
    - "Candidate breadth uses candidate_top_n = max(limit * 4, 20) per D-03."
    - "Root is excluded or penalized unless no local region exists per D-04."
    - "Runtime-risk symbols are edited only after GitNexus impact analysis per D-08."
  artifacts:
    - path: "llamaindex_runtime/tree/semantic_distribution.py"
      provides: "NodeSemanticHit, ClusterCandidate, ClusterHotspotSelector"
      contains: "class ClusterHotspotSelector"
  key_links:
    - from: "ClusterHotspotSelector.select_hotspots"
      to: "_cosine_similarity"
      via: "similarity-only first pass"
      pattern: "_cosine_similarity(query_embedding, prototype)"
---

<objective>
Implement the core level-agnostic cluster hotspot algorithm in `semantic_distribution.py`.

Purpose: Replace route-role-biased selection with post-hoc cluster inference while keeping old `SubtreeHotspotSelector` intact for rollback.
Output: Immutable dataclasses and `ClusterHotspotSelector` passing Wave 1 tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/11-level-agnostic-hotspot-cluster-tracking/11-CONTEXT.md
@.planning/phases/11-level-agnostic-hotspot-cluster-tracking/11-DESIGN.md
@.planning/phases/11-level-agnostic-hotspot-cluster-tracking/11-RESEARCH.md
@.planning/phases/11-level-agnostic-hotspot-cluster-tracking/11-PATTERNS.md
@.planning/phases/11-level-agnostic-hotspot-cluster-tracking/11-01-SUMMARY.md
@CLAUDE.md
@llamaindex_runtime/tree/semantic_distribution.py
@tests/llamaindex_runtime/test_tree_semantic_cluster_hotspot.py

<interfaces>
Create these immutable dataclasses in `llamaindex_runtime/tree/semantic_distribution.py`:
```python
@dataclass(frozen=True)
class NodeSemanticHit:
    node_id: UUID
    similarity: float
    heading_path: str | None
    parent_node_id: UUID | None
    path_to_root: tuple[UUID, ...]

@dataclass(frozen=True)
class ClusterCandidate:
    ancestor_node_id: UUID
    member_node_ids: tuple[UUID, ...]
    member_scores: tuple[float, ...]
    max_score: float
    avg_score: float
    support_count: int
    subtree_candidate_count: int
    density: float
```
Selector public method must mirror `SubtreeHotspotSelector.select_hotspots` and return `list[SubtreeHotspot]` so runtime can switch selectors without downstream type churn.
</interfaces>
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Run GitNexus impact analysis and add immutable cluster contracts</name>
  <read_first>
    - CLAUDE.md
    - llamaindex_runtime/tree/semantic_distribution.py
    - tests/llamaindex_runtime/test_tree_semantic_cluster_hotspot.py
    - .planning/phases/11-level-agnostic-hotspot-cluster-tracking/11-CONTEXT.md
  </read_first>
  <files>llamaindex_runtime/tree/semantic_distribution.py</files>
  <action>Before editing, run GitNexus impact analysis for `SubtreeHotspotSelector`, `RecursiveTreeTraversalRunner`, and `QueryHit`; report direct callers, affected processes, and HIGH risk from D-08 in the execution summary. Then add `NodeSemanticHit` and `ClusterCandidate` exactly as immutable `@dataclass(frozen=True)` contracts near existing `SubtreeHotspot`/`QueryHit` dataclasses. Do not modify `QueryHit` fields. Do not delete or rename `SubtreeHotspotSelector`.</action>
  <verify>
    <automated>rtk test python -m pytest tests/llamaindex_runtime/test_tree_semantic_cluster_hotspot.py::TestClusterHotspotSelector::test_cluster_hotspot_selector_scores_all_nodes_without_route_bonus -q</automated>
  </verify>
  <acceptance_criteria>
    - `llamaindex_runtime/tree/semantic_distribution.py` contains exact strings `class NodeSemanticHit` and `class ClusterCandidate`.
    - Both new classes use exact decorator `@dataclass(frozen=True)`.
    - Existing exact string `class SubtreeHotspotSelector` remains present.
    - Existing exact string `class QueryHit` remains present and its hotspot provenance fields are not removed.
    - Execution summary reports GitNexus impact for `SubtreeHotspotSelector`, `RecursiveTreeTraversalRunner`, and `QueryHit` per D-08.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement similarity-only candidate collection and ancestor clustering</name>
  <read_first>
    - llamaindex_runtime/tree/semantic_distribution.py
    - tests/llamaindex_runtime/test_tree_semantic_cluster_hotspot.py
    - .planning/phases/11-level-agnostic-hotspot-cluster-tracking/11-DESIGN.md
    - .planning/phases/11-level-agnostic-hotspot-cluster-tracking/11-RESEARCH.md
  </read_first>
  <files>llamaindex_runtime/tree/semantic_distribution.py</files>
  <action>Add `ClusterHotspotSelector` with method `select_hotspots(...)-> list[SubtreeHotspot]`. Implement dimension validation identical to `SubtreeHotspotSelector`. Build candidates by iterating every `node_stats` item with `node_id` and `prototype_embedding` or `centroid`, computing `similarity = _cosine_similarity(query_embedding, prototype)`, and keeping positive scores only. Sort by similarity descending and take `candidate_top_n = max(limit * 4, 20)`. Build each `NodeSemanticHit.path_to_root` by walking `parent_node_id` through node_stats with `_MAX_TRAVERSAL_DEPTH` guard and cycle detection. Group candidate hits under each ancestor/local subtree per D-04. Do not read `is_route_node`; do not add route bonus, depth bonus, direct chunk bonus, parent preference, or direct/indirect chunk preference in the first pass per D-01.</action>
  <verify>
    <automated>rtk test python -m pytest tests/llamaindex_runtime/test_tree_semantic_cluster_hotspot.py::TestClusterHotspotSelector::test_cluster_hotspot_selector_scores_all_nodes_without_route_bonus -q</automated>
  </verify>
  <acceptance_criteria>
    - File contains exact string `class ClusterHotspotSelector`.
    - File contains exact string `candidate_top_n = max(limit * 4, 20)`.
    - `ClusterHotspotSelector` code path contains `_cosine_similarity(query_embedding, prototype)`.
    - `ClusterHotspotSelector` code path does not contain `_ROUTE_NODE_BONUS`, `_DEPTH_BONUS_PER_LEVEL`, `_SUPPORT_BONUS_PER_CHUNK`, or `is_route_node` as scoring inputs.
    - Test `test_cluster_hotspot_selector_scores_all_nodes_without_route_bonus` passes.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement cluster scoring, root avoidance, and hotspot return shape</name>
  <read_first>
    - llamaindex_runtime/tree/semantic_distribution.py
    - tests/llamaindex_runtime/test_tree_semantic_cluster_hotspot.py
    - .planning/phases/11-level-agnostic-hotspot-cluster-tracking/11-RESEARCH.md
    - .planning/phases/11-level-agnostic-hotspot-cluster-tracking/11-PATTERNS.md
  </read_first>
  <files>llamaindex_runtime/tree/semantic_distribution.py</files>
  <action>Implement `ClusterCandidate` creation and scoring formula exactly as `max_score * 0.40 + avg_score * 0.30 + normalized_support_count * 0.20 + density * 0.10`. Normalize support as `min(support_count / candidate_top_n, 1.0)` or an equivalent bounded ratio. Exclude root clusters when any non-root local cluster exists; if only root exists, allow root as fallback per D-04. Tie-break by higher support_count, higher max_score, deeper ancestor path, then shorter navigation distance to strongest member. Return `SubtreeHotspot` objects using inferred `ancestor_node_id` as `node_id`, cluster score as `score`, and preserve heading/level fields from node_stats. Ensure short exact nodes can survive as their own hotspot when they are the densest meaningful region per D-02/D-07.</action>
  <verify>
    <automated>rtk test python -m pytest tests/llamaindex_runtime/test_tree_semantic_cluster_hotspot.py -q</automated>
  </verify>
  <acceptance_criteria>
    - File contains exact numeric constants `0.40`, `0.30`, `0.20`, `0.10` in cluster scoring.
    - File contains logic that excludes or penalizes root when a non-root candidate exists.
    - All tests in `tests/llamaindex_runtime/test_tree_semantic_cluster_hotspot.py` pass.
    - Existing fallback class `SubtreeHotspotSelector` is still present and not renamed.
  </acceptance_criteria>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| persisted node stats -> cluster selector | Node dictionaries from registry can contain missing/invalid embeddings or cyclic parent relationships. |
| query embedding -> cosine scoring | Query vector dimension must match tree embedding dimension. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-05 | Tampering | `ClusterHotspotSelector` | mitigate | Validate dimensions; ignore missing node/prototype records; do not use route-role flags per D-01. |
| T-11-06 | Denial of Service | ancestor walking | mitigate | Use `_MAX_TRAVERSAL_DEPTH` and cycle detection while building `path_to_root`. |
| T-11-07 | Spoofing | hotspot metadata meaning | mitigate | Return inferred ancestor as `SubtreeHotspot.node_id` only after cluster scoring per D-02. |
| T-11-08 | Repudiation | D-08 risk boundary | mitigate | Record GitNexus impact analysis for HIGH-risk symbols in summary before claiming completion. |
</threat_model>

<verification>
- `rtk test python -m pytest tests/llamaindex_runtime/test_tree_semantic_cluster_hotspot.py -q`
- `rtk test python -m pytest tests/llamaindex_runtime/test_tree_semantic_hotspot.py -q`
</verification>

<success_criteria>
- `NodeSemanticHit`, `ClusterCandidate`, and `ClusterHotspotSelector` exist in `semantic_distribution.py`.
- New cluster tests pass.
- Old `SubtreeHotspotSelector` behavior remains available and unchanged for rollback.
- GitNexus impact analysis for HIGH-risk semantic symbols is reported in summary.
</success_criteria>

<output>
After completion, create `.planning/phases/11-level-agnostic-hotspot-cluster-tracking/11-02-SUMMARY.md`.
</output>
