---
phase: 13-hotspot-traverse-logic-redesign
plan: 03
type: execute
wave: 3
depends_on: [13-01, 13-02]
files_modified:
  - llamaindex_runtime/tree/runtime.py
  - tests/llamaindex_runtime/test_hotspot_traversal_logic.py
autonomous: true
requirements: [P13-12, P13-13, P13-15]
must_haves:
  truths:
    - "The fallback dict in _score_tree_nodes_with_fallback now contains a chunk_id field (MISSING_CHUNK_ID) so no fallback path can emit chunk_id=null"
    - "_map_query_hits_to_backend_hits still skips waypoint hits (chunk_id=MISSING_CHUNK_ID) — backend output never exposes waypoints"
    - "p6 DNA regression still routes the AI-product-manager query to the 产品特性对比 region (selection layer unaffected by traverse changes)"
    - "Full Phase 13 test suite is green end to end"
  artifacts:
    - path: "llamaindex_runtime/tree/runtime.py"
      provides: "Defensive chunk_id=MISSING_CHUNK_ID key in the fallback scored-node dict"
      contains: "\"chunk_id\": MISSING_CHUNK_ID"
    - path: "tests/llamaindex_runtime/test_hotspot_traversal_logic.py"
      provides: "GREEN TestFallbackDictChunkId (P13-12) — skip marker removed"
      contains: "class TestFallbackDictChunkId"
  key_links:
    - from: "_score_tree_nodes_with_fallback"
      to: "MISSING_CHUNK_ID"
      via: "fallback scored-node dict chunk_id field"
      pattern: "\"chunk_id\": MISSING_CHUNK_ID"
    - from: "_map_query_hits_to_backend_hits"
      to: "MISSING_CHUNK_ID"
      via: "waypoint skip filter (must remain)"
      pattern: "hit.chunk_id == MISSING_CHUNK_ID"
---

<objective>
Add the defense-in-depth backstop and run the final regression gate. After Plans 01-02 a hotspot with evidence-bearing children no longer hits the empty-result fallback. But the "route-only children" edge case (RESEARCH.md Pitfall 2) still produces a waypoint-only result that `_map_query_hits_to_backend_hits` filters out, so `backend_hits=[]` and the fallback still fires. This plan ensures the fallback dict itself carries `chunk_id=MISSING_CHUNK_ID`, eliminating `chunk_id=null` from every remaining path, and verifies the selection-layer p6 DNA regression and the waypoint-skip behavior are intact.

Purpose: Close the last `chunk_id=null` source and prove the existing hotspot/selection/runtime behavior is unregressed.

Output:
- `runtime.py` fallback scored-node dict gains a `"chunk_id": MISSING_CHUNK_ID` key.
- P13-12 (`TestFallbackDictChunkId`) flipped to GREEN.
- P13-13 (p6 DNA) and P13-15 (waypoint skip) regressions confirmed green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/TRAVERSE-LOGIC-REDESIGN-PLAN.md
@.planning/phases/13-hotspot-traverse-logic-redesign/13-RESEARCH.md
@.planning/phases/13-hotspot-traverse-logic-redesign/13-PATTERNS.md
@.planning/phases/13-hotspot-traverse-logic-redesign/13-VALIDATION.md
@.planning/phases/13-hotspot-traverse-logic-redesign/13-01-SUMMARY.md
@.planning/phases/13-hotspot-traverse-logic-redesign/13-02-SUMMARY.md

<interfaces>
<!-- Contracts the executor needs. From live code. -->

From llamaindex_runtime/tree/runtime.py — MISSING_CHUNK_ID already in module scope (line 86), no import needed:
```python
MISSING_CHUNK_ID = UUID(int=0)
```

From llamaindex_runtime/tree/runtime.py — _score_tree_nodes_with_fallback scored-node dict (lines 486-494), currently MISSING the chunk_id key:
```python
scored_nodes.append(
    {
        "node_id": node["node_id"],
        "score": score,
        "text_preview": node.get("summary_text") or node.get("title") or "",
        "heading_path": node.get("heading_path"),
        "span_ids": span_ids_by_node.get(node["node_id"], []),
    }
)
```

From llamaindex_runtime/tree/runtime.py — _map_query_hits_to_backend_hits waypoint filter (lines 511-513), MUST remain unchanged:
```python
for hit in query_hits:
    if hit.chunk_id == MISSING_CHUNK_ID or hit.chunk_id in seen_chunks:
        continue
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add chunk_id=MISSING_CHUNK_ID backstop to fallback scored-node dict</name>
  <read_first>
    - llamaindex_runtime/tree/runtime.py (_score_tree_nodes_with_fallback lines 473-497; MISSING_CHUNK_ID line 86; _map_query_hits_to_backend_hits hit_dict lines 544-562 as the reference dict that already carries chunk_id)
    - .planning/phases/13-hotspot-traverse-logic-redesign/13-PATTERNS.md (section 5 — exact minimal change)
    - .planning/phases/13-hotspot-traverse-logic-redesign/13-RESEARCH.md (Pitfall 2 — why the backstop is still needed for route-only children)
  </read_first>
  <behavior>
    - Every dict appended by _score_tree_nodes_with_fallback contains a "chunk_id" key set to MISSING_CHUNK_ID.
    - No other field in the fallback dict changes (node_id, score, text_preview, heading_path, span_ids preserved).
    - _map_query_hits_to_backend_hits waypoint-skip filter is untouched.
  </behavior>
  <action>
In `_score_tree_nodes_with_fallback` (runtime.py lines 486-494), add one key to the appended dict. `MISSING_CHUNK_ID` is already module-level at line 86 — no import:
```python
scored_nodes.append(
    {
        "node_id": node["node_id"],
        "chunk_id": MISSING_CHUNK_ID,  # Phase 13 backstop: no fallback path emits chunk_id=null
        "score": score,
        "text_preview": node.get("summary_text") or node.get("title") or "",
        "heading_path": node.get("heading_path"),
        "span_ids": span_ids_by_node.get(node["node_id"], []),
    }
)
```
Do NOT modify `_map_query_hits_to_backend_hits`. The waypoint-skip filter at line 512 stays as-is.

Then remove the skip marker on `TestFallbackDictChunkId` (P13-12) and finish its body: call `_score_tree_nodes_with_fallback(nodes=[{"node_id": <uuid>, ...}], query_text="x", span_ids_by_node={}, limit=10)` with a node whose `score_node` yields > 0 (provide a node dict with title/summary text that TreeScoring will score positively, e.g. include `query_text` substring in `summary_text`), and assert every returned dict has key `"chunk_id"` equal to `MISSING_CHUNK_ID`. If scoring returns empty for the chosen fixture, assert on the dict shape via a node that scores > 0 (inspect TreeScoring.score_node behavior in runtime.py and craft the fixture so at least one node passes the `score <= 0.0: continue` gate).
  </action>
  <verify>
    <automated>python -m pytest tests/llamaindex_runtime/test_hotspot_traversal_logic.py::TestFallbackDictChunkId -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '"chunk_id": MISSING_CHUNK_ID' llamaindex_runtime/tree/runtime.py` >= 2 (existing hit_dict path + new fallback path)
    - `grep -c 'hit.chunk_id == MISSING_CHUNK_ID' llamaindex_runtime/tree/runtime.py` == 1 (waypoint-skip filter unchanged)
    - `python -c "from llamaindex_runtime.tree.runtime import _score_tree_nodes_with_fallback"` exits 0
    - TestFallbackDictChunkId has its skip marker removed (`grep -c 'pytest.mark.skip' tests/llamaindex_runtime/test_hotspot_traversal_logic.py` == 0 — no skip markers remain anywhere in the file)
    - `python -m pytest tests/llamaindex_runtime/test_hotspot_traversal_logic.py::TestFallbackDictChunkId -q` exits 0
  </acceptance_criteria>
  <done>Fallback dict carries chunk_id=MISSING_CHUNK_ID; waypoint filter untouched; P13-12 GREEN; no skip markers remain in the test file.</done>
</task>

<task type="auto">
  <name>Task 2: Run full Phase 13 regression gate (p6 DNA, waypoint skip, whole suite)</name>
  <read_first>
    - .planning/phases/13-hotspot-traverse-logic-redesign/13-VALIDATION.md (Full suite command; P13-13, P13-15 rows)
    - tests/llamaindex_runtime/test_tree_semantic_cluster_hotspot.py (p6 DNA test name: test_p6_ai_product_manager_core_dna_routes_to_product_characteristics)
  </read_first>
  <action>
This is a verification/gate task — no production-code edits beyond what Task 1 made. Run the three regression commands and the full Phase 13 suite. If any fails, the failure is a regression introduced by Plans 01-03 and MUST be fixed in the owning file before this plan completes (do not weaken or skip a test to make it pass).

Commands to run and record results for in the SUMMARY:
1. p6 DNA regression (P13-13): `python -m pytest "tests/llamaindex_runtime/test_tree_semantic_cluster_hotspot.py::TestClusterHotspotSelector::test_p6_ai_product_manager_core_dna_routes_to_product_characteristics" -q`
2. Waypoint-skip / runtime integration (P13-15): `python -m pytest tests/llamaindex_runtime/test_tree_runtime_traversal_integration.py -q`
3. Full Phase 13 suite: `python -m pytest tests/llamaindex_runtime/test_tree_traversal.py tests/llamaindex_runtime/test_tree_semantic_hotspot.py tests/llamaindex_runtime/test_tree_semantic_cluster_hotspot.py tests/llamaindex_runtime/test_hiro_traversal_integration.py tests/llamaindex_runtime/test_tree_runtime_traversal_integration.py tests/llamaindex_runtime/test_hotspot_traversal_logic.py -q`

Record the pass counts (baseline was 35 existing tests + 12 new = 47 expected) in the SUMMARY.
  </action>
  <verify>
    <automated>python -m pytest tests/llamaindex_runtime/test_tree_traversal.py tests/llamaindex_runtime/test_tree_semantic_hotspot.py tests/llamaindex_runtime/test_tree_semantic_cluster_hotspot.py tests/llamaindex_runtime/test_hiro_traversal_integration.py tests/llamaindex_runtime/test_tree_runtime_traversal_integration.py tests/llamaindex_runtime/test_hotspot_traversal_logic.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `python -m pytest "tests/llamaindex_runtime/test_tree_semantic_cluster_hotspot.py::TestClusterHotspotSelector::test_p6_ai_product_manager_core_dna_routes_to_product_characteristics" -q` exits 0 (P13-13)
    - `python -m pytest tests/llamaindex_runtime/test_tree_runtime_traversal_integration.py -q` exits 0 (P13-15)
    - Full Phase 13 suite command exits 0 with 0 failures and 0 unexpected skips (the 12 new unit cases all run; no skip markers remain)
  </acceptance_criteria>
  <done>p6 DNA green, waypoint-skip integration green, full Phase 13 suite green (no regressions, no remaining skips).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | This plan adds one constant-valued dict key in a runtime-layer fallback and runs tests. No new network, auth, file, or secret boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-06 | Spoofing | fallback chunk_id sentinel | accept | MISSING_CHUNK_ID is a well-known sentinel (UUID(int=0)) already filtered by `_map_query_hits_to_backend_hits`; it explicitly signals "no real chunk", preventing a null masquerading as real evidence. |
| T-13-07 | Tampering | regression of selection layer | mitigate | p6 DNA (P13-13) and runtime integration (P13-15) regressions are run as a blocking gate; any selection-layer regression fails the plan and must be fixed in the owning file, not by weakening the test. |
</threat_model>

<verification>
- `grep -c '"chunk_id": MISSING_CHUNK_ID' llamaindex_runtime/tree/runtime.py` >= 2
- `grep -c 'pytest.mark.skip' tests/llamaindex_runtime/test_hotspot_traversal_logic.py` == 0
- Full Phase 13 suite green (P13-01 through P13-15).
</verification>

<success_criteria>
- No code path can emit chunk_id=null: hotspot-with-children returns real chunk_ids; route-only-children and any other empty-traversal case fall to a fallback dict that now carries MISSING_CHUNK_ID.
- Waypoint hits remain filtered from backend output (P13-15 green).
- p6 DNA selection regression preserved (P13-13 green).
- Full Phase 13 suite green with all 12 new unit cases active (no skips remaining).
</success_criteria>

<output>
After completion, create `.planning/phases/13-hotspot-traverse-logic-redesign/13-03-SUMMARY.md`.
</output>
